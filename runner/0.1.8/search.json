{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"chumicro-runner","text":"<p>A tick-based task runner for CircuitPython, MicroPython, and CPython — no async required.</p> <p>Components implement a <code>check(now_ms) -&gt; bool</code> check that gates when a handler fires.  A <code>Runner</code> captures time once per tick, checks each service, and batch-fires all due handlers.</p>","path":["chumicro-runner"],"tags":[]},{"location":"#quick-example","level":2,"title":"Quick example","text":"<pre><code>from chumicro_runner import Runner\nfrom chumicro_timing import Heartbeat\n\nrunner = Runner()\n\nrunner.add(\n    task=Heartbeat(period_ms=1000),\n    handler=lambda: print(\"one second\"),\n)\nrunner.add(\n    task=Heartbeat(period_ms=5000),\n    handler=lambda: print(\"five seconds\"),\n)\n\nwhile True:\n    runner.tick()\n</code></pre>","path":["chumicro-runner"],"tags":[]},{"location":"#documentation","level":2,"title":"Documentation","text":"<ul> <li>User Guide — getting started and usage patterns</li> <li>API Reference — full API documentation</li> <li>Testing Helpers — using fakes in your tests</li> </ul> <p>← All ChuMicro Libraries · Source · PyPI · Bundle</p>","path":["chumicro-runner"],"tags":[]},{"location":"api/","level":1,"title":"API Reference","text":"","path":["API Reference"],"tags":[]},{"location":"api/#core","level":2,"title":"Core","text":"","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.core","level":2,"title":"<code>chumicro_runner.core</code>","text":"<p>Core tick-runner abstractions for the Chumicro ecosystem.</p> <p>Provides two ways to register work with a <code>Runner</code>:</p> <ol> <li>Gate-based — a check function decides whether a handler fires.    Register with <code>add(check_function, handler=function)</code> (both callables) or    <code>add(obj)</code> where obj has <code>.check(now_ms) -&gt; bool</code> and    <code>.handle(now_ms)</code> methods.</li> <li>Periodic — <code>add_periodic(handler, period_ms)</code>: the handler fires    every period_ms milliseconds with no check.</li> </ol> <p>All classes are cross-runtime compatible (CPython, MicroPython, CircuitPython).</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.core.TaskHandle","level":3,"title":"<code>TaskHandle</code>","text":"<p>Opaque handle returned by <code>Runner.add()</code> or <code>add_periodic()</code>.</p> <p>Provides runtime mutation of a registered task: change its period or remove it from the runner entirely.</p> <p>Read-only properties expose the current state for inspection and testing.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.core.TaskHandle.period_ms","level":4,"title":"<code>period_ms</code>  <code>property</code>","text":"<p>Return the task period in milliseconds, or <code>None</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.core.TaskHandle.run_count","level":4,"title":"<code>run_count</code>  <code>property</code>","text":"<p>Return the remaining run count, or <code>None</code> if unlimited.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.core.TaskHandle.active","level":4,"title":"<code>active</code>  <code>property</code>","text":"<p>Return whether the task is still registered.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.core.TaskHandle.__init__","level":4,"title":"<code>__init__(entry, runner)</code>","text":"<p>Create a handle wrapping entry owned by runner.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.core.TaskHandle.set_period","level":4,"title":"<code>set_period(period_ms)</code>","text":"<p>Add, change, or remove the period for this task.</p> <p>Pass <code>None</code> to remove an existing period (task runs every tick). A non-None value resets the timer so the next fire is period_ms from now.</p> <p>Parameters:</p> Name Type Description Default <code>period_ms</code> <p>New interval in milliseconds, or <code>None</code> to clear the period.</p> required","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.core.TaskHandle.remove","level":4,"title":"<code>remove()</code>","text":"<p>Remove this task from the runner.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.core.TaskHandle.__repr__","level":4,"title":"<code>__repr__()</code>","text":"<p>Return a developer-friendly representation.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.core.Runner","level":3,"title":"<code>Runner</code>","text":"<p>Run tasks on a tick-based schedule.</p> <p>Captures <code>ticks_ms()</code> once per <code>tick()</code> call and passes the shared timestamp to every due component, ensuring all components see the same moment in time.</p> <p>Registration paths:</p> <ul> <li><code>add(obj)</code> — obj has <code>.check(now_ms) -&gt; bool</code> and   <code>.handle(now_ms)</code>.  The runner calls <code>.check()</code>; if <code>True</code>,   <code>.handle()</code> is queued.</li> <li><code>add(check_function, handler=function)</code> — callable check gates   callable handler.</li> <li><code>add(handler=function)</code> — handler fires every tick (or per period).</li> <li><code>add_periodic(handler, period_ms)</code> — fires <code>handler(now_ms)</code>   every period_ms milliseconds.</li> </ul> <p><code>tick()</code> runs in two phases:</p> <ol> <li>Check all entries (period gate, then check gate) and collect    due handlers.</li> <li>Batch-fire all collected handlers.</li> </ol> <p>Period gating uses <code>ticks_diff</code> and <code>ticks_add</code> directly — no <code>Heartbeat</code> objects are created internally.</p> <p>Parameters:</p> Name Type Description Default <code>ticks</code> <p>Optional tick source (must have <code>ticks_ms</code>, <code>ticks_diff</code>, and <code>ticks_add</code> methods). Defaults to the <code>chumicro_timing</code> module-level functions.</p> <code>None</code>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.core.Runner.__init__","level":4,"title":"<code>__init__(ticks=None)</code>","text":"<p>Create a runner.</p> <p>Parameters:</p> Name Type Description Default <code>ticks</code> <p>Optional tick source (must have <code>ticks_ms</code>, <code>ticks_diff</code>, and <code>ticks_add</code> methods). Defaults to the <code>chumicro_timing.ticks</code> module.</p> <code>None</code>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.core.Runner.add","level":4,"title":"<code>add(task=None, handler=None, period_ms=None, start_after_ms=None, run_count=None)</code>","text":"<p>Register a task with the runner.</p> <p>Object-based (task only): task must have <code>.check(now_ms) -&gt; bool</code> and <code>.handle(now_ms)</code> methods.</p> <p>Callable-based (task + handler): task is a callable <code>check_function(now_ms) -&gt; bool</code> that gates <code>handler(now_ms)</code>.</p> <p>Handler-only (handler, no task): <code>handler(now_ms)</code> fires on every tick (or per period if period_ms is set).</p> <p>Returns a <code>TaskHandle</code> for runtime mutation.</p> <p>Parameters:</p> Name Type Description Default <code>task</code> <p>Object with <code>.check()</code> and <code>.handle()</code>, or a callable <code>check_function(now_ms) -&gt; bool</code>.</p> <code>None</code> <code>handler</code> <p>Optional callable <code>handler(now_ms)</code>.</p> <code>None</code> <code>period_ms</code> <p>Optional interval in milliseconds.</p> <code>None</code> <code>start_after_ms</code> <p>Optional initial delay before the task becomes eligible.  Overrides the first period; subsequent fires use period_ms if set.</p> <code>None</code> <code>run_count</code> <p>Optional number of times the handler may fire before auto-removing.  <code>None</code> means unlimited.</p> <code>None</code>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.core.Runner.add_periodic","level":4,"title":"<code>add_periodic(handler, period_ms, start_after_ms=None, run_count=None)</code>","text":"<p>Register a periodic handler with no check.</p> <p><code>handler(now_ms)</code> is called every period_ms milliseconds. Returns a <code>TaskHandle</code> for runtime mutation.</p> <p>Parameters:</p> Name Type Description Default <code>handler</code> <p>Callable <code>handler(now_ms)</code> to fire periodically.</p> required <code>period_ms</code> <p>Interval in milliseconds (required).</p> required <code>start_after_ms</code> <p>Optional initial delay before first fire. Overrides the first period.</p> <code>None</code> <code>run_count</code> <p>Optional number of times the handler may fire before auto-removing.  <code>None</code> means unlimited.</p> <code>None</code>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.core.Runner.tick","level":4,"title":"<code>tick()</code>","text":"<p>Capture time, check tasks, then batch-fire handlers.</p> <ol> <li>Check each entry (period gate -&gt; check gate).    Collect entries whose handlers should fire.</li> <li>Batch-fire all collected handlers.</li> <li>Decrement run counts; auto-remove exhausted entries.</li> </ol> <p>Returns:</p> Type Description <p>The <code>now_ms</code> value used this tick.</p>","path":["API Reference"],"tags":[]},{"location":"api/#testing-helpers","level":2,"title":"Testing Helpers","text":"","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.testing","level":2,"title":"<code>chumicro_runner.testing</code>","text":"<p>Test helpers for libraries that use chumicro-runner.</p> <p>Provides <code>CallRecorder</code> — a callable that records handler invocations for assertion in host-side tests.</p> <p>Usage::</p> <pre><code>from chumicro_runner.testing import CallRecorder\n\nrecorder = CallRecorder()\nrunner.add_periodic(recorder, period_ms=100)\n# ... advance time, tick() ...\nassert recorder.calls == [100]\n</code></pre>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.testing.CallRecorder","level":3,"title":"<code>CallRecorder</code>","text":"<p>Callable that records each invocation for test assertions.</p> <p>Use as a handler passed to <code>Runner.add()</code> or <code>add_periodic()</code>::</p> <pre><code>recorder = CallRecorder()\nrunner.add_periodic(recorder, period_ms=100)\nrunner.tick()\nassert len(recorder) == 0  # not due yet\n</code></pre>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.testing.CallRecorder.__init__","level":4,"title":"<code>__init__()</code>","text":"<p>Create an empty recorder.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.testing.CallRecorder.__call__","level":4,"title":"<code>__call__(now_ms)</code>","text":"<p>Record a call with the given timestamp.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.testing.CallRecorder.__len__","level":4,"title":"<code>__len__()</code>","text":"<p>Return the number of recorded calls.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_runner.testing.CallRecorder.clear","level":4,"title":"<code>clear()</code>","text":"<p>Discard all recorded calls.</p>","path":["API Reference"],"tags":[]},{"location":"guide/","level":1,"title":"User Guide","text":"","path":["User Guide"],"tags":[]},{"location":"guide/#overview","level":2,"title":"Overview","text":"<p><code>chumicro-runner</code> provides a standard pattern for active components in the Chumicro ecosystem.  Instead of each library inventing its own <code>poll()</code> / callback API, every active component implements two methods:</p> <pre><code>def check(self, now_ms):\n    \"\"\"Check whether the handler should fire.  Return True or False.\"\"\"\n\ndef handle(self, now_ms):\n    \"\"\"React to the condition detected by check().\"\"\"\n</code></pre> <p>A shared <code>Runner</code> captures time once per tick, checks each service, and batch-fires all due handlers.  This replaces ad-hoc polling loops with a single standard contract.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#the-pattern","level":2,"title":"The pattern","text":"<ol> <li>Services implement <code>check(now_ms) -&gt; bool</code> — they check a condition and return whether the handler should fire.</li> <li>Handlers implement <code>handle(now_ms)</code> — they react when the service says \"go\".</li> <li>Runner ties it together: capture time → check all services → batch-fire all due handlers.</li> </ol> <p>Services can be objects with <code>.check()</code> and <code>.handle()</code> methods, or plain callables (lambdas, functions, bound methods).</p>","path":["User Guide"],"tags":[]},{"location":"guide/#getting-started","level":2,"title":"Getting started","text":"<pre><code>from chumicro_runner import Runner\n\n\nclass TemperatureSensor:\n    \"\"\"Alert when temperature exceeds a threshold.\"\"\"\n\n    def __init__(self, threshold=30.0):\n        self._threshold = threshold\n        self._last_reading = 0.0\n\n    def read_temperature(self):\n        \"\"\"Read from hardware — fast I2C or ADC operation.\"\"\"\n        # On a real board: return self._i2c_device.temperature\n        return self._last_reading\n\n    def check(self, now_ms):\n        self._last_reading = self.read_temperature()\n        return self._last_reading &gt; self._threshold\n\n    def handle(self, now_ms):\n        print(f\"ALERT: {self._last_reading}°C exceeds {self._threshold}°C\")\n\n\nsensor = TemperatureSensor(threshold=30.0)\nrunner = Runner()\nrunner.add(sensor, period_ms=5000)\n\nwhile True:\n    runner.tick()\n</code></pre>","path":["User Guide"],"tags":[]},{"location":"guide/#shared-timestamps","level":2,"title":"Shared timestamps","text":"<p><code>Runner.tick()</code> captures <code>ticks_ms()</code> once and passes the resulting timestamp to every service.  This ensures all services in the loop see the same moment in time, preventing drift between independent clock reads on slow microcontrollers.</p> <p>The method returns <code>now_ms</code> so user code can use it alongside the service loop:</p> <pre><code>while True:\n    now = runner.tick()\n    if some_heartbeat.poll(now):\n        do_something()\n</code></pre>","path":["User Guide"],"tags":[]},{"location":"guide/#registration-patterns","level":2,"title":"Registration patterns","text":"","path":["User Guide"],"tags":[]},{"location":"guide/#object-based","level":3,"title":"Object-based","text":"<p>Pass an object with <code>.check(now_ms) -&gt; bool</code> and <code>.handle(now_ms)</code>:</p> <pre><code>class MotionDetector:\n    def __init__(self):\n        # On a real board: self._pin = digitalio.DigitalInOut(board.D5)\n        pass\n\n    def detect_motion(self):\n        \"\"\"Read PIR sensor pin — fast digital read.\"\"\"\n        # On a real board: return self._pin.value\n        return False\n\n    def check(self, now_ms):\n        return self.detect_motion()\n\n    def handle(self, now_ms):\n        print(\"Motion!\")\n\nrunner.add(MotionDetector())\n</code></pre> <p>You can override <code>.handle()</code> by passing a <code>handler</code> argument:</p> <pre><code>runner.add(detector, handler=lambda now_ms: send_alert())\n</code></pre>","path":["User Guide"],"tags":[]},{"location":"guide/#callable-based","level":3,"title":"Callable-based","text":"<p>Pass a check function and a handler — both can be lambdas, functions, or bound methods:</p> <pre><code>runner.add(\n    lambda now_ms: light_sensor.level() &lt; 20,\n    handler=lambda now_ms: turn_on_lights(),\n)\n</code></pre>","path":["User Guide"],"tags":[]},{"location":"guide/#handler-only","level":3,"title":"Handler-only","text":"<p>Pass just a handler with no check — it fires every tick (or per period):</p> <pre><code>runner.add(handler=lambda now_ms: scan_buttons(now_ms))\n</code></pre>","path":["User Guide"],"tags":[]},{"location":"guide/#periodic","level":3,"title":"Periodic","text":"<p>No check needed — the handler fires on a schedule:</p> <pre><code>runner.add_periodic(\n    lambda now_ms: print(\"blink!\"),\n    period_ms=500,\n)\n</code></pre>","path":["User Guide"],"tags":[]},{"location":"guide/#period-gated-services","level":2,"title":"Period-gated services","text":"<p>Pass <code>period_ms</code> to <code>add()</code> and the runner will only check the service when the period elapses.  Services without a period are checked every tick.</p> <pre><code>runner = Runner()\n\n# Sensor is only checked every 5 seconds.\nhandle = runner.add(sensor, period_ms=5000)\n\n# Button scanner runs every tick.\nrunner.add(button_scanner)\n</code></pre> <p>You can change or remove the period at runtime via the <code>TaskHandle</code>:</p> <pre><code># Speed up.\nhandle.set_period(1000)\n\n# Remove the period — service runs every tick again.\nhandle.set_period(None)\n\n# Remove the service entirely.\nhandle.remove()\n</code></pre>","path":["User Guide"],"tags":[]},{"location":"guide/#delayed-start","level":3,"title":"Delayed start","text":"<p>Pass <code>start_after_ms</code> to delay the first check.  Subsequent checks use <code>period_ms</code>:</p> <pre><code># Wait 2 seconds, then check every 5 seconds.\nrunner.add(sensor, period_ms=5000, start_after_ms=2000)\n</code></pre>","path":["User Guide"],"tags":[]},{"location":"guide/#limited-runs","level":3,"title":"Limited runs","text":"<p>Pass <code>run_count</code> to auto-remove a task after a set number of handler fires:</p> <pre><code># Fire exactly 3 times, then stop.\nrunner.add_periodic(calibrate, period_ms=1000, run_count=3)\n</code></pre>","path":["User Guide"],"tags":[]},{"location":"guide/#multiple-services","level":2,"title":"Multiple services","text":"<p>The pattern scales to many services with no extra boilerplate:</p> <pre><code>runner = Runner()\nrunner.add(motion_detector)\nrunner.add(temperature_sensor, period_ms=5000)\nrunner.add(\n    lambda now_ms: light_level &lt; 20,\n    handler=lambda now_ms: turn_on_lights(),\n)\nrunner.add_periodic(toggle_led, period_ms=500)\nrunner.add_periodic(log_status, period_ms=10000)\n\nwhile True:\n    runner.tick()\n</code></pre>","path":["User Guide"],"tags":[]},{"location":"guide/#batch-firing","level":2,"title":"Batch firing","text":"<p>All services are checked first, then all due handlers fire in sequence.  This guarantees that handlers see a consistent view of the world — no handler modifies state while other services are still being checked.</p> <pre><code>tick():\n  1. Capture ticks_ms() → now_ms\n  2. For each entry:\n     - Period gate: skip if not due\n     - Check gate: skip if check(now_ms) returns False\n     - Queue handler\n  3. Fire all queued handlers with now_ms\n</code></pre>","path":["User Guide"],"tags":[]},{"location":"guide/#memory-notes","level":2,"title":"Memory notes","text":"<ul> <li><code>_TaskEntry</code> and <code>TaskHandle</code> use <code>__slots__</code> to minimise per-instance memory.</li> <li>Handlers are collected into a pre-allocated list and batch-fired, avoiding per-tick allocation.</li> <li>No <code>collections.deque</code> or ring buffers are required.</li> </ul>","path":["User Guide"],"tags":[]},{"location":"guide/#testing-tasks","level":2,"title":"Testing tasks","text":"<p>The <code>chumicro_runner.testing</code> module provides <code>CallRecorder</code> — a callable that records handler invocations for assertions in host-side tests:</p> <pre><code>from chumicro_runner.testing import CallRecorder\nfrom chumicro_timing.testing import FakeTicks\n\nfake = FakeTicks()\nrecorder = CallRecorder()\nrunner = Runner(ticks=fake)\nrunner.add_periodic(recorder, period_ms=100)\n\nrunner.tick()\nassert len(recorder) == 0  # not due yet\n\nfake.advance(100)\nrunner.tick()\nassert recorder.calls == [100]\n</code></pre> <p>See the testing helpers page for detailed usage.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#examples","level":2,"title":"Examples","text":"<p>The examples directory contains complete runnable scripts:</p> Example What it shows <code>basic_handler.py</code> Simplest handler-only registration <code>periodic_blink.py</code> Periodic handler with <code>add_periodic()</code> <code>sensor_threshold.py</code> Object-based check/handle with simulated sensor <code>multi_service.py</code> Multiple services in one runner <code>runtime_control.py</code> <code>TaskHandle</code> for dynamic period changes and removal <code>circuitpython_blink.py</code> LED blink on CircuitPython hardware <code>micropython_blink.py</code> LED blink on MicroPython hardware <code>circuitpython_button_led.py</code> Button + LED gate pattern on CircuitPython <code>micropython_button_led.py</code> Button + LED gate pattern on MicroPython <p>Simulated examples run on CPython.  Hardware examples (<code>circuitpython_*</code> / <code>micropython_*</code>) require a real board — see the setup notes in each file.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#platform-notes","level":2,"title":"Platform notes","text":"<p>All classes use only basic Python features and work identically on CPython, MicroPython, and CircuitPython.  No <code>abc</code>, <code>typing</code>, or <code>asyncio</code> dependencies.</p>","path":["User Guide"],"tags":[]},{"location":"testing/","level":1,"title":"Testing Helpers","text":"<p><code>chumicro_runner.testing</code> provides <code>CallRecorder</code> for verifying that handlers fire at the right times in host-side tests — a simple callable that records invocations.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#usage-as-a-handler","level":2,"title":"Usage as a handler","text":"<p>Pass a <code>CallRecorder</code> as the handler to <code>Runner.add()</code> or <code>add_periodic()</code>:</p> <pre><code>from chumicro_runner import Runner\nfrom chumicro_runner.testing import CallRecorder\nfrom chumicro_timing.testing import FakeTicks\n\nfake = FakeTicks()\nrecorder = CallRecorder()\nrunner = Runner(ticks=fake)\nrunner.add_periodic(recorder, period_ms=100)\n\n# Not due yet — no calls.\nrunner.tick()\nassert len(recorder) == 0\n\n# Advance past the period.\nfake.advance(100)\nrunner.tick()\nassert recorder.calls == [100]\n</code></pre>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#inspecting-calls","level":2,"title":"Inspecting calls","text":"<p><code>CallRecorder.calls</code> is a plain list of <code>now_ms</code> values from each invocation:</p> <pre><code>assert recorder.calls[0] == 100\nassert len(recorder) == 1\n</code></pre>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#clearing-between-tests","level":2,"title":"Clearing between tests","text":"<p>Call <code>clear()</code> to reset the recorder between test phases:</p> <pre><code>recorder.clear()\nassert len(recorder) == 0\n</code></pre>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#usage-with-gate-based-services","level":2,"title":"Usage with gate-based services","text":"<p><code>CallRecorder</code> works equally well as a handler for gate-based registrations:</p> <pre><code>recorder = CallRecorder()\nrunner.add(\n    lambda now_ms: True,  # always fire\n    handler=recorder,\n)\nrunner.tick()\nassert len(recorder) == 1\n</code></pre>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#usage-from-other-libraries","level":2,"title":"Usage from other libraries","text":"<p>Libraries that use the runner pattern can import <code>CallRecorder</code> directly:</p> <pre><code># In another library's test file\nfrom chumicro_runner.testing import CallRecorder\n</code></pre> <p>This follows the project convention from Decision 0010: libraries that expose injectable services ship their own test fakes.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#api-reference","level":2,"title":"API Reference","text":"","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_runner.testing","level":2,"title":"<code>chumicro_runner.testing</code>","text":"<p>Test helpers for libraries that use chumicro-runner.</p> <p>Provides <code>CallRecorder</code> — a callable that records handler invocations for assertion in host-side tests.</p> <p>Usage::</p> <pre><code>from chumicro_runner.testing import CallRecorder\n\nrecorder = CallRecorder()\nrunner.add_periodic(recorder, period_ms=100)\n# ... advance time, tick() ...\nassert recorder.calls == [100]\n</code></pre>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_runner.testing.CallRecorder","level":3,"title":"<code>CallRecorder</code>","text":"<p>Callable that records each invocation for test assertions.</p> <p>Use as a handler passed to <code>Runner.add()</code> or <code>add_periodic()</code>::</p> <pre><code>recorder = CallRecorder()\nrunner.add_periodic(recorder, period_ms=100)\nrunner.tick()\nassert len(recorder) == 0  # not due yet\n</code></pre>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_runner.testing.CallRecorder.__init__","level":4,"title":"<code>__init__()</code>","text":"<p>Create an empty recorder.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_runner.testing.CallRecorder.__call__","level":4,"title":"<code>__call__(now_ms)</code>","text":"<p>Record a call with the given timestamp.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_runner.testing.CallRecorder.__len__","level":4,"title":"<code>__len__()</code>","text":"<p>Return the number of recorded calls.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_runner.testing.CallRecorder.clear","level":4,"title":"<code>clear()</code>","text":"<p>Discard all recorded calls.</p>","path":["Testing Helpers"],"tags":[]}]}