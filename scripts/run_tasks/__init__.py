"""Task modules for scripts/run.py.

Each module owns a group of subcommands plus a ``register(subparsers,
parents)`` hook that the thin ``scripts/run.py`` entry point calls to
build the top-level CLI parser.
"""
