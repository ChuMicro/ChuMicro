<?xml version="1.0" encoding="UTF-8"?>
<!-- Generated by scripts/run.py sync-ide.
     Tracked in git so source roots stay in sync across contributors.
     PyCharm may rewrite this file when interpreter or module settings change;
     if that happens, re-run `python scripts/run.py sync-ide` (or click the
     "Sync IDE" run configuration) to restore the managed layout before committing. -->
<module type="PYTHON_MODULE" version="4">
  <component name="NewModuleRootManager" inherit-compiler-output="true">
    <exclude-output />
    <content url="file://$MODULE_DIR$">
{sources}
      <excludeFolder url="file://$MODULE_DIR$/.venv" />
      <excludeFolder url="file://$MODULE_DIR$/.tools" />
    </content>
    <orderEntry type="inheritedJdk" />
    <orderEntry type="sourceFolder" forTests="false" />
  </component>
</module>
