  <style>
    .topbar {
      border-bottom: 1px solid var(--border);
      background: rgba(14, 15, 16, .92);
      position: sticky; top: 0; z-index: 10;
      backdrop-filter: saturate(140%) blur(6px);
    }
    .topbar-inner {
      max-width: 860px; margin: 0 auto; padding: .6rem 1.5rem;
      display: flex; align-items: center; gap: 1rem; flex-wrap: wrap;
    }
    .topbar .wordmark {
      display: flex; align-items: center; gap: .5rem;
      font-weight: 700; color: var(--text); text-decoration: none;
      margin-right: auto;
    }
    .topbar .wordmark img { width: 28px; height: 28px; border-radius: 50%; }
    .topbar a.nav { color: var(--muted); text-decoration: none; font-size: .92rem; font-weight: 600; }
    .topbar a.nav:hover { color: var(--accent); }
    .topbar a.nav-strong { color: var(--accent); }
    @media (max-width: 520px) { .topbar .wordmark span { display: none; } }
  </style>
  <nav class="topbar">
    <div class="topbar-inner">
      <a class="wordmark" href="$host/">
        <img src="$assets/chumicro-head.png" alt="">
        <span>ChuMicro</span>
      </a>
      <a class="nav" href="$host/ChuMicro/">Documentation</a>
      <a class="nav" href="$host/ChuMicro/guides/">Guides</a>
      <a class="nav" href="https://pypi.org/search/?q=chumicro">PyPI</a>
      <a class="nav nav-strong" href="https://github.com/ChuMicro">GitHub</a>
    </div>
  </nav>
