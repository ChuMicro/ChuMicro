<?xml version="1.0" encoding="UTF-8"?>
<!-- Initial hint generated by scripts/run.py sync-ide.
     PyCharm manages this file per-user (it is gitignored) and may rewrite it
     whenever the project SDK changes.  The generated {jdk_name} matches
     PyCharm's default naming for a .venv added via the UI.  If you register
     your interpreter under a different name, just pick it in
     File -> Settings -> Project -> Python Interpreter; PyCharm will update
     this file in place. -->
<project version="4">
  <component name="Black">
    <option name="sdkName" value="{jdk_name}" />
  </component>
  <component name="ProjectRootManager" version="2" project-jdk-name="{jdk_name}" project-jdk-type="Python SDK" />
</project>
