"""Test helpers for libraries that depend on chumicro-websockets.

Slice 4 of Decision 0045 fills this in with ``FakeConnection`` and
``FakeListener`` so client / server consumers can wire tests without
real sockets.  Until then the slot is reserved.
"""
