# Sample library notes

This package exists to prove the Chumicro workspace conventions:

- per-package packaging metadata
- shared source layout
- host-side `pytest` tests
- device-side test files
- a runtime seam that can work across CPython, MicroPython, and CircuitPython

