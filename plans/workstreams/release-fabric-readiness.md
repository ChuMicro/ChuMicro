# Workstream: release-fabric readiness — audit before first publish

Status: **second audit round + wave progress 2026-07-11** (see "Second audit" below).  A 13-agent fleet audit confirmed 52 findings (5 blockers); the mechanical and first-release-fatal fixes landed same day, the rest are decision items.  Experimental PyPI now 12/18 live; publisher attachment and token cleanup blocked on a PyPI web login.  Release dry-run validated end to end (run 29164070209, all 18 legs green).  Prior status: audited AND remediated 2026-07-05 — all five blockers fixed in code the same day; GitHub-side infra stood up (see Standup state below).

## Verdict

The mip/circup device-bundle path is coherent and publish-ready end to end (producer, validator, bundle_push branch, docs all agree).  Every other path has at least one defect that fires on the very first real release.  Five blockers, two policy decisions, and a GitHub-side prerequisite bill must clear before the first VERSION bump publishes.

## The fabric (source → mechanism → consumer)

| path | mechanism | state |
|---|---|---|
| PyPI experimental | release.yml auto on VERSION bump; `patch-experimental` renames | wired; **B1 + B2 break it** |
| PyPI stable | promote.yml manual per experimental tag, replays frozen source zip | wired; **no preflight gate, no concurrency group** |
| Device bundle (mip/circup) | release.yml/promote.yml → ChuMicro-Bundle[-Experimental], date tags + circup zips | **ready** |
| Workspace library channel | `chumicro-workspace library add/browse/update` ← ChuMicro-Libraries[-Experimental] `index.json` + tag tarballs | consumer + producer (`scripts/libraries_channel.py`) exist, contract matches byte-for-byte, **producer wired into NO workflow** (Decision 0078 unimplemented) — **B4** |
| Sister repo bootstrap | `run.py setup` → pip install stable `chumicro-workspace`/`-pytest-device`/`-checks` | **B3 + B5**; only dev-mode (`chumicro-dev.toml` → sibling checkout) works today — why blockers are invisible locally |
| Docs | docs_deploy → gh-pages `<lib>/{stable,experimental}/` | wired; `/stable/` alias only created by first promote |

## Blockers (code fixes in this repo unless noted)

- **B1 — `patch-experimental` name derivation** (`scripts/bundle_manager.py` ~line 780): builds `chumicro-{dir.name}` → `chumicro-http_server`, but `libraries/http_server/pyproject.toml` says `chumicro-http-server` → `sys.exit` on every http_server release; a failed matrix leg also skips `bundle` + `validate-mip` for the WHOLE run (partial publish: PyPI yes, device bundle no).  Fix: read `[project].name` from pyproject instead of deriving from the directory.
- **B2 — experimental wheels declare stable deps**: patch rewrites only the package's own name; `chumicro-mqtt-experimental` ships `Requires-Dist: chumicro-config` etc.  Uninstallable at bootstrap (no stable projects exist), silent cross-channel mixing after.  PyPI path only — the bundle path rewrites deps correctly.  Fix per Decision-point 1 below.
- **B3 — sister `run.py setup` never installs `[dev]`** (`run.py` + workspace `cli/setup.py` both do bare `pip install -e .`): pytest/ruff/chumicro-checks absent → `run.py test` dies "No module named pytest"; worse, `run.py lint` prints a hint and **returns 0** when tools are missing — green lint that lints nothing (this is also why a CHU008 violation ships in `projects/example_sensor/app.py` — a Decision-0108 pointer in user-facing template code).  The sister pyproject comment falsely claims setup installs dev extras.
- **B4 — workspace library channel never published**: wire `scripts/libraries_channel.py` into release.yml/promote.yml (it shares `bundle_push`) and create the two ChuMicro-Libraries repos + deploy keys.  Until then every regular-mode `library add/browse/update` 404s — the sister repo's dedicated library-acquisition path does not function off-bench.
  - **Wired 2026-07-05**: `libraries-channel` job added to release.yml (experimental repo, date-tagged) and promote.yml (stable repo, gated to library promotions).  New secrets `LIBRARIES_DEPLOY_KEY` / `EXPERIMENTAL_LIBRARIES_DEPLOY_KEY` (SSH push only — the consumer reads raw `index.json` + codeload tag tarballs, so no GH release / `BUNDLE_TOKEN` is needed).  **Semantic to bless**: the producer only supports a *whole-tree snapshot* (it wipes + rebuilds every library, no per-library overlay), so both channels publish the entire `libraries/` tree at once and the stable channel tracks main-as-of-last-promotion for **all** libraries — not just the one being promoted, and possibly ahead of stable PyPI.  A per-library accumulate mode would be a producer change if that divergence is unacceptable.
- **B5 — tag-as-idempotency written mid-pipeline** (release.yml): PyPI publish has no `skip-existing`; four distinct permanent re-run traps (publish-ok/tag-fail → re-publish rejected forever; tag-ok/zip-attach-fail → version can never be promoted; tag-ok/bundle-fail → device bundle never produced, detect skips forever; partial matrix across runs → succeeded legs never bundled).  Fix: `skip-existing: true` on the publish step AND make the tag the last write (or add resume logic).  `workflow_dispatch --libraries` is the manual escape hatch meanwhile.

## Design flaws (should fix, not first-run-fatal)

- promote.yml: **no preflight-gate** (stable = immutable PyPI with zero re-checks; source is the frozen tested zip, but rebuilt with current tooling) and **no concurrency group** (racing bundle pushes have no non-fast-forward retry).
- `release_matrix.py` releases **everything untagged**, not what changed: first bump publishes all 18 packages at once; no `0.0.0` floor guard (a freshly scaffolded library would ship on the next unrelated bump).
- A VERSION bump whose preflight-gate fails is stranded — a follow-up fix commit not touching VERSION doesn't re-trigger release.yml (paths filter).  Recovery: re-touch VERSION or dispatch.
- Docs/channel inversion: every user-facing install default (INSTALL.md, all library READMEs, landing page, sister README, `library add` default `--channel stable`) points at **stable**, which only exists after manual promotion.  See Decision-point 2.
- Sister `test.yml` header claims tool-owned but `template_zones.py` classifies `.github/workflows/` USER_OWNED → `update` skips it; CI fixes never propagate to existing workspaces.
- ~~mip dep refs pin `HEAD` in the bundle (`bundle_manager.py`), so an install pulls the dep's newest bundle state, not the version tested against.~~ **FIXED 2026-07-11**: `build_bundle` stages `HEAD` as an explicit placeholder; the new `finalize-tag` subcommand (called by both workflows in place of `next-date-tag`) pins every staged manifest's deps to the push's own date tag, with a pin-then-check sequence that keeps `--reuse-if-clean` re-run reuse byte-identical.  Manifests from earlier pushes keep their own snapshot pins; legacy `HEAD` pins heal on the next push.

## Decision points (user)

1. **Experimental-PyPI policy** — **RESOLVED 2026-07-05: option (a)** (user). `patch_experimental` (`scripts/bundle_manager.py`) now appends `-experimental` to the name portion of every intra-chumicro `[project].dependencies` entry (specifiers preserved), so the experimental channel stays pip-installable. Options as considered: (a) rewrite intra-chumicro deps to `-experimental` in the patch (keeps pip usable on the experimental channel; recommended) — **chosen**; (b) stop publishing experimental to PyPI entirely (bundle repos + source zips carry experimental; PyPI is stable-only via promote — halves the trusted-publisher count to 18 but removes the pip surface for early tool adopters).
2. **Launch story for stable** — the auto-experimental / manual-promote design is fine, but launch must include a **promotion wave** (all 13 active libraries + `chumicro-workspace`, `-deploy`, `-pytest-device`, `-checks`) or every documented instruction 404s on day one.  Alternative (not recommended): re-point all docs defaults at experimental.

## GitHub-side prerequisites (user; none verifiable from the bench account)

- Repos existing, default branch `main`: `ChuMicro-Bundle`, `ChuMicro-Bundle-Experimental` (+ `ChuMicro-Libraries`, `ChuMicro-Libraries-Experimental` once B4 is wired).
- Secrets on the source repo: `BUNDLE_DEPLOY_KEY`, `EXPERIMENTAL_BUNDLE_DEPLOY_KEY`, `BUNDLE_TOKEN` (PAT, contents:write on bundle repos), `GH_PAGES_DEPLOY_KEY` (promote docs job); `pypi` **environment** configured.
- PyPI trusted publishers (OIDC, per project): 18 experimental names via release.yml + 18 stable names via promote.yml = **36 pending-publisher configs** (drops to 18 under Decision-point 1b).  Parked `logging` excluded.
- Tag-protection/ruleset bypass for `github-actions[bot]` on `chumicro-*` tags.
- Branch-protection required-status-checks list still names the **old** workflow's 13 checks (observed in push output 2026-07-05: "13 of 13 required status checks are expected — bypassed"); replace with the new job names or prune to `preflight`.
- Sister repo hygiene before any publicity: untracked bench probes (`projects/frag_probe*`, `mqtt_bake*`, TLS `ca.pem`/`ca.der`, etc.) — `.gitignore` does NOT cover `projects/*`, one `git add -A` away from publishing bench material; tracked orphan `projects/wifi_only/project_config.toml` (app.py untracked).

## Standup state (2026-07-05, end of day)

Live inventory replaced the audit's assumptions: the April release infrastructure mostly survived.  Executed same day (Claude via chuxmaker gh where the permission model allowed, user via `.scratch/standup_release_infra.sh` otherwise):

- **Done**: repo-level Actions re-enabled (was the entire reason nothing fired since 2026-04-18 — workflows were individually active); main ruleset required checks retargeted to the new five (`preflight`, `compatibility (3.11/12/13)`, `Validate mpy bytecode`); fresh write deploy key + rotated secret for ChuMicro-Bundle-Experimental (repo key had been deleted); new `LIBRARIES_DEPLOY_KEY` / `EXPERIMENTAL_LIBRARIES_DEPLOY_KEY` pairs; empty Bundle-Experimental remote seeded with a `main` branch.  BUNDLE_TOKEN confirmed still valid (user).  `pypi` environment, `BUNDLE_DEPLOY_KEY`, `GH_PAGES_DEPLOY_KEY` already existed.
- **All five blockers + promote hardening landed in code** (B1/B2/B5 + concurrency + tag-last/skip-existing on promote too, 0.0.0 floor, `--include-tagged` escape hatch, libraries-channel jobs in both workflows, sister `[dev]` install + loud-fail lint + zone fix + hygiene).
- **New first-run trap found and fixed during review**: `bundle_manager.py` imports `chumicro_deploy` at module level (0090 strip reuse, post-dated the April green runs), so every job invoking it (release legs' patch-experimental, both bundle jobs, ci.yml validate-mpy) now runs `run.py setup` instead of bespoke pip installs.
- **Accepted-for-launch semantics**: the stable libraries channel is a whole-tree snapshot of main-at-promotion (producer has no per-library overlay), so it can run ahead of stable PyPI between promotions.  Livable while promotions happen in waves; a per-library accumulate mode is the follow-up if the divergence starts to bite.
- **PyPI reality (updated after the Chrome drive)**: 8 of 36 projects exist (compat / msgpack / runner / timing, both channels, old 0.1.x versions — no collision with current).  All 8 verified: every experimental project has exactly (`ChuMicro/ChuMicro`, `release.yml`, env `pypi`); every stable one has the right `promote.yml` publisher (4 leftover `release.yml` publishers from the pre-channel days pruned 2026-07-05, user-approved — least privilege now holds: experimental = release.yml only, stable = promote.yml only).  Pending publishers registered for the `chumicro-config` pair — and then PyPI's design wall: **pending publishers are unique per (repo, workflow, environment) tuple account-wide**, so a monorepo can pre-register at most ONE project per workflow ("A pending trusted publisher matching this configuration has already been registered for a different project name").  The remaining 26 CANNOT be pre-registered.  Bootstrap plan instead (no workflow changes needed): on launch day, build + twine-upload the first experimental wave locally with a scoped API token (creates the missing projects; the workflow's skip-existing makes the CI release re-run harmless), then add per-project trusted publishers (no uniqueness constraint at project level — the 8 existing already share configs), then delete the token.  Steady-state is pure OIDC.
- **Experimental wave, first pass (2026-07-05 evening)**: 8/18 fully live at current versions (4 pre-existing projects updated + `checks`/`config`/`deploy`/`http-server` created, per-project publishers attached and verified); stale `chumicro-config-experimental` pending row removed; remaining 10 blocked by PyPI's new-project creation limiter.  **Standing rule learned the hard way: PyPI project creation is a scarce resource — pace it like a human (minutes between creations, never burst) and treat a 429 as "stop for hours", not "retry".**  The first burst created 4 projects in seconds and tripped the limiter ("429 Too many new projects created"); repeated 15–30 min retries kept poking it.  Remaining 10 resume another day (user call) at 5-min-per-creation cadence, stop-on-first-429.  The stable promotion wave (17 more creations; `chumicro-config` comes via its pending publisher) gets the same pacing from the start.  Token cleanup after the wave: revoke on PyPI + delete `~/.pypirc` (token was surfaced into this session's transcript, so revocation is mandatory, not optional).
- **Experimental wave, second pass (2026-07-11)**: 12/18 live.  kvstore/mqtt/ntp/requests created from fresh HEAD builds (patch-experimental → build → twine, 5-min pacing); the 5th creation (sockets) tripped the limiter anyway.  **MEASURED same evening (instrumented probe, Ratelimit-Policy header): `project.create.user` and `project.create.ip` are both `q=4; w=86400` — exactly 4 new projects per rolling 24 h, per user AND per IP.  Production overrides warehouse's documented 20/hr default, resolving the second audit's uncertain finding 40.**  Pacing within a window buys nothing; the header's `t=` countdown gives the exact reset (2026-07-12 18:46 UTC for this window).  Remaining schedule at quota: 4 creations at reset + 2 the next day finish experimental; the 17-creation stable wave needs ~5 windows.  **User call 2026-07-11: no pypi/support limit-lift request — spread the creations over the quota windows** (precedent threads sit unanswered for weeks anyway; draft deleted).  Plan the stable promotion wave with the 4-per-24h budget in mind: publisher attachment and promote_wave.py dispatches follow each window's creations.  Publishers attached same evening after the user re-authenticated: all 12 experimental projects verified (ChuMicro/ChuMicro, release.yml, env pypi), all 4 stable verified (promote.yml, env pypi — the environment field the second audit flagged as unverified), chumicro-config pending publisher intact (promote.yml, env pypi).  Release workflow validated end to end same day via dry-run dispatch (run 29164070209: preflight-gate + all 18 build legs green, publish/tag/bundle legs correctly skipped) and re-disabled after.
- **Remaining 6 to create** (next wave day, budget ~4/day): sockets, websockets, wifi, pytest-device, then repl, workspace.  Procedure per package — copy the library dir to scratch, `python scripts/bundle_manager.py patch-experimental <copy>`, `python -m build`, verify with `twine check` + a METADATA glance (name suffixed, intra-chumicro Requires-Dist rewritten), `twine upload` — REBUILD FROM CURRENT HEAD ON THE DAY (never upload a dist snapshot older than the last commit touching that package; that trap already bit http_server).  workspace must build at ≥0.46.1.  After each creation: attach the per-project trusted publisher (ChuMicro/ChuMicro, release.yml, environment `pypi`).  When all 18 exist + publishers verified: revoke the bootstrap token on PyPI and delete `~/.pypirc`.
- **Open user decision**: ChuMicro-Bundle is PRIVATE; must go public before the first stable promotion (mip/circup fetch raw files anonymously).

## Second audit (2026-07-11, 13-agent fleet + adversarial verify)

Full verdicts: `.scratch/release-fabric-audit-2026-07-11.json` (71 raw → 55 merged → 52 confirmed / 2 uncertain / 1 refuted).

**Fixed same day (commit carries the details):**

- Frozen source zip was mutable and incomplete (confirmed blocker): release.yml now `git archive`s the FULL library tree, from the existing tag on `--include-tagged` re-runs; `release_source.py` wipes the package dir and verifies the archive before promote builds from it; `--include-tagged` now requires a scoped `--libraries`.
- Bundle validation validated staging alone, so any release/promotion whose intra-workspace deps weren't in the same run import-failed AFTER PyPI publish (confirmed blocker): both workflows now validate the MERGED clone+overlay tree.
- Bulk-dispatched promotion waves silently cancel all but two runs (confirmed blocker, shared `release` concurrency group): `scripts/promote_wave.py` dispatches one at a time in dependency order; releases.md documents it; both workflows carry the trap warning.
- check-version/check-api silently no-oped on every direct-to-main push (origin/main == HEAD): both now fall back to `HEAD^` via `repo_layout.effective_diff_base`.
- `circup install chumicro-<hyphen>` fails against the bundle's underscore directories: every install line in docs/READMEs/templates/examples swept to underscores; INSTALL.md troubleshooting corrected.
- release_matrix: unknown `--libraries` names now fail loudly (green no-op trap); VERSION content validated against `\d+.\d+.\d+`; patch-experimental is idempotent and handles single-line dependency arrays; stage-matrix `--matrix` help documented the wrong key.
- http_server VERSION 0.18.0 → 0.18.1: the live `chumicro-http-server-experimental` 0.18.0 wheel (uploaded 2026-07-05 16:09) predates the lazy-`__init__` rewrites (267e95ee, 18:24) at an unchanged version; the bump prevents release.yml minting a v0.18.0 tag whose tree mismatches the immutable wheel.  mqtt/requests do NOT need bumps — their projects were created 2026-07-11 from fresh HEAD builds.  Standing rule: any package whose src changed after its live experimental upload at an unchanged VERSION needs a bump before the first release dispatch.
- Docs-gate hole: promote.yml was still active (a fully wired stable publish path); now `disabled_manually` like Release + Deploy Documentation.
- stable-promotion issue template + releases.md showed the pre-rename tag format; labels.yml + bug_report.yml covered 4 of 13 libraries; releases.md anchor fix.

**User actions required:**

- Restrict the `pypi` environment to main (currently NO deployment branch policy — a workflow_dispatch from any ref can mint the OIDC token; permission classifier blocked the bench from setting it):
  `gh api -X PUT repos/ChuMicro/ChuMicro/environments/pypi -f "deployment_branch_policy[protected_branches]=false" -F "deployment_branch_policy[custom_branch_policies]=true"` then
  `gh api -X POST repos/ChuMicro/ChuMicro/environments/pypi/deployment-branch-policies -f name=main`
- Log into pypi.org (2FA) so the bench can attach trusted publishers for the new projects and revoke the bootstrap token.

**Second batch FIXED 2026-07-11 (findings 11, 12, 13, 14, 20, 22, 27, 38, 41 — commit 22b302f5, adversarially reviewed, four review catches folded in):** per-toolset .tools cache keys; promote docs job replays the frozen archive and is gated by has_docs computed from the TAG's tree; version-monotonicity guard + `allow_downgrade`; `resume` input for post-tag-death promotions (composes with allow_downgrade for superseded resumes); README links absolute + patch-experimental rewrites install-command lines; `next-date-tag --reuse-if-clean` + release-asset reconciliation via `upload --clobber`; py.typed in all five workbench packages (VERSION bumps: checks 0.15.1, deploy 0.37.1, pytest-device 0.17.5, repl 0.4.1, workspace 0.46.2); ssh-agent v0.10.0.  Review note recorded: the EXPERIMENTAL BUNDLE repo still carries stable-channel READMEs (the bundle job stages from an unpatched checkout) — pre-existing surface, not a regression.

**Confirmed, needs a decision (top of the list; full set in the JSON):**

- `[test]` extras are uninstallable on BOTH channels: they pin stable names (patch-experimental deliberately skips optional-dependencies) and every one requires `chumicro-test-harness`, which is never published anywhere — yet sdist_content.py's docstring states the curated-consumer contract depends on `pip install chumicro-<lib>[test]` working.  Options: publish test-harness (both channels) + rewrite extras in the patch; or strip intra-chumicro test extras from published wheels and drop the contract claim.  Related: ~27 advertised names (stable set + test-harness) are unregistered and squattable until the promotion wave.
- No published artifact carries the MIT license text (all 18 declare `License: MIT`, ship no file); fix needs per-package license-file wiring plus the full-tree zip (now landed) — coordinated change across 19 pyprojects.
- Coverage gate asymmetry (CI 85 vs release preflight-gate 94) can strand VERSION bumps; first wave applies 94 tree-wide.
- Supply-chain/design set: mutable action refs in the OIDC job (`release/v1` is a moving branch), workflow-scope `contents:write + id-token:write`, full dev toolchain installed in the token-minting job, no yank/rollback story on any channel, ~~HEAD-pinned mip dep refs~~ (fixed 2026-07-11, see Design flaws), intra-repo deps carry no version specifiers, parked libraries never leave the bundle, libraries-channel wipe destroys hand-added root files, multi-package `src/` half-bundles silently, no version→bundle-tag map, twine-bootstrapped versions lack PEP 740 attestations.

## Launch dress-rehearsal (2026-07-12, regular-mode consumer path)

Rehearsed the whole consumer path the launch acceptance test exercises ("sister CI green on a fresh runner"), the regular-mode bootstrap that has never run end to end because the stable PyPI packages don't exist yet.  Stood in for the missing stable channel with HEAD-built dists on `PIP_FIND_LINKS` (local versions outrank the stale 0.1.x on real PyPI, so local wins; third-party deps still resolve from PyPI).

**Exercised and passing:**

- **Stage 1 (build):** sdist+wheel for all 19 publishable packages (14 `libraries/*` with a VERSION including parked `logging`, 5 `workbench/*`) built into one flat dist.  `twine check` clean on every artifact.  No build failures.
- **Stage 2 (bootstrap + loop):** fresh `file://` clone carries no `chumicro-dev.toml` / `workspace.yml` / `secrets.toml` (all gitignored), so regular mode self-selects.  `run.py setup` green: `chumicro-workspace` / `-deploy` / `-checks` / `-pytest-device` resolved from the local dists, third-party from PyPI, CLI import verified, `devices.yml` / `workspace.yml` / `secrets.toml` materialized.  `run.py lint` green with both `ruff` and `chumicro-checks` actually running (B3 loud-fail-on-missing-tool confirmed).  `run.py new <project>` and `run.py new --library` both scaffold cleanly.  `deploy <clean-project> --dry-run` stages the boot-shim + import-graph file map; `deploy example_sensor --dry-run` refuses cleanly on its unfetched device libraries (coached, no traceback).
- **Stage 3 (libraries channel):** `scripts/libraries_channel.py` staged the channel (13 libraries, `logging` correctly excluded as parked) into a scratch git repo + a codeload-shaped tag tarball.  Drove the real consumer `library` CLI (`browse` / `add` / `list` / `update`) against it through the only injection point, `CliEnv.http_get`.  `resolve_snapshot` / `extract_library` / `fetch_closure` all correct; `add chumicro_mqtt` fetched the exact transitive closure {config, sockets, timing, msgpack} with complete trees; pinned-skip and `--floating` re-fetch both correct.  Producer<->consumer contract confirmed byte-correct end to end.

**Findings fixed:**

- **P0, acceptance-test blocker (sister `371864f`):** `projects/example_sensor/app.py` imported its device libraries (`chumicro_config` / `kvstore` / `mqtt` / `runner` / `wifi`) at module top.  The workspace smoke test imports every `projects/*/app.py`, and in regular mode those libraries are not host-installed, so `run.py test` failed on every fresh runner; only dev-mode (editable sibling installs) ever passed, which is why it stayed invisible on the bench.  Fixed by deferring the imports into `run()` (matches the `_template` boot-time-setup guidance; deploy still stages them because its resolver AST-walks function bodies).
- **Sister `15f989c`:** the project scaffold gives every project a `tests/test_app.py`, and pytest's default prepend import made the second project's copy collide with the first (shared module name, no package `__init__.py`), aborting `run.py test` collection.  A user hits it on their first `new` (colliding with shipped `projects/_template`).  Fixed with `--import-mode=importlib` in the template's pytest `addopts`.
- **Monorepo `016c5309` (`chumicro-workspace` 0.48.1):** `deploy` against a fresh workspace's empty `devices: []` dumped `load_devices_yml`'s raw `ValueError` traceback.  `cli/_common.py:_resolve_device` now converts it (and the other device-resolution `ValueError`s) into a coached one-line exit, fixing `deploy` / `repl` / `firmware` / `examples` uniformly.

**Findings open (recorded, not redesigned on the fly):**

- **P0 structural (channel seam):** no consumer-facing seam (env var, config, `workspace.yml` key, CLI flag) redirects the library channel off GitHub; `library_channel.py` hard-codes `raw.githubusercontent.com` / `codeload.github.com` and `CHANNEL_REPOS`, and the only override is the test-only `CliEnv.http_get`.  So the library-acquisition path cannot be integration-tested by a real consumer before the `ChuMicro-Libraries[-Experimental]` GitHub repos go live (B4).  Validated in-process here instead.
- **Medium (lint-after-`library add`):** the workspace template pins ruff `line-length = 100`, but curated libraries are authored under the monorepo's `line-length = 110`, and `libraries/` + the `_library-backups/` re-fetch backups are not in the template's `extend-exclude`.  So `run.py lint` fails on fetched library source after any `library add`.  Design call: align the template to 110, or exclude fetched-library trees + backups from the workspace lint.
- **Low (scaffold branding):** `new --library` neutralization drops the banner, family note, bundle links, project URLs, and docs footers, but still stamps the downstream package with the `chumicro-` name prefix (`scaffold.py:83`, `:176`) and ships "ChuMicro library" worked-example prose (`readme.md.template:78`, `core.py.template:5`).  Design call on whether a downstream library should carry the prefix.
- **Minor (isolation leak):** `deploy`'s unresolved-import refusal hint tells the user to edit `DEVICE_BUILTIN_MODULES in chumicro_deploy/import_allowlist.py`, a mono-repo-internal path a pip-installed consumer can't reach.

**Verdict:** With `371864f` + `15f989c` landed and the stable promotion wave published, sister CI (checkout -> `setup` -> `lint` -> `test`, no board) would go green on a fresh runner.  Without the `example_sensor` fix it goes red on every runner.  Everything the acceptance test touches now passes against published-package resolution; `deploy` and the library channel work but stay gated on their GitHub repos being live (bundle visibility, B4 channel repos) and on the line-length reconciliation for a clean post-`add` lint.  KNOWN-deferred and unchanged: `pip install chumicro-<lib>[test]` still fails (test-harness unpublished), hardware-gated deploy, the PyPI project-creation quota.

## Sequence

1. ~~Verify first ci.yml run green~~ — VERIFIED 2026-07-05: run 28752439230 all five checks green.  The three shakedown failures were each real catches (platform-gated wedge test, threadbare compatibility install, tarfile filter deprecation fatal only on 3.12/3.13 under `-W error`) — fixed at the root, no lanes relaxed.
2. ~~Land the monorepo code fixes~~ — DONE 2026-07-05 (B1, B2, B5 + promote skip-existing/tag-last, concurrency, 0.0.0 floor, B4 wiring, run.py-setup in every bundle_manager-invoking job).
3. ~~Land the sister/workspace fixes~~ — DONE 2026-07-05 (B3 `[dev]` install + loud-fail lint, test.yml zone classification, CHU008 fix, tree hygiene, wifi_only orphan).
4. GitHub/PyPI prerequisites — GitHub side DONE except the Bundle visibility flip (deferred, see 5); PyPI publishers (verify 8 + register 28) can happen any time, they expose nothing.
5. **DOCS GATE (user call 2026-07-05): nothing goes public until the docs/guides redo lands.**  `Release` and `Deploy Documentation` workflows are `disabled_manually` so an accidental VERSION bump or push can't publish to PyPI/bundles/gh-pages meanwhile; CI stays active.  The docs redo also absorbs the channel-inversion fixes (D1) and the launch promotion-wave story.
6. Going public (after the docs redo): re-enable both workflows, flip ChuMicro-Bundle public, then first release: one VERSION bump → experimental wave (expect all 18) → verify bundle + validate-mip + libraries-channel → promotion wave per Decision-point 2 → docs `/stable/` aliases live → sister CI green on a fresh runner is the acceptance test.
