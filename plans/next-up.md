# Next Up

> Work queue.  One bullet per item, no sub-bullets — anything needing more than a title goes to [`workstreams/<name>.md`](workstreams/) and surfaces here as a one-line pointer.  Tracks status, not research.  No `## Done` section — `git --no-pager log` carries history.

## Now

- [ ] **check/handle-vs-generators re-pose — the last open thread of the realignment campaign.**  Field-data census + three-way verdict (flip / keep / reshape) in flight 2026-07-04; report lands in `plans/reviews/`.  Campaign record: [workstreams/core-design-realignment.md](workstreams/core-design-realignment.md) — everything else shipped (ADRs 0095–0099 accepted; sockets 0.17.1 closed the TLS-handshake `io_interest` residual 2026-07-04).
- [ ] **Bench: swap the s2-cp board's USB cable before the next session** — four wedges in two days (2026-07-03/04) across three distinct failure modes while its three bench-mates logged zero; if a fifth wedge shows on the new cable, swap the board itself.

## Next

- [ ] **Wire `Runner(on_handler_error=...)` fault reporting into the demo apps + sister-repo flagship.**  Death *recording* shipped long ago (runner 0.14.0, `407700b7`: `handle.error` set on a raised body, re-raised by `run_until`; fire-and-forget deaths also count into `handler_errors` and reach the hook) — but no demo or template app wires the hook, which is why the 2026-07-03 websockets bake could still lose a generator death to a marker timeout.  One print-the-fault callback per app turns that into a one-line diagnosis.  Re-bake demos after.
- [ ] **mqtt bloat review (direct read, no skill).**  Post-0099 measurement 2026-07-04: `client.py` 2008 + `_wire.py` 825 = 2833 lines vs the ~1043-line reference impl — still ~2.7× even after the pattern router + intact tier deletions.  Capability-paid-for size includes the two-tier decoder, multi-pending-response, backpressure-as-error, pre-connect queue, and self-heal; candidates to weigh: `from_config` plumbing, the `WhenOversized` policy shape, docstring weight.  Separate capability from fat before publication.
- [ ] **Cooperative-tick convergence residuals: self-heal cost sandboxing + recv-N-vs-send-1 rate convergence (with its PUBACK-backlog guard).**  [workstreams/chumicro-mqtt-runner-cooperative-tick-convergence.md](workstreams/chumicro-mqtt-runner-cooperative-tick-convergence.md) — steps 1–8 landed + negative-bake-validated 2026-05-23 (header trued 2026-07-04); Decision 0098's tick-driven connector now provides the machine to spread self-heal across ticks.
- [ ] **MQTT negative-testing suite.**  [workstreams/mqtt-negative-testing-suite.md](workstreams/mqtt-negative-testing-suite.md) — audited 2026-07-04: written at protocol/behavior level, unaffected by 0099's deletions; A5/A8 (session resumption, subscription survival) are strengthened now that CONNACK session-present is honored.
- [ ] **Wifi-recovery bake — gate LIFTED 2026-07-04.**  The sister flagship (`projects/example_sensor/app.py`, campaign Wave 4) now drives `WifiService` + `MQTTClient` from the runner.  Re-queue: reboot the router mid-bake against it; confirm `WifiService` re-associates and the MQTT connector resumes from FAILED.  (The old `mqtt_bake_diag_plain` harness still calls raw `wlan.connect()` — don't measure against that one.)
- [ ] **`websockets` oversize-frame length on Lolin S2.**  [workstreams/websockets-oversize-frame-lolin-s2.md](workstreams/websockets-oversize-frame-lolin-s2.md) — hardware-gated.  websockets is now 0.24.0 and the 0.22.0 "honest recv budget" rework touched exactly the buffer accounting the workstream fingers, so re-measure on the board before debugging from the old 600-vs-(0,400] numbers.
- [ ] **Device test matrix membership** — user weighing esp32 (baseline), esp32-s3, and/or another pico; adding a board is just a `devices.yml` entry.  Sweep machinery is done (`sweep-devices` runs demo + functional + workbench layers; all four boards green 2026-07-04).
- [ ] **Re-evaluate Decision 0041 §8 — CP-rp2 TLS server ban — after CircuitPython 10.3.0 ships.**  Checked 2026-07-04: [adafruit/circuitpython#10339](https://github.com/adafruit/circuitpython/issues/10339) still open, milestone 10.3.0, latest release 10.3.0-alpha.3 (2026-06-24); bench pins 10.2.0.  When 10.3.0 lands: re-bench `listen_tls` on Pico W CP; if the ban lifts, drop the platform check + update 0041 §8 + the `docs/guide.md` runtime-support table.
- [ ] **Decision 0062 corrective: rerun the entrypoint-factory bench and refresh the 12-file table** — see `## Corrective ADR work` in [workstreams/walker-unresolved-import-failure.md](workstreams/walker-unresolved-import-failure.md).  Audited 2026-07-04: the sibling import-cleanup precondition shipped (`73568121` / `678c3094`), and 0098's 12→8 connect collapse + fleet-wide `transport_factory=` rename make the stale table more misleading — unblocked and more warranted than when filed.
- [ ] **requests streaming-body API (open design).**  The chunked-body O(n²) got its interim geometric-growth mitigation in campaign Wave 0; the real streaming surface is undesigned — see the hot-path buffer audit §4 ([reviews/2026-07-03-hot-path-buffer-audit.md](reviews/2026-07-03-hot-path-buffer-audit.md)).
- [ ] **test_harness promotion (Decision 0082) — deliberately parked.**  The `chumicro_test_harness.network` submodule + endpoint taxonomy (0083) shipped; the tree move to `libraries/` + `kind` field stays deferred until a downstream consumer needs it (0082 records "promotion has not landed" honestly).  [workstreams/test-harness-promotion-and-network-helper.md](workstreams/test-harness-promotion-and-network-helper.md)
- [ ] **Deploy-path unification — Phases 4–5 only.**  [workstreams/deploy-path-unification.md](workstreams/deploy-path-unification.md) — Phases 0–3 done: every entrypoint (`deploy`, `deploy --tail`, `deploy-example`, `demo`) routes through the one `Deployer.deploy_diff()` primitive; `repl` no longer stages code; `install-libraries` deleted (Decision 0078).  Remaining: `packages/` fate + CHU lint mechanization.
- [ ] **Missing ADRs (drift-audit debt, not gated):** the ~2,500-line `webui/` picker subsystem, the ship-channel manifest contract, `Runner.run_until`, CHU029/031/032.
- [ ] **Performance + resource benchmarking infrastructure** — heap + CPU per library op, regression gates, on-schedule CI.  First consumers: the runner reactor loop + websockets frame parser under load (the 2026-06 deep-review residual re-verify).
- [ ] **CI infrastructure workstream (unscoped — fires when CI is re-enabled).**  [workstreams/archive/audit-remediation-and-drift-mechanization.md](workstreams/archive/audit-remediation-and-drift-mechanization.md)
- [ ] **usage-path lens: contract-aware tracing.**  Replace name-grep with LSP find-references (retires `GENERIC_NAME_FILE_CAP = 15` — still 15 as of 2026-07-04 — which makes `tick`/`handle`/`run` untraceable), and prompt the judges toward risk-sampled contract-edge scenarios rather than enumerating every implementation of a generic name.  Deferred sub-idea: a POLICY_FACTS seed still needs a rethink — ADRs carry no structured invariant field to pre-populate from.
- [ ] **Comment-fact spot-check: websockets + wifi only.**  The old restoration bullet is largely moot — the named mqtt facts (recv-budget rationale, 65535 `OverflowError`, §4.3.2 DUP ref) and timing's wrap-period docs all verified PRESENT 2026-07-04, post-campaign rewrites.  Residual: read websockets + wifi docstrings for audit-dropped non-derivable facts.  Deferred (user call — comment-content work stays user-present).
- [ ] **Strict-no-body docstring rule: codify + realign /audit-comments + body-slim sweep.**  [workstreams/strict-no-body-realignment.md](workstreams/strict-no-body-realignment.md)  Deferred (user call — comment-content work stays user-present).
- [ ] **Comment-generation round 18 (guided vs bare no-guidance control) — built, awaiting dispatch + analysis.**  [handoffs/2026-05-30-comment-generation-round18-guided-vs-bare.md](handoffs/2026-05-30-comment-generation-round18-guided-vs-bare.md)  Deferred (user call — clean-room spend stays user-present).
- [ ] **Deferred clean-room and audit benches, gated on bench-token spend.**  [workstreams/deferred-clean-room-benches.md](workstreams/deferred-clean-room-benches.md)  Deferred 2026-06-12 (user call).
- [ ] **regen-comments interactive layer: cold-session gate walk (C1–C14)** per `TESTPLAN.md` layer C, plus one 2-voice comparison run before trusting library-scale work; layers A+B+D and C15 were validated live 2026-06-10.  User-present.
- [ ] **regen-comments voice-pick menu rework** — user feedback at the 2026-06-12 demo gate ("not a fan of the way the voices was laid out"); collect what grates and rework SKILL.md's voice-pick-menu section.  Rider for the same pass: audit-skill's own voice-question label (`SKILL.md:89`, "Numbered voice — type the menu number under Other") trips the ask-wording craft check and needs rewording.
- [ ] **regen-comments: voiced speaker for the report's prose bits.**  [workstreams/regen-report-voiced-speaker.md](workstreams/regen-report-voiced-speaker.md) — G (domain-fluent learn-then-speak) leads pending the user's voice verdict.
- [ ] **audit-code + audit-branch pipeline continuity.**  [workstreams/audit-pipeline-continuity.md](workstreams/audit-pipeline-continuity.md)
- [ ] **Retrofit `/audit-library` with the walk-pattern + verifier-subagent** (anchors verified 2026-07-04: `.github/skills/_shared/walk-pattern.md`, `.claude/agents/audit-comments-verifier.md`); design the ESCALATE-finding home before the retrofit lands.
- [ ] **Reconcile + retire the `voice-writers-validation` branch.**  Commits `b61f6df2` (shared dark-override theming) + `52934e58` (voice-compare onto the shared picker) exist only on `origin/voice-writers-validation`, not on main; the syn3 checkpoint handoff lives ON that branch, never merged (it is not in main's `plans/handoffs/`).  Conflicts with main on `regen-comments/SKILL.md` + `render_compare.py`; the branch's voices revert is obsolete (who-form retained, 2026-06-20 user call).
