# Next Up

> Work queue.  One bullet per item, no sub-bullets — anything needing more than a title goes to [`workstreams/<name>.md`](workstreams/) and surfaces here as a one-line pointer.  Tracks status, not research.  No `## Done` section — `git --no-pager log` carries history.

## Now

- [ ] **Deep code review remediation (2026-06): 1 CRITICAL (runner fault isolation) + 11 HIGH + 18 MEDIUM across runner / test-infra / http+ws security / tick-budget / alloc / cross-runtime / workbench.** [`workstreams/deep-code-review-remediation.md`](workstreams/deep-code-review-remediation.md) (report: [`reviews/2026-06-13-deep-code-review.md`](reviews/2026-06-13-deep-code-review.md)). Status: code complete; Phases 1-9 shipped (DEP-4 deferred — see the dedup bullet below). Pending only: real-board bakes for RUN-1 / SOCK-2 / WS-1 / WS-2 / MQTT-3. [`workstreams/deep-code-review-remediation.md`](workstreams/deep-code-review-remediation.md) (report: [`reviews/2026-06-13-deep-code-review.md`](reviews/2026-06-13-deep-code-review.md)). Status: code complete; Phases 1-9 shipped (DEP-4 deferred — see the dedup bullet below). Pending only: real-board bakes for RUN-1 / SOCK-2 / WS-1 / WS-2 / MQTT-3.
- [ ] **Deploy-bundle bloat, minimum-tier flash budget: steps 1+2 shipped; CA-bundle lever blocked on a data-file runtime-marking decision.**  [workstreams/deploy-bundle-bloat-min-tier.md](workstreams/deploy-bundle-bloat-min-tier.md) — step 1 (strip docstrings and comments on every device-stage path, [Decision 0090](decisions/0090-deploy-strips-docstrings-and-comments.md)) shipped and baked 2026-06-13: chumicro_sockets `.py` dropped ~57→~12 KB on the board and the Pico W CP unit sweep that overflowed now fits.  Step 2 (demo deploys stage only the harness bootstrap closure, dropping ~6.7 KB on-flash) shipped host-side 2026-06-14, Pico W CP demo bake pending.  Step 3 reframed: the 16 KB CA `.der` is MicroPython-only (CP uses its firmware x509 bundle), so a CP deploy should drop it.  A data-file marking convention now exists — `__chumicro_data_files__` on a module, read by the deploy walker (added 2026-07-03 for C7); step 3 can build on it to skip the `.der` on CP target runtimes.
- [ ] **Generator (`yield from`) I/O surfaces for the networking libraries + MQTT demo cleanup.**  [workstreams/generator-networking-apis.md](workstreams/generator-networking-apis.md) — Phase 1 (socket helpers relocated to `chumicro_sockets.generators`) landed 2026-06-13.
- [ ] **Comment-generation round 18 (guided vs bare no-guidance control; honest naming; 4 guided voices + 2 bare) — built, awaiting dispatch + analysis** — see [`handoffs/2026-05-30-comment-generation-round18-guided-vs-bare.md`](handoffs/2026-05-30-comment-generation-round18-guided-vs-bare.md).  Status: deferred 2026-06-12 (user call — clean-room spend stays for a user-present session).
- [ ] **Post-fix hardware+UX remediation (2026-06): shipped Phases 0 + A–D (library correctness, workbench failure-handling + coaching, hardware-reality docs).** [workstreams/post-fix-hardware-ux-remediation.md](workstreams/post-fix-hardware-ux-remediation.md). Residual: the runner-tick (L2) real-board bake folds into the RUN-1 bake above; a top-level-only import-graph hard-reset scan (W8) is a deferred follow-up.

## Next

- [ ] **Full-sweep audit remediation (2026-07-02/03): shipped fixes across all 15 libraries + workbench** — 5 criticals + ~all highs from the 144-agent audit, re-verified against current main then fixed with cross-runtime tests; end-to-end validated on both Pico W boards (sockets echo demo) + mqtt/sockets unit sweeps on MP silicon.  Deferred lower-tier items are called out in each commit body; the ones needing their own follow-up are the four bullets below.
- [ ] **Unix-port test lane runs the host's multi-MB heap (H37)** — `chumicro_pytest_device` `UnixPortBackend` spawns the MP unix port with no `-X heapsize`, so import-time / test-time OOM on a 264 KB board is invisible to preflight (the RAM-only-dev-loop gap).  Needs per-library heap budgets (some test files legitimately exceed 192 KB) — its own workstream, not a one-line flag.
- [ ] **Runner/task API design pass — four decisions briefed, awaiting user verdicts.**  [workstreams/runner-api-design-pass.md](workstreams/runner-api-design-pass.md) — M49 task-error surface, M51 registration shapes, M31 mqtt pump facade, factory/from_config contract (absorbs ntp L52/L53 + M77); demos L9's unwrap half already resolved by runner 0.13.0's central `.sock` unwrap.
- [ ] **Runner M49 — record generator/task death instead of silent `done`** — promoted by the 2026-07-03 websockets bake: the demo generator died on its first text frame and the only symptom was a marker timeout; an `on_task_error` hook (or a stored `handle.error` re-raised on join) turns that into a one-line diagnosis.  Pairs with the escalated API design pass.
- [ ] **Ship-channel manifest unification — one resolver decides what files a package ships per target.**  [workstreams/ship-channel-manifest-unification.md](workstreams/ship-channel-manifest-unification.md) — absorbs the test-support wheel/mip/circup exclusion policy and deploy-bundle-bloat step 3 as its first two consumer wins.
- [ ] **Final generator-I/O review (2026-06-13): G1 (runner io_error isolation) + G2 (sockets EAGAIN-slice alloc) + G3 (deadline threaded through `generators.connect`, shipped in sockets 0.14.0 2026-07-03) done; open — G4 the async-ban rule covers `workbench/` but its rationale says device-bound + cross-runtime while `scripts/` is absent (narrow scope or fix the rationale). G1's real-board check folds into the RUN-1 bake.** [`reviews/2026-06-13-generator-io-final-review.md`](reviews/2026-06-13-generator-io-final-review.md).
- [ ] **Deploy transport dedup (deep-review DEP-4): extract `_drop_active_mount()` + `_exec_or_classify()` and hoist the duplicated device-side scripts to a shared `_device_scripts.py` across `circuitpython_transport.py` + `micropython_transport.py` (~3.4k duplicated lines).** Behavior-preserving; deferred from the remediation pass because it needs real-board deploy validation on both runtimes. Plan: DEP-4 in [`reviews/2026-06-13-deep-code-review.md`](reviews/2026-06-13-deep-code-review.md).
- [ ] **regen-comments interactive layer: cold-session gate walk (C1–C14).**  The writer/selector/auto-route interplay HAS now run against real `claude -p` passes (TESTPLAN layer A+B+D sweep 2026-06-10: 13 phase-1 rooms, 7 phase-2 rooms incl. --tight/--less, autoroute fired live, B8 refine-shape held, 20/20 trigger evals; two pipeline defects found+fixed: zero-fact ledger handoff, docstring-only-body stray `pass`), and C15's served-report submit loop was validated live with real browser submits the same day.  Remaining: the human-driven gates — voice menu, picker, refine loop, apply/discard — per `TESTPLAN.md` layer C (C1–C14), plus one 2-voice comparison run before trusting library-scale work.
- [ ] **Deferred clean-room and audit benches, all gated on bench-token spend.**  [workstreams/deferred-clean-room-benches.md](workstreams/deferred-clean-room-benches.md) — three runs held for a spend-approved session: regen-comments round-35 prompt-package variance, audit-code/branch register-sample injection at n≥5, and `/audit-skill` across the over-budget skills.  Status: deferred 2026-06-12 (user call — bench spend).
- [ ] **regen-comments voice pick menu reads badly at the gate.**  User feedback at the 2026-06-12 demo gate ("not a fan of the way the voices was laid out"); collect what grates (spacing, preview placement, length, ordering) and rework the menu format in SKILL.md's voice-pick-menu section.
- [ ] **regen-comments: voiced speaker for the report's prose bits.**  [workstreams/regen-report-voiced-speaker.md](workstreams/regen-report-voiced-speaker.md) — prompt shape raced A–G 2026-06-12, G (domain-fluent learn-then-speak) leads pending the user's voice verdict.
- [ ] **audit-code + audit-branch: pipeline continuity — persistence, agent-driven baseline carryover, waiver ledger, incremental re-audit.**  [workstreams/audit-pipeline-continuity.md](workstreams/audit-pipeline-continuity.md) — directions decided in the 2026-06-12 audit-skill discussion; absorbs the prior baseline-re-audit bullet.
- [ ] **usage-path lens: contract-aware tracing.**  Replace name-grep with LSP find-references (retires `GENERIC_NAME_FILE_CAP = 15`, which makes `tick`/`handle`/`run` untraceable), and prompt the judges toward contract-edge scenarios sampled by risk — e.g. which call sites break if `ticks_diff` wraps during a 3-day window — rather than enumerating every implementation of a generic name (2026-06-12 discussion).  Deferred sub-idea: a POLICY_FACTS seed for this in-session lens (no clean-room contact) still needs a rethink, since ADRs carry no structured invariant section to pre-populate from — zero `## Invariant` headings, ~10 of 88 mention it in prose — so seeding means hand-curation or first adding an ADR field.
- [ ] **Restore audit-trimmed facts in degraded library comments.**  Subtractive `audit-comments` passes dropped non-derivable facts (wrap magnitudes, byte caps, protocol refs) from docstrings.  timing 0.4.5 restored its ~6.2-day wrap period, the ~3.1-day `ticks_diff` limit, and the big-int rationale that the 0.4.4 cold-write rewrite (`d139e882`) had deleted.  Likely also affected: mqtt `client.py` (recv-budget rationale, 65535-id `OverflowError`, MQTT §4.3.2 DUP-flag ref), websockets, wifi.  Approach under evaluation: *fix* existing comments (read for facts, rewrite prose) rather than cold strip-and-rewrite, which caused the loss.  Status: deferred 2026-06-12 (user call — comment-content work stays for a user-present session).
- [ ] **Strict-no-body docstring rule: codify + realign /audit-comments + body-slim sweep.**  [workstreams/strict-no-body-realignment.md](workstreams/strict-no-body-realignment.md) — surfaced by the kvstore retrofit-test (commit `2356830e`); audit-comments dim 4 / dim 6 allow bodies that the verifier persona flags CRITICAL; resolution chosen 2026-05-26 = strict no-body is canonical.  Workstream covers AGENTS.md codification, audit-comments dimension realignment, re-run validation on kvstore, and per-library body-slim sweep with content lift to `docs/guide.md`.  Status: deferred 2026-06-12 (user call — comment-content work stays for a user-present session).
- [ ] **Retrofit `/audit-library` with the walk-pattern + verifier-subagent** — proven on `/audit-comments` 2026-05-26; reference `.github/skills/_shared/walk-pattern.md` for the walk and pattern the verifier persona on `.claude/agents/audit-comments-verifier.md`.  Retrofit lever is steps 5 (present punch-list) and 7 (execute medium-confidence items) per the original session handoff.  ESCALATE handling is a separate unresolved gap — today's one-line `## Next` bullet loses the file:line + dimension + surrounding-code context the auditor had; design the home (`plans/audits/<library>-<date>.md`, workstream-attach, or in-place enrichment) before this retrofit lands.
- [ ] **Re-evaluate Decision 0041 §8 — CP-rp2 TLS server ban — after CircuitPython 10.3.0 ships.**  The wedge is tied to [adafruit/circuitpython#10339](https://github.com/adafruit/circuitpython/issues/10339), still open 2026-06-12 with milestone 10.3.0; bench boards run 10.2.0, so a re-bench now would re-measure a known-unfixed substrate.  When 10.3.0 lands on the bench: re-bench `listen_tls` on Pico W CP, and if the ban can lift, drop the platform check + update Decision 0041 §8 + the `docs/guide.md` runtime-support table.
- [ ] **chumicro_mqtt + chumicro_runner cooperative-tick convergence.**  [workstreams/chumicro-mqtt-runner-cooperative-tick-convergence.md](workstreams/chumicro-mqtt-runner-cooperative-tick-convergence.md)
- [ ] **MQTT negative-testing suite.**  [workstreams/mqtt-negative-testing-suite.md](workstreams/mqtt-negative-testing-suite.md)
- [ ] **Wifi-recovery bake — gated on `chumicro_wifi.WifiService` adoption in the bake.**  Today's `projects/mqtt_bake_diag_plain/app.py` calls a raw `wlan.connect()` / `wifi.radio.connect()` at boot and never reconnects.  A router-reboot test against that harness would mostly measure the substrate's auto-rejoin behaviour, not our library.  When the bake (or any real project) starts driving `chumicro_wifi.WifiService` from the runner, re-queue: reboot the router mid-bake, confirm `WifiService` re-associates + the MQTT connector resumes from FAILED.
- [ ] **`/audit-library libraries/mqtt` for bloat** — chumicro_mqtt is ~2x the reference impl's size (`client.py` 1283 + `_wire.py` 889 = 2172 lines vs ~1043).  Some of the size is legitimate (three-tier decoder, multi-pending-response, pattern handlers, backpressure-as-error), but past the publish/subscribe-raw collapse there's likely more: `from_config` + sockets-factory plumbing is another ~100 lines that could move to a separate helper, the `WhenOversized` policy might fold into a callback, etc.  Identify the actual bloat vs. capability-paid-for size.
- [ ] **`/audit-embedded chumicro_runner.core` and `chumicro_websockets`** — largely covered by the 2026-06 deep code review: `RUN-1` (runner per-handler fault isolation), `WS-1` (websockets hot-path alloc), and `WS-2` (send-path memoryview reuse) shipped, and both libraries were read against the embedded rubric (≤5 ms tick, gc policy, alloc, slots/property bans).  Residual is an optional bench-driven re-verify of the runner reactor loop and the websockets frame parser under load, lower priority now.  See [`reviews/2026-06-13-deep-code-review.md`](reviews/2026-06-13-deep-code-review.md).
- [ ] **CI infrastructure workstream (unscoped — fires when CI is re-enabled).**  [workstreams/archive/audit-remediation-and-drift-mechanization.md](workstreams/archive/audit-remediation-and-drift-mechanization.md)
- [ ] **`websockets` oversize-frame length on Lolin S2.**  [workstreams/websockets-oversize-frame-lolin-s2.md](workstreams/websockets-oversize-frame-lolin-s2.md)
- [ ] **Device test matrix: restore the Pico W CP bench board + fold the S2 pair into routine sweeps.**  4-board bench registered on this laptop 2026-06-12 (pico-w cp+mp, s2-mini cp+mp; deploy demo green on all four).  The pico-w-cp board reproducibly drops off USB during flash-staging sessions even after `reset-board` (sockets staged fine, runner staging kills it; custom 10.2.0-dirty build of 2026-05-22) — reflash stock firmware via `install-firmware` and re-run `test-unit-on-device --library runner`, then make the S2 boards part of the routine sweep + functional matrix.
- [ ] **Performance + resource benchmarking infrastructure** — heap + CPU per library op, regression gates, on-schedule CI.
- [ ] **Deploy-path unification — one mechanism puts code on a board.**  [workstreams/deploy-path-unification.md](workstreams/deploy-path-unification.md)
- [ ] **test_harness promotion + `chumicro_test_harness.network` + endpoint-taxonomy alignment (Decisions [0082](decisions/0082-test-harness-as-infrastructure-library.md) + [0083](decisions/0083-functional-test-endpoint-taxonomy.md)).**  [workstreams/test-harness-promotion-and-network-helper.md](workstreams/test-harness-promotion-and-network-helper.md) — supersedes the previous `examples/helpers.py` drift + `functional_tests/` conftest-dedup workstreams (both absorbed).
- [ ] **audit-skill's own voice-question labels trip the new ask-wording craft check.**  `SKILL.md:88-89` prescribes the option label `"Numbered voice — type the menu number under Other"`, an em-dash gluing an instruction that `ask_gate.py` bounces at runtime and the 2026-06-14 craft-lens check now flags at audit time; rewording belongs with the voice-menu UX pass, not inline.
- [ ] **Decision 0062 corrective follow-up: rerun the entrypoint-factory bench and refresh the 12-file table.**  Kept open by the walker fail-fast workstream (shipped 2026-06-12); see the `## Corrective ADR work` section of [workstreams/walker-unresolved-import-failure.md](workstreams/walker-unresolved-import-failure.md).
- [ ] **Reconcile + retire the `voice-writers-validation` branch.**  Its two webui commits (`b61f6df2` shared dark-override theming, `52934e58` voice-compare onto the shared picker) and the syn3 convergence handoff (`plans/handoffs/2026-06-14-voice-writer-syn3-checkpoint.md`) still need to reach `main`; the webui side conflicts with `origin/main` on `regen-comments/SKILL.md` and `render_compare.py`, and the branch's voices revert is obsolete now that who-form is retained (2026-06-20 user call, since the 2026-06-14 trait-cluster convergence was an unverified single-session harness claim against machinery, samples, and previews all built for who-form).
