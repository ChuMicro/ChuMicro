# Next Up

> Work queue.  One bullet per item, no sub-bullets — anything needing more than a title goes to [`workstreams/<name>.md`](workstreams/) and surfaces here as a one-line pointer.  Tracks status, not research.  No `## Done` section — `git --no-pager log` carries history.

## Now

- [ ] **Resume audit-skills interactive-walk retrofit from session handoff** — see [`handoffs/2026-05-26-audit-skills-interactive-walk-pattern.md`](handoffs/2026-05-26-audit-skills-interactive-walk-pattern.md).

## Next

- [ ] **Candidate skill: session-end retrospective for surfacing dropped threads + un-validated claims.** Distinct from `task-checkpoint` (fast end-of-unit-of-work gate) and `session-handoff` (state-transfer for the next session): this one scans shipped artifacts for claims that were never validated (e.g. SKILL.md modes documented but never exercised), questions raised but not resolved, and explicit deferrals that lost context.  Triages by confidence — verified pending / possibly pending / captured for later / uncertain — with a grounding-discipline rule that ungrounded claims default to "uncertain" rather than confident assertions.  Pattern surfaced 2026-05-26 during the `/regen-comments` session: would have caught the `--tree all` mode that shipped with documented support but no test, the persona-tool restrictions deferred without follow-up, and the emoji-indicator format used in the SKILL.md without explicit sign-off.  Could fit as an extension to `task-checkpoint` (lighter; per-checkpoint scan with shape but no mandatory triage), to `session-handoff` (end-of-session frame; natural fit but invoked too late to catch pre-commit slips), or as its own slash command.  Open question whether the grounding discipline can be encoded in skill text reliably or whether it needs an external verifier-blind agent reading the conversation transcript blind to the agent's own draft.  See [`plans/2026-05-26-agent-collaboration-reflections.md`](2026-05-26-agent-collaboration-reflections.md) and [`handoffs/2026-05-26-audit-skills-interactive-walk-pattern.md`](handoffs/2026-05-26-audit-skills-interactive-walk-pattern.md) for the source observations.  Promote to a workstream when someone has time to design it — current shape is still observation, not specification.
- [ ] **Simplify io_socket bridging in chumicro_mqtt / chumicro_requests / chumicro_websockets** — after `chumicro_sockets` 0.10.0 made `SocketConnector.socket` a live reference throughout the lifecycle and `SocketConnector.io_socket` a thin delegate of it, each consumer's `client.py` carries a dual-source bridge (delegate to `connector.io_socket` during connect, fall back to `connector.socket` after) that can collapse to single-source delegation.  Same for `io_wants_read` / `io_wants_write`.  Backward-compatible — current code still works; the simplification is opt-in per consumer.  Touch sites: `libraries/mqtt/src/chumicro_mqtt/client.py` (io_socket / io_wants_read / io_wants_write block), `libraries/requests/src/chumicro_requests/client.py` (same), `libraries/websockets/src/chumicro_websockets/client.py` (same).  Each consumer also needs its FakeSocketConnector usage re-checked since the fake's io_socket still returns None when terminal — either update `chumicro_sockets.testing.FakeSocketConnector` to match the new shape (mirror `socket` as live ref) OR keep the consumer bridges that handle both shapes.  Patch bump per consumer; sockets `FakeSocketConnector` update would be a sockets patch bump.
- [ ] **Cross-runtime harness: add the test file's parent dir to `sys.path`** — today `support/test_harness/run_cross_runtime.py` puts `libraries/*/src/` and `support/*/src/` on `sys.path` but not `libraries/<name>/tests/`.  Blocks promoting `_SwapAttribute` / `_SwapItem` / `_FakeModule` / `_SocketpoolStub` / `_BareStub` out of the per-file duplicates in `libraries/sockets/tests/test_factories.py`, `test_cp_adapter.py`, `test_udp.py` (and the parallel copies in `libraries/websockets/tests/test_sockets_factory.py`).  Once the harness can resolve a sibling `tests/_swap_helpers.py`, dedup the ~80 LOC × 4 sites.
- [ ] **Re-evaluate Decision 0041 §8 — CP-rp2 TLS server ban.**  Today's `chumicro_sockets._adapters.cp.listen_tls` refuses up-front on rp2 platforms (`OSError(32)` mid-handshake + CYW43 station-mode wedge tied to [adafruit/circuitpython#10339](https://github.com/adafruit/circuitpython/issues/10339)).  Worth re-bencing against current CP firmware — the original failure may not still reproduce, or the wedge may be addressable differently.  If the ban can lift, drop the platform check in `listen_tls`, update Decision 0041 §8 + the `docs/guide.md` runtime-support table.
- [ ] **chumicro_mqtt + chumicro_runner cooperative-tick convergence.**  [workstreams/chumicro-mqtt-runner-cooperative-tick-convergence.md](workstreams/chumicro-mqtt-runner-cooperative-tick-convergence.md)
- [ ] **MQTT negative-testing suite.**  [workstreams/mqtt-negative-testing-suite.md](workstreams/mqtt-negative-testing-suite.md)
- [ ] **Wifi-recovery bake — gated on `chumicro_wifi.WifiService` adoption in the bake.**  Today's `projects/mqtt_bake_diag_plain/app.py` calls a raw `wlan.connect()` / `wifi.radio.connect()` at boot and never reconnects.  A router-reboot test against that harness would mostly measure the substrate's auto-rejoin behaviour, not our library.  When the bake (or any real project) starts driving `chumicro_wifi.WifiService` from the runner, re-queue: reboot the router mid-bake, confirm `WifiService` re-associates + the MQTT connector resumes from FAILED.
- [ ] **`/audit-library libraries/mqtt` for bloat** — chumicro_mqtt is ~2x the reference impl's size (`client.py` 1283 + `_wire.py` 889 = 2172 lines vs ~1043).  Some of the size is legitimate (three-tier decoder, multi-pending-response, pattern handlers, backpressure-as-error), but past the publish/subscribe-raw collapse there's likely more: `from_config` + sockets-factory plumbing is another ~100 lines that could move to a separate helper, the `WhenOversized` policy might fold into a callback, etc.  Identify the actual bloat vs. capability-paid-for size.
- [ ] **`chumicro_runner.add_periodic` phase slip on a 1 Hz schedule** — observed ~1.08 s per publish on Pi Pico W CP custom firmware (2026-05-23, post-step-1+2 bake), ~8% slow, compounds across long runs.  `Runner.tick` at `libraries/runner/src/chumicro_runner/core.py:318` reschedules a fired periodic as `entry.next_due_ms = ticks_add(now_ms, entry.period_ms)` — re-anchoring from *now* instead of from the previous deadline.  When the handler takes 50 ms (publish + send + ack-wait), the next fire shifts ~50 ms later every period; metering / sampling tasks would lose phase.  The drift-resistant alternative is `ticks_add(entry.next_due_ms, entry.period_ms)`.  Tradeoff: phase-preserving rescheduling causes catch-up bursts after a long stall (5 fires in 10 ms if the board was busy 5 s), which is wrong for LED / UI tasks.  Cleanest fix is probably a per-registration knob (`catch_up=False` default for current behaviour, opt-in phase preservation for sensing) — needs an ADR.
- [ ] **`/audit-embedded chumicro_runner.core` and `chumicro_websockets`** — both are large libraries left over from the prior session's "next session" list.  Per-change bench loop is now established; can use the harness instead of static review.
- [ ] **CI infrastructure workstream (unscoped — fires when CI is re-enabled).**  [workstreams/archive/audit-remediation-and-drift-mechanization.md](workstreams/archive/audit-remediation-and-drift-mechanization.md)
- [ ] **Workspace-template gap #4b — regular-mode README update.**  [workstreams/archive/workspace-template-dev-and-regular-mode-gaps.md](workstreams/archive/workspace-template-dev-and-regular-mode-gaps.md)
- [ ] **`websockets` oversize-frame length on Lolin S2.**  [workstreams/websockets-oversize-frame-lolin-s2.md](workstreams/websockets-oversize-frame-lolin-s2.md)
- [ ] **Expand the device test matrix beyond ESP32-S2.**
- [ ] **Performance + resource benchmarking infrastructure** — heap + CPU per library op, regression gates, on-schedule CI.
- [ ] **Deploy-path unification — one mechanism puts code on a board.**  [workstreams/deploy-path-unification.md](workstreams/deploy-path-unification.md)
- [ ] **`_load_fallback_device` — stash sessionstart error.**  [workstreams/load-fallback-device-stash-sessionstart.md](workstreams/load-fallback-device-stash-sessionstart.md)
- [ ] **Deploy walker fails on unresolved imports.**  [workstreams/walker-unresolved-import-failure.md](workstreams/walker-unresolved-import-failure.md)
- [ ] **Concurrent-agent commit scrambling — process / skill note.**  [workstreams/concurrent-agent-commit-scrambling.md](workstreams/concurrent-agent-commit-scrambling.md)
- [ ] **test_harness promotion + `chumicro_test_harness.network` + endpoint-taxonomy alignment (Decisions [0082](decisions/0082-test-harness-as-infrastructure-library.md) + [0083](decisions/0083-functional-test-endpoint-taxonomy.md)).**  [workstreams/test-harness-promotion-and-network-helper.md](workstreams/test-harness-promotion-and-network-helper.md) — supersedes the previous `examples/helpers.py` drift + `functional_tests/` conftest-dedup workstreams (both absorbed).
- [ ] **`/audit-library workbench/pytest-device` — 7 shape candidates surfaced during the comment audit Pass 2 (collection.py `_device_closure_source_dirs` / `_device_own_source_dirs` near-parallel loops, two inline `import warnings` at 322 and 958, `_ensure_batch_result` guard duplication across `DeviceTestItem.runtest` / `DeviceRunFileItem.runtest`; pr_summary.py `_format_markdown_table` growth; runtime_config.py validator-class candidate; session.py nine near-identical `_session_*` getters; test_runner.py CircuitPython-RAM coupling via cast + dynamic import).**
- [ ] **`/audit-workspace` — 12 unit-test `conftest.py` files (compat, logging, wifi, msgpack, kvstore, events, timing, http_server, mqtt, requests, ntp, websockets, sockets) carry an identical one-line `"""Test configuration for the chumicro-<name>.</name>"""` docstring on an otherwise-empty file; strip them all together or leave as a workspace convention.**
- [ ] **`/audit-workspace` — `# noqa: CODE` and `# pragma: no cover` inline-comment separator drift; most of `libraries/` uses ` - ` but msgpack / sockets / runner / compat (now fixed) used em-dash; normalize and add CHU lint rule.**
- [ ] **CHU lint for cross-reference comments in `libraries/*/src/` and `workbench/*/src/`.**  Catch `:mod:` references, "see ``module``'s docstring", "follows the pattern in", and sibling-library names in `#` comments and docstrings.  Backstop for the AGENTS.md "comments stand alone for a cold reader" rule (the rule is the gate; the lint is the safety net for the next time judgment fails).  False-positive risk: legitimate `:mod:` cross-references between own-package modules (e.g. linking to a sibling class in the same library) — scope the lint to *external* references (different `libraries/<name>/` package) to keep it useful.
- [ ] **`/audit-library workbench/checks` — module-level docstrings in `chu027.py`'s `_extract_docstring_blocks` are not suppressible (the ClassDef/FunctionDef header check doesn't fire for `ast.Module`, leaving `suppressed = False`). Decide whether to add a header-line-1 check or document the gap as intentional.**
- [ ] **`/audit-embedded chumicro_mqtt` — re-validate the three `gc.collect()` calls in the import chain.**  `__init__.py` calls collect twice (around the `client` import) and `_wire.py` calls collect once (between constants and `PacketDecoder`).  The +33 KB-on-Pi-Pico-W-MP bench measurement is worth re-validating; confirm whether all three are load-bearing on current firmware or some are over-defensive.  Surfaced by `/audit-library libraries/mqtt` (2026-05-23).
- [ ] **`/audit-embedded chumicro_http_server` — `RequestParser._buffer.extend(chunk)` has no per-request `max_request_line_bytes` / `max_headers_bytes` cap.  Peer can dribble bytes with no CRLF, growing the buffer until the per-connection deadline (default 10 s).  At default budgets that's ~400 KB demanded across 4 connections vs ~150 KB free on a Pi Pico W — OOM possible before timeout.  Adding the cap is a spec change (new constructor knob + 400 emission path), not a mechanical fix.  Surfaced by `/audit-library libraries/http_server` (2026-05-23).**
- [ ] **`/audit-integration chumicro_http_server,chumicro_requests` — `CaseInsensitiveDict` + `parse_charset` (and the `_try_parse_headers` shape) are intentionally duplicated between the two libraries with `# noqa: CHU027` markers + "candidate for shared http-wire library hoist" annotations.  ~125 lines of shared HTTP/1.1 primitives.  Hoist into a leaf `chumicro_http_core` (or similar), or accept the duplication and drop the workstream-pointer language.  Surfaced by `/audit-library libraries/http_server` (2026-05-23).**
- [ ] **Sweep 11 stale "4 MB flash" references after Decision 0015 correction** — `README.md`, `INSTALL.md`, `libraries/README.md`, three library guides (`sockets`, `websockets`, `workspace`), ADRs 0044/0067/0073, one archived workstream, and two audit skills (`audit-workspace`, `audit-embedded`).  Decision 0015 + AGENTS.md are canonical at "2 MB physical / ~800 KB usable"; rest drift until touched.
