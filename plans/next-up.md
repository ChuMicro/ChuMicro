# Next Up

> **Agent-managed file.  Single source of truth for the work queue.**  Each top-level bullet is capped at 5 bullet points (lead + sub-bullets, CHU011) — anything that needs more lives in a workstream file under [`workstreams/`](workstreams/), surfaced from here as a one-line pointer.  `## Done (recent)` is capped at 25 entries — drop the oldest when adding a new one.  Commit messages + `workstreams/archive/` keep the durable record; `git log` is the journal.  Session warm-up: `git --no-pager log --oneline -20` then this file.

## Now

_idle — pickup candidates live in `## Next`._

## Next

Independent items.  Most have either shipped phases (status in the linked workstream) or are unscoped placeholders waiting on a forcing function.

- [ ] **Multi-board CP deploy + FSKit recovery follow-ups** — captured 2026-05-09 at end of workbench-deploy-reliability.  Five investigation items: `list_files_in_scope` + `delete_files` skip the drive auto-correct (real correctness bug, can delete from wrong board's drive); `detect_fskit_wedge` is wired only into InteractiveDeployer (could promote `wipe_filesystem` timeouts + functional-test conftest); a suite-state regression where the CP wipe test passes in isolation but fails in the full suite (Step 1's "leave board in friendly REPL" change is the leading suspect); a doc note that running the recovery command twice damages mounted volumes; and end-to-end bench validation of the 2026-05-09 corrected `MACOS_FSKIT_RECOVERY_COMMAND` (source-side validated, not bench-validated).  Each item carries evidence + hypothesis + verification steps.  Detail: [`workstreams/deploy-multi-board-and-fskit-followups.md`](workstreams/deploy-multi-board-and-fskit-followups.md).

- [ ] **Code-comment anti-pattern audit** — captured 2026-05-09 from the workbench-deploy-reliability session.  AGENTS.md commit `e13df11` formalised "no historical / dated / removed-code framing in code comments" but didn't sweep existing code; initial scan found two source files with leftover dated narration (`workbench/pytest-device/.../plugin.py`, `workbench/deploy/.../circuitpython_transport.py`).  Wider grep + decision on lint-vs-one-off needed.  Detail: [`workstreams/comment-anti-pattern-audit.md`](workstreams/comment-anti-pattern-audit.md).

- [ ] **Two-board client/server example** — captured 2026-05-06 during F5 boot-shim simplification.  Replace deprecated `examples/two_projects/` with two physical-board demo (one HTTP server + one client, or MQTT pub + sub).  Single-project shape per board; lives at `examples/two_board_handshake/{server,client}/`.  Hardware-validates two-board acceptance scenarios chumicro doesn't otherwise exercise.

- [ ] **Beginner-comfort onramp Phase 3+** — Phases 1 + 2 + audit-of-the-audit shipped 2026-05-02 (see `## Done`).  Phase 3+ unscoped; reopen when the next first-encounter friction surfaces from real users.  Detail: [`workstreams/beginner-onramp.md`](workstreams/beginner-onramp.md).

- [ ] **Scripts → workbench migration backlog** (Decision 0032 rule 8).  Phases 1–3 shipped 2026-04-27.  Smaller candidates remain: `scripts/generate_config_files.py` could thin-wrap `chumicro_workspace`; the rest of `scripts/{audit_gates,bundle_manager,docs_deploy,prepare_*,ide_sync,verify_examples,check_*}.py` reviewed 2026-04-27 and confirmed mono-repo CI plumbing — stays in `scripts/`.

- [ ] **`discover` interactive multi-board registration** — option (c) shipped 2026-04-26 (`add-device --address <port> <id>` auto-detects runtime).  Still deferred: (a) `add-device --auto` interactive port sweep, (b) `discover --register` doing the sweep as a side-effect.  Hold until a 5+-board user complains.

- [ ] **Expand the device test matrix beyond ESP32-S2** now that transport tooling is proven on both MicroPython and CircuitPython.

- [ ] **OTA (over-the-air updates)** — placeholder; trigger to revisit is a real thing on a wall / yard / inconvenient location that needs an update without physical access.  Detail: [`workstreams/ota.md`](workstreams/ota.md).

- [ ] **CHU008 — workspace-template repo isolation lint, distributed via `chumicro-workspace`** — captured 2026-05-06.  Template repo's leak shape is mono-repo-shape prose framing the user's workspace as an upstream derivative.  Cannot live in mono-repo `scripts/`; right home is a `chumicro-workspace check-isolation` subcommand.  Same machinery as CHU006, different forbidden-pattern set.  Holds on the CHU-rules-home decision below.

- [ ] **CHU rules → workbench package (structural)** — captured 2026-05-06.  Should the CHU lint logic move to a focused `chumicro-checks` package, fold into `chumicro-workspace`, or stay in mono-repo `scripts/`?  Needs an ADR before CHU008 can land in the right home.  Bundles the speculative-public-API + cargo-cult-class-method candidates that share the same cross-repo-grep distribution problem.

- [ ] **Enable GitHub Copilot code review as a PR quality gate** (low priority — defer until community contributions begin).

- [ ] **Add digital I/O as the second library seam** (alongside CI/release work, not sequentially).

- [ ] **Explore test ergonomics — reduce repeated boilerplate across test files.**

- [ ] **Performance + resource benchmarking infrastructure** — measure heap + CPU per library operation with explicit GC control; per-benchmark thresholds that fail on regression; separate `bench` task or deeper test tier so slow benchmarks don't run on standard `test`; CI on a schedule.

## Out of scope (until revisited)

- CI-hosted device testing (`device-test.yml` / `workflow_dispatch`, CI-injected `devices.yml` / `workspace.yml` / `secrets.yml`). Parked over security concerns around shared-runner device access; bring back up before any design work resumes.
- CI simulation/emulation path (renode etc.). Not being explored until the above is revisited.

## Investigations

- [ ] **Investigate slow MicroPython RAM-mode functional test runs** — observed during 2026-04-19 live PyCharm testing that MicroPython RAM-mode functional tests took noticeably longer than expected.  CircuitPython RAM-mode is fast in comparison.  Suspects: per-file `mpremote mount` cost, cold-start interpreter overhead, batch-vs-per-test trade-off.  Profile against the new batch-execute path.

## Done (recent)

> **Where the detail lives.**  This section is a one-line pointer log capped at 25 entries — drop the oldest when adding a new one.  Verbose session detail lives in commit messages or [`plans/workstreams/<name>.md`](workstreams/) (per-phase acceptance).  Past entries link to those records — don't paste detail back into `next-up.md`.

- [x] **Multi-board CP deploy + FSKit follow-ups — Items 1 + 3 shipped, 2/4/5 still open** (2026-05-09) — bench-reproduced the suite-mode wipe failure (minimum repro: `test_circuitpython_diff_deploy_round_trip` then `test_circuitpython_wipe_reformats_circuitpy_drive`).  Volume-UUID before-and-30s-after compare proved the FAT was genuinely never reformatted — not a host-view-stale issue.  Real trigger: the test's host-side `sentinel.write_bytes(b"marker")` directly to `/Volumes/CIRCUITPY` queues a soft-reboot in the device runtime that pre-empts the next raw-REPL `storage.erase_filesystem()` before it reaches its reformat step (raw REPL acks `OK`, but the script never finishes its `usb_disconnect → 1000 ms delay → reformat → hard reset` sequence; the soft-reboot takes the board back through code.py against the un-reformatted FAT, USB-CDC stays alive, drive contents unchanged).  Setting `supervisor.runtime.autoreload = False` inside the wipe path *does not* prevent the pre-empt (byte trace confirmed autoreload-off succeeds and the next `erase_filesystem` is still pre-empted, so the underlying mechanism is not reached by the user-toggleable autoreload flag at the moment we can disable it).  User confirmed the wipe code itself is healthy (manual `import storage; storage.erase_filesystem()` works).  Fix: rewrite the wipe test to plant the sentinel via `Deployer.deploy(FileMapSource({"/code.py": noop, "/wipe_test_sentinel.txt": b"marker"}))`, which routes through rsync + autoreload-disable + post-soft-reboot settle.  Item 1 also shipped same session: `list_files_in_scope` and `delete_files` now both pair `_resolve_circuitpy_drive` with `_verify_drive_for_board` (matching `_push_staging_to_drive`), with two regression tests proving the multi-board stale-`circuitpy_drive_path` case.  A "proof-of-life check in `wipe_filesystem`" follow-up was considered and rejected (Item 6 in the workstream): the only concrete trigger was the now-fixed test, the speculative "future causes" had no grounded examples, and the current `try: ... except Exception: pass` is correct for the only real-world outcome (USB-CDC drops mid-call as the success signal).  Detail: [`workstreams/deploy-multi-board-and-fskit-followups.md`](workstreams/deploy-multi-board-and-fskit-followups.md).
- [x] **Workbench deploy + tail reliability — Steps 1, 1b, 2, 2b, 3, 4 shipped + 4-board sweep** (2026-05-09) — root cause: `deploy_files` actively Ctrl-C'd the running entrypoint via the trailing `_enter_raw_repl()` call, killing every `while True:` example after a 10 s capture window.  Same pattern in `MicropythonTransport` (Step 1b discovered during the 4-board sweep).  Five structural fixes plus a 4-board bench sweep validated end-to-end across Lolin S2 CP, Pi Pico W CP, Lolin S2 MP, Pi Pico W MP — all four boards leave the entrypoint running after deploy returns; `chumicro-repl --tail` afterward sees code keep printing.  CP transport changes in commits `9d259be` (Step 1: drop post-Ctrl-D `_enter_raw_repl` + simplify `disconnect`), `771f8fa` (Step 2: bump `_BOARD_FILE_VISIBLE_POST_SETTLE` 0.5s → 2.0s after correcting an autoreload misdiagnosis from CP source), `9fdcd9b` (Step 2b: `flash_drive.verify_rsync` post-rsync verification before Ctrl-D), `35171f1` (Step 3: `--tail-seconds N` plumbed end-to-end + chumicro-deploy 0.10.0), `8b6562c` + `92d7dbc` (Step 4: rewrote stale `FLASH_COPY_FAILED` recovery hint with `storage.erase_filesystem()` as the second-line recovery), `7b99677` (Step 1b: same fix on MP side).  842 deploy tests pass at 95 % coverage; full preflight green at 96 %.  Two follow-ups surfaced during sweep, both unrelated to workstream goals: `list_files_in_scope` doesn't run the same drive-verify auto-correct as `deploy_files` (multi-board hosts with stale `circuitpy_drive_path` silently diff against the wrong drive); `test_circuitpython_wipe_reformats_circuitpy_drive` hits the documented macOS FSKit wedge after `storage.erase_filesystem()` and could detect-and-skip rather than retrying for 10 s.  Detail: [`workstreams/workbench-deploy-reliability.md`](workstreams/workbench-deploy-reliability.md).
- [x] **Pyright cleanup Phase 0 + Phase 1 + Family A + scoping pass + example spot-fixes** (2026-05-09) — first scan since the `pyright-python` plugin was set up surfaced 1054 errors / 44 warnings across 442 files.  Phase 0 (config-only) added `pythonVersion: "3.11"` + `reportMissingModuleSource: none` then a second pass added `ignore` for `**/tests/**`, `**/functional_tests/**`, and `support/test_harness/src/chumicro_test_harness/runner.py` (the cross-runtime adapter seam — its purpose is to pretend it's MP/CP on the host).  Phase 1 wrote [`workstreams/pyright-cleanup.md`](workstreams/pyright-cleanup.md) with the load-bearing principle that comments aren't free either (they ship in `.py` files to flash) so prefer pyrightconfig scoping over per-line ignores.  Family A landed four shape-preserving fixes with no runtime cost.  Examples got real-defect spot-fixes only (mqtt `is_connected` rename, config `load_section(prefix=...)`, sockets `sender` unbound, http_server `WifiConfig.from_dict` → `try_from_config`, http_server `_State` widening annotations) — no `# type: ignore` sprinkles.  First-attempt source restructuring (Family C `getattr` helpers, Family B local-bind + assert) was reverted on user feedback.  Net: 1054 → 259 errors / 44 → 0 warnings.  Preflight green at 96 % coverage.
- [x] **Docs phase griffe-cache fix + surfaced 50+ pre-existing annotation gaps** (2026-05-08) — `scripts/run.py` `_build_one` now wipes `<library>/.cache/` before invoking zensical, so griffe re-parses every source file.  Without the wipe, mkdocstrings' griffe cache reused parsed-AST results across runs and the warning-detection `if "griffe" in line` post-scan saw zero output → silent PASS regardless of source content.  Run 1 (cold cache) failed correctly; runs 2-3 with populated cache silently passed.  Surfaced ~50 pre-existing annotation gaps across `chumicro_config`, `chumicro_mqtt`, `chumicro_ntp`, `chumicro_wifi` `from_config` factories + `MQTTClient.__init__` / `publish` / `subscribe` + `encode_connect` + `canned_connack_bytes`; annotated all of them (`object` / `object | None` for the polymorphic seams, real types where the default value implies one).  Fixed three `username / password` slash-separated docstring entries in mqtt that griffe correctly flagged as not matching the function signatures.  Also fixed `chumicro-workspace test --functional` test-data strings in `workbench/pytest-device/tests/test_pr_summary.py` — that flag doesn't exist; `chumicro-workspace test` forwards args to pytest.  Replaced with `pytest libraries/timing/functional_tests`-shape strings that reflect the real default command (`pytest`).
- [x] **CHU006 wider-pattern scan — lint ratcheted to every scanned file** (2026-05-08) — inlined 144 leaking refs across 80+ files (97 `Decision NNNN`, 32 `scripts/run.py`, 10 bare `run.py`, 5 "chumicro mono-repo") in docs / tests / functional_tests / examples / pyproject.toml, then flipped `check_no_repo_refs.py` predicates from `_src_only` to `_everywhere` (bare `run.py` keeps the `chumicro_workspace` package exemption, renamed to `_everywhere_outside_chumicro_workspace`).  `chumicro-workspace deploy-example` replaced `python scripts/run.py deploy-example` in 6 library examples; `support/test_harness/README.md` rewritten standalone-friendly; `chumicro-workspace` console-script entry point added to `workbench/workspace/pyproject.toml` so the framing in published examples actually resolves.  Follow-up commit dropped "the canonical promise" filler (7 occurrences) + workspace-internal `CHU009` / `CHU010` lint-code mentions from published READMEs.  45/45 CHU006 unit tests pass; full preflight green at 96 % coverage.
- [x] **Library config-aware refactor — workstream complete (3/3 follow-ups shipped)** (2026-05-08) — Phases 0/1/2/3 plus all three bench-session follow-ups landed and bench-validated.  (1) mqtt PEP 448 `**unpack` rewritten to a temp-variable merge.  (2) `MicropythonTransport.deploy_files` gained a `follow="exec"|"soft_reboot"` kwarg adopting CP's flash-mode soft-reboot pattern; `Deployer` auto-routes for `(MP, FLASH, /main.py)`; `while True` MP app code now deploys end-to-end with timeout-bounded partial output (validated on Pi Pico W MP + Lolin S2 MP).  (3) CIRCUITPY UID stale-warning silenced on success.  Decision [0028 §MicroPython flash transport](decisions/0028-deploy-modes.md#micropython-flash-transport) is the canonical ADR-side reference.  Detail: [`workstreams/archive/library-config-aware-refactor.md`](workstreams/archive/library-config-aware-refactor.md).
- [x] **Library config-aware refactor — bench-validated CP-side** (2026-05-08) — six end-to-end `python scripts/run.py deploy-example <lib> <example> --device <id> --non-interactive` invocations across Pi Pico W CP + Lolin S2 CP for `timing/circuitpython_blink` + `ntp/circuitpython_ntp_query` + `requests/circuitpython_periodic_get`.  All six staged + executed cleanly with the deploy command's serial capture printing real WIFI_OK ip + NTP_OK unix_seconds (matching wall-clock) + HTTP 200 with content bytes.  Phase 3 deploy-example pipeline + the five `from_config` factories validated end-to-end.  Surfaced three follow-ups (mqtt PEP 448 / MP infinite-loop deploy / CIRCUITPY drive UID warning) recorded in workstream's "Follow-ups from the bench session" section.
- [x] **Library config-aware refactor — Phase 2 http_server** (2026-05-08) — closes the workstream's host-side scope.  `HttpServer.from_config(config, *, handler=None, radio=None, ssl_context=None, listener_factory=None)` reads `http_server.bind_host` / `bind_port` / `max_connections` / `request_timeout_ms` / `max_request_body_bytes` (all optional with library defaults: `0.0.0.0:8080`, etc.) plus the optional TLS pair `tls.cert_path` / `tls.key_path`.  TLS open question resolved: **both-or-neither** — half-TLS raises `MissingConfigKey` so a half-configured deploy fails loudly instead of silently falling through to plain TCP.  Resolution priority: explicit `listener_factory` > explicit `ssl_context` > both config TLS paths > plain TCP.  `circuitpython_two_thing_server.py` example refactored; `circuitpython_two_thing_sensor.py` left as-is (it's an HTTP *client*, covered by Phase 2 requests).  http_server 0.1.4 → 0.2.0; 11 new TestFromConfig tests at 95 % coverage.  Hardware sweep queued.
- [x] **Library config-aware refactor — Phase 3 deploy-example** (2026-05-07) — three commits implementing the front-door command per Decision [0059](decisions/0059-deploy-example-front-door.md): (3a) `NO_PYTHON_RUNTIME` failure kind in `DeployFailureKind` with classifier patterns + install-firmware recovery plan; (3b) `chumicro_workspace.example_source` FileSource composing `ImportGraphSource` + `WithRuntimeConfig` + runtime-marker filtering, with default output_path under `.scratch/`; (3c) `chumicro-workspace deploy-example` CLI subcommand handling all four first-touch board states with TTY-auto-detect interactive vs `--non-interactive`, distinct exit codes 0/2/3/4/5/6, `--list` discoverability, optional `--tail` REPL drop, and a `python scripts/run.py deploy-example` shim.  Mono-repo only per ADR §C.3.  17 + 24 + 4 = 45 new tests; recovery + workspace coverage ≥94 %.  Hardware sweep queued.
- [x] **Library config-aware refactor — Phase 2 websockets** (2026-05-07) — two factories: `WebSocketClient.from_config(config, *, radio=None, ssl_context=None, connection_factory=None)` reads `websockets.client.max_message_bytes` (optional, library default); `WebSocketServer.from_config(config, on_connection, *, radio=None, listener=None, accept_path=None, max_connections=2)` reads `websockets.server.host` / `port` / `max_message_bytes` and auto-builds a listener via `chumicro_sockets.tcp_listening_socket` (default bind `0.0.0.0:8765`).  Manifest-quirk documented: `websockets.client.connect_url` is in the manifest because users set it per-project, but `from_config` doesn't read it (URL is a per-connection arg passed to `connect()`).  `accept_path` stays a kwarg — per-deploy app routing, not library-shape.  Both `circuitpython_client.py` + `circuitpython_server.py` examples refactored to mirror the mqtt + ntp + requests shape; 284 tests pass at 96 % coverage; websockets 0.9.2 → 0.10.0.  Hardware sweep queued together with mqtt + ntp + requests + Phase 3.  Detail: [`workstreams/archive/library-config-aware-refactor.md`](workstreams/archive/library-config-aware-refactor.md).
- [x] **Library config-aware refactor — Phase 2 requests** (2026-05-07) — `HttpClient.from_config(config, *, radio=None, ssl_context=None, connection_factory=None)` reads `requests.default_timeout_ms` / `requests.default_max_redirects` / `requests.user_agent` / `requests.max_body_bytes` (all optional with library defaults); resolves the `requests.now_utc_tuple` open question (stays **outside** the manifest — RTC seeding is app-level, the TLS test reads it via per-conftest required-keys); `circuitpython_periodic_get.py` example refactored to mirror mqtt + ntp shape, fixing post-migration broken nested-section reads; 183 tests pass at 97 % coverage; requests 0.1.4 → 0.2.0.  Hardware sweep queued together with mqtt + ntp + Phase 3.  Detail: [`workstreams/archive/library-config-aware-refactor.md`](workstreams/archive/library-config-aware-refactor.md).
- [x] **Diagnostic: surface failing phase label in "Preflight failed" banner** (2026-05-07) — `_run_parallel_phases` now returns `(int, str | None)`; preflight body prints `Preflight failed at: <label>` so the failing phase survives interleaved-output schedulers where the `[FAIL]` line scrolls past.  All 5 callers + 6 test fakes updated; 620 scripts tests + full preflight green.
- [x] **ADR 0059 — `deploy-example` as the mono-repo front-door command** (2026-05-07) — locks Phase 3 policy: command home, fast precheck stack, four first-touch board states (incl. `NO_PYTHON_RUNTIME` / Arduino case), interactive vs `--non-interactive` with TTY auto-detect, distinct exit codes, destructive-action consent rule.  Implementation lives in the workstream's Phase 3.
- [x] **Library config-aware refactor — Phase 2 ntp** (2026-05-07) — `NTPClient.from_config(config, *, radio=None, socket=None, socket_factory=None)` reads `ntp.server` / `ntp.port` / `ntp.timeout_ms` (all optional with public-pool fallback for `ntp.server`); `circuitpython_ntp_query.py` example refactored to mirror mqtt telemetry shape (load runtime config + `WifiConfig.try_from_config` + `NTPClient.from_config`); 38 tests pass at 98 % coverage; ntp 0.1.1 → 0.2.0.  Hardware sweep queued together with mqtt + Phase 3.  Detail: [`workstreams/archive/library-config-aware-refactor.md`](workstreams/archive/library-config-aware-refactor.md).
- [x] **Single-source-of-truth: retire now.md, age out Done log** (2026-05-07) — deleted `plans/now.md`; `next-up.md` `## Now` is the only active-work pointer.  CHU011 dropped its now.md file-line cap and gained a `## Done (recent)` entry-count cap (≤25 entries).  AGENTS.md non-negotiable + plans/README.md + task-checkpoint skill updated to match.  Pruned every Done entry dated 2026-05-01 or earlier — detail is in commit messages + workstream archives.
- [x] **Plans-doc brevity sweep + CHU011 lint** (2026-05-07) — compacted now.md to ≤25 lines, swept next-up.md `## Now` / `## Next` paragraph-bodies into one-liner Done pointers, landed CHU011 lint capping each top-level bullet at 5 bullet points.
- [x] **Silent-skip test audit** (2026-05-07, commits [`96a6736`](https://github.com/ChuMicro/ChuMicro/commit/96a6736)..[`08f637f`](https://github.com/ChuMicro/ChuMicro/commit/08f637f)) — eight phases eradicated four shapes of silent PASS in `libraries/*/{tests,functional_tests}/`; `chumicro_test_harness.skip(reason)` primitive + `__chumicro_features__` capability marker + CHU009/CHU010 lint + Decision [0058](decisions/0058-test-skips-must-be-loud.md).  Adjacent: chumicro-mqtt 0.3.0 dropped silent fallback ([`3d0ade9`](https://github.com/ChuMicro/ChuMicro/commit/3d0ade9), [`e8d54f6`](https://github.com/ChuMicro/ChuMicro/commit/e8d54f6)).
- [x] **scripts/ ↔ workbench/ ↔ workspace-template config unification** (2026-05-04) — five-phase config convergence across both repos; `devices.yml` empty-registry shape, library config manifests, `workspace.yml` + `secrets.yml` workbench-owned starters, conftests via `compose_runtime_config()`, `chumicro-workspace 0.7.0` exposes `materialize_workbench_starters`.  Detail: [`workstreams/archive/scripts-workbench-config-unification.md`](workstreams/archive/scripts-workbench-config-unification.md).
- [x] **Two-file workspace config** (2026-05-04) — Decision [0057](decisions/0057-two-file-config.md): `!secret` marker retired; `workspace.yml` + per-project `config.toml` deep-merged via `compose_runtime_config(workspace_yaml, project_config)`.  Setup materialises both from the canonical workbench-shipped starters.
- [x] **On-device functional tests dogfood `chumicro_config.load_runtime_config()`** (2026-05-06) — migration shipped incrementally as Decision 0056 + scripts-workbench-config-unification + config-shape-beginner-ergonomics carried the seven on-device test rewrites along.  chumicro-pytest-device 0.4.0 ships `set_runtime_config`.  Detail: [`workstreams/archive/on-device-config-dogfooding.md`](workstreams/archive/on-device-config-dogfooding.md).
- [x] **Config-shape beginner ergonomics** (2026-05-06) — across mono-repo `30e2878` / `8303d17` / `7d36f27` + template-repo `72c6ffb`.  Three files (workspace.yml machinery / secrets.toml device-bound / project_config.toml per-project), flat-key `RuntimeConfig`, `WifiConfig.from_config`, manifest format, `config-validate` CLI, additive setup re-apply.  Bumps: chumicro-config 0.2.0, chumicro-wifi 0.1.0, chumicro-workspace 0.12.0.  Detail: [`workstreams/archive/config-shape-beginner-ergonomics.md`](workstreams/archive/config-shape-beginner-ergonomics.md).
- [x] **Finding 3 — `add-device` firmware-version parser breaks on RC builds** (2026-05-06, `8ecf728`) — walk the version tuple element-by-element and stop at first non-int; both probe and parser unified.
- [x] **Finding 4 — `add-device` suggests IDs from probed `board_id`** (2026-05-06, `f5539e9`) — reused Step 4 of beginner-onramp's bootstrap-wizard id-suggestion path.
- [x] **Finding 5 — `deploy <name>` auto-detects boot-shim mode** (2026-05-06, `3fde27c`) — `app.py` with a `run()` callable triggers a synthesised three-line `code.py` / `main.py` entrypoint, no flag required.
- [x] **Finding 6 — mpremote orphan port-holders block subsequent deploys** (2026-05-06, `224c489`) — port-busy failure mode now reports the offending PID so the user can `kill <pid>` decisively.
