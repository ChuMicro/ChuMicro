# Next Up

> **Agent-managed file.  Single source of truth for the work queue.**  Each top-level bullet is capped at 5 bullet points (lead + sub-bullets, CHU011) — anything that needs more lives in a workstream file under [`workstreams/`](workstreams/), surfaced from here as a one-line pointer.  `## Done (recent)` is capped at 25 entries — drop the oldest when adding a new one.  Commit messages + `workstreams/archive/` keep the durable record; `git log` is the journal.  Session warm-up: `git --no-pager log --oneline -20` then this file.

## Now

_idle — pickup candidates live in `## Next`._

## Next

Independent items.  Most have either shipped phases (status in the linked workstream) or are unscoped placeholders waiting on a forcing function.

- [ ] **Multi-board CP deploy + FSKit follow-ups — Item 5 deferred to next natural wedge** — Items 1, 2, 3, 4 + the doctor wrapper shipped 2026-05-09; Item 6 considered and rejected.  Item 5 (end-to-end recovery-command bench proof) needs a real FSKit wedge.  A 2026-05-09 attempt at deliberate triggers (alternating-board deploys, repeated wipe-test, full functional suite, host-side write+delete loops, concurrent diskutil probes) didn't reproduce — the wedge is intermittent enough that try-on-demand isn't reliable.  Validation rides on the next natural occurrence; checklist in the workstream's Item 5 section ("when doctor reports MACOS FSKIT ✗, run through these five steps").  Detail: [`workstreams/deploy-multi-board-and-fskit-followups.md`](workstreams/deploy-multi-board-and-fskit-followups.md).

- [ ] **Beginner-comfort onramp Phase 3+** — eight-step sequence + four 2026-05-06 papercuts all shipped; workstream archived 2026-05-10.  Phase 3+ unscoped; reopen when the next first-encounter friction surfaces from real users.  Detail: [`workstreams/archive/beginner-onramp.md`](workstreams/archive/beginner-onramp.md).

- [ ] **Workspace-template gaps — 2 remaining of 9** — gaps doc archived 2026-05-10 after 7 of 9 fixes shipped.  Still open: (gap #4b) regular-mode README update on the [workspace-template repo](https://github.com/ChuMicro/ChuMicro-Workspace-Template) documenting `chumicro-workspace install-libraries <project>` + a manual `circup` / `mip` fallback recipe (mono-repo half shipped — `install-libraries` CLI lands now); (gap #8) fold `scripts/prepare_workspace.py` into the top of `scripts/run.py` to mirror the workspace template's single-file bootstrap pattern, then delete `prepare_workspace.py`.  Detail: [`workstreams/archive/workspace-template-dev-and-regular-mode-gaps.md`](workstreams/archive/workspace-template-dev-and-regular-mode-gaps.md).

- [ ] **`discover` interactive multi-board registration** — option (c) shipped 2026-04-26 (`add-device --address <port> <id>` auto-detects runtime).  Still deferred: (a) `add-device --auto` interactive port sweep, (b) `discover --register` doing the sweep as a side-effect.  Hold until a 5+-board user complains.

- [ ] **Expand the device test matrix beyond ESP32-S2** now that transport tooling is proven on both MicroPython and CircuitPython.

- [ ] **OTA (over-the-air updates)** — placeholder; trigger to revisit is a real thing on a wall / yard / inconvenient location that needs an update without physical access.  Detail: [`workstreams/ota.md`](workstreams/ota.md).

- [ ] **Enable GitHub Copilot code review as a PR quality gate** (low priority — defer until community contributions begin).

- [ ] **Add digital I/O as the second library seam** (alongside CI/release work, not sequentially).

- [ ] **Explore test ergonomics — reduce repeated boilerplate across test files.**

- [ ] **Performance + resource benchmarking infrastructure** — measure heap + CPU per library operation with explicit GC control; per-benchmark thresholds that fail on regression; separate `bench` task or deeper test tier so slow benchmarks don't run on standard `test`; CI on a schedule.

- [ ] **`scripts/run.py new-library --workbench` parity shim** — `chumicro-workspace new --workbench <name>` scaffolds a workbench package today (calls `scaffold_library` with `package_kind="workbench"`); the mono-repo's `scripts/run.py new-library` only forwards to `--library`.  Add the flag to the shim so contributors don't have to know about the underlying workspace CLI.  Small (~10 lines + one test).  Surfaced 2026-05-11 during the contributing-docs audit (`new-library.md:9` was claiming the scaffolder doesn't support workbench at all).

## Out of scope (until revisited)

- CI-hosted device testing (`device-test.yml` / `workflow_dispatch`, CI-injected `devices.yml` / `workspace.yml` / `secrets.yml`). Parked over security concerns around shared-runner device access; bring back up before any design work resumes.
- CI simulation/emulation path (renode etc.). Not being explored until the above is revisited.

## Investigations

- [ ] **MicroPython default `ssl.wrap_socket` skips certificate validation — surface in chumicro_sockets** (filed 2026-05-11) — bench-verified during a README TLS-claim audit: on Pi Pico W MP and Lolin S2 MP, `ssl.wrap_socket(sock, server_hostname=host)` with no explicit context accepts ANY cert, including the expired one at `expired.badssl.com`.  Same bench: CircuitPython on the same two boards correctly rejects `expired.badssl.com` with `MBEDTLS_ERR_X509_FATAL_ERROR` via the firmware-bundled mbedTLS CA store (CP ships `x509-crt-bundle`; MP doesn't).  Affects: (a) `libraries/sockets/src/chumicro_sockets/_adapters/mp.py` `connect_tls` (~line 161) defaults to no-verify silently; (b) `libraries/sockets/src/chumicro_sockets/__init__.py` `connect_tls` docstring claims `ssl.create_default_context()` "picks up the system trust store" on every runtime — true on CP, false on MP; (c) every consumer that uses `HttpClient.from_config({}, radio=...)` / `MQTTClient.from_config({}, radio=...)` on MP gets insecure TLS by default with no warning, and (d) the README claim about default trust stores was rolled back 2026-05-11 to avoid misleading users until this is resolved.  Fix shape TBD — candidates: bundle a curated PEM (Let's Encrypt / DigiCert / Cloudflare roots) and load it in the MP adapter; or raise on MP when `connect_tls(context=None)` to force opt-in via `ssl_context_with_ca(...)`; or set `cert_reqs=ssl.CERT_REQUIRED` and surface the missing-CA failure loudly.  Reproducer scripts (CP + MP variants of an `expired.badssl.com` probe) were live-tested under `.scratch/tls_validation/` during the audit — paths gone but the shape is straightforward to recreate.

- [ ] **Investigate slow MicroPython RAM-mode functional test runs** — observed during 2026-04-19 live PyCharm testing that MicroPython RAM-mode functional tests took noticeably longer than expected.  CircuitPython RAM-mode is fast in comparison.  Suspects: per-file `mpremote mount` cost, cold-start interpreter overhead, batch-vs-per-test trade-off.  Profile against the new batch-execute path.

## Done (recent)

> **Where the detail lives.**  This section is a one-line pointer log capped at 25 entries — drop the oldest when adding a new one.  Verbose session detail lives in commit messages or [`plans/workstreams/<name>.md`](workstreams/) (per-phase acceptance).  Past entries link to those records — don't paste detail back into `next-up.md`.

- [x] **README convergence pass — 20 publishable READMEs + scaffold template + new shared docs page** (2026-05-11, commits `c6b2e85a`, `43b93c7a`, `d29fc349`) — Batch 1 (HIGH-confidence sweep): dead `#whats-in-the-box` anchor on 4 workbench READMEs → `/tree/main/workbench`; License MIT footer on the 14 library READMEs missing one; `## Installation` → `## Install` across 4 workbench tools; hero-block form (inline → multi-line wrap) unified across 8 files; AI-tic "canonical" rewrites in wifi / workspace / repl; "v1 ships:" / "v1 non-goals:" / `### Status > Pre-alpha` paragraphs cut from requests / http_server / deploy / pytest-device / workspace; http_server gained the missing Experimental-bundle entry; websockets TODO replaced with the two real example rows.  Batch 2 (workshop + structure): bold tagline + 2-line ecosystem pitch on all 15 libraries + 4 workbench tools; wifi tagline + new `### CircuitPython connect is blocking` subsection added after CP-adapter source-verified the prior "without freezing your loop" claim was false during connect (15 s stall window); `## Developing this library` → `## Contributing` on 18 files with timing's audience-split + pytest-led body shape; new `## Where this fits` section on every README naming upstream + downstream chumicro deps; `chumicro-checks` brought from minimal shape into template conformity (hero, family blurb, License, full spine); workspace tagline lost "One-stop"; pytest-device's Batch-1-added Docs link backed out (no hosted docs); new canonical `docs/wiring-wifi-credentials.md` replaces the ~10-line duplicate across wifi / requests / http_server / mqtt with 2-line links.  Template rewrite: `readme.md.template` is now a worked example (fictional `chumicro-foo` "single-shot countdown timer") with HTML-comment annotations inside each section — `.format(name, import_name)` still substitutes cleanly.  `chumicro-workspace` 0.20.1 → 0.21.0 (template-shape change is user-visible for future `new-library` scaffolds).  Full preflight green at 96 % coverage; ruff + chumicro-checks both clean.

- [x] **First-clone bootstrap fix — `prepare_workspace.py` re-execs into the venv it creates** (2026-05-11) — README-pass validation by fresh-cloning into `.scratch/` and running `python3 scripts/prepare_workspace.py` from `/usr/bin/python3` (3.9.6) surfaced a long-standing bootstrap bug.  `resolve_python()` correctly created `.venv` via uv (Python 3.12.2), but `install_dependencies()` then called `shared.install_workspace()` *in the host process* — which crashed on a `tomllib` import (3.9 has no stdlib + no `tomli` polyfill) and, on a 3.11+ host, on `from chumicro_workspace.template_apply import ...` because the host Python doesn't have the just-installed packages on its path.  Fixed with two small changes: (a) new `_reexec_into_venv_if_needed(python)` helper in `prepare_workspace.py` that subprocesses the script into the venv interpreter and exits with its return code — called once between `_check_python_version` and `install_dependencies`, no-op when already inside a venv/conda env; (b) `shared.install_workspace()` now calls `site.addsitedir()` on each entry from `site.getsitepackages()` after `install_editable()` so the freshly-written editable `.pth` files are processed mid-process (those are normally only consumed by `site.py` at interpreter startup, so without this refresh the `chumicro_workspace` import that follows still fails even after re-exec).  Both changes are gated — addsitedir only runs when installing into `sys.executable` (cross-interpreter installs would need to subprocess the post-install steps instead).  New tests cover the three re-exec branches (inside venv → no-op, inside conda → no-op, system Python → subprocess + SystemExit with rc) and `test_main_invokes_steps_in_order` updated to assert the new helper fires between version-check and install.  Bench-validated on the `.scratch/fresh-clone-test/` clone: system Python 3.9.6 → "Workspace is ready" end-to-end (exit 0, lint + host tests green inside the freshly-created `.venv`).  38 prepare_workspace tests pass; full preflight green at 96 % coverage.

- [x] **chumicro-deploy CLI hardening — rename + friendly errors + method inference + erase-by-default + 4-board reflash bench** (2026-05-11) — full-matrix CLI test pass from the prior session surfaced four UX fixes; landed them in two commits.  **Commit 1 (rename + wrapper):** `chumicro-deploy flash` → `flash-firmware` (bare `flash` collided with `--deploy-mode flash` / persistent-storage semantics; the new name mirrors the Python API `flash_firmware()`); `cli.main()` gained a try/except wrapping `args.func(args)` over a documented tuple of exception types (CircuitpythonTransportError, MicropythonTransportError, FlashFirmwareError, UnresolvedFirmwareError, DeviceConfigError, FileNotFoundError, ValueError) — users now see `error: <message>` on stderr instead of `error: <good message>` followed by a 20-line Python traceback (six rows in the prior test pass).  chumicro-deploy 0.13.3 → 0.14.0.  **Commit 2 (defaults):** `--method` is now optional — inferred from URL extension (`.uf2` → uf2, `.bin` → esptool, anything else → require explicit); `--erase` (off-by-default) → `--no-erase` (on-by-default), and `flash_firmware(erase_flash=...)` API default flipped False → True.  Rationale: esptool reflashes are first-install / recovery 99 % of the time; leaving leftover sectors creates confusing "works but boots weird" outcomes.  chumicro-deploy 0.14.0 → 0.15.0.  **Bench validation (4 boards, all reflashed to current versions):** Pi Pico W CP 10.2.0 via UF2 (programmatic bootloader entry, drag-n-drop to RPI-RP2); Pi Pico W MP 1.28.0-20260406 via UF2; Lolin S2 CP 10.1.4 via esptool with `--erase` (UF2 path failed because Lolin S2 mini doesn't ship TinyUF2 — the bootloader drive never enumerated, fell back to ROM bootloader at `/dev/cu.usbmodem01`); Lolin S2 MP 1.28.0-20260406 via esptool with `--erase --offset 0x1000` (programmatic ESP32 ROM bootloader entry via `machine.bootloader()` is not implemented on Lolin S2 MP and there's no DTR/RTS auto-strap circuitry on the board, so manual BOOT+RESET was required — surfaced as the next follow-up).  All four UIDs stable post-reflash.  Also patch-bumped workspace 0.20.0 → 0.20.1 to satisfy check-version on the c1d61af4 example_verify docstring change.  886 deploy tests pass; full preflight green at 96 % coverage.  **Open follow-up:** non-auto-strap ESP32 boards (Lolin S2 mini and similar bare DevKits) need either an interactive button-hold prompt or a clear "please hold BOOT + press RESET, retry with --address /dev/cu.usbmodem01" exit message — the current `FlashFirmwareError` is correct but less actionable than it could be.

- [x] **Plans-folder hygiene pass — stale statuses, dead path refs, archive moves** (2026-05-10) — audit of `plans/` for staleness.  Status-header fixes: `workbench-deploy-reliability.md` flipped `active` → `closed` (all 6 steps shipped 2026-05-09 per next-up's own Done log); `deploy-multi-board-and-fskit-followups.md` header rewritten ("Items 1, 2, 3, 4 + doctor wrapper landed 2026-05-09; Item 6 dropped; Item 5 deferred to next natural occurrence" — was claiming only Items 1+3 shipped while the body sections all said DONE).  ADR edits: Decision 0042 (library-dependency-policy) flipped `proposed` → `accepted` per `library-pipeline.md` item #3 (libraries shipping under it confirm the rule); Decision 0027 (device-testing-infrastructure) had `device-config.yml` references rewritten to point at the two-file `workspace.yml` + `secrets.toml` shape that Decision 0057 took over.  Open-question move: "Library dependency policy" pulled from Active → Resolved with a pointer to Decision 0042.  Archive moves: `beginner-onramp.md` (closed end-to-end, self-flagged), `websockets-cleanup.md` (slices A/C/D/E/F/G shipped, Slice B deferred), `device-validation.md` (heavily stale path references to `support/device_transport/`, `device-config.yml`, `scripts/device_config.py`, `scripts/result_parser.py`, `scripts/pytest_device.py` — all moved into `workbench/`), and the root-level `workspace-template-dev-and-regular-mode-gaps.md` (7 of 9 fixes shipped) all `git mv`'d to `workstreams/archive/`.  Cross-refs in Decisions 0039 / 0040 / 0041 and `library-pipeline.md` repointed at archive paths.  Remaining 2 open items from the gaps doc surfaced as a new `## Next` bullet in `next-up.md`: workspace-template README update (gap #4b) and folding `prepare_workspace.py` into `scripts/run.py` (gap #8).
- [x] **Full 4-board sweep validation + CP button-pin autodetect refactor + timing analysis** (2026-05-10) — re-ran the full 6-group sweep (128 deploys across Lolin S2 CP, Pi Pico W CP, Lolin S2 MP, Pi Pico W MP) to catch regressions from the day's substantial changes (helpers.py refactor x2, 5 example files using `helpers.ticks_ms`, ntp library → chumicro_timing + DI alignment, chumicro-wifi detection → `os.uname().machine` whitelist, deploy-example `--tail-seconds` flag, tls_with_custom_ca rewrite).  Result: **127/128 PASS** + one transient FAIL (websockets/circuitpython_server @ pi-pico-w-cp at deploy 10/20 — KeyboardInterrupt mid-`time.sleep(0.02)` loop, no clear source in disconnect path; manual re-test passed cleanly).  Initial Group 4 hit a known design limitation: `runner/circuitpython_button_led` + `timing/circuitpython_debounce` hardcoded `board.D5` (Wemos-only pin); refactored both to a `BUTTON_PIN = ""` override + `BOARD_BUTTON_PINS` whitelist keyed on `board.board_id` (one entry per line, today covers Pi Pico W/2 W → GP14, Lolin S2 mini/pico → D5, Adafruit Feather S2/S3 → BUTTON) — autodetect picks the right pin when BUTTON_PIN is empty, explicit override wins when set.  Re-run Group 4 with autodetect: 8/8 PASS on both CP boards.  **Timing analysis (median per group, PASS only):** smoke (group 1) — S2-cp=19.3s, PicoW-cp=7.0s, S2-mp=5.3s, PicoW-mp=2.5s; network (group 6) — S2-cp=43.8s, PicoW-cp=21.8s.  S2 CP is 3-10× slower than the fastest combo (PicoW MP) — root cause confirmed as S2's slow USB-CDC throughput producing a ~20s rsync floor on `/Volumes/CIRCUITPY` for any deploy (vs PicoW-cp ~6-7s; MP bypasses entirely via mpremote RAM mode).  Network examples on cyw43 show 1.5-2× run-to-run variance from wifi-up timing (mqtt/circuitpython_telemetry on S2-cp: 35.8 → 52.4 → 57.1s across three runs).  Detail: [`workstreams/example-sweep-stability.md`](workstreams/example-sweep-stability.md).
- [x] **Scripts → workbench re-audit + CI rewiring (3 slices)** (2026-05-10) — three-channel parallel investigation (scripts/ inventory, workbench CLI surface, CI workflow audit) re-validated the 2026-04-27 stays-list (`audit_gates`, `bundle_manager`, `docs_deploy`, `prepare_*`, `ide_sync`, `check_*`, plus `render_dep_graph.py` — all mono-repo CI plumbing or hardcoded to the `libraries/` shape), then shipped three independent slices.  **Slice 1 — CI delegation debt:** 4 swaps in `ci.yml` (`check_version.py`, `check_api.py`, `validate_mip_install.py` → `python scripts/run.py <task>`); `run.py`'s `check-version` + `check-api` wrappers gained `--base` passthrough; `check-version` moved out of the no-arg dispatch dict.  **Slice 2 — 5 inline-bash extractions:** `release.yml` 79-line detect job → `scripts/release_matrix.py` (walks `libraries/*/VERSION` + `workbench/*/VERSION`, builds full + library-only matrices, kind=library/workbench, skip-existing-tags safety); `promote.yml` 64-line validate job → `scripts/promote_validate.py` (parse tag regex, locate `libraries/` vs `workbench/`, check preconditions); two duplicated source-archive download/unpack blocks → `scripts/release_source.py` (`gh release download` + zipfile unpack); two near-duplicated bundle commit/tag/push blocks → `scripts/bundle_push.py` (supports both `--matrix JSON` and `--commit-message` shapes); 29-line docs deploy retry loop → `scripts/docs_deploy_retry.py`.  **Slice 3 — `verify_examples` workbench migration:** AST verification logic moved from `scripts/verify_examples.py` to `chumicro_workspace.example_verify` with `display_root` parameterization; `scripts/verify_examples.py` is now a thin shim pinning `display_root=ROOT`; tests migrated to `workbench/workspace/tests/test_example_verify.py`; workspace-template repo can now dogfood the same gate.  `chumicro-workspace` 0.17.0 → 0.18.0 (new public export `verify_examples`).  4 new scripts + 1 new module (~750 LOC) replacing ~250 lines of inline bash + 1 mono-repo-only script.  476 scripts tests + 749 workspace tests pass; full lint clean.
- [x] **example-sweep follow-on — fix runner-shaped time-base mismatch + ticks-everywhere migration + ntp DI alignment** (2026-05-10) — bench-discovered while investigating an HttpTimeoutError side-observation from the `--tail-seconds` work: 5 examples (requests / mqtt / websockets×2 / http_server) were passing `time.monotonic_ns() // 1_000_000` as `now_ms` to runner-shaped clients whose internal deadline math uses `chumicro_timing.ticks_ms` (wrapping mod 2^29).  Different time domains — deadline check fired immediately whenever the two clocks had drifted apart (worked accidentally on fresh-boot boards where both started near 0; broke after multiple deploys this session).  Per user "examples can't import other chumicro libraries" rule + "ticks for everything" directive: added `ticks_ms` / `ticks_add` / `ticks_diff` to `helpers.py` (inline impl matching `chumicro_timing`'s shape, no chumicro_timing import), switched all 5 examples to `from helpers import ticks_ms, ...`, and dropped remaining `time.monotonic`-based wait loops in mqtt + requests for ticks-based equivalents.  ntp library was the outlier — used `time.monotonic` internally for elapsed-time math; switched to `chumicro_timing` (declared as a dep) so its deadline math matches the new helpers.ticks_ms shape; ntp example then converted uniformly.  Follow-on commit aligned ntp's DI shape with the canonical `chumicro_requests` / `chumicro_mqtt` / `chumicro_websockets` / `chumicro_http_server` pattern (per AGENTS.md "Constructor injection for time, I/O, network deps") — accepts `ticks_ms_func` / `ticks_add_func` / `ticks_diff_func` instead of just `ticks_ms`; tests updated to the new kwarg name.  ntp `chumicro-ntp` 0.2.0 → 0.4.0 (chumicro-timing dep + DI shape alignment; pre-1.0 minor for kwarg rename).  Bench-validated 2026-05-10 on Pi Pico W CP: `periodic_get` `[1] status=200 bytes=528 led_ticks=2` (was failing with HttpTimeoutError after 0.5s); `ntp_query` `NTP_OK unix_seconds=…`; `simple_server` listening at `:8080`.  Helpers.py change propagates to all 7 sibling files + scaffold template (md5-identical).  896 unit tests across all 6 affected libraries pass; full lint + verify-examples + dependency-graph regen clean.  Detail: [`workstreams/example-sweep-stability.md`](workstreams/example-sweep-stability.md).
- [x] **example-sweep follow-up #7 follow-on — CYW43 detection via os.uname().machine whitelist (helpers.py + chumicro_wifi)** (2026-05-10) — followed user feedback that both indirect heuristics (helpers.py's first-cut `sys.platform == "rp2"` AND chumicro_wifi's pre-existing `try: import esp32`) are inferences-by-elimination; switched to positive whitelist on `os.uname().machine`.  Today's list is `("Raspberry Pi Pico W with RP2040",)` — exactly THE board we want the CYW43 power-save flag on; new CYW43-bearing boards extend the list rather than relying on an exception path.  Aligned shape across both surfaces: helpers.py uses module-level `_CYW43_MACHINES`, chumicro_wifi exports `CYW43_MACHINES` publicly from `_adapters/mp.py`.  chumicro_wifi `_detect_stack` rewritten — CPython auto-detect now lands on `espidf` (host machine isn't in the whitelist) instead of `cyw43`; the espidf default is safe because its ESP-specific tuning (`reconnects=0`) has its own try/except guard.  Test updates: `test_default_stack_detection_on_cpython_is_cyw43` → `test_default_stack_detection_on_cpython_is_espidf`; new `test_default_stack_detection_picks_cyw43_for_pico_w_machine` monkeypatches `os.uname` to assert positive cyw43 detection on the Pi Pico W machine string; `test_construction_with_default_stack_uses_auto_detect` updated for the espidf default.  Bench-validated 2026-05-10: chumicro_wifi adapter (`wifi/connect_to_ap.py`) prints `ADAPTER: mp_rp2` on Pi Pico W MP and `ADAPTER: mp_esp32` on Lolin S2 MP; helpers.py path (`sockets/tcp_roundtrip.py`) green on both boards (Pi Pico W MP keeps fast connect, Lolin S2 MP no longer hits 0x0102).  `chumicro-wifi` 0.1.0 → 0.2.0 (public detection-behavior shift; pre-1.0 minor).  Helpers.py change propagates to all 7 sibling files (md5-identical).
- [x] **example-sweep follow-up #7 follow-on — gate CYW43 power-save call to rp2 only** (2026-05-10) — bench-discovered while validating the TLS example: Lolin S2 MP wifi-up was failing with `RuntimeError: Wifi Unknown Error 0x0102` (`ESP_ERR_INVALID_ARG` from ESP-IDF).  Root cause: helpers.py called `wlan.config(pm=0xA11140)` on every MP board with `try / except (OSError, ValueError)`, but ESP32 raises `RuntimeError` (the catch-all branch in MP's `esp_exceptions_helper`) — so the exception bubbled out before `wlan.connect()`, and worse, left the wifi stack in `ESP_ERR_WIFI_STATE` until a hard reset.  The "fire and hope" design was wrong-headed regardless of catch-list completeness; switched to positive gating: `if sys.platform == "rp2": wlan.config(pm=0xA11140)`.  Pi Pico W is the only rp2 MP firmware with wifi (plain Pi Pico has no `network` module so wifi_up never reaches this branch on it), and ESP boards skip the call entirely — no exception to catch, no stale-state risk.  Propagated to all 7 helpers.py + scaffold template (md5-identical post-edit).  Bench-validated 2026-05-10: Pi Pico W MP regression green (power-save still disabled, fast TCP round-trip); Lolin S2 MP after hard-reset green (`WIFI_OK ip=172.16.1.16` → HTTP/1.1 200 OK → closed cleanly).  Detail: [`workstreams/example-sweep-stability.md`](workstreams/example-sweep-stability.md).
- [x] **Example-sweep workstream — closed (5 final follow-ups: #3, #5, #7, #8, #12)** (2026-05-10) — three commits closed the last 5 open follow-ups, retiring the workstream from `## Next` entirely.  Commit `961784e` shipped the planning + docs batch (#5 ESP32-S2 hard-fault diagnostic moved to `plans/open-questions.md` since the sweep harness can't trigger it on demand; #7 CYW43 power-save constant duplication accepted with `chumicro_wifi._adapters.mp.CYW43_PM_DISABLE` pointer added to all 6 helpers.py + the new-library scaffold template, md5-identical post-edit; #12 MP missing `TimeoutError` documented as a "Missing builtins on MicroPython 1.28" section in `plans/patterns.md`, no compat polyfill needed since survey found zero callers want bare `TimeoutError`).  Commit `7206061` shipped #8 — `chumicro-workspace deploy-example` gained `--tail-seconds N` threading through to `runner.deploy(tail_seconds=...)` (chumicro-workspace 0.16.0 → 0.17.0; chumicro-deploy 0.13.1 → 0.13.2 for the FakeTransport kwarg + last_tail_seconds attribute), bench-validated on Pi Pico W CP showing the longer window catches a second polling cycle that the default 10 s window missed.  This commit shipped #3 — `libraries/sockets/examples/tls_with_custom_ca.py` rewritten to embed ISRG Root X1 (Let's Encrypt's self-signed root, valid through 2035-06-04) targeting letsencrypt.org:443 (chosen over example.com after bench check showed example.com migrated to Cloudflare/SSL.com — vendor lock-in for the demo target was important), CP-only via `__chumicro_runtimes__ = ("circuitpython",)` after bench discovery that MP rp2 boots with system clock at 2021-01-01 (TLS leaf cert validity check fails), bench-validated on Pi Pico W CP + Lolin S2 CP (full `HTTP/1.0 200 OK` response after TLS handshake using only the embedded root as trust anchor).  Side observation: Lolin S2 MP currently fails any wifi-up call with `RuntimeError: Wifi Unknown Error 0x0102` (reproduced with `tcp_roundtrip.py` too, no TLS involved) — independent pre-existing wifi issue, not blocking.  Detail: [`workstreams/example-sweep-stability.md`](workstreams/example-sweep-stability.md).
- [x] **example-sweep follow-up #13 — ImportGraphSource case-strictness** (2026-05-10) — bench-found during follow-up #6 validation: `from chumicro_timing import Heartbeat` (a class in `heartbeat.py`) was deploying to the device as `Heartbeat.py` instead of `heartbeat.py`.  CP+FAT32's case-insensitive lookup masked it; MP+LittleFS's case-sensitivity exposed it as `ImportError: no module named 'chumicro_timing.heartbeat'`.  Root cause: `Path.is_file()` returns True for case-mismatched lookups on macOS APFS / NTFS, so `_resolve_module` couldn't distinguish a real submodule import from a class-import alias whose name happened to match an existing file modulo case.  Fix: new `_exists_case_strict(path)` helper walks the parent's `iterdir()` and requires an exact-case match, plus `_device_path_for` now uses `resolved_path.name` (on-disk case) instead of `dotted_parts[-1] + ".py"` (import-statement case) as defense in depth.  New regression test `test_class_import_does_not_pull_in_case_mismatched_module` builds the exact "class lives in a lowercase module" shape and asserts both `heartbeat.py` lands and `Heartbeat.py` does NOT.  Bench-validated on Pi Pico W MP — the previously-failing `deploy-example timing micropython_blink` now succeeds (`heartbeat.py` lowercase on device, blink runs).  `chumicro-deploy` 0.13.0 → 0.13.1 (patch, bugfix).  Full preflight + lint clean; 2252 workbench tests pass.  Detail: [`workstreams/example-sweep-stability.md`](workstreams/example-sweep-stability.md).
- [x] **example-sweep follow-up #6 — MicroPython transport's `clean=` kwarg actually cleans** (2026-05-10) — `MicropythonTransport.deploy_files(clean=True)` previously accepted the kwarg "for API symmetry with CP transport" but was a `# noqa: ARG002` no-op.  Now wipes `:/lib` on the device via `mpremote fs rm -r :/lib` before the `fs cp -r` push when `mode="copy"`, mirroring the CP rsync `--delete` semantics for the actual accumulation site (chumicro_* and other library packages under `/lib`).  Top-level user-managed files (`boot.py`, `main.py`, `settings.toml`, `runtime_config.msgpack`) live outside `/lib` and survive unchanged.  Mount mode is a no-op (transient by design).  New `_clean_device_lib` helper tolerates a missing `/lib` (first deploy on a clean device).  Four new unit tests: default-`clean=False` doesn't wipe, `clean=True` issues `fs rm` BEFORE `fs cp` (ordering matters), missing-`/lib` first-deploy case, mount-mode no-op.  Bench-validated on Pi Pico W MP: `/lib` carried 11 packages from prior sweeps; after `deploy-example timing micropython_blink --non-interactive` (passes `clean=True` by default), `/lib` had just `chumicro_timing/`.  Surfaced a side bug — `ImportGraphSource._device_path_for` emits import-statement case (`Heartbeat.py`) rather than on-disk case (`heartbeat.py`); CP+FAT32 case-insensitive lookup masks it, MP+LittleFS doesn't.  Filed as follow-up #13.  `chumicro-deploy` 0.12.0 → 0.13.0 (behavior change on MP transport).  Full preflight green at 96% coverage; 2251 workbench tests pass.  Detail: [`workstreams/example-sweep-stability.md`](workstreams/example-sweep-stability.md).
- [x] **example-sweep follow-up #4 — `chumicro-config` docs rewritten for flat-key `from_config`** (2026-05-10) — all three hand-written doc files (README, docs/index.md, docs/guide.md) rewrote `WifiConfig.from_dict(config["wifi"])` (nested-section-dict pattern) → `WifiConfig.from_config(config)` (flat-key pattern), matching what library code + every consumer + the auto-generated API ref has been using since the migration in commit `30e2878`.  Key conceptual shifts: opener tagline reframed; library-author template grew the `prefix="wifi"` kwarg; runtime-config shape rewritten as flat dotted keys (with a separate "source TOML stays nested for humans, deploy flattens it" subsection); added a `try_load_section` section the old docs never taught; named the `RuntimeConfig` dict-like view explicitly throughout.  Pre-existing `end_to_end.py` example was already correct — matched its shape across the docs.  `chumicro-config` 0.2.1 → 0.2.2 (patch, docs-only).  `python scripts/run.py docs` green; lint + per-library tests green.  Detail: [`workstreams/example-sweep-stability.md`](workstreams/example-sweep-stability.md).
- [x] **example-sweep follow-up #10 — strict drive verify + boot_out.txt preservation + find_circuitpy_drive removal** (2026-05-10) — follow-on to the field-removal in commit `764124be`, surfaced by a bench run on a multi-CP-board host.  Two bugs the old `circuitpy_drive_path` field had been masking, plus one cleanup: (a) `deploy_files(clean=True)` (deploy-example's default) was wiping `boot_out.txt` via rsync --delete because the exclude tuple only listed `settings.toml` + `boot.py` — added `boot_out.txt` so the file CP only writes on hard reset survives clean deploys.  (b) `_verify_drive_for_board` failed open when `boot_out.txt` was absent on `drive_path`, silently routing the deploy to `candidates[0]`; rewritten to probe the connected board and scan sibling candidates' `boot_out.txt` for a UID/machine match (new `_identify_drive_via_probe` helper), raising `CircuitpythonTransportError` with a hard-reset recovery hint when no candidate can be identified.  (c) Dropped `find_circuitpy_drive()` (the bare-name single-mount finder) — strictly redundant with `_circuitpy_volume_candidates()` + verify now that no caller wants the legacy mount-order-first shape.  Test helpers (`_make_flash_transport`, `_connect`) gained `_plant_verifiable_circuitpy(drive, monkeypatch)`: plants `boot_out.txt` with a standard identity AND patches `probe_implementation` to return the matching `DeviceImplementation`, so flash-mode tests cheap-path through verify without queuing probe responses in their fake-port read sequences.  `TestDriveVerification.test_skips_verification_when_boot_out_missing` rewritten as `test_raises_when_boot_out_missing_and_probe_unavailable` to assert the new strict contract; new `test_auto_corrects_when_boot_out_missing_but_sibling_matches` covers the auto-correct path.  chumicro-deploy 0.11.0 → 0.12.0.  Bench-validated 2026-05-10: Pi Pico W CP deploy auto-correct fires (lands on `/Volumes/CIRCUITPY 1` despite `candidates[0]` being the Lolin S2's `/Volumes/CIRCUITPY`); the mistake of writing `microcontroller.reset()` to `code.py` as a one-shot reset trick (boot-looped the Lolin S2 into safe mode) yielded a new feedback memory (never persist reset calls — use raw REPL exec only).  Full preflight green at 96 % coverage; 2247 workbench tests pass.  Detail: [`workstreams/example-sweep-stability.md`](workstreams/example-sweep-stability.md).
- [x] **example-sweep follow-up #9 — doctor `SERIAL PORTS` check** (2026-05-10) — added `check_serial_ports_held` to `workbench/workspace/src/chumicro_workspace/health.py`; doctor now walks every device in `devices.yml`, runs `lsof` against each address (via the existing `chumicro_deploy.recovery.diagnose_port_holders`), and surfaces a WARN finding listing held ports + PIDs + commands when something is holding a port.  Workstream entry's CoolTerm-specific framing dropped — the symptom is identical for any serial-terminal app (Mu, Thonny, screen, minicom, CoolTerm, PyCharm / VS Code serial console, orphan mpremote).  Doctor-only — `lsof` is too heavy for the per-second status loop.  Six unit tests cover Windows skip, no-devices.yml, empty registry, no-holders OK, held-port WARN with PID + command in the hint, and best-effort behavior on `lsof` errors.  Existing `recovery.py` after-the-fact diagnosis stays unchanged — both surfaces (doctor pre-check + recovery post-check) remain useful.  Considered but skipped: `chumicro-workspace doctor --release-port <id>` AppleScript / dbus auto-disconnect (OS-specific, doesn't generalize across the long tail of holder apps), and a sweep-harness fast-fail (sweep harness is gitignored — moot).  `chumicro-workspace` 0.15.0 → 0.16.0.  Detail: [`workstreams/example-sweep-stability.md`](workstreams/example-sweep-stability.md).
- [x] **example-sweep follow-up #11 — library pyproject.toml ↔ src/imports audit (with surprise)** (2026-05-10) — AST-walked `libraries/*/src/` for actual cross-library imports (eager + lazy) and compared against each `pyproject.toml`'s `dependencies`.  Original workstream claim (`chumicro-sockets` missing `chumicro-timing`) was wrong — the sockets library has zero cross-library imports today; an older shape may have used `ticks_ms` for socket-side timeouts but it's been refactored out.  Real finding: `chumicro-http-server` imports `chumicro_config` lazily for the `MissingConfigKey` exception type (raised from `HttpServer.from_config()` on half-TLS configuration) but did not declare it as a dep — users without `chumicro-config` installed got an `ImportError` instead of the proper validation error.  Fix: added `chumicro-config` to `libraries/http_server/pyproject.toml` `dependencies`; bumped VERSION 0.2.1 → 0.2.2.  Audit also confirmed `ntp` / `requests` / `websockets` correctly *don't* depend on chumicro-config — their `from_config()` factories use plain `config.get(key, default)` so they work on either RuntimeConfig or plain dict, never raising `MissingConfigKey`.  Regenerated `support/docs/dependency-graph.svg`; full preflight green.  Detail: [`workstreams/example-sweep-stability.md`](workstreams/example-sweep-stability.md).
- [x] **Bench validation — helpers.py refactor + simple_server + two_board_handshake all green** (2026-05-10) — Group 6 (CP-only network sweep, `.scratch/sweep_examples.py`) re-ran with the refactored examples: 18/18 PASS across `lolin-s2-cp` + `pi-pico-w-cp`, covering the 8 helpers.py'd library examples plus the new `http_server/circuitpython_simple_server`.  Spot-checked logs confirm full helpers.py path firing end-to-end on real hardware (`ntp_query` → `WIFI_OK ip=172.16.1.29 / NTP_OK unix_seconds=1778433511` against the public NTP pool, proving the inline msgpack decoder reads creds from `/runtime_config.msgpack` correctly).  Workspace-template's `two_board_handshake/` validated **cross-runtime**: server on `lolin-s2-cp`, client on `pi-pico-w-mp`, five round-trips at `status=201` in client tail, `sensor=demo-temp value=24.95` lines in server tail, host-side `curl` returns latest reading as JSON.  Mid-session a transient pi-pico-w-cp see-the-file timeout was filed as a new follow-up; on retest after the session settled the same workspace-deploy boot-shim path ran clean — the timeouts were rapid-reset / FSKit-residue-state artifacts, not a real bug.  Filed-and-retracted finding kept the lesson in the commit history; workstream open count stayed at 10.  `chumicro-workspace` 0.14.0 → 0.15.0 (scaffold integration; `simple_server` replaced two_thing_* pair, dep-rule cleanup).  Detail: [`workstreams/example-sweep-stability.md`](workstreams/example-sweep-stability.md).
- [x] **example-sweep follow-up #2 — `helpers.py` standalone with inline msgpack decoder + library-scaffold integration** (2026-05-10) — replaced the `try: import msgpack except ImportError: return {}` path in `helpers.py` with a 60-line inline decoder covering every msgpack type the `chumicro-workspace` deploy pipeline produces (nil / bool / every int width / float 32+64 / str / bin / array / map; no ext / timestamp).  Pi Pico W MicroPython no longer needs `mpremote mip install msgpack` — same code path on every runtime, validated against the reference `msgpack` impl on the host across small + large maps, all int widths, every string-length tier, nested structures, floats, bools, and arrays.  Single `if not ssid or ssid == "your-wifi-ssid"` placeholder check replaces the 3-element sentinel set.  All 6 library `helpers.py` files identical (md5 verified) — single canonical version.  Promoted to library scaffold: `workbench/workspace/src/chumicro_workspace/_payloads/library_template/helpers.py.template` ships with every `python scripts/run.py new-library`, so a fresh library starts with a working copy that the user deletes if its examples don't need wifi.  `new-library` skill updated with the delete-if-unused instruction; `test_scaffold.py` asserts the file lands.  Lint clean, full preflight green at 96 % coverage.  Detail: [`workstreams/example-sweep-stability.md`](workstreams/example-sweep-stability.md).
- [x] **Two-board client/server example shipped (workspace-template)** (2026-05-10) — `examples/two_board_handshake/{server,client}/` lives in the workspace-template repo (commit `129d3ee` there).  Server side runs `chumicro-http-server` on board A with `GET /` (HTML status), `GET /api/latest` (JSON), `POST /api/sensor` (accepts JSON readings) routes.  Client side runs `chumicro-requests` on board B and POSTs a synthetic sine-wave reading to the server every 5 s, self-healing on wifi drop via `chumicro_sockets_factory`'s rebuild-on-connect.  Both follow the established workspace example shape (`def run()` in `app.py`, runner-shaped via `chumicro_runner.Runner`, config from `chumicro_config.load_runtime_config`) — same pattern as `periodic_get/` and `telemetry_publisher/`.  Server prints its own IP at startup so users know what to plug into the client's `project_config.toml`.  Updated `examples/README.md` index — replaced stale `two_projects/` row with `two_board_handshake/` entry.  Resolves the next-up entry captured 2026-05-06 during F5 boot-shim simplification.  Bench validation across the 4-board matrix queued together with the helpers.py refactor sweep.
- [x] **example-sweep follow-up #1 — 8 network examples migrated to per-library helpers.py** (2026-05-10) — 8 examples in `libraries/{ntp,requests,mqtt,websockets,http_server,sockets}/examples/` had been importing `chumicro_wifi` (and `chumicro_config`) even though their owning library's `pyproject.toml` didn't declare those as deps — direct violation of the "examples can only import owning-lib's declared deps" rule.  Solution mirrors the canonical sockets/examples/helpers.py shape from commit `30de95ce`: each network library now ships its own `examples/helpers.py` exposing `runtime_config()` + `wifi_up(ssid, password) -> (radio, ip)`, using raw runtime primitives (CP `wifi.radio` / MP `network.WLAN`) so the helper itself doesn't pull in any library the owning lib doesn't depend on.  Renamed `_runtime_config` → `runtime_config` in the canonical file to expose it publicly.  Examples lost ~700 lines of `WifiService` / `_drive_until` / `service.check / handle / state` plumbing and gained focused API demonstrations.  Examples now read the deployed config as a plain dict via `helpers.runtime_config()` and pass it directly to `from_config()` factories (which accept dict-or-RuntimeConfig per `chumicro_config.section.load_section`'s contract).  `verify_examples.py` clean (48 examples), full lint clean, full preflight green at 96 % coverage.  Bench validation across the 4-board matrix still queued.  Detail: [`workstreams/example-sweep-stability.md`](workstreams/example-sweep-stability.md).
- [x] **chumicro-checks polish — granular tool selection + segment-match scope predicates** (2026-05-09) — both follow-ups from the chu-rules-home migration shipped.  (a) `quality.lint.tools` knob on `workspace.yml`'s `quality.lint` block: defaults to `["ruff", "chumicro-checks"]`, drop one to disable that tool without disabling the whole phase, empty list short-circuits like `enabled = false`.  Closed-set validator rejects unknown entries at config-load time so a typo like `"mypy"` surfaces with a `WorkspaceConfigError` naming the known tools instead of silently no-op'ing.  Threaded through `_cmd_lint` so each tool's import-probe + subprocess block runs only when listed; chumicro-checks failure now propagates its own exit code.  Workspace template's commented `quality:` block grew a `lint.tools` example.  (b) `_outside_chumicro_workspace` / `_outside_chumicro_checks` in `chu006.py` converged to a shared `_under_subdir(filepath, parent=..., child=...)` helper that walks `Path.parts` and matches the literal segment pair — same shape `_outside_runpy_owners` already uses.  Substring matches on `as_posix()` retired; pathological paths like `my-workbench/workspace-staging/...` and `foo-workbench-workspace-bar/...` no longer satisfy the predicate.  9 new unit tests (5 quality config + cli, 4 chu006 segment-match), 957 workspace tests + 234 checks tests pass.
- [x] **Example sweep — 4-board stability audit + structural cleanup** (2026-05-09) — 128 deploys across the canonical 4-board matrix (Lolin S2 CP, Pi Pico W CP, Lolin S2 MP, Pi Pico W MP) over six grouped passes; harness in `.scratch/sweep_examples.py`.  Real fixes shipped along the way: `FLASH_COPY_FAILED` recovery hint corrected (reformat is primary, not RESET — bench experience, not msdos hiccup); `devices.yml` CIRCUITPY drive paths swapped to match boot_out UIDs; 8 cpython-only / template-shaped examples deleted (7 fake-using `quickstart.py` files + `udp_echo_loopback.py`); new `wifi/examples/connect_to_ap.py` (real AP demo); 4 leftover `quickstart.py` renamed to descriptive names (`pub_sub_drain` / `boot_counter` / `stream_handler` / `connect_to_ap`); scaffold template + test renamed (`basic_usage.py.template`); `config/end_to_end.py` modernized to flat-key `from_config(..., prefix=...)` (was crashing on every runtime since the `30e2878` migration); `sockets/tcp_roundtrip.py` rewritten to hit `example.com:80` with wifi-up; CP button examples got `BUTTON_PIN = ""` override + autodetect fallback (`board.D5` was Wemos-only); `deploy-example` gained `--no-clean` opt-out (default wipes stale `lib/` so each demo lands fresh — Pi Pico W's 491 KB filled at 98% pre-fix); sweep classifier extended for `Hard fault` / `Running in safe mode`.  Three follow-ups open: `tls_with_custom_ca.py` design call (delete vs embed CA), `chumicro-config` README/index/guide doc drift (still document `from_dict`), and a CP+ESP32-S2 hard-fault on `tcp_client_socket` to unreachable host with stale wifi state (clean OSError on fresh boot).  Detail: [`workstreams/example-sweep-stability.md`](workstreams/example-sweep-stability.md).
- [x] **CHU rules home — `chumicro-checks` workbench package shipped, all 12 rules migrated, CHU008 written** (2026-05-09) — full implementation of [Decision 0060](decisions/0060-chu-rules-home.md) across 11 commits.  New `workbench/checks/` → `chumicro-checks` package on PyPI, hosting `CHU001` (names), `CHU002`–`CHU005` (whitespace family), `CHU006` (publishable-tree leaks), `CHU007` (workbench-no-lib-imports), `CHU008` (workspace-template isolation), `CHU009`/`CHU010` (silent test skips), `CHU011` (plans-doc brevity), `CHU012` (dated narration).  Each rule self-scopes — walks paths it targets, returns no findings in repos where those paths don't exist — so the same package is safe to run in the chumicro mono-repo, the workspace-template repo, or a downstream user workspace.  CHU006 + CHU008 share a `LeakRule` base abstraction; both honor distinct rule codes.  `[tool.chumicro-checks] ignore = [...]` in `pyproject.toml` for per-repo opt-out, with CLI `--select` / `--ignore` overrides.  `# noqa: CHU0NN` (`<!-- noqa: CHU0NN -->` for Markdown) suppression unchanged.  `scripts/run.py:lint()` collapsed from per-rule callers into a single `python -m chumicro_checks` invocation after ruff; the seven legacy `scripts/check_*.py` scripts + their tests deleted.  CHU008 catches a real leak today on the template repo: `projects/wifi_only/project_config.toml:7` for "Decision 0057".  Cross-repo checks (speculative-public-API, cargo-cult-class-method) split out as future agent-skill territory.  202 chumicro-checks unit tests at 95% coverage; full preflight green; AGENTS.md + ADR 0022 + ADR 0058 + style-guide + audit-publishable-isolation skill all rewritten to point at the new package home.  Workspace-template repo follow-up (its own `[tool.chumicro-checks]` block) remains, deferred to a downstream commit there.
- [x] **Code-comment anti-pattern audit — CHU012 lint shipped + 122 violations across 38 files cleaned** (2026-05-09) — wider grep against AGENTS.md's "no historical / dated / removed-code framing" rule surfaced 86 violations in source / test / docs trees on top of the 36 the initial scan caught, then the wider Slice / Phase / Step pattern set caught a further wave.  `scripts/check_dated_narration.py` (CHU012) ships 12 patterns: dated incidents (Surfaced / Discovered / Observed / Captured / Caught / shipped / landed + ISO date), dated verifications (verified live | on hardware + date, live-tested | bench-tested + date), workstream-phase pointers (Phase | Step | Slice + workstream, workstream Phase N), F-numbered + ISO-date pairs anywhere on a line, verification-pass / finding labels with a date, "Regression for YYYY-MM-DD", hyphenated workstream-named passes ("in the deploy-audit pass"), removed-code framing (Earlier versions / Previously this / We used to / Used to be), Slice + N (Slice is workstream-only jargon in this codebase), and Phase | Step + N when NOT followed by a colon (procedural numbered steps stay clean).  78 unit tests at the pattern + end-to-end level; lint wired into `python scripts/run.py lint` so future regressions fail at preflight.  Edits across libraries / workbench / scripts trees were docstring / comment / help-text only; full preflight green and 4534 tests pass.  Detail: [`workstreams/archive/comment-anti-pattern-audit.md`](workstreams/archive/comment-anti-pattern-audit.md).
- [x] **Multi-board CP deploy + FSKit follow-ups — Items 4 + 2 (source-side) shipped, doctor --fix-fskit-wedge wrapper added + plain doctor now detects, Item 6 dropped, Item 5 runbook drafted** (2026-05-09 follow-on) — Item 4 caveat (`docs/troubleshooting/macos-circuitpy.md` paragraph above the bash code block + new fix_step in the `MACOS_FSKIT_WEDGED` `RecoveryPlan`) names the I/O-error state and physical replug as recovery, so a user pasting the killall command sees "run it ONLY ONCE per wedge" before the second-run damage scenario can happen.  Item 2 source-side reframed `_wait_for_circuitpy_remount`'s timeout message to match `_CIRCUITPY_DRIVE_PATTERNS["circuitpy drive not mounted"]` so the recovery layer's existing `CIRCUITPY_DRIVE_MISSING → MACOS_FSKIT_WEDGED` promotion fires when the FAT volume stalls during a wipe, plus a session-scoped autouse fixture in `workbench/deploy/functional_tests/conftest.py` that pre-checks the wedge and skips the suite with the recovery command instead of grinding through 10-second-per-test timeouts.  Initially deferred `chumicro-workspace doctor --fix-fskit-wedge` as ADR-worthy; on push-back recognised it's a regular feature decision and built it in the same session: detect-refuse-run-recheck wrapper around `subprocess.run(MACOS_FSKIT_RECOVERY_COMMAND.split())` with distinct exit codes per refuse class (non-darwin / not-wedged / no-tty / no-sudo / persists), 7 unit tests stubbing the detector + subprocess.  Plain `chumicro-workspace doctor` (no flag) didn't catch wedges either — added `check_macos_fskit_wedge()` to `health.py` as the sixth doctor check (no-op on non-darwin), surfacing an ERROR finding pointing at `--fix-fskit-wedge` when wedged; status stays light by not running it.  Item 6 (proof-of-life check inside `wipe_filesystem`) considered and rejected — only concrete trigger was the now-fixed Item 3 test, no grounded "future causes", current `try: ... except Exception: pass` is correct for the only real-world outcome (USB-CDC drops mid-call as the success signal).  Item 5 (deliberate FSKit wedge on a fresh boot) needs hardware — runbook in the workstream covers three approaches: natural trigger via 4-board CP sweep, synthetic small FAT12 + tight attach/detach loop, or wait-for-it; once wedged, `chumicro-workspace doctor` confirms detection, `doctor --fix-fskit-wedge` (or manual paste) clears it, doctor confirms cleared.  846 deploy + 705 workspace unit tests pass.  Detail: [`workstreams/deploy-multi-board-and-fskit-followups.md`](workstreams/deploy-multi-board-and-fskit-followups.md).
