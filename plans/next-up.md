# Next Up

> **Agent-managed file.  Single source of truth for the work queue.**  Each top-level bullet is capped at 5 bullet points (lead + sub-bullets, CHU011) — anything that needs more lives in a workstream file under [`workstreams/`](workstreams/), surfaced from here as a one-line pointer.  `## Done (recent)` is capped at 5 entries — drop the oldest when adding a new one.  Each Done entry: subject + commit hashes + headline result + workstream pointer; aim for under ~500 chars.  Detail lives in the commit message and the workstream doc.  **No zombie placeholders:** a fully-shipped effort whose only residual is "redo when problem X recurs" is *closed*, not parked in `## Next` — the archived workstream + git history carry the context, and a fresh bullet gets *created* when X actually recurs.  Session warm-up: `git --no-pager log --oneline -20` then this file.

## Now

_(nothing in flight)_

## Next

Independent items.  Most have either shipped phases (status in the linked workstream) or are unscoped placeholders waiting on a forcing function.

- [ ] **CHU prose/isolation lint gaps** (opened 2026-05-18, parked) — three CHU-rule changes that close gaps through which dateless landed-history (CHU012), `AGENTS.md`/`.scratch/` mono-repo pointers (CHU006), and the "the one/single X" prose tic pass a green lint; all surfaced when they escaped preflight in deploy-path-unification 2c. All route through `new-decision`. (#2 history: "mechanical" → revised when `example_source.py` looked like a legitimate `.scratch/` runtime-path use → **re-reverted 2026-05-18**: that use was itself a deviation from the `_generated/` build-artifact convention, now fixed, so `.scratch/` in publishable `src/` has no known legitimate hit and the CHU006 add is mechanical again — still routes `new-decision` per Decision 0074, no longer blocked by a legitimate-use exception.) Reconcile with the `open-questions.md` comment-lint candidate — prior art. Detail: [`workstreams/chu-prose-isolation-lint-gaps.md`](workstreams/chu-prose-isolation-lint-gaps.md).

- [ ] **CI infrastructure workstream (unscoped — fires when CI is re-enabled)** — CI / GitHub Actions is disabled and has more structurally wrong with it than any single audit surfaced; it gets its own workstream rather than point-patches. Two concrete starting items inherited from audit-remediation P1 (analysis preserved in [`workstreams/audit-remediation-and-drift-mechanization.md`](workstreams/audit-remediation-and-drift-mechanization.md) Phase 1): (a) `ci.yml` lint job installs only `ruff`, so `run.py lint`'s `python -m chumicro_checks` can't run there — CHU enforcement never executes in CI; (b) `release.yml` publishes to immutable PyPI on a VERSION bump with `needs: detect` only, ungated by ci.yml success and before its own `validate-mip`. Neither is a safe isolated point-fix while CI is off (can't validate against a real run; the file is due for rework) — both wait for the dedicated pass.

- [ ] **Workspace-template gaps — gap #4b (regular-mode README)** — 7 of 9 shipped; gap #8 dropped 2026-05-12 (reversed; reasoning in archive §8).  One scoped task left: regular-mode README update on the [workspace-template repo](https://github.com/ChuMicro/ChuMicro-Workspace-Template) documenting `chumicro-workspace install-libraries <project>` + a manual `circup` / `mip` fallback recipe (mono-repo half shipped).  Detail: [`workstreams/archive/workspace-template-dev-and-regular-mode-gaps.md`](workstreams/archive/workspace-template-dev-and-regular-mode-gaps.md).

- [ ] **`websockets` oversize-frame length on Lolin S2** (surfaced 2026-05-16, 4-board campaign) — `websockets/test_drop_with_event_drains_oversize_and_stays_open` is deterministic on Lolin S2 (MP **and** CP), passes on Pico W: oversize frame reports length 600, test expects `(0, 400]`.  Real S2-specific websockets/buffer-chunking behaviour question, not environmental.  (The campaign's other findings closed: fragmentation suites deleted, CP-RAM `ntp` ceiling characterized under Decision 0072, network-bench gap was an environmental subnet/`mosquitto` mismatch — all in git history / their target workstreams.)

- [ ] **Expand the device test matrix beyond ESP32-S2** now that transport tooling is proven on both MicroPython and CircuitPython.

- [ ] **Performance + resource benchmarking infrastructure** — measure heap + CPU per library operation with explicit GC control; per-benchmark thresholds that fail on regression; separate `bench` task or deeper test tier so slow benchmarks don't run on standard `test`; CI on a schedule.

- [ ] **Deploy-path unification — one mechanism puts code on a board** (opened 2026-05-18) — divergent staging paths for one operation; additive-vs-clean-slate was the core defect. **[Decision 0077](decisions/0077-one-device-staging-path.md) `accepted`**; AGENTS.md one-staging-path non-negotiable in force. Done + bench-verified Pi Pico W CP: Phase 0 (ADR), Phase 1 (preflight `_env` fix; repl `chumicro_timing` board-dead regression fixed by retiring `repl <project>` → `deploy --tail`), **Phase 2 complete** (Commits 1a/1b/2a/2b + delta 6 + Commit 2c): single source owner; CP empty-dir reaping; keep-set unification; clean-slate default-flip with `--no-wipe`/`--wipe`; `settings.toml` evicted + one-time notice; 0059 §1 edited in place; MP whole-device clean-slate (delta 6, bench-verified Pi Pico W MP); Commit 2c — `FUNCTIONAL_TEST_EXTRA_EXCLUDES` deleted (CP functional = byte-identical clean rsync), `PostStageStep` names the post-stage fork, MP functional first-stage `mkfs`→keep-set-preserving `_clean_slate_device` (CP unit-tested; functional-suite bench the one open hardware step). **Phase 3 design settled 2026-05-18 (ADR-before-code, all forks user-decided): [Decision 0078](decisions/0078-library-acquisition-is-host-local.md) `accepted`** — Collapse A (`deploy-example` → `ExampleSpec` branch on `resolve_project_deploy_source` + `deploy_diff`; `Deployer.deploy()` deleted, demo + low-level `chumicro-deploy` CLI fold onto `deploy_diff`; `wrap_with_runtime_config` tail-dedup; interactive REPL drop kept), Collapse B (`install-libraries` board-push retired → host-local `_libraries/` fetch + the one deploy bundles it). **Remaining:** Phase 3 *implementation* (next unit of work), Phases 4–5 (`libraries/` root convergence; CHU mechanization per 0074). Cross-refs: deploy-mode-unification (complete, mode axis), walker-unresolved-import-failure (open neighbor). Detail: [`workstreams/deploy-path-unification.md`](workstreams/deploy-path-unification.md).

- [ ] **CHU020 — mechanize the closed AI-tic prohibited-phrase set** (raised 2026-05-18) — AGENTS.md "Writing tone" bans (`comprehensive`/`robust`/`seamlessly`/`cutting-edge`/`best-in-class`/`It is worth noting that`/`Let's dive into`/…) plus the empty-definition openers (`the single source of truth`, `the one closed <noun>`) are doc-guidance only — unmechanized. Per [Decision 0074](decisions/0074-drift-mechanization-as-project-policy.md) a deterministically-lintable drift class must be linted. Scope = a closed phrase set (substring/regex), `# noqa: CHU020`-escapable, same publishable+docs scan as CHU017, exempting quoted/ban-discussion text (CHU006 pattern). Explicitly NOT a general `the one X`/`the single X` matcher — that false-positives on legitimate invariant prose (0077's "exactly one mechanism", "the single owner of …"); that half stays ADR/review judgement per 0074. Routes through `new-decision` (a 0074 consequence) + AGENTS.md cross-ref. Surfaced from a tone correction on the `DEVICE_KEEP_SET` docstring.

- [ ] **Deploy walker fails on unresolved imports** (proposed 2026-05-12, separate session) — `workbench/deploy/src/chumicro_deploy/sources.py:367-378` silently skips imports that don't resolve against `search_paths`.  Result: a file containing `from chumicro_sockets import ...` can be deployed without `chumicro_sockets` in `library_sources`, ImportError surfaces only at first boot.  Today's DI cleanup (Done) removed the unconditional top-level imports that were riding on this silent-skip; the walker fix is the load-bearing correction so that any *future* unresolved import is a deploy-time failure with a clear message naming the importing file + the missing module.  Decision 0062's bench-validation table also needs a corrective rerun once landed.  Detail: [`workstreams/walker-unresolved-import-failure.md`](workstreams/walker-unresolved-import-failure.md).

## Investigations

- [ ] **Investigate slow MicroPython RAM-mode functional test runs** — observed during 2026-04-19 live PyCharm testing that MicroPython RAM-mode functional tests took noticeably longer than expected.  CircuitPython RAM-mode is fast in comparison.  Suspects: per-file `mpremote mount` cost, cold-start interpreter overhead, batch-vs-per-test trade-off.  Profile against the new batch-execute path.

## Done (recent)

> Capped at 5 entries — subject + commit hashes + headline result + workstream pointer; under ~500 chars each. Verbose detail lives in the commit message body or [`workstreams/<name>.md`](workstreams/) (per-phase acceptance).

- [x] **Deploy-path unification Phase 3 — design settled + Decision 0078** (2026-05-18, this commit) — ADR-before-code per the workstream charter; all forks user-decided via clarifying questions. Collapse A (implements accepted 0077 + 0059 §1, no new ADR): `deploy-example` becomes a thin front-end — new `ExampleSpec` branch on `cli/deploy.py:resolve_project_deploy_source` (one source owner for project + example), stages via `runner.deploy_diff(...)` byte-identical to `deploy`, so `Deployer.deploy()` + the wrapper `.deploy()` methods are deleted (dead code — nothing published, sole consumer is us), `_cmd_demo` + low-level `chumicro-deploy deploy` CLI fold onto `deploy_diff`, 4 `deploy/examples/*.py` rewritten; `wrap_with_runtime_config` helper in `deploy_source.py` dedupes the cross-front-end tail; interactive REPL drop kept (0077-sanctioned post-stage fork). Collapse B: **[Decision 0078](decisions/0078-library-acquisition-is-host-local.md) `accepted`** — `install-libraries` board-push retired; library acquisition is host-local (`_libraries/` fetch via pip/mip/circup `--target`, registered in `library_sources:`), the one deploy bundles it. 0077 §Consequences + Related, 0059 Related, AGENTS.md one-staging-path rule + `_libraries/` convention edited in place. No code yet — implementation is the next unit of work. Detail: [`workstreams/deploy-path-unification.md`](workstreams/deploy-path-unification.md).

- [x] **AGENTS.md over-compression reversal — argument-stopping *why* back inline** (2026-05-18, this commit) — `7f19a109`'s "directive-only ~27 % trim" had cut operational directive (abbrev table, suppression-legit set, comment bad-examples, coverage-scope clause, Writing-tone worked pair) and demoted rule-rationale to a not-auto-loaded `AGENTS.notes.md` agents never open. Restored inline (slots-why, PSRAM≠HAL, coverage scope, loud-skip + git-staging incidents, boot-loop path); `AGENTS.notes.md` deleted + its 4 refs cut; the genuinely-good compressions (skills table, ADR-pointer relocation) kept. 24.0→27.6 KB. Principle (why-that-stops-arguing is directive, stays inline; no second not-auto-loaded governance file) is an ADR candidate — routes through `new-decision`, not yet filed.

- [x] **Deploy-path unification Phase 2 Commit 2c — entrypoint exclude removed; post-stage fork named** (2026-05-18, this commit; resumed from the `2026-05-18-deploy-path-unification-phase2` handoff) — delta 5. `FUNCTIONAL_TEST_EXTRA_EXCLUDES` deleted so the CP functional-test stage issues the byte-identical clean rsync a production deploy does (`--delete` + `DEVICE_KEEP_SET`); a stale board `code.py`/`settings.toml` is now reconciled away, not preserved (user-approved behavior change — running the functional suite wipes a deployed project, keep set surviving). Post-stage fork named `PostStageStep` (`SOFT_REBOOT_AND_TAIL` vs `HARNESS_EXEC_OVER_REPL`), recorded on the transport so the identical-bytes/named-fork invariant is unit-assertable and Phase 5's lint has a symbol. Scope expanded (user-decided) to converge the MP functional path: its first copy-mode stage blind-`mkfs`'d the whole filesystem (destroying `_chu_kv.msgpack`/`boot_out.txt` — a deeper 0077 violation than the CP carve-out), now calls the same `_clean_slate_device`; Decision 0071's subsequent-stage tracked-delete unchanged (keep-set-safe by construction). 0077 entrypoint clause corrected in place + 0077↔0071 reconciled both ways; `open-questions.md` MP-dirty-board entry resolved + deleted; handoff file removed. CP unit-tested (deploy + pytest-device suites green, gated coverage); functional-suite bench the one open hardware step. deploy 0.27.0→0.28.0. Detail: [`workstreams/deploy-path-unification.md`](workstreams/deploy-path-unification.md).

- [x] **Deploy-path unification Phase 2 delta 6 — MP whole-device clean-slate** (2026-05-18, this commit) — completes the MP side of the keep-set/clean-slate convergence (CP landed in 2a/2b). `_clean_device_lib`→`_clean_slate_device`: scoped delete of every device-root entry except `DEVICE_KEEP_SET` (was `fs rm -r :/lib` only), then `fs cp` repopulates payload — MP analog of CP's `rsync --delete` + keep set. MP `list_files_in_scope(clean_slate=True)` walks the whole device (`_LIST_ALL_SCRIPT`) and host-filters keep set + dotfiles, so the diff (CLI default) reconciles stale root files incl `settings.toml`, not just `/lib`. Bench-caught + fixed an ordering bug: the clean-slate must run as an mpremote `exec` subprocess, not the persistent serial (which holds the port the following `fs cp` needs). Bench-verified Pi Pico W MP: a clean `deploy-example` on a board full of stale `test_*.py` + planted `settings.toml`/`junk.txt` collapsed to payload + `_chu_kv.msgpack` (keep set). 0077's MP-mechanic bullet updated (now implemented). 3 MP tests rewritten/added; deploy 0.26.0→0.27.0; 1771 deploy+workspace tests green, gated coverage 94 %, lint+CHU clean. Detail: [`workstreams/deploy-path-unification.md`](workstreams/deploy-path-unification.md).

- [x] **Deploy-path unification Phase 2 Commit 2b — clean-slate default-flip + 0077 accepted** (2026-05-18, `3738c3e8`) — `deploy_diff(clean=True)` is now the default: the CP diff scope is the whole drive minus `DEVICE_KEEP_SET` + macOS noise (`_list_scope_on_drive(clean_slate=)` — noise is all dot-prefixed, excluded without enumeration), so a stale board `settings.toml`/leftover file is reconciled away with the one-time eviction notice (now also fired from diff-path `delete_files`, once-guarded). `--no-wipe` (`store_false`) is the additive opt-out; pre-existing `--wipe` re-scoped as full-erase (keep set included) — three coherent levels. `clean`/`clean_slate` threaded protocol→CP/MP transports→FakeTransport→both recovery wrappers; MP accepts but keeps legacy scope (delta 6 = its survive-set, needs an MP board). Bench Pi Pico W CP: default `deploy` evicted planted `settings.toml`+`junk.txt`, kept `boot_out.txt`, ran clean; `--no-wipe` preserved both. **Decision 0077 promoted `proposed`→`accepted`**; 0059 §1 + Rejected bullet edited in place to cross-link 0077; AGENTS.md one-staging-path non-negotiable added. deploy 0.25.0→0.26.0, workspace 0.32.0→0.33.0; 1770 deploy+workspace tests green, gated coverage 94 %, lint+CHU clean. Detail: [`workstreams/deploy-path-unification.md`](workstreams/deploy-path-unification.md).
