# Next Up

## Now

(empty — pick from Next)

## Next
- [ ] **Rebrand ChuMicro → ChipPy** (see `plans/workstreams/rename-to-chippy.md`). Full org + package + bundle rename, all library `VERSION` files reset to `0.0.0`. Execute when the project is ready for first public opening; sheds accumulated test-churn releases from PyPI since the namespace changes.
- [ ] **Migrate `scripts/device_config.py` onto `chumicro_deploy.config.default`** (Decision 0032 rule 8 — scripts consume workbench packages, not the other way around).  Current state: `scripts/device_config.py` (379 lines) reimplements YAML parsing, default resolution, and field validation that `chumicro_deploy.config.default.load_devices_yml` already owns.  Mono-repo-only surface (richer `DeviceEntry` with `id` / `description` / `setup_command`, `DeviceDefaults` with `ide_runtime`, `filter_devices`, `resolve_ide_devices`) stays in `scripts/` but sits as a thin wrapper on top of the workbench loader rather than a parallel implementation.  Sketch: expose a `load_raw_entries(path) -> (list[dict], defaults_dict)` helper from `chumicro_deploy.config.default` (pure YAML + defaults merge, no `Device` construction), have `scripts/device_config.py` call that and layer its script-only fields on top.  Consumers to check: `scripts/pytest_device.py`, `scripts/device_testing.py`, `workbench/deploy/functional_tests/conftest.py`, the three `scripts/tests/` files that cover device config.  Payoff: one place defines the YAML schema, mono repo + chumicro-deploy tests stay in sync, eventual project-workspace template repo (Phase 4) inherits the same schema automatically.
- [ ] **Scripts → workbench migration backlog** (Decision 0032 rule 8).  General sweep of `scripts/` for other candidates that should live in a workbench package.  Initial candidates to audit: transport / deploy logic still in `scripts/device_testing.py` beyond what `Deployer` + `InteractiveDeployer` already own; `scripts/pytest_device.py` session-cache / transport-fixture logic (potentially a `chumicro-pytest-device` workbench package); anything else that a future project-workspace template would want without cloning the mono repo.  Not a forcing function — migrate incrementally as workbench packages mature.
- [ ] Validate VS Code workspace **on-device** `functional_tests/` end-to-end. Host-side validation completed 2026-04-24 (extensions prompt, tasks, Testing panel, host pytest discovery — see Done log).  Outstanding: explicit `functional_tests/` targets routed through the pytest device plugin against a live board from VS Code's Testing panel, mirroring the PyCharm-side verification.
- [ ] Expand the device test matrix beyond ESP32-S2 now that transport tooling is proven on both MicroPython and CircuitPython.
- [ ] **ChuMicro project-workspace workstream (Phase 3+)** — Phase 1 (`chumicro-deploy` extraction, slices 1a–1f) shipped 2026-04-22, hardware-verified on four boards.  Phase 2 (`chumicro-repl` minimum-viable core) shipped 2026-04-24 — pyserial wrapper, UTF-8 safe framing, traceback / safe-mode / hard-fault / soft-reboot pattern detection + ANSI highlighting, `tail()` follow-mode, `ReplSession` raw-REPL context manager, interactive TUI with mpremote keybindings, `chumicro-repl` CLI; 119 host-side tests at 95.2 % coverage; on-device functional test deferred to next hardware session.  See `plans/workstreams/project-workspace.md`. Remaining phases: `chumicro-kvstore` + `chumicro-wifi` (Phase 3), Phase 4 split into `chumicro-workspace-runtime` (4a, workbench), `chumicro-workspace-template` scaffolder package (4b, workbench — Copier-style init/update; consumes `chumicro_deploy.config.default` for the `devices.yml` schema), and the `chumicro-workspace-template` companion repo (4c, separate repo hosting the canonical template files), `chumicro-sockets` per Decision 0031 (Phase 5), `chumicro-mqtt` refactor (Phase 6), first sensor thing template (Phase 7), OTA deferred (Phase 8).
- [ ] Enable GitHub Copilot code review as a PR quality gate (low priority — defer until community contributions begin).
- [ ] Implement `chumicro-kvstore` — tiny mutable key-value store for persisted runtime state. Replaces the previously-scoped `chumicro-settings` (see Decision 0030).
  - Not a config system. Documented contract: small per-backend capacities (256 B on SAMD21 up to ~24 KB on MP ESP32 NVS), wear mitigation via `commit_if_changed`, CRC-wrapped payload on CP NVM for power-loss-corruption detection.
  - Per-runtime backends: `microcontroller.nvm` (CP, all boards), `esp32.NVS` namespace with per-key msgpack blobs (MP ESP32), single LittleFS file with tmpfile+rename atomic update (MP Pi Pico W / other non-NVS MP boards), in-memory dict (CPython / tests).
  - Values round-trip via `chumicro-msgpack` — strings, ints, bytes, lists, dicts.
  - `KVStore(backend="auto")`, `commit()`, `commit_if_changed()`, `capacity`, `bytes_used`, `is_corrupt`. `KVStoreFull` / `KVStoreCorrupt` exceptions.
  - `testing.py` with `FakeKVStore` (wraps MemoryBackend + call recording).
  - App **config** (wifi creds, MQTT broker, pin map, feature flags) is NOT stored here. Config ships as `things/<name>/config.toml`, transformed to `/runtime_config.msgpack` at deploy time by `chumicro-workspace-runtime`, read once at boot. See Decision 0030.
- [ ] Add digital I/O as the second library seam (alongside CI/release work, not sequentially).
- [ ] Explore test ergonomics: reduce repeated boilerplate across test files.
- [ ] Design a performance and resource benchmarking infrastructure. Goals:
  - Measure memory footprint (heap allocations, peak usage) and CPU cost of library operations.
  - Control GC explicitly during benchmarks so allocation measurements are stable and reproducible across runs.
  - Define per-benchmark thresholds that fail the run if exceeded, catching regressions over time.
  - Benchmarks may be slow; they should not run as part of the standard `test` path. Consider a separate `bench` task or a deeper test tier that can also run in CI on a schedule.

## Out of scope (until revisited)

- CI-hosted device testing (`device-test.yml` / `workflow_dispatch`, CI-injected `devices.yml` / `device-config.yml`). Parked over security concerns around shared-runner device access; bring back up before any design work resumes.
- CI simulation/emulation path (renode etc.). Not being explored until the above is revisited.

## Investigations

- [ ] **Investigate slow MicroPython RAM-mode functional test runs** — observed during 2026-04-19 live PyCharm testing that MicroPython RAM-mode functional tests took noticeably longer than expected. CircuitPython RAM-mode is fast in comparison. Suspects: per-file `mpremote mount` cost, cold-start interpreter overhead, batch-vs-per-test trade-off. Profile against the new batch-execute path and identify whether amortization can be improved.

## Done (recent)

- [x] **Project-workspace Phase 2: `chumicro-repl` minimum-viable core** — new `workbench/repl/` v0.0.0 publishable workbench package shipping the pyserial-backed serial REPL surface called for by Decision 0029.  Public API: `ReplSession` raw-REPL context manager (`exec(code)`, `call(function_name, *args, **kwargs)`, `read_until(pattern, timeout)` with proper remainder-buffer framing across the `OK ... \x04 ... \x04 >` protocol), `tail(device, seconds, fail_on_traceback) -> ExitCode` follow-mode for deploy orchestration, `interactive(device)` TUI loop with mpremote-compatible Ctrl-C/D/E forwarding plus local Ctrl-X exit, `colorize()` + `Theme` ANSI highlighter, `detect_patterns()` + `StreamingPatternDetector` for CP `Traceback`, `safe mode`, `Hard fault`, MP `Traceback`, MP `MPY: soft reboot`, `Utf8StreamDecoder` for chunk-boundary-safe UTF-8 decoding (handles emoji split across pyserial reads), and a thin `chumicro-repl` CLI that accepts either `--address` or `--devices-file`/`--device` (via `chumicro_deploy.config.discover_config_loaders`).  Full `FakeSerialPort` / `FakeKeyboard` / `FakeTime` fake suite mirrors `chumicro_deploy.testing` so downstream packages can drive the surface in tests without hardware.  119 host-side tests pass; coverage 95.21 % (above the 94 % agent gate).  mkdocs site builds cleanly under `python scripts/run.py docs`.  Functional hardware test deferred — next on-device session will exchange Ctrl-C / Ctrl-D against a CP and an MP board to confirm clean exit.  Open follow-up: `chumicro-deploy`'s `CircuitpythonTransport` still carries its own raw-REPL framing; consolidating onto `chumicro_repl.session` is captured as an open follow-up in the Phase 2 section of the project-workspace workstream rather than tackled in this slice (deploy's transport is bound up with bootstrap-script chunking, recovery classification, probe, and reset paths that exceed the minimum-viable repl scope).

- [x] **VS Code first-open hardening** (host-side validation pass).  Live VS Code session against the chumicro workspace surfaced three gaps that left first-time contributors unable to use the IDE; all three landed in 03b0480 + c5146f1 + db5782f.  (1) `.vscode/extensions.json` was missing — VS Code only prompted to install the Python extension after the user opened a `.py` file, so the interpreter picker / Testing panel / Pylance were silently absent on first folder-open; `sync-ide` now generates the file recommending `ms-python.python`, `ms-python.vscode-pylance`, `ryanluker.vscode-coverage-gutters`.  (2) Every task in `.vscode/tasks.json` invoked a bare `python`, which fails on stock homebrew Macs that only ship `python3` (and would fail on Windows venvs at `.venv\Scripts\python.exe`); switched the generated `command` field to `${command:python.interpreterPath}` so the Python extension supplies the right interpreter per OS, with a scripts-test guarding against regression.  (3) Cross-platform host-platform audit of `chumicro-deploy` produced a new `host_platform.py` module — `WindowsNotSupportedError` (raised from `Deployer.__init__` and `probe_device`), `RsyncMissingError` with a package-manager-aware install hint (raised before flash-mode CP deploy opens a serial connection), `_resolve_username` fallback in `find_circuitpy_drive` covering containers without `$USER`.  Verified live: tasks run, Testing panel populates, host-side pytest discovery + execution work end-to-end.  Outstanding (left in **Next**): on-device `functional_tests/` routing from VS Code's Testing panel against a live board, mirroring the PyCharm verification.

- [x] **`chumicro-deploy` config schema renamed to claim ownership** — pre-publish cleanup of the devices.yml loader's naming.  `workbench/deploy/src/chumicro_deploy/config/chumicro.py` → `config/default.py`, registry key `"chumicro"` → `"default"` (still reserved against third-party shadowing), docstrings reworded to frame the schema as owned by `chumicro-deploy` rather than bridged from the mono repo.  The devices.yml shape itself is unchanged — same required fields, same optional fields, same extra-key tolerance — so existing `devices.yml` files keep working without edits.  Callers updated: `config/__init__.py` (registry + reserved-name check + `BUILTIN_LOADER_NAME` constant exposed), `device.py` docstring cross-reference, `cli.py` `--devices-format` default + docstring, test module renamed (`test_config_chumicro.py` → `test_config_default.py`) and its registry-key assertions updated, `test_config_discovery.py` assertions updated.  Decision 0032 grew a new rule 8 ("scripts consume workbench packages, not the other way around") to make the directional pattern explicit.  Decision 0029 + `plans/workstreams/project-workspace.md` updated to reflect the new loader path.  Follow-up entry added under **Next** to migrate `scripts/device_config.py` onto the built-in loader so the mono repo stops duplicating the schema.  All 435 host-side tests still pass; coverage 94.17 %.

- [x] **`chumicro-deploy` recovery-layer hardening + macOS FSKit wedge detection** — follow-on pass after live hardware testing surfaced gaps.  New `chumicro_deploy.macos_fskit` module + `DeployFailureKind.MACOS_FSKIT_WEDGED` (tenth kind): on darwin, when a `CIRCUITPY_DRIVE_MISSING` error fires, `InteractiveDeployer` probes `ps -o state= -p $(pgrep diskarbitrationd)` and, if the daemon is in uninterruptible wait (the signature of the user-space msdos FSKit extension wedging `diskarbitrationd`), promotes the kind and surfaces the exact recovery command — `sudo killall -9 com.apple.fskit.msdos fskit_helper fskitd fskit_agent diskarbitrationd && launchctl kickstart -k gui/$(id -u)/com.apple.DiskArbitrationAgent`.  The per-user `DiskArbitrationAgent` has to go through `launchctl kickstart -k` rather than `killall` because its plist has `KeepAlive=false`; without the agent restart the drives mount but Finder's Locations sidebar never registers them.  Classifier fixes from the same session: CIRCUITPY drive patterns now checked before port-unavailable (stale-mount `EACCES` was misrouting to `PORT_UNAVAILABLE`); `"failed to access"` + `"it may be in use by another program"` added to port-unavailable patterns so mpremote's missing-device message stops landing in `BOOTSTRAP_EXEC_FAILED`; `CIRCUITPY_DRIVE_MISSING` plan text rewritten so RESET is the primary fix (works for both flash-mode and Finder-eject) with unplug/replug as fallback for boards without a RESET button; `MACOS_FSKIT_WEDGED` plan includes a heads-up about the known Apple FSKit-Finder sidebar regression.  Transport-level: `CircuitpythonTransport._resolve_circuitpy_drive` writes a `.chu-probe` marker to catch EACCES up-front instead of halfway through rsync.  Demo additions: new `scenario_flash_copy_failed` (forces a 2 MiB payload against ~1 MiB CIRCUITPY, verifies `FLASH_COPY_FAILED` coaching); `scenario_circuitpy_drive_missing` now re-resolves `_build_flash_device` on demand so drives that came back mid-session don't require restarting the script.  435 host-side tests (+2 regression, +12 new FSKit-module cases); `macos_fskit.py` at 100 % coverage; workspace stays at 94.17 %.

- [x] **`chumicro-deploy` interactive recovery layer** — new `InteractiveDeployer` + `classify_deploy_failure` + `RecoveryPlan` + `DeployFailureKind` exported alongside the deterministic `Deployer`.  Catches `CircuitpythonTransportError` / `MicropythonTransportError`, classifies into nine kinds (port unavailable, raw REPL unresponsive, CIRCUITPY drive missing, flash copy failed, bootstrap exec failed, insufficient memory, traceback returned, configuration error, unknown; `macos_fskit_wedged` added in the follow-on entry above), prints a canned `RecoveryPlan` with ordered fix-steps, and runs a retry-loop with injectable `prompt` / `output` callables.  Fixes the spurious `WARNING: Failed to restore board state on disconnect` noise users saw when going into bootloader mode by marking `_reset_pending` on the CP transport so disconnect skips the restore dance after an intentional reset.  New `workbench/deploy/functional_tests/demo_recovery_hand_holding.py` walks every configured `devices.yml` board through the scenario set (happy-path baseline, traceback-on-board, unplug, eject-CIRCUITPY, bootloader-reset-silent).  59 host-side tests + coverage gate at 94.25 %.

- [x] **Project-workspace Phase 1: `chumicro-deploy` extraction (Decision 0029 + Decision 0032)** — new `workbench/` folder for publishable host-only CPython tools, `workbench/deploy/` v0.0.0 shipping `Device`, `Deployer`, `DeployResult`/`DeployError`, `FileSource` protocol + `FileMapSource` / `DirectorySource` / `ImportGraphSource`, `probe_device`, `resolve_firmware_url`, `flash_firmware` (UF2 + esptool paths with programmatic bootloader entry + interactive fallback, per-runtime offsets, optional pre-erase), config-loader entry-point discovery + built-in `chumicro` loader for `devices.yml`, thin `chumicro-deploy` CLI (probe / flash / deploy / resolve-firmware-url), mkdocs docs (guide + API reference) picked up by `docs-deploy`, landing page split into separate **Libraries** and **Workbench** sections. `scripts/device_testing.py` and `scripts/pytest_device.py` migrated to consume the new Python API directly (no subprocess CLI shell-out). Hardware-verified on ESP32-S2, ESP32-S3, and Pi Pico W across both runtimes. Third-party portability proven by `workbench/deploy/tests/fixtures/third_party_template/` — a standalone non-chumicro fixture with a custom `FileSource` deploys end-to-end through `chumicro_deploy` alone, with a `sys.modules` leak check confirming no mono-repo coupling.
- [x] Docs and planning sync for device testing and IDE workflows — refreshed `README.md`, `CONTRIBUTING.md`, IDE guides, `support/test_harness/README.md`, library README development notes, and the active planning/docs files so they describe `devices.yml`, `device-config.yml`, `test-libraries-functional`, deploy modes, and current VS Code/PyCharm status.

- [x] Device-testing UX refinements — bare `test-libraries-functional` now uses `devices.yml` defaults, `--runtime both` is explicit, the legacy `--device` flag was removed in favor of `--micropython-device` / `--circuitpython-device`, and large CircuitPython RAM-mode bootstraps are chunked against live free-heap measurements.

- [x] Device testing Phase 3: IDE integration — `scripts/pytest_device.py` routes explicit `functional_tests/` targets to hardware using `devices.yml` as the gate. AST-based test discovery (no import), session-scoped transport caching, per-file batch execution, synthetic setup/run-overhead nodes, per-function `name_filter` execution, and defaults-backed runtime/device selection are in place. Decision 0027. Verified live in PyCharm (multi-runtime test tree); a dedicated VS Code Testing-panel verification pass against hardware is still pending.

- [x] Deep developer test sweep — originally landed as `test-everything` (CPython + scripts + unix-port matrix in one pass, with optional `--with-device` for real-board functional tests).  Superseded by the 2026-04-24 `run.py` rename audit: `preflight` covers the CI mirror with `--with-functional` for the optional hardware phases, and `test-functional` runs both library and workbench functional suites end-to-end.  See `plans/history.md`.

- [x] CircuitPython flash/RAM hardening — many April-19 fixes: bulk-stage all sources in one rsync pass, soft-reset between library groups, `recover()` on transports after a failed test, FAT32 race fixes, `os.sync()` after flash writes, `__pycache__` exclusions, raw-REPL re-entry on disconnect, RAM bootstrap chunking against live `gc.mem_free()`, ESP32 budget relaxation, and per-file batch execution to amortize mpremote serial-connect overhead.

- [x] Whitespace linter (CHU002–CHU005) — `scripts/check_whitespace.py` wired into `run.py lint`, fixed 42 pre-existing violations. Rules: file ends with one newline, no excess blank lines, no trailing whitespace, no blank line after block opener.
- [x] Scripts consolidation — `ensure_build_tools` → `shared.py`; `load_tomllib`, `GITHUB_ORG`, `discover_library_dirs`, `read_pyproject_description`, `discover_doc_dirs`, `is_ref_reachable` → `workspace.py`. Tests aligned.
- [x] Support package rename — `support/testing/` (`chumicro_testing`) → `support/abstractions/` (`chumicro_abstractions`). Now exports `FakeTime` only; production code defaults to Python's `time` module directly (`RealTime` was removed in commit `70393db` as a trivial wrapper).
- [x] Editable-install support packages — `install_editable()` now installs both publishable libraries and support packages (`find_support_packages()` in `workspace.py`). Removed the legacy `_ensure_support_importable()` runtime `sys.path` fallback in `scripts/device_testing.py`.
- [x] Deploy modes: RAM and flash (Decision 0028) — `--deploy-mode ram|flash` flag on `test-libraries-functional`, CircuitPython flash transport (USB drive copy with autoreload control), `circuitpy_drive_path` device config field, bootstrap routing (inline for CP ram, standard imports for CP flash). MicroPython `ram`→`mount`, `flash`→`copy`.
- [x] Device testing Phase 2: CircuitPython serial transport — `CircuitpythonTransport` (pyserial raw REPL: Ctrl-C interrupt, Ctrl-A enter, Ctrl-D execute, OK/stdout/stderr parsing), `build_circuitpython_bootstrap` (class-as-module injection, inline harness, test exec), orchestrator routing for CP devices. `pyserial` added to dev deps.
- [x] Device testing infrastructure — Phase 1 complete (Decision 0027): `device_config.py` config loader, `result_parser.py` structured output parsing, `support/device_transport/` with `MicropythonTransport` (mount + copy modes), `name_filter` on `runner.run_module`, real `test-libraries-functional` orchestration in `run.py`, and `mpremote` + `pyyaml` in dev dependencies.
- [x] Populate "What's new" sections in library guides — all four libraries now have version entries.
- [x] CI build and cache optimizations: `--no-isolation` build (~7x faster), MicroPython submodule pruning (87% cache size reduction), explicit pip caching for docs deploy.
- [x] Documentation sync: run.py commands synced across README, AGENTS.md, and development-cli.md.
- [x] Validate-mpy CI job for PRs: builds mpy-cross, stages all libraries, validates mip install + import from staged bundle. Catches broken mpy compilation or manifest errors before merge.
- [x] Pre-publish bundle validation: `--staging-dir` mode validates mip install against locally staged bundles before pushing to live repos. Integrated as a gate in both `release.yml` and `promote.yml`.
- [x] Mip install validation in CI: `validate-mip` job in `release.yml` and `promote.yml` tests mip install + import for both `.py` and `.mpy6` formats after every bundle push. `validate-mip` run.py subcommand for local use.
- [x] Mpy folder restructuring (Decision 0024): `.mpy` bytecode moved out of root package dirs into `mpy6/` (MicroPython) and `circuitpython-10.x-mpy/` (CircuitPython). Root `package.json` lists `.py` source for universal compatibility; `mpy6/` manifests for users who want pre-compiled bytecode.
- [x] Mip dependency routing: experimental `package.json` references experimental bundle repo for deps (was hardcoded to stable). Fixed "latest" → "HEAD" for git ref resolution.
- [x] CI mpy-cross integration: `release.yml` and `promote.yml` build both mpy-cross compilers from source (cached) instead of `pip install mpy-cross`. Both CircuitPython and MicroPython `.mpy` files are now compiled during bundle staging. New `prepare-mpy-cross` command builds only the compilers without the full unix-port interpreters.
- [x] Promote workflow fixes: inlined stable docs deployment (concurrency group was silently canceling deploys), added attestations to stable PyPI publish, fixed garbled bundle release description.
- [x] CI micropython cache sharing: `validate-mpy`, `runtime-compatibility`, `release.yml`, and `promote.yml` all share the same micropython cache key.
- [x] Docs branding overhaul: warm palette matching badger logo, favicon regeneration, landing page reads descriptions from pyproject.toml, centered logo header in root README, plain-language library descriptions.
- [x] Library README overhaul: absolute URLs for PyPI compatibility, badger tip images, Source links to library directories, README.md included in bundle staging, scaffold template aligned.
- [x] Brand normalization: "Chumicro" → "ChuMicro" across 50+ occurrences in prose, docstrings, templates, and docs.
- [x] Contributor fork workflow: complete fork-to-PR walkthrough in CONTRIBUTING.md, fork sync/rebase guidance, GitHub UI steering for PRs.
- [x] Contributor experience audit v4: "First contribution?" signpost, Contributor FAQ, "abbreviations we spell out" reframing, memory-pattern reassurance repositioning, "Part of ChuMicro" discovery line in all library READMEs, AGENTS.md working-style consolidation.
- [x] Test harness heap deltas: per-test allocation tracking with manual GC control.
- [x] Plans cleanup: removed plans/sessions/, commit history is the primary context recovery mechanism.
- [x] Scripts test suite: 203 pytest tests for scripts/ infrastructure, `test-scripts` subcommand integrated into preflight.
- [x] IDE audit: scripts/ added to source roots, test discovery, and extraPaths in PyCharm and VS Code configs. Stale .iml entries cleaned up. `scripts/tests` added to pytest testpaths.
- [x] Contributor experience audit v3: root README "Your first program" example and REPL snippet, circup bundle explanation, install section cleanup (details blocks for experimental/channel switching in all library READMEs), common-mistakes FAQ in CONTRIBUTING.md, FakeTicks.ticks_add overflow validation, self-contained testing.py constants (fixes CircuitPython compat import skip), architecture guide, editable-install clarification in Other Editors guide.
- [x] Enable GitHub Discussions (Q&A, Ideas, Show and Tell categories).
- [x] Contributor experience audit v2: README reorder (Installation before Development setup), dependency graph, cross-library references (timing ↔ runner), "What's new" sections in all library guides, PR template simplification (N/A defaults), coverage hint in `run.py`, `CLAUDE.md` and `.cursorrules` pointers, GitHub Discussions link in CONTRIBUTING.md and issue template config.
