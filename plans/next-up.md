# Next Up

> Work queue.  One bullet per item, no sub-bullets — anything needing more than a title goes to [`workstreams/<name>.md`](workstreams/) and surfaces here as a one-line pointer.  Tracks status, not research.  No `## Done` section — `git --no-pager log` carries history.

## Now

- [ ] **Public repo mode — process and settings BOTH LANDED 2026-08-09; punch list remains.**  [workstreams/public-repo-mode.md](workstreams/public-repo-mode.md) — PR #11 merged (Decision 0120 main-is-PR-only, workflow least-privilege + SHA pins, `@claude /review`, maintainer runbook, contributor-docs updates), and the five settings flips are done and verified: ruleset bypass actors to PR-mode, workflow-token default back to read, fork-PR CI approval for all outside collaborators, pypi environment restricted to main, and private vulnerability reporting ENABLED (found disabled while flipping — SECURITY.md's only reporting channel pointed at a switched-off feature).  `ANTHROPIC_API_KEY` verified live: the first `@claude /review` ran end to end on PR #14 and its findings were applied there.  Dependabot #7/#9 closed as superseded by #11's SHA pins.  Open: the workstream punch list (changelogs, py.typed, labeler, provenance, chunraid hardware lane someday) and the repo-level require-SHA-pinning setting.

## Out of scope

- **Page titles stay long.**  Bing's site scan warns "title too long" on 36 pages, which is 18 pages counted once per channel.  Truncation is display only: search engines index the whole title, so nothing ranks worse for it.  Shortening would cost the distinctive half of each title ("storage that survives a reboot" becoming "storage"), and the runtime names it would save are in every page's meta description anyway.  Decided 2026-08-18: leave them.  Do not re-open this from a fresh scan report.

## Next

- [ ] **Display libraries: every GC9A01A matrix cell is code-ready; bench passes remain.**  GC9A01A's definition of done is the full matrix: S2+MicroPython is validated; S2+CircuitPython and RP2040+CircuitPython run through `gc9a01a_displayio.make_display` (the init table into `busdisplay.BusDisplay`, firmware owns refresh); RP2040+MicroPython runs through `GC9A01AIndexed` (GS8 frame expanded per strip by blit's palette at C speed; viper rejected because arch-neutral `mpy-cross` refuses it, see patterns.md).  Bench cards: the CircuitPython example is the card for the CP cells; `.scratch/gc9a01a_card_indexed.py` is the Pico W card.  `chumicro-charlcd` (issue #26 lift) has tests green on all three runtimes and needs its bench pass on the LCM1602 under both runtimes before release.  Then: SSD1306/SH1106, PCD8544, `chumicro-segments` (MAX7219), and an automated functional-test story for displays.
- [ ] **`chumicro_test_harness.markers.marker` has no caller left.**  Decision 0123 moved every demo `app.py` to plain `print` for its `NAME key=value` lines, and the demos were the helper's only consumer; the sole remaining reference is `support/test_harness/tests/test_markers.py`.  Decide between deleting it with its lazy export in `chumicro_test_harness/__init__.py` and keeping it documented for people writing their own on-device tests.  Either way `markers.py` can join `_DEMO_UNUSED_HARNESS_MODULES` in `deploy_api.py`, which is flash back on a 256 KB board.  Note the guarantee that goes with it: `marker()` raised at develop time on a value carrying whitespace or `=`, where a hand-written print fails silently on the host parser instead.

- [ ] **No lint rule gates tick math, so `ticks_diff` is convention-only.**  `.claude/rules/library-code.md` requires all time math through `chumicro_timing`, but its named offenders are tick *sources* (`time.monotonic`, `supervisor.ticks_ms`), not the naive `now_ms >= deadline_ms` compare that `ticks_diff` exists to prevent; the style guide omits the rule entirely and no CHU rule covers it.  A violation passes every CPython test and every board run until `TICKS_PERIOD` (`1 << 29` ms, ~6.2 days) wraps and the compare inverts.  Library sources are clean today (grepped 2026-08-17); scope a CHU rule plus a style-guide entry, and watch false positives on size/index/count compares.

- [ ] **Search visibility: the move to `chumicro.com` is done; two cosmetic items remain.**  2026-08-17 to 2026-08-18 shipped titles and descriptions on all 16 sites, channel-named docs URLs so `/<library>/stable/` stops moving each release, sitemaps, `llms.txt`, structured data on the landing page and the FAQ, the repository guides published as a site, repo topics and descriptions across the org, MicroPython implementation classifiers, a host-root site with `robots.txt` and a sitemap index, the move to `chumicro.com` (Decision 0121), and PyPI Documentation links on every package that ships a docs site.  Both engines are verified on the new host, both have the sitemap, IndexNow answers 200, and CI publishes the root site through `SITE_ROOT_DEPLOY_KEY`.  The hub and the docs landing page share a navigation bar and the project artwork, and the workbench template is the front-page invitation to build something.  The guides site now opens with a start-here page that runs board to running code, publishes the install guide, and carries an install section on all 13 library sites.  Open: redirect `chumicro.net` and `chumicro.org` to `chumicro.com`, and set the repository's social-preview image.

- [ ] **Nothing ties the deploy guide's `DeployFailureKind` list to the enum.**  Issue #24's drift (the guide named 10 of 14 kinds, and 3 of the 7 non-retryable ones) was corrected by hand 2026-08-17; the four kinds added since the guide was written had drifted silently because no test or CHU rule connects `workbench/deploy/docs/guide.md`'s classification line to `recovery_kind.py` + `recovery_plans.py`.  Scope a targeted test in the deploy suite (every member name present, non-retryable set equals the `retryable=False` set), weighing it against how often that prose gets reworded.

- [ ] **Workspace → workbench rename: chumicro-side follow-through (template repo renamed 2026-08-10).**  [workstreams/workbench-rename.md](workstreams/workbench-rename.md) — near-term half LANDED 2026-08-10 (`DEFAULT_TEMPLATE_URL` + its pinned test, the doc links, the two checks-rule comments; workspace 0.54.1 → 0.54.2).  Remaining: the sized full-rename menu (PyPI package, module, `workspace.yml`, hosted docs path), each needing a per-surface user call, and the workbench-word collision spots to reword when touched.
