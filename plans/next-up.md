# Next Up

> **Agent-managed file.  Single source of truth for the work queue.**  Each top-level bullet is capped at 5 bullet points (lead + sub-bullets, CHU011) — anything that needs more lives in a workstream file under [`workstreams/`](workstreams/), surfaced from here as a one-line pointer.  `## Done (recent)` is capped at 5 entries — drop the oldest when adding a new one.  Each Done entry: subject + commit hashes + headline result + workstream pointer; aim for under ~500 chars.  Detail lives in the commit message and the workstream doc.  Session warm-up: `git --no-pager log --oneline -20` then this file.

## Now

- [ ] **Resume workspace-library-curation Phase 1 from session handoff** — see [`handoffs/2026-05-12-workspace-library-curation-design-resolved.md`](handoffs/2026-05-12-workspace-library-curation-design-resolved.md).
- [ ] **deploy-mode-unification 4c/4d/Phase 5 remain** — 4b.2 resolved (Decisions 0070 + 0071, see Done). 4c (wifi RAM-on-CP hard-crash), 4d (sweep validation + scope decision), Phase 5 (docs + AGENTS) still open. Detail: [`workstreams/deploy-mode-unification.md`](workstreams/deploy-mode-unification.md).

## Next

Independent items.  Most have either shipped phases (status in the linked workstream) or are unscoped placeholders waiting on a forcing function.

- [ ] **Audit remediation + drift mechanization** (proposed 2026-05-16 from a 16-agent adversarial sweep) — meta-finding: CHU-mechanized code rules held at 0 violations across all library source while every contract guarded only by the prose "docs in lockstep" rule drifted and shipped wrong (false TLS docstring confirmed vs MP C source, README example crashing on MP, "future work" docstring for shipped features, phantom CLI commands, coverage claim the measurement doesn't support, CI lint that can't run the CHU rules).  Phased: P0 decide (msgpack trust boundary, coverage-gate honesty, prose-lockstep → all filed in [`open-questions.md`](open-questions.md)); P1 stop-the-bleeding (msgpack decode hardening + CI-lint gap + release-not-gated-on-CI); P2 High correctness; P3 docs drift; P4 mechanize the drift class as CHU lints (the load-bearing fix).  An embedded-cost gate governs P1–P2: cheapest fix that closes the *realistic* threat, "mostly fix" with documented residual where the complete fix fights the 256 K/4 MB constraint (msgpack→push integrity to CRC not a validating parser; runner→re-entrancy guard not a deferred-ops queue).  Detail: [`workstreams/audit-remediation-and-drift-mechanization.md`](workstreams/audit-remediation-and-drift-mechanization.md).

- [ ] **Cross-runtime harness: class-based test discovery** — the harness discovers only module-level `def test_*`; ~800 class-organized test methods across 17 files run CPython-only.  Interim-marked `__chumicro_runtimes__=("cpython",)`; durable fix is harness class-method discovery, then revert the markers (one reverts to `__chumicro_host_only__`).  A loud collection guard now prevents new silent zero-item files.  Detail: [`workstreams/cross-runtime-harness-class-support.md`](workstreams/cross-runtime-harness-class-support.md).

- [ ] **Workspace library curation — chumicro-workspace as library host** (proposed 2026-05-12 alongside Decision 0062, design fully resolved 2026-05-12 — Phase 1 unblocked) — chumicro-workspace becomes the on-device library host for chumicro libs, replacing `mip`/`circup` for the workspace-template happy path.  Drivers: mip/circup recursive install with no `--no-deps`, FAT-stability concerns with host-driven file writes (Decision 0033), and Decision 0062's deploy-time opt-out only working under the chumicro-workspace deploy path.  Resolved design: (Q1) PyPI sdist for both channels — extend each library's `[tool.hatch.build.targets.sdist].include` to ship `tests/` + `examples/` + `docs/` (15 one-line edits + build-time regression test, no `bundle_manager.py` change); (Q2) new `libraries:` table in `workspace.yml` sibling to `library_sources:`; (Q3) default channel `stable`, `--channel experimental` per-add override, `--floating` records `version: HEAD` for always-fresh resolution (no SHA path — `release.yml` already publishes every main merge to `chumicro-<lib>-experimental`, so tracking main HEAD ≡ tracking experimental latest); (Q4) declined transitive deps recorded as `declined: true` in `workspace.yml`.  Per-library dev-mode override: `chumicro-dev.toml` redirects only the chumicro libraries that exist in the sibling checkout — partial sibling checkouts work cleanly, user/third-party libraries in `libraries/` never touched.  Phases: (1) sdist content extension + PyPI fetch backend; (2) `chumicro-workspace library {list,add,update,remove,switch-channel}` CLI; (3) Adafruit / micropython-lib upstreams deferred until demand; (4) `library run-example` / `library test` deferred until Phase 2 in anger.  `mip`/`circup` remain supported.  Detail: [`workstreams/workspace-library-curation.md`](workstreams/workspace-library-curation.md).

- [ ] **Multi-board CP deploy + FSKit follow-ups — Item 5 deferred to next natural wedge** — Items 1, 2, 3, 4 + the doctor wrapper shipped 2026-05-09; Item 6 considered and rejected.  Item 5 (end-to-end recovery-command bench proof) needs a real FSKit wedge.  A 2026-05-09 attempt at deliberate triggers (alternating-board deploys, repeated wipe-test, full functional suite, host-side write+delete loops, concurrent diskutil probes) didn't reproduce — the wedge is intermittent enough that try-on-demand isn't reliable.  Validation rides on the next natural occurrence; checklist in the workstream's Item 5 section ("when doctor reports MACOS FSKIT ✗, run through these five steps").  Detail: [`workstreams/deploy-multi-board-and-fskit-followups.md`](workstreams/deploy-multi-board-and-fskit-followups.md).

- [ ] **Beginner-comfort onramp Phase 3+** — eight-step sequence + four 2026-05-06 papercuts all shipped; workstream archived 2026-05-10.  Phase 3+ unscoped; reopen when the next first-encounter friction surfaces from real users.  Detail: [`workstreams/archive/beginner-onramp.md`](workstreams/archive/beginner-onramp.md).

- [ ] **Workspace-template gaps — 1 remaining of 9** — gaps doc archived 2026-05-10 after 7 of 9 fixes shipped; gap #8 dropped 2026-05-12 (re-evaluated and reversed — see archive section #8 for the chicken-and-egg + code-bloat reasoning).  Still open: (gap #4b) regular-mode README update on the [workspace-template repo](https://github.com/ChuMicro/ChuMicro-Workspace-Template) documenting `chumicro-workspace install-libraries <project>` + a manual `circup` / `mip` fallback recipe (mono-repo half shipped — `install-libraries` CLI lands now).  Detail: [`workstreams/archive/workspace-template-dev-and-regular-mode-gaps.md`](workstreams/archive/workspace-template-dev-and-regular-mode-gaps.md).

- [ ] **`discover --register` multi-board sweep** — `add-device` (no `--address`) now drops into the same port-picker `bootstrap` already had: auto-uses the only port, prompts on multiple, errors under `--non-interactive` (resolved-by-design 2026-05-14, what was historically option (a)).  `bootstrap` collapsed to a thin shim over `add-device` + next-steps footer; `--demo` is opt-in everywhere (was opt-out via `--no-demo`).  Still deferred: `discover --register` doing the sweep as a side-effect (registering N boards in one run).  Hold until a 5+-board user complains.

- [ ] **4-board validation campaign — pre-existing findings + memory-test tuning** (surfaced 2026-05-16 by a full 4-board campaign: 148 example deploys 144✓/0✗/4 skip, functional on all 4, unit-sweep bake flash×3+RAM×1).  MP-copy fix (`e2898522`) comprehensively validated — Pico W MP + Lolin S2 MP **flash 3/3 STABLE each** (407/12/0), MP RAM stable; ~770+ campaign deploys, 0 deploy failures.  Findings, all pre-existing, none regressions:
  - `*_memory_fragmentation` leak/fragmentation tolerance assertions **flap on ESP32-S2 CircuitPython** (2 MB PSRAM allocator) — Lolin S2 CP flash 5–6 non-deterministic fails/run across requests/websockets/http_server `test_memory_fragmentation.py`, plus S2 MP `test_short_text_frame_no_leak` (145 KB > 4096 tol).  The CP/S2-PSRAM sibling of the MP gating the prior session did (Decision 0058 loud-skip); the fix is to extend that gate (or retune tolerances for PSRAM).  **Also the top speed lever**: these tests are 24–30 s each on slow-USB-CDC S2 CP and flap — gating them on S2 CP both de-flakes and shortens the ~1000 s S2-CP sweep materially.
  - `websockets/test_drop_with_event_drains_oversize_and_stays_open` — deterministic on Lolin S2 (MP **and** CP), passes on Pico W: oversize frame reports length 600, test expects `(0, 400]`.  Real S2-specific websockets/buffer-chunking behaviour question, not env.
  - CP **RAM-mode** is not viable for the full sweep on the 264 KB Pico W (cumulative inline-bootstrap `MemoryError` at `ntp` after ~6 libs) — covered by the existing deploy-mode-unification 4c / `supports_ram_mode` scope; recording the concrete datapoint here.
  - Functional network tests need a bench with `mosquitto` up + host/boards on one subnet (campaign bench had neither: host 192.168.10.x vs boards 172.16.1.x) — environmental, not a code defect.

- [ ] **Expand the device test matrix beyond ESP32-S2** now that transport tooling is proven on both MicroPython and CircuitPython.

- [ ] **Performance + resource benchmarking infrastructure** — measure heap + CPU per library operation with explicit GC control; per-benchmark thresholds that fail on regression; separate `bench` task or deeper test tier so slow benchmarks don't run on standard `test`; CI on a schedule.

- [ ] **`scripts/run.py new-library --workbench` parity shim** — `chumicro-workspace new --workbench <name>` scaffolds a workbench package today (calls `scaffold_library` with `package_kind="workbench"`); the mono-repo's `scripts/run.py new-library` only forwards to `--library`.  Add the flag to the shim so contributors don't have to know about the underlying workspace CLI.  Small (~10 lines + one test).  Surfaced 2026-05-11 during the contributing-docs audit (`new-library.md:9` was claiming the scaffolder doesn't support workbench at all).

- [ ] **Deploy walker fails on unresolved imports** (proposed 2026-05-12, separate session) — `workbench/deploy/src/chumicro_deploy/sources.py:367-378` silently skips imports that don't resolve against `search_paths`.  Result: a file containing `from chumicro_sockets import ...` can be deployed without `chumicro_sockets` in `library_sources`, ImportError surfaces only at first boot.  Today's DI cleanup (Done) removed the unconditional top-level imports that were riding on this silent-skip; the walker fix is the load-bearing correction so that any *future* unresolved import is a deploy-time failure with a clear message naming the importing file + the missing module.  Decision 0062's bench-validation table also needs a corrective rerun once landed.  Detail: [`workstreams/walker-unresolved-import-failure.md`](workstreams/walker-unresolved-import-failure.md).

- [ ] **workbench/deploy CircuitpythonTransport tests — eliminate real `subprocess.run(["sync"])` calls** (filed 2026-05-12 during preflight-timing investigation) — six tests in `test_circuitpython_transport.py` (`test_waits_for_board_to_see_new_entrypoint`, `test_post_rsync_verification_failure_raises`, four `TestWipeFilesystem::test_flash_mode_*` cases) collectively cost **~80 s of wall-clock time** out of preflight's 73-s `test` phase.  Each runs `flash_drive.flush_volume`, which on macOS calls `subprocess.run(["sync"], check=True)` — a real OS-level filesystem flush that blocks until ALL pending host writes commit.  With concurrent test runs / IDE indexing / `git` operations in the background, each `sync` blocks for many seconds.  In isolation each test takes 0.10 s; in the file together they take 10–24 s each.  Fix shape: thread a `subprocess_run` injectable through `flash_drive.flush_volume` defaulting to `subprocess.run`, with tests passing a fake that returns immediately.  ~30 LOC + test updates.  Preflight wall time should drop from ~75 s to ~15 s post-fix (workbench/deploy goes from 107 s → ~25 s, and the slowest *remaining* phase becomes either docs at 4 s or the test fan-out for one of the larger libraries).  This is the single biggest preflight-speed lever in the workspace today.

- [ ] **Deploy-mode unification + on-device unit sweeps** ([Decision 0068](decisions/0068-unified-deploy-mode-resolution.md) **accepted 2026-05-15; signed off, ready to implement**) — one deploy-mode resolver shared by `chumicro-deploy` + `chumicro-pytest-device` (today they diverge — the test path has no `requires_flash`/non-`.py` policy, so a `--deploy-mode ram` sockets-TLS functional run on CP silently drops `_ca_bundle.der`); transitive `requires_flash` (closure-scoped) + caller-scoped `staged_files`; new `devices.yml` `supports_ram_mode` boolean; on-device unit-sweep command (RAM-default, non-coverage-gating) + `preflight --with-device-unit` opt-in.  RAM mode kept (most libs are RAM-capable; deletion considered and rejected).  5 phases, sequence + acceptance + concrete code map in [`workstreams/deploy-mode-unification.md`](workstreams/deploy-mode-unification.md).  Handoff: [`handoffs/`](handoffs/).

## Investigations


- [ ] **Investigate slow MicroPython RAM-mode functional test runs** — observed during 2026-04-19 live PyCharm testing that MicroPython RAM-mode functional tests took noticeably longer than expected.  CircuitPython RAM-mode is fast in comparison.  Suspects: per-file `mpremote mount` cost, cold-start interpreter overhead, batch-vs-per-test trade-off.  Profile against the new batch-execute path.

## Done (recent)

> Capped at 5 entries — subject + commit hashes + headline result + workstream pointer; under ~500 chars each. Verbose detail lives in the commit message body or [`workstreams/<name>.md`](workstreams/) (per-phase acceptance).

- [x] **MP copy-mode sweep cascade fixed — resumed from `2026-05-16-mp-copy-reset-defect` handoff** (2026-05-16, this commit) — repro confirmed the handoff's 139-failed/275-passed `[VERIFIED]` figure, but its `[HYPOTHESIS]` mechanism ("0071 subprocess reset while *holding* the persistent serial") was **falsified by evidence**. Real root cause: MP copy-mode `stage()` is `mpremote fs cp -r` with **no `--delete` analog** (that's CP-flash-only via rsync), so 9 libraries' trees piled onto the Pi Pico W until `requests` hit `No space left on device`, wedging the USB-CDC; 0071's per-library reset then couldn't grab the re-enumerating port and cascaded. Fix: copy-mode `stage()` deletes the prior library's tracked tree before each `fs cp`; `recover()`/`soft_reset()` ride out re-enumeration with bounded settle-and-retry. Hardware-verified Pi Pico W MP flash: **407 passed / 12 skipped / 0 failed** (was 139F/275P). Decision 0071 body corrected in place; wrong open-questions entry deleted; handoff deleted. New low-prob residual in [`open-questions.md`](open-questions.md) (dirty device from a prior aborted run not self-healed on first stage).

- [x] **Sweep audit: zero-item guard + class-based-test markers** (2026-05-16, follow-on to 0070/0071) — user flagged only one websockets test file staging.  Root cause (audit): the cross-runtime harness discovers only module-level `def test_*`; **17 class-based files (~800 methods)** across http_server, mqtt×6, requests, sockets×4, websockets×5 silently yielded zero on the unix-port/device-unit lanes (CPython-pytest-only) — pre-existing, exposed by the new sweep.  Added a loud `pytest.Collector.CollectError` guard (no silent zero-item device-lane files), interim-marked the 17 `__chumicro_runtimes__=("cpython",)` (one reverts to host-only), amended Decision 0016; durable harness-class-support + marker-revert plan in [`workstreams/cross-runtime-harness-class-support.md`](workstreams/cross-runtime-harness-class-support.md).  Also feature-gated `*_memory_fragmentation` (Decision 0058 loud-skip on real MicroPython — its unbounded allocate-until-OOM histogram wedged a Lolin S2 MP copy-mode sweep).  Preflight green.

- [x] **Decision 0068 4b.2 resolved — Decisions 0070 + 0071** (2026-05-16, resumed from `2026-05-16-deploy-mode-unification-phase4` handoff) — an instrumented Pico W CP flash sweep disentangled 4b.2 into two independent bugs: (i) host-lane test misclassification → **[Decision 0070](decisions/0070-host-only-test-marker.md)** (`__chumicro_host_only__` marker + `__chumicro_runtimes__=("cpython",)` reuse for 14 ex-`_pytest`; collection reads markers not filenames; Decision 0016 amended in place); (ii) cumulative-`sys.modules` `MemoryError` on the never-reset flash session → **[Decision 0071](decisions/0071-per-library-soft-reset-flash-sweep.md)** (soft-reset before staging each library). Hardware-verified on Pi Pico W CP: 193→16 failures, `runner`/`wifi` green, free mem flat ~124 K every library. Follow-up: 0071's `soft_reset` stale-`code.py` race **closed** via `clear_entrypoints()` (new `TransportProtocol` method — CP host-unlink+flush+verify, MP device-side `os.remove`+re-stat — called once per device before the first reset; hardware re-verified 374/16/0, no regression). The 16 residual sweep failures fixed: msgpack 13 now assert the `_pure` subset directly (the public symbol resolves to CircuitPython's native `msgpack` on-device, which is intentionally not subset-constrained — `_pure` owns the subset contract and behaves identically on every runtime); kvstore 3 host-context resolver tests split into a `__chumicro_host_only__` sibling `test_kvstore_host.py` (0070-consistent). Still parked in [`open-questions.md`](open-questions.md): per-file flash reset only. 4c/4d/Phase 5 remain — [`workstreams/deploy-mode-unification.md`](workstreams/deploy-mode-unification.md).

- [x] **Deploy-mode unification Phases 1–4b + Decision 0069** (2026-05-15, resumed from `2026-05-15-deploy-mode-unification` handoff) — one byte-identical `resolve_deploy_mode` + frozen `DeviceCaps` in `chumicro_deploy.preflight`; `Deployer` re-pointed behavior-preserving; pytest-device session-scoped resolution feeds full closure + own-src (4a) + per-device `supports_ram_mode` (Phase 3).  `--target device-unit` + `scripts/run.py test-unit-on-device` mode-grouped sweep + `preflight --with-device-unit` (4b).  4-board regression PASSED (sockets TLS RAM→flash 3/3 each; `timing` stays RAM 15/15).  On-hardware the sweep surfaced that `testing.py`'s `("cpython",)` marker is false marker-abuse blocking 21/37 fake-using suites on-device → **[Decision 0069](decisions/0069-test-support-module-marker.md)** accepted: explicit `__chumicro_test_support__` marker (0037 §3/§5 + 0044 + AGENTS edited in place).  ADR 0068 §1 edited in place.  0069 implementation + 4c/4d/Phase 5 remain — [`workstreams/deploy-mode-unification.md`](workstreams/deploy-mode-unification.md).

- [x] **chumicro-sockets Shape Y default-secure TLS** (2026-05-15, `fb37cf0e` truth-restore + `bfb9fab6` ADR 0067 proposed + this commit) — `tls_client_socket(context=None)` now verifies on every runtime: CP via firmware bundle, CPython via OS store, MP via a library-shipped 9-root curated PEM (ISRG X1/X2, DigiCert CA/G2/G3, Amazon Root CA 1, GTS R1/R4, GlobalSign).  ~11.5 KB PEM, strict subset of ESP-IDF v5.4's 151-root `cacrt_all.pem`.  Lazy-load + module cache, `set_default_ca_bundle(pem|None)` override, `ssl_context_no_verify()` for explicit opt-out (greppable in code review).  4-board acceptance: Lolin S2 + Pi Pico W × CP/MP all reject `expired.badssl.com`.  sockets 0.4.0 → 0.5.0; 147 unit + 3866 workspace tests green.





