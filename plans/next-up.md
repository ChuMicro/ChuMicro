# Next Up

> **Agent-managed file.  Single source of truth for the work queue.**  Each top-level bullet is capped at 5 bullet points (lead + sub-bullets, CHU011) — anything that needs more lives in a workstream file under [`workstreams/`](workstreams/), surfaced from here as a one-line pointer.  `## Done (recent)` is capped at 5 entries — drop the oldest when adding a new one.  Each Done entry: subject + commit hashes + headline result + workstream pointer; aim for under ~500 chars.  Detail lives in the commit message and the workstream doc.  **No zombie placeholders:** a fully-shipped effort whose only residual is "redo when problem X recurs" is *closed*, not parked in `## Next` — the archived workstream + git history carry the context, and a fresh bullet gets *created* when X actually recurs.  Session warm-up: `git --no-pager log --oneline -20` then this file.

## Now

_(nothing in flight)_

## Next

Independent items.  Most have either shipped phases (status in the linked workstream) or are unscoped placeholders waiting on a forcing function.

- [ ] **CHU prose/isolation lint mechanization — implement Decision 0079** (design landed 2026-05-19) — **Batch 1 + Batch 2 landed 2026-05-19**: CHU006/CHU012 pattern augments (incl. CHU012 absorbing the originally-drafted CHU023 verb-less dated-incident shape — survey showed the broader date-anywhere framing had 4-of-7 FP rate) + CHU020 (closed AI-tic phrase set with paired-delimiter exemption) + CHU024 (banned ADR-authoring section markers) + CHU025 (`Superseded by:` pointer integrity) + CHU026 (orphan-governance via CLAUDE.md `@`-include walk). 6 new rule codes total, plus pattern augments on 2 existing rules. **Deferred per corpus calibration finding** (ADR edited in place): CHU021 `the (one|single|sole) <noun>` and CHU022 `the (next|first|only|new|sole) <noun>` — 42 and 180 hits respectively on current `src/`, overwhelmingly legitimate (Decision 0077-style invariants, specific-instance refs, technical idioms); AGENTS.md line 138 already classifies these tics as **judgment guidance, not a lint**, so mechanization is either a noqa-heavy rule (0074 rejects this posture) or it needs a `/audit-comments` pilot to bench-derive a tight noun-allowlist first. **Remaining**: Engine A — CHU027 `src/` normalized-block dedup + CHU028 ADR-body dedup (deferred until CHU027 corpus-calibrated). Anti-decision: no AGENTS.md size/line ceiling lint. Detail: [`decisions/0079-prose-drift-mechanization.md`](decisions/0079-prose-drift-mechanization.md).
- [ ] **CI infrastructure workstream (unscoped — fires when CI is re-enabled)** — CI / GitHub Actions is disabled and has more structurally wrong with it than any single audit surfaced; it gets its own workstream rather than point-patches. Two concrete starting items inherited from audit-remediation P1 (analysis preserved in [`workstreams/audit-remediation-and-drift-mechanization.md`](workstreams/audit-remediation-and-drift-mechanization.md) Phase 1): (a) `ci.yml` lint job installs only `ruff`, so `run.py lint`'s `python -m chumicro_checks` can't run there — CHU enforcement never executes in CI; (b) `release.yml` publishes to immutable PyPI on a VERSION bump with `needs: detect` only, ungated by ci.yml success and before its own `validate-mip`. Neither is a safe isolated point-fix while CI is off (can't validate against a real run; the file is due for rework) — both wait for the dedicated pass.
- [ ] **Workspace-template gaps — gap #4b (regular-mode README)** — 7 of 9 shipped; gap #8 dropped 2026-05-12 (reversed; reasoning in archive §8).  One scoped task left: regular-mode README update on the [workspace-template repo](https://github.com/ChuMicro/ChuMicro-Workspace-Template) documenting `chumicro-workspace library add <name>` / `library browse` (acquire chumicro libraries from a published snapshot channel into committed `libraries/`, edit-preserving via `_library-backups/`, per Decision 0078) — plus a `.gitignore` `_library-backups/` add.  Mono-repo half shipped.  Detail: [`workstreams/archive/workspace-template-dev-and-regular-mode-gaps.md`](workstreams/archive/workspace-template-dev-and-regular-mode-gaps.md).
- [ ] **`websockets` oversize-frame length on Lolin S2** (surfaced 2026-05-16, 4-board campaign) — `websockets/test_drop_with_event_drains_oversize_and_stays_open` is deterministic on Lolin S2 (MP **and** CP), passes on Pico W: oversize frame reports length 600, test expects `(0, 400]`.  Real S2-specific websockets/buffer-chunking behaviour question, not environmental.  (The campaign's other findings closed: fragmentation suites deleted, CP-RAM `ntp` ceiling characterized under Decision 0072, network-bench gap was an environmental subnet/`mosquitto` mismatch — all in git history / their target workstreams.)
- [ ] **Expand the device test matrix beyond ESP32-S2** now that transport tooling is proven on both MicroPython and CircuitPython.
- [ ] **Performance + resource benchmarking infrastructure** — measure heap + CPU per library operation with explicit GC control; per-benchmark thresholds that fail on regression; separate `bench` task or deeper test tier so slow benchmarks don't run on standard `test`; CI on a schedule.
- [ ] **Deploy-path unification — one mechanism puts code on a board** (opened 2026-05-18) — divergent staging paths for one operation; additive-vs-clean-slate was the core defect. **[Decision 0077](decisions/0077-one-device-staging-path.md) `accepted`**; AGENTS.md one-staging-path non-negotiable in force. Done + bench-verified Pi Pico W CP: Phase 0 (ADR), Phase 1 (preflight `_env` fix; repl `chumicro_timing` board-dead regression fixed by retiring `repl <project>` → `deploy --tail`), **Phase 2 complete** (Commits 1a/1b/2a/2b + delta 6 + Commit 2c): single source owner; CP empty-dir reaping; keep-set unification; clean-slate default-flip with `--no-wipe`/`--wipe`; `settings.toml` evicted + one-time notice; 0059 §1 edited in place; MP whole-device clean-slate (delta 6, bench-verified Pi Pico W MP); Commit 2c — `FUNCTIONAL_TEST_EXTRA_EXCLUDES` deleted (CP functional = byte-identical clean rsync), `PostStageStep` names the post-stage fork, MP functional first-stage `mkfs`→keep-set-preserving `_clean_slate_device` (CP unit-tested; functional-suite bench the one open hardware step). **Phase 3 complete 2026-05-19 ([Decision 0078](decisions/0078-library-acquisition-is-host-local.md) `accepted`; design settled 2026-05-18 ADR-before-code, all forks user-decided; Commits 3a–3d, preflight-green, functional bench pending)** — Collapse A: `wrap_with_runtime_config` tail-dedup (3a); `deploy-example` → `ExampleSpec` branch on `resolve_project_deploy_source` + `deploy_diff`, interactive REPL drop kept (3b); `Deployer.deploy()` + wrapper `.deploy()` deleted, demo + low-level `chumicro-deploy` CLI folded onto `deploy_diff`, examples/guide rewritten (3c) — one stage primitive, zero CLI bypass. Collapse B **superseded mid-flight**: the 3d `_libraries/` pip/mip-`--target` fetch was the wrong design (parallel acquisition subsystem, blob `--target` extract, AST-discovery cart-before-horse). **[Decision 0078](decisions/0078-library-acquisition-is-host-local.md) rewritten in place** (not a new ADR — wrong-from-the-start correction): declarative `library add` pulls full editable trees + pyproject closure from a published snapshot channel (`ChuMicro/ChuMicro-Libraries[-Experimental]`, circup/mip-shaped tags) into **committed `libraries/`**, edit-preserving (`_library-backups/`), one engine behind a prompt_toolkit browser + scriptable `library add`. Acquisition engine rewired: `library_channel.py` (snapshot resolve/extract, 100 % cov) + `library.py` onto the curated `libraries:` table, `CliEnv.http_get` seam, fixture-backed.  Collapse landed (`install-libraries`/`_libraries`/pip-mip subsystem deleted, `e85f7604`→`4e5ccaa6`); `library browse` prompt_toolkit front-end landed (`b0ea444e`); producer `scripts/libraries_channel.py` + `index.json` catalog landed fixture-backed (`5d8aa52b`).  Phase 4 root-convergence subsumed.  **Remaining:** Phase 5 CHU mechanization per 0074; CI-workstream wires `release.yml`→`libraries_channel.main` (channel repos `ChuMicro/ChuMicro-Libraries[-Experimental]` manually staged 2026-05-19, tags `2026.05.19` / `2026.05.19.1`); cross-repo workspace-template `.gitignore`/README (gap #4b). **Validation residuals** (offline coverage is solid — preflight green incl. ~76 new/rewired unit tests; the gaps are physical/terminal-bound): (a) functional 4/4-board bench — Lolin S2 + Pi Pico W × CP+MP, staging committed `libraries/<name>/` onto silicon and importing (deploy walker treats it identically to dev-mode sibling checkout, which *was* bench-verified, so this should be a no-op delta — verify, don't assume); (b) ~~live-channel smoke~~ **closed 2026-05-19** — `library add chumicro_timing --channel experimental` against real HTTPS round-tripped `index.json` + tarball, curated trees landed cleanly; producer fixed mid-stage (preserve `.git/` on stage-into-checkout, filter `__pycache__`/`.pyc`/`.DS_Store`); (c) manual `library browse` TTY smoke — the prompt_toolkit shell is `# pragma: no cover` (full-screen app needs a real terminal), `BrowserModel` behind it is fully tested but rendering + keybindings have no automated coverage; one ~60 s session in a real terminal closes it.  Per-library version pinning → `open-questions.md` (pyproject-version-constraint gate). Cross-refs: deploy-mode-unification (complete, mode axis), walker-unresolved-import-failure (open neighbor). Detail: [`workstreams/deploy-path-unification.md`](workstreams/deploy-path-unification.md).
- [ ] **Deploy walker fails on unresolved imports** (proposed 2026-05-12, separate session) — `workbench/deploy/src/chumicro_deploy/sources.py:367-378` silently skips imports that don't resolve against `search_paths`.  Result: a file containing `from chumicro_sockets import ...` can be deployed without `chumicro_sockets` in `library_sources`, ImportError surfaces only at first boot.  Today's DI cleanup (Done) removed the unconditional top-level imports that were riding on this silent-skip; the walker fix is the load-bearing correction so that any *future* unresolved import is a deploy-time failure with a clear message naming the importing file + the missing module.  Decision 0062's bench-validation table also needs a corrective rerun once landed.  Detail: [`workstreams/walker-unresolved-import-failure.md`](workstreams/walker-unresolved-import-failure.md).

## Investigations

- [ ] **Investigate slow MicroPython RAM-mode functional test runs** — observed during 2026-04-19 live PyCharm testing that MicroPython RAM-mode functional tests took noticeably longer than expected.  CircuitPython RAM-mode is fast in comparison.  Suspects: per-file `mpremote mount` cost, cold-start interpreter overhead, batch-vs-per-test trade-off.  Profile against the new batch-execute path.

## Done (recent)

> Capped at 5 entries — subject + commit hashes + headline result + workstream pointer; under ~500 chars each. Verbose detail lives in the commit message body or [`workstreams/<name>.md`](workstreams/) (per-phase acceptance).

- [x] **CHU prose-drift mechanization — Batch 2 (CHU020 + CHU012 verb-less dated-incident shape)** (2026-05-19, this commit) — CHU020 lands as closed AI-tic phrase set (5 adjectives + 5 sentence openers, paired-delimiter exemption so the AGENTS.md ban list and ADR data don't self-flag). CHU012 gains the `the YYYY-MM-DD <audit|sweep|incident|pass|investigation|review|finding>` shape — closes the verb-less variant that escaped CHU012's existing verb-prefixed patterns and motivated the originally-drafted CHU023; corpus survey showed CHU023's "any date anywhere" framing had 4-of-7 FP rate (NTP epoch constants, XML namespace URLs, board-output examples — all legit technical data), so the verb-less-but-context-anchored shape rides on CHU012 instead. Real fixes: 3 dated "the 2026-05-16 audit" references in CHU rule docstrings (chu014/015/016) rewritten as evergreen "this rule catches" prose; 2 unquoted AI-tic adjectives in ADRs (0054 "robust in any terminal" → "uses no cursor escapes"; 0075 "robust enough to be the only path" → "stand as the only path without a fallback"); AGENTS.md Writing tone demo line rewrote unquoted usages as quoted (`*"comprehensive"*` / `*"robust"*`) so the rule's own documentation exempts cleanly. **CHU021/CHU022 deferred** — corpus shows 42 + 180 occurrences of `the (one|single|sole|next|first|only|new) X` today, overwhelmingly legitimate; AGENTS.md line 138 already classifies these tics as judgment-bound, mechanization needs a curated noun-allowlist from a `/audit-comments` pilot first. ADR edited in place to reflect both deferrals + the CHU023 → CHU012 redirect. 25 new tests; preflight green at 96%.
- [x] **CHU prose-drift mechanization — Batch 1 (CHU006/012 augments + CHU024/025/026)** (2026-05-19, `a9d5ceb8`) — 5 of the 9 lints from Decision 0079 land: CHU006 gains `AGENTS.md`/`CONTRIBUTING.md` + `.scratch/` patterns; CHU012 gains the dateless landed-history shapes (`retained until ... lands`, `before ... landed`, `deferred until`, `until we have a`); CHU024 catches banned ADR-authoring section markers (`## Update (`, `## Amendments`, `## Changelog`, `## Progress notes`, `Amended by` blockquotes, `This was revised` / "this decision has been revised" narrative, `Revised: YYYY-MM-DD` banner) anchored at line-start so backtick-quoted naming-in-prose doesn't FP; CHU025 fills CHU019's gap with dangling-target detection on `Superseded by:` cites + filename markers + half-finished `accepted`+`Superseded by:` contradiction; CHU026 walks CLAUDE.md `@`-include transitively and flags governance-doc-shaped files (`AGENTS.notes.md`, `RULES.md`, `*GUIDELINES*.md`) referenced from auto-loaded docs but missing from the chain.  Pre-existing leaks cleaned: mqtt README `.scratch/run_mqtt_perf.py` dead pointer + dated soak data dropped; mqtt memory-pressure test docstring `.scratch/` pointer dropped; deploy-transport test fixture dateless landed-history dropped; 0012-ide-type-stubs in-place revision narrative + history-consequence rewritten.  46 new tests (382 chumicro-checks total); preflight green at 96% with the 94% gate.
- [x] **Decision 0079 — mechanize prose drift through closed-set and dedup engines** (2026-05-19, `876a9360`) — One ADR covers `chu-prose-isolation-lint-gaps` #1–#7 + folds in the `plans/open-questions.md` "comments document current-code why" prior art + the CHU020 next-up entry, per 0074. Principle: closed-set lexical drift gets a deterministic CHU lint behind `# noqa: CHU0NN — <why>`; semantic classes stay with reviewer + audit skills. Engine B = existing `LeakRule` shape extended (CHU006/CHU012 augments + CHU020-024 fresh codes); CHU025 + CHU026 single-purpose structural checks; Engine A = new normalized-block dedup walker (CHU027 `src/`, CHU028 `plans/decisions/` deferred till CHU027 corpus-calibrated). Anti-decision: no AGENTS.md size/line ceiling lint — would mechanically reward the compression behavior the load-bearing argument-stopping rule forbids (cf. `53313cf6`→`165c9331` oscillation). Workstream stays as deeper analysis reference; open-questions entry deleted; CHU020 next-up entry folded. Detail: [`decisions/0079-prose-drift-mechanization.md`](decisions/0079-prose-drift-mechanization.md).
- [x] **`the X` forward-reference test + partial deploy comment audit (submodule a HIGH only)** (2026-05-19, `638f17a6`→`de2a1817`) — AGENTS.md → Writing tone: new 4th `the`-bullet ("`the X` only when X is a specific established instance") carries the three-way per-noun call (`the` / `a-an` / bare) with the worked example *"Send an autoreload-off REPL command on the open raw-REPL session"* — command is forward-reference (`an`), session is established singular (`the`).  `/audit-comments` dim 4 cites it without restating (single-home pattern from grep-shortcut lift).  Paired memory `feedback_the_forward_reference` paired with `feedback_audit_proposals_self_test` — inherited `the`s compound across REWRITE passes the same way the superlative tic does.  First application: HIGH batch on workbench/deploy submodule (a) — 6 findings across 4 src files (3 pure-label deletes in `_parse_raw_repl_response`, dateless-"historically" + class-noun-the trim in `disconnect`, 7-line deploy_files clean= paragraph → 2 lines, 4-line wipe settle comment → 2 lines via cross-site dedup, "legacy" dropped from `additive scope`/`additive callers` per ADR 0077).  Submodule (a) MEDIUMs and submodules (b)/(c)/(d) **dropped** — a full re-audit of `workbench/deploy` will run from scratch with the codified per-noun test in force from the first draft, superseding the split-pass plan.  Preflight green at 96% with the 94% gate.
- [x] **Hatchling sdist walker race — `include` → `only-include`** (2026-05-19, this commit) — Sdist walker descended into `libraries/<name>/site/`, `.cache/`, `dist/` while the parallel `docs` phase rewrote them; intermittent, tracked in commit-body breadcrumbs until recurrence promoted it. `include` filters paths after walking; `only-include` restricts the walk root. Applied across 20 publishable pyprojects + 2 scaffold templates; `sdist_content.py` lint hint updated. Sdist contents byte-identical before/after (`config`, `workspace` verified); preflight green with build+docs concurrent. Parallelism preserved.
