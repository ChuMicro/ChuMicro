# Next Up

> **Agent-managed file.  Single source of truth for the work queue.**  Each top-level bullet is capped at 5 bullet points (lead + sub-bullets, CHU011) — anything that needs more lives in a workstream file under [`workstreams/`](workstreams/), surfaced from here as a one-line pointer.  `## Done (recent)` is capped at 5 entries — drop the oldest when adding a new one.  Each Done entry: subject + commit hashes + headline result + workstream pointer; aim for under ~500 chars.  Detail lives in the commit message and the workstream doc.  **No zombie placeholders:** a fully-shipped effort whose only residual is "redo when problem X recurs" is *closed*, not parked in `## Next` — the archived workstream + git history carry the context, and a fresh bullet gets *created* when X actually recurs.  Session warm-up: `git --no-pager log --oneline -20` then this file.

## Now

_(nothing in flight)_

## Next

Independent items.  Most have either shipped phases (status in the linked workstream) or are unscoped placeholders waiting on a forcing function.

- [ ] **CI infrastructure workstream (unscoped — fires when CI is re-enabled)** — CI / GitHub Actions is disabled and has more structurally wrong with it than any single audit surfaced; it gets its own workstream rather than point-patches. Two concrete starting items inherited from audit-remediation P1 (analysis preserved in [`workstreams/archive/audit-remediation-and-drift-mechanization.md`](workstreams/archive/audit-remediation-and-drift-mechanization.md) Phase 1): (a) `ci.yml` lint job installs only `ruff`, so `run.py lint`'s `python -m chumicro_checks` can't run there — CHU enforcement never executes in CI; (b) `release.yml` publishes to immutable PyPI on a VERSION bump with `needs: detect` only, ungated by ci.yml success and before its own `validate-mip`. Neither is a safe isolated point-fix while CI is off (can't validate against a real run; the file is due for rework) — both wait for the dedicated pass.
- [ ] **Workspace-template gaps — gap #4b (regular-mode README)** — 7 of 9 shipped; gap #8 dropped 2026-05-12 (reversed; reasoning in archive §8).  One scoped task left: regular-mode README update on the [workspace-template repo](https://github.com/ChuMicro/ChuMicro-Workspace-Template) documenting `chumicro-workspace library add <name>` / `library browse` (acquire chumicro libraries from a published snapshot channel into committed `libraries/`, edit-preserving via `_library-backups/`, per Decision 0078) — plus a `.gitignore` `_library-backups/` add.  Mono-repo half shipped.  Detail: [`workstreams/archive/workspace-template-dev-and-regular-mode-gaps.md`](workstreams/archive/workspace-template-dev-and-regular-mode-gaps.md).
- [ ] **`websockets` oversize-frame length on Lolin S2** (surfaced 2026-05-16, 4-board campaign) — `websockets/test_drop_with_event_drains_oversize_and_stays_open` is deterministic on Lolin S2 (MP **and** CP), passes on Pico W: oversize frame reports length 600, test expects `(0, 400]`.  Real S2-specific websockets/buffer-chunking behaviour question, not environmental.  (The campaign's other findings closed: fragmentation suites deleted, CP-RAM `ntp` ceiling characterized under Decision 0072, network-bench gap was an environmental subnet/`mosquitto` mismatch — all in git history / their target workstreams.)
- [ ] **Expand the device test matrix beyond ESP32-S2** now that transport tooling is proven on both MicroPython and CircuitPython.
- [ ] **Performance + resource benchmarking infrastructure** — measure heap + CPU per library operation with explicit GC control; per-benchmark thresholds that fail on regression; separate `bench` task or deeper test tier so slow benchmarks don't run on standard `test`; CI on a schedule.
- [ ] **Deploy-path unification — one mechanism puts code on a board** (opened 2026-05-18) — divergent staging paths for one operation; additive-vs-clean-slate was the core defect. **[Decision 0077](decisions/0077-one-device-staging-path.md) `accepted`**; AGENTS.md one-staging-path non-negotiable in force. Done + bench-verified Pi Pico W CP: Phase 0 (ADR), Phase 1 (preflight `_env` fix; repl `chumicro_timing` board-dead regression fixed by retiring `repl <project>` → `deploy --tail`), **Phase 2 complete** (Commits 1a/1b/2a/2b + delta 6 + Commit 2c): single source owner; CP empty-dir reaping; keep-set unification; clean-slate default-flip with `--no-wipe`/`--wipe`; `settings.toml` evicted + one-time notice; 0059 §1 edited in place; MP whole-device clean-slate (delta 6, bench-verified Pi Pico W MP); Commit 2c — `FUNCTIONAL_TEST_EXTRA_EXCLUDES` deleted (CP functional = byte-identical clean rsync), `PostStageStep` names the post-stage fork, MP functional first-stage `mkfs`→keep-set-preserving `_clean_slate_device` (CP unit-tested; functional-suite bench the one open hardware step). **Phase 3 complete 2026-05-19 ([Decision 0078](decisions/0078-library-acquisition-is-host-local.md) `accepted`; design settled 2026-05-18 ADR-before-code, all forks user-decided; Commits 3a–3d, preflight-green, functional bench pending)** — Collapse A: `wrap_with_runtime_config` tail-dedup (3a); `deploy-example` → `ExampleSpec` branch on `resolve_project_deploy_source` + `deploy_diff`, interactive REPL drop kept (3b); `Deployer.deploy()` + wrapper `.deploy()` deleted, demo + low-level `chumicro-deploy` CLI folded onto `deploy_diff`, examples/guide rewritten (3c) — one stage primitive, zero CLI bypass. Collapse B **superseded mid-flight**: the 3d `_libraries/` pip/mip-`--target` fetch was the wrong design (parallel acquisition subsystem, blob `--target` extract, AST-discovery cart-before-horse). **[Decision 0078](decisions/0078-library-acquisition-is-host-local.md) rewritten in place** (not a new ADR — wrong-from-the-start correction): declarative `library add` pulls full editable trees + pyproject closure from a published snapshot channel (`ChuMicro/ChuMicro-Libraries[-Experimental]`, circup/mip-shaped tags) into **committed `libraries/`**, edit-preserving (`_library-backups/`), one engine behind a prompt_toolkit browser + scriptable `library add`. Acquisition engine rewired: `library_channel.py` (snapshot resolve/extract, 100 % cov) + `library.py` onto the curated `libraries:` table, `CliEnv.http_get` seam, fixture-backed.  Collapse landed (`install-libraries`/`_libraries`/pip-mip subsystem deleted, `e85f7604`→`4e5ccaa6`); `library browse` prompt_toolkit front-end landed (`b0ea444e`); producer `scripts/libraries_channel.py` + `index.json` catalog landed fixture-backed (`5d8aa52b`).  Phase 4 root-convergence subsumed.  **Remaining:** Phase 5 CHU mechanization per 0074; CI-workstream wires `release.yml`→`libraries_channel.main` (channel repos `ChuMicro/ChuMicro-Libraries[-Experimental]` manually staged 2026-05-19, tags `2026.05.19` / `2026.05.19.1`); cross-repo workspace-template `.gitignore`/README (gap #4b). **Validation residuals** (offline coverage is solid — preflight green incl. ~76 new/rewired unit tests; the gaps are physical/terminal-bound): (a) ~~functional 4/4-board bench~~ **closed 2026-05-19** — fresh `temp-project` clone of workspace-template + `library add chumicro_timing --channel experimental` → `import_smoke.app` (`chumicro_timing.ticks_ms()`) ran green on all four Lolin S2 + Pi Pico W × CP+MP pairs; **secondary finding logged separately**: S2 MP needed `--wipe` because the default additive-deploy diff left a stale empty `chumicro_timing/` directory behind that shadowed the new module write (deploy walker tracked in its own ## Next entry below); (b) ~~live-channel smoke~~ **closed 2026-05-19** — `library add chumicro_timing --channel experimental` against real HTTPS round-tripped `index.json` + tarball, curated trees landed cleanly; producer fixed mid-stage (preserve `.git/` on stage-into-checkout, filter `__pycache__`/`.pyc`/`.DS_Store`); (c) ~~manual `library browse` TTY smoke~~ **closed 2026-05-19** — user-driven TTY session shipped `chumicro_websockets` via the browse → add path; first-real-session UX feedback landed in this commit (Enter is now the add action, `i` drills into info, Backspace joins `b`/Esc as back, help-text mentions which keys move between examples); scroll + multi-example sifting tracked separately in its own ## Next entry below.  Per-library version pinning → `open-questions.md` (pyproject-version-constraint gate). Cross-refs: deploy-mode-unification (complete, mode axis), walker-unresolved-import-failure (open neighbor). Detail: [`workstreams/deploy-path-unification.md`](workstreams/deploy-path-unification.md).
- [ ] **`chumicro_deploy.config` docstring references deleted symbol** (surfaced 2026-05-19 in repl audit) — `workbench/deploy/src/chumicro_deploy/config/__init__.py:59` cites ``chumicro_repl.cli._device_from_args`` as the precedent for runtime-kwarg gating, but that function was deleted in `681e9408` (cli slim, 2026-05-14).  Rewrite the explanation inline or point at a still-extant callsite.  Out-of-scope for the repl audit pass that found it.
- [ ] **`/audit-comments pytest-device` — three degraded comments** (surfaced 2026-05-19 in pytest-device audit) — three stretches of prose in `workbench/pytest-device/src/chumicro_pytest_device/plugin.py` failed the cold-maintainer test under a trim-only audit: the per-file / per-library deploy-path inline blocks at ~1045-1067 and ~1115-1133 (longer than the code they wrap, mixing two deploy modes with RP2040 board facts and idempotency notes), the `_TransportCache.disconnect_all` docstring at ~988-1004 (16 of 17 lines spent on the ESP32-S2 USB-CDC wedge incident), and the overlapping `_IN_MEMORY_MODES` + `_should_soft_reset_before_stage` prose at ~1582-1635 (two homes for one concept).  Route to `/audit-comments` for rewrites from a fresh read, not further subtractive trim.
- [ ] **`/audit-integration deploy,pytest-device` — transport-cache + fallback-device overlap** (surfaced 2026-05-19 in pytest-device audit) — `chumicro_pytest_device.plugin._TransportCache` re-implements "one transport per device id + invalidation" semantics that `chumicro_deploy` already exposes via `transport.recover()` / `transport.disconnect()`; worth a sibling-cohesion pass to decide whether the cache should move into `chumicro_deploy` as a reusable bit or stay plugin-private.  Companion question: `_load_fallback_device` (plugin.py) duplicates the `DeviceConfigError` handling already done in `pytest_sessionstart`; if it survives the in-session investigation, the duplication shape is also worth airing in the integration pass.
- [ ] **Deploy walker fails on unresolved imports** (proposed 2026-05-12, separate session) — `workbench/deploy/src/chumicro_deploy/sources.py:367-378` silently skips imports that don't resolve against `search_paths`.  Result: a file containing `from chumicro_sockets import ...` can be deployed without `chumicro_sockets` in `library_sources`, ImportError surfaces only at first boot.  Today's DI cleanup (Done) removed the unconditional top-level imports that were riding on this silent-skip; the walker fix is the load-bearing correction so that any *future* unresolved import is a deploy-time failure with a clear message naming the importing file + the missing module.  Decision 0062's bench-validation table also needs a corrective rerun once landed.  Detail: [`workstreams/walker-unresolved-import-failure.md`](workstreams/walker-unresolved-import-failure.md).

## Investigations

- [ ] **Investigate slow MicroPython RAM-mode functional test runs** — observed during 2026-04-19 live PyCharm testing that MicroPython RAM-mode functional tests took noticeably longer than expected.  CircuitPython RAM-mode is fast in comparison.  Suspects: per-file `mpremote mount` cost, cold-start interpreter overhead, batch-vs-per-test trade-off.  Profile against the new batch-execute path.

## Done (recent)

> Capped at 5 entries — subject + commit hashes + headline result + workstream pointer; under ~500 chars each. Verbose detail lives in the commit message body or [`workstreams/<name>.md`](workstreams/) (per-phase acceptance).

- [x] **`/audit-library repl` — three-commit pass** (2026-05-19, this commit) — Read pass over `workbench/repl/src/` + tests + README/guide.  HIGH batch (commit A): four broken `:func:`chumicro_repl.tail.tail`` Sphinx cross-refs (`cli.py`, `session.py`, `patterns.py`, `testing.py` — no such submodule, `tail` lives in `_follow` and re-exports as `chumicro_repl.tail`); `recovery.py` module-docstring claimed `RecoveryPlan` mirrors deploy's 3-field shape `(headline + fix_steps + retryable)` but the dataclass has only the first two — drop the `retryable` mention and note the deliberate shape divergence (repl's coaching loop retries every classified kind).  Behavior commit B: `chumicro-repl --mode` default flipped from `passthrough` to `auto`, resolving to `line` on a TTY / `passthrough` when stdin is piped — closes the long-running README + `docs/guide.md` drift that described line as the default while the code shipped passthrough; matches the `chumicro-workspace repl` dispatcher already does.  Two new CLI tests for the auto-resolve branches; line_mode.py module docstring rewritten from a fresh read (the old "default loop is byte-passthrough" opener is now inaccurate).  Dedup commit C: promote `_follow.py::_read_chunk` to `_serial.py::read_chunk` and collapse four sites that each spelled out the same `in_waiting` / `read(1)` fallback (`_follow.py`, two methods in `session.py`, `completion.py::_read_until_marker`); helper stays exception-neutral so each caller keeps its own disconnect contract.  `VERSION` bump `0.1.0` → `0.2.0` (CLI default flip is user-visible).  Out-of-scope finding filed in ## Next: deploy/config docstring references a `chumicro_repl.cli._device_from_args` deleted in `681e9408`.  1687 tests across repl + workspace + deploy + pytest-device green.
- [x] **Comment audit — `workbench/workspace/cli/` subpackage** (2026-05-19, this commit) — `/audit-comments` over 14 files (~5.6K lines), follow-up to the non-CLI batch.  Three ADR-number leaks deleted from publishable trees (`bootstrap.py` Decisions 0039/0059/0066, `examples.py` Decision 0077, `library.py` Decision 0066).  Two stale change-narrative residues from the Decision-0077 one-staging-path landing trimmed (`deploy.py` "surface now" → drop "now"; `library_browser.py` `_i`-binding "Mirrors the old Enter behavior" → state current invariant).  Two forward-looking speculations dropped (`examples.py` LED-blink "future enhancement"; `deploy.py` `_cmd_projects` "follow-on once the REPL one-shot pattern lands").  One workstream-pointer prefix (`setup.py` "# Gap 3(a):").  One stale `/active.py` rewritten in `devices.py` rename docstring (post-Decision-0077 the project's files land at the device root, no `/active.py`).  One AI-tic ("canonical workspace template" → "workspace template") in `quality.py`.  Plus a cross-site invariant collapse — the full staging-pipeline statement stays on `resolve_project_deploy_source` (the policy owner); `ExampleSpec` trimmed to its narrower role.  11 edits total; preflight green.
- [x] **Comment audit — `workbench/workspace/` non-CLI source** (2026-05-19, `ad602589`) — `/audit-comments` over 30 files (~6.8K lines).  Deletes / trims: workstream pointer (`chumicro_dev.py` "Gap 3(a) of..."), mono-repo paragraphs in publishable trees (`example_verify.py` `scripts/verify_examples.py` ref, `example_source.py` "upstream chumicro source tree" framing), 5 `_cmd_*` downstream `:func:` pointers (`scaffold`/`health`/`boot_shim`/`firmware_support`), change-narrative residue (`additive_apply.py` "the YAML path used to do this explicitly"), mirror-pointer reasoning (`onboarding.py` `_UF2_MOUNT_SEARCH_PATHS` block), filler praise (`writer.py` "the well-maintained host-side library").  Two routed CODE fixes done in-pass: `recovery.py missing-config-key` hint was pointing users at `workspace.yml`'s `defaults:` block — corrected to `project_config.toml` / gitignored `secrets.toml` (`workspace.yml` is committed machinery, not the secrets/defaults file); `boot_shim.SHIM_ENTRYPOINT_SOURCE` shim provenance corrected from "chumicro-deploy" to "chumicro-workspace" (the shim is synthesized here, not by deploy).  CLI subpackage follow-up entry added to `## Next`.
- [x] **`library browse` second-pass UX — scrollable content, visible selected example, reordered help** (2026-05-19, this commit) — User's second TTY session surfaced: Up/Down "did nothing" (was cycling examples but they rendered below the README, invisible), README + example content didn't scroll, header led with `[Up/Down] pick example` before `[Backspace] back` which felt backwards.  Reshaped the layout: status line + help line + horizontal rule + a `DynamicContainer` that swaps between a `FormattedTextControl` (list view) and a focused `TextArea(scrollbar=True, wrap_lines=True)` (detail / example views).  Up/Down (declared `eager=True`) wins over the TextArea's own binding so it still cycles examples in detail view; PgUp/PgDn/Home/End scroll the buffer natively.  Status line shows `<name> — README (example N/M: filename)` so the cycling target is always visible without scrolling to the bottom.  Help line leads with `[Backspace] back`, then the navigation keys, then `[Enter] view example` last.  16 model tests still pass (no model change; the rework is shell-only `# pragma: no cover`).
- [x] **`.chumicro-local` sentinel — user-claimed library trees survive transitive re-fetches** (2026-05-19, this commit) — `_place_library` now checks for `.chumicro-local` at `libraries/<name>/` and skips the back-up-then-replace step when it's present, emitting a one-line notice that names the on-disk and channel versions so the user knows what they're not pulling.  Bench-confirmed on the temp project: edited `libraries/chumicro_sockets/src/chumicro_sockets/__init__.py`, dropped the sentinel, then `library add chumicro_http_server --channel experimental` (which pulls sockets transitively) — the edit survived, no new `_library-backups/chumicro_sockets/` entry was created, and the notice fired.  Direct `library add chumicro_sockets` against the same state also honors the sentinel.  `library list` gains a `[local]` flag on held rows.  Design beats the workspace.yml-flag + hash-detection alternatives surfaced earlier: claim lives with the artifact, survives directory moves, no new verb (`rm .chumicro-local` to resume tracking), matches the existing artifact-level marker pattern (`__chumicro_test_support__`, `__chumicro_runtimes__`).  3 new tests cover the sentinel detection + direct fetch + transitive-walk paths; 33 workspace tests pass.
