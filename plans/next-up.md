# Next Up

> Work queue.  One bullet per item, no sub-bullets — anything needing more than a title goes to [`workstreams/<name>.md`](workstreams/) and surfaces here as a one-line pointer.  Tracks status, not research.  No `## Done` section — `git --no-pager log` carries history.

## Now

- [ ] **Public repo mode — process flip landed on the `public-repo-mode` branch, settings flips pending (user call 2026-08-09).**  [workstreams/public-repo-mode.md](workstreams/public-repo-mode.md) — Decision 0120 (main is PR-only), workflow least-privilege + SHA pins, `@claude /review`, maintainer runbook, and the contributor-docs updates ride one PR; the one-time maintainer settings flips (ruleset bypass to PR-mode, Actions default to read, fork-PR approval to all outside collaborators, pypi environment branch policy, `ANTHROPIC_API_KEY` with a spend cap) are listed in the workstream and the PR body.

- [ ] **Release tag step 403s, leaving partial releases (found 2026-08-08 dogfooding PR #10).**  `release.yml`'s `Create GitHub Release and tag` step fails with `403 Resource not accessible by integration` on the create-a-release endpoint, so `chumicro-deploy-experimental` 0.39.0 is live on PyPI with no `chumicro-deploy-v0.39.0-experimental` tag, and the four jobs keyed off that tag (bundle publish, libraries channel, mip validation, release manifest) all skipped; stable promotion is blocked because `promote.yml` keys off the experimental tag.  Not the workflow file: the failing job reports `Contents: write` exactly as the last working run (deploy 0.38.2, 13:58) did, there is no tag ruleset or legacy tag protection, the tag does not already exist, and it reproduces identically on re-run.  Suspect an org-level GitHub App installation permission capping the Actions token, which is the one layer the run log does not show and which `orgs/ChuMicro/actions/permissions/workflow` refuses to read without `admin:org`.  Repo `default_workflow_permissions` is `read`; widening it to `write` is the cheap test but loosens every workflow, so the narrower fix is swapping `softprops/action-gh-release` for an explicit `gh release create` step that also fails legibly.  Recovery once fixed is a re-run of run 31271808127: `skip-existing: true` on the PyPI step and the tag being the last write plus the idempotency key were built for exactly this, so no PyPI surgery is needed.  Hazard while open: any further VERSION bump creates another partial release the same way.

- [ ] **Dependabot #7 and #9 are stuck BEHIND.**  Auto-merge is armed on both, but the branch update rewrites `.github/workflows/`, which an OAuth token without `workflow` scope may not do; clear with the UI "Update branch" button or `gh auth refresh -s workflow`.

- [ ] **Structural pass before stable launch (user call 2026-07-18).**  [docs/superpowers/specs/2026-07-18-structural-pass-design.md](../docs/superpowers/specs/2026-07-18-structural-pass-design.md) — **Phase 1 COMPLETE 2026-07-18 night** on branch `worktree-structural-pass-phase1` ([plan](../docs/superpowers/plans/2026-07-18-structural-pass-phase1.md), 9 commits): factory glue lives in `chumicro_sockets.sockets_factory` (Decision 0115 supersedes 0093; sockets mpy +159 B measured, five libraries shrank, skip-family mechanism untouched), generator-first guides on requests/websockets/mqtt, "Choosing a wait" table in the timing guide, runner service contract documented + `validate_service` in `chumicro_runner.testing`, logging de-advertised + compat claims trued, template-repo README tracked the change (commit `7c4a198` there).  **Phases 2+3 COMPLETE 2026-07-19:** `run.py` 4,222 → 364 lines (nine modules under `scripts/run_tasks/`, dead `validate-picker` path dropped, Decision 0048 phase wiring preserved, all spot-checks green), decisions README carries the 0074 evidence bar for new lints/process, SECURITY.md + CODE_OF_CONDUCT.md landed, `demos/laptop_roundtrip` proves the loop on a bare laptop, README repo map completed; template first-run breakages verified already fixed (template `7262807`+`b2b8608` ruff exclude, mono `e1154e10` branding).  **Stable wave COMPLETE 2026-07-19** (all 19 active publishable packages live on stable PyPI, `ChuMicro-Bundle` public; see [workstreams/release-fabric-readiness.md](workstreams/release-fabric-readiness.md) "Stable channel closed").  **Launch flips LANDED 2026-07-19** (user go): `DEFAULT_CHANNEL` → stable (workspace 0.51.0, released + promoted), template `requirements.txt` on stable names (template `73db190`), bootstrap token revoked + `~/.pypirc` deleted.  **GA docs pass COMPLETE 2026-07-19** ([spec](../docs/superpowers/specs/2026-07-19-ga-docs-pass-design.md)): six-scout recon, all 14 library README+guide pairs rewritten to one consistency spec (7 drift bugs fixed incl. broken `Signal`/`wait_for` imports, logging's parked disclosure, canonical Contributing block + scaffold generator synced), docs/troubleshooting grown 3 → 12 symptom-first pages from a 138-item gotcha harvest, contributing docs de-duplicated + de-anchored, landing page curated (timing-first), em-dash ban enforced corpus-wide in both repos, template first-run fixes (`setup --help` pure, dev-toml errors name the fix, quickstart scaffolds `wifi_only`), channel-prose flips landed both repos.  **Flip list CLOSED 2026-08-07** (user go): the pyproject-description sweep published as patch bumps across the wave below, and the gh-pages 0.1.x prune landed (16 stale mike dirs — timing 6, msgpack 5, compat 3, runner 2 — deleted at `d881234e`; every stable/experimental alias verified 200, every pruned version 404).  Side-finding CLOSED 2026-08-07, **not reproducible**: the template's `run.py lint` does not rewrite anything.  It resolves to `chumicro_workspace.cli.quality._cmd_lint`, which runs `ruff check` with no `--fix` and no `ruff format` — verified in source, in the published 0.52.0 wheel, and by live runs in both chumicro-dev and regular mode (violation reported, exit 1, file md5 unchanged); its CI step has no `continue-on-error`, so violations do fail the job.  The likely memory is the adjacent green-washing bug commit `151b6ed1` already fixed, where a missing ruff or chumicro-checks returned 0 and linted nothing.  The hunt did turn up a real defect, fixed in workspace 0.52.1: the documented `lint -- <flags>` passthrough forwarded the `--` itself, so ruff read the separator as a filename and the passthrough worked for no flag at all.

- [ ] **Release-fabric — publishing pipeline restored + experimental wave live 2026-07-18.**  [workstreams/release-fabric-readiness.md](workstreams/release-fabric-readiness.md) — All four workflows re-enabled (they were `disabled_manually` as a launch gate, not broken).  Experimental wave published end to end via release.yml (run 29650203215): 18/18 PyPI current including the http_server 0.18.1 / checks 0.17.0 / deploy 0.37.1 stragglers, first real device bundle live (`ChuMicro-Bundle-Experimental` tag 20260718, circup zips + mip layout, 26/26 mip validations), libraries channel index refreshed (20260718.153658, anonymous reads verified), docs deployed to gh-pages.  Sister-repo acquisition coherence landed (Decision 0111): fresh workspaces default to experimental, `chumicro-test-harness` is publishable (VERSION + release_matrix `kind=support`) and `patch-experimental` now rewrites `[test]` extras, regular-mode bootstrap installs tooling from `requirements.txt` (chumicro-repl gap fixed, verified on a fresh clone).  **Stable wave COMPLETE 2026-07-19:** all 19 active publishable packages promoted to stable via promote.yml and verified (each serves at its target version, licensed, clean stable-name deps — no `-experimental` leakage); `ChuMicro-Bundle` flipped public; the new-project limiter was sidestepped by the manual pypi.org web-UI path (one pending publisher per package, registered just-in-time as the previous converted).  Decision 0111 support-package publishing FINISHED (commit `f764efc9`): `find_publishable_packages` / `PUBLISHABLE_ROOTS` / `promote_validate` now scan `support/`, `chumicro-test-harness` migrated setuptools→hatchling (the one non-hatchling package, which broke the `--no-isolation` build once publishable) + bumped 0.3.2, auto-released experimental then promoted stable as the first support package through the whole fabric — `pip install chumicro-<lib>[test]` now resolves on stable (verified `--dry-run`, pulls test-harness 0.3.2).  **Launch flips LANDED 2026-07-19** (user go): `DEFAULT_CHANNEL` → stable in `curated_libraries.py` (workspace 0.51.0), template `requirements.txt` on stable names with a 0.51.0 workspace floor, bootstrap token revoked + `~/.pypirc` deleted; a full provenance audit (PyPI attestations, both channels, all 19 packages) confirmed every artifact was published by release.yml/promote.yml — zero token uploads.  Trusted publishers are DONE all around: every experimental project pure-OIDC, every stable project carries a promote.yml publisher.  **Evening validation 2026-07-18:** first organic push-triggered releases ran end to end (mqtt 0.27.0, then checks 0.19.0 + workspace 0.48.2 — the workspace follow-up release is DONE); the re-run safety held live (two failed publish legs recovered via dispatch, no permanent traps); `chumicro-test-harness-experimental` 0.3.0 published, and `pip install chumicro-mqtt-experimental[test]` now resolves its full 24-package closure (verified `--dry-run`) — the audit's top `[test]`-extras finding is closed on experimental.  **Wave 2026-08-07 (18 bumps, 17 published both channels).**  Nothing had shipped since 07-19; the 07-28 packaging and docstring work was stranded on main.  Patch for the prose-and-annotation packages, minor for two that changed behavior (`checks` 0.20.0 gains CHU037, `workspace` 0.52.0 emits canonical doc shapes).  `sockets` and `timing` had nothing unreleased and were left alone, so their stable docs were refreshed by a direct `docs-deploy --channel stable` dispatch instead — a bump-less library's stable docs otherwise stay frozen, since only promote.yml writes that alias.  The stranded `workspace` 0.51.1 (published experimental 07-19, never promoted) is resolved: stable went 0.51.0 → 0.52.0.
  **Follow-ups:** two open questions recorded (version-namespace correlation, mpy ABI proliferation).

- [x] **Promotion waves are one run — CLOSED 2026-08-07, [Decision 0117](decisions/0117-promote-wave-in-one-run.md).**  The 08-07 wave measured the old shape: 92.3 min as 17 promote runs against 9.2 min for the same 17 packages through release.yml, same runners, same day.  `promote.yml` now takes a tag list; `promote_matrix.py` loops `promote_validate` per tag so a one-tag promotion and a wave apply identical rules; the preflight gate fans out over *distinct commits* rather than tags (a wave cut from one push gates once, which was ~45 of those 92 min); publishes run as a matrix; bundle, docs and channel each write once.  Batching removed the dependency ordering rather than relocating it — one snapshot stages every library and `finalize-tag` pins them together, which is why release.yml never needed ordering either.  `promote_wave.py` deleted: the concurrency cancel trap it dodged is unreachable with a single run.  Dry run (17 tags, `include_tagged`) green in 3.7 min, one gate commit, all 17 build legs parallel.  **Fully exercised live 2026-08-07** on workspace 0.52.1 (workbench: publish + docs) then runner 0.19.2 (library: publish + bundle + channel + mip + docs), all seven jobs green in **5.3 min**.  Live testing earned its keep by finding two rewrite regressions the dry run structurally could not: the docs job called `run.py docs-deploy`, which commits locally, never pushes and still exits 0 — a green job that shipped nothing, the same silent-success shape as the diff-base gate above; and it then checked out with `GITHUB_TOKEN`, which the gh-pages ruleset rejects with GH013 (it bypasses only OrganizationAdmin, DeployKey and admin), so the push failed *after* the PyPI publish.  Both restored from the job the rewrite replaced: `docs_deploy_retry.py` plus `ssh-key: secrets.GH_PAGES_DEPLOY_KEY`.

- [x] **CI was red for 14 straight commits — CLOSED 2026-08-07.**  Every push from the first surfaces commit (08-01) to `d373e3b4` (08-02) failed on one cause: two ADR `Summary:` lines over the CHU029 200-char cap.  All 13 other preflight phases were passing the whole time.  The deeper finding: `check-version` and `check-api` were only grading the LAST commit of a push, because `origin/main` IS the pushed commit on a direct-to-main push and `effective_diff_base` fell back to `HEAD^`.  That is how the 07-28 push landed 17 unbumped `pyproject.toml` edits under a green CI.  Replaying `f81694c0` proves it: old code prints "No changed files detected" and exits 0; with the push base it names all 18 packages that owed a bump.  CI now passes `github.event.before` as `CHUMICRO_DIFF_BASE`, ignored for the three cases that would break it (pull_request's empty value, a first push's zero SHA, a force-pushed-away SHA).

- [ ] **Library size cut — structural −40% on the heavy trio (user calls 2026-07-05).**  [workstreams/library-size-cut.md](workstreams/library-size-cut.md) — Phase 1 SHIPPED 2026-07-05 evening: lazy `__init__` for mqtt/requests/http_server — package-import heap −62/−49/−56 % on the MP unix proxy, all lanes green, no budget bumps.  **mpy channel coverage audited 2026-08-08** (user asked whether mpy reaches the channels that should carry it): the two device channels do, on both stable and experimental — `mpy6/` holds all 13 libraries with MicroPython magic `4d06`, `circuitpython-10.x-mpy/` holds all 13, and both circup zip variants (`-10.x-mpy-` / `-py-`) attach to every bundle release, the mpy one verified as 51 real `.mpy` members with CircuitPython magic `C\x06`.  PyPI correctly carries none (CPython target) and the libraries channel correctly carries none (editable source).  The one path with no mpy is the workspace deploy walker, which is exactly phase 0 below — and it is now the *primary* consumer path, since template-workspace users get `.py` while a circup user of the same library gets bytecode.  A real dry-run measured it: 36 files / 200.1 KiB of `.py` for a five-library project.  Next: **phase 0 ship-.mpy deploy-walker, alone** — the mqtt `client.py` split is CANCELLED (measured 2026-08-07, see the workstream): shipping `.mpy` cuts the import floor 69 % against the best split's 40 %, and once it ships the split inverts to a net loss on flash, retained heap and floor alike.  Phase 0's first bench must measure `.mpy` on real hardware, which nothing has yet done.  Then the structural diet on the heavy trio.

## Next

- [x] **Non-blocking MP TLS handshake: attempt two CLOSED 2026-08-09, parked as a recorded substrate compromise.**  The readinto-probe design works for error surfacing (verified in the pinned source and stub-tested) but MP 1.27 exposes no safe handshake-completion signal (`getpeercert` compiled out, `cipher()` crashes pre-session, zero-length writes short-circuit in `py/stream.c`), so a stepped bring-up cannot promote provably.  Blocking wrap restored, functional TLS matrix green, and the compromise is recorded in Decision 0081 beside CircuitPython's per-phase blocking.  Upstream engagement is off the table (user call, standing).  Revisit only if a future pinned MicroPython exposes handshake state.
- [ ] **CP-serial raw-REPL framing desync on long native-USB sessions:** the Pico W over `deploy_transport: serial` intermittently answers "Raw REPL did not acknowledge code" with the OK trailing stale banner bytes (`'\x04\x04>OK'`), failing whole files at the clear-stale-entrypoint step; short suites and the UART-bridge board never hit it.  Every failure is transport-level, zero behavioral.  Harden the post-soft-reset drain in `circuitpython_serial_transport` (workstream device-matrix-reliability owns the serial path).
- [ ] **Roll the drive-less boot.py + `deploy_transport: serial` treatment out to lolin-s2-cp-swap and tinys3-cp (user call):** the Pico W's FAT corrupted twice in one day under macOS-mounted flash sessions and the TinyS3 died the same death earlier; the serial transport plus a `storage.disable_usb_drive()` boot.py removes the shared-FAT owner race entirely, at the cost of no CIRCUITPY drive on the host.
- [ ] **msgpack depth-cap contract across a runtime reflash (decision needed):** CircuitPython firmware's native msgpack has no nesting cap by design, so a CP board can persist an NVM or littlefs payload nested past 8 that the pure path (MicroPython, CPython) refuses to load; reflashing that board to MP turns the store into KVStoreCorrupt.  Either accept the gap as a documented migration caveat or enforce the pure cap in the native packb/unpackb wrappers (one object-walk per cold-path commit).  Surfaced by kvstore's first real-CP-board run, 2026-08-09.
- [ ] **Compat gate stays warn-only for now (user call 2026-08-09).**  Decision 0092's self-retirement (flip `PUBLISHED = True` in `scripts/check_api.py`, then the successor SemVer/deprecation policy it promised) is deliberately deferred: publication happened 2026-07-19 but we are the only consumer of the published packages even though they are public, so the bar is "the whole ecosystem works when we are done", not per-package deprecation windows.  griffe keeps running warn-only.  Revisit when a real external consumer appears; the per-library version-pinning open question rides along.

- [ ] **Seam coherence + workspace-tooling hygiene (design review 2026-08-09, user call: fix).**  [workstreams/seam-coherence.md](workstreams/seam-coherence.md) — `from_config` seam drift across the five networking libraries (`ticks=` forwarded only by mqtt, websockets server binds its listener eagerly at construction, five wordings of the skip-factories error), wifi's odd-one-out list (no service-level `from_config`, constructor side effect, CPython fake-adapter fallback, three documented `radio=` spellings), runner's write-only fault counter, and the workspace-tooling items (`update` has no dirty-worktree guard and never deletes, `library add` writes tracked `pyrightconfig.json`, hand-derived ownership tables, CI installs from floors with no lockfile).  Parked while another session owns the repo; the library fixes need version bumps and board time.  **Partially landed by the 2026-08-09 design-smell audit:** `ticks=` now forwarded by ntp/websockets `from_config`, wifi's constructor side effect removed (configure deferred to first attempt) and the CPython fallback moved out of `testing.py` into `_adapters/cpython.py`.  **Library side CLOSED 2026-08-09 night:** every service `from_config` forwards `**kwargs` ([Decision 0118](decisions/0118-from-config-constructor-passthrough.md)), the websockets listener bind is deferred to the first tick behind a `listener_factory=` seam, the skip-factories error is one canonical wording at all six sites, wifi gained `WifiService.from_config` and one `wifi.adapter.radio` spelling across the docs, runner's fault counter became the readable `last_handler_error` pair, and `udp_socket_factory` guarantees a non-blocking socket — all board-validated on both runtimes.  Remaining here: only the workspace-tooling items (`update` dirty-worktree guard and deletion, `library add`'s tracked `pyrightconfig.json`, hand-derived ownership tables, CI lockfile).

- [x] **CI preflight-gate is core-starved, not clock-bound — CLOSED 2026-07-19.**  Opus profiler: the full suite runs ~15s local (12-core) but ~108s on the 2-vCPU `ubuntu-latest` because it fans out 13 phases × 8-wide and can't breathe on 2 cores; the device libraries are clean (no test over 0.06s) and the only real idle-wait waste (~2.5–3.5s) sat in the pytest-device harness.  Landed the tests-only trims (HTTPServer `serve_forever(poll_interval=0.01)`, tighter hanging-worker/LAN/marker timeouts) and the echo-fixture stop-poll trims (250→50 ms + matching teardown sleeps, pytest-device 0.17.8).  The 4-core larger-runner flip was DECLINED (user call 2026-07-19): the remaining preflight wall-clock rides on the free 2-vCPU tier rather than paying larger-runner rates for a non-blocking gate.

- [x] **Workbench + webui prose voice pass — CLOSED 2026-07-28, grown into a full second docs pass.**  Four-scout recon then 14 fix agents: all 15 workbench README/docs pages rewritten against `--help` and src (workspace README's 36-broken-import API block rebuilt, retired `workspace_runtime` boot-shim story replaced, deploy `--erase` inversion fixed, phantom `Deployer.deploy()` cleared from five files, workspace api.md stub grown to 19 verified sections), webui module prose swept, all 42 library index/api/testing pages respec'd per [docs/superpowers/specs/2026-07-28-secondary-docs-respec.md](../docs/superpowers/specs/2026-07-28-secondary-docs-respec.md) (broken WiFi-seam quick examples fixed, lazy-export API pages now render `MQTTClient`/`WebSocketClient`/requests generators, dead runner gate-API section deleted), scaffold + workbench templates synced to canonical (kind-aware install/find sections, runnable Runner example, zero em-dashes in rendered scaffolds), troubleshooting re-voiced to `chumicro-workspace` commands, demos flag-synced to their drivers, AGENTS.md's four expired facts fixed, examples' secrets-placeholder guard now catches `replace-with-your-ap-ssid` (template + all copies), Decision 0019's unapplied branch-protection step corrected in place, and `CHU037` (em-dash ban, user-facing prose + md templates) landed with tests so the sweep can't rot again.

- [x] **Runner stale-fd busy-spin — REAL, fixed 2026-08-07 (runner 0.19.2).**  `_sync_poll_set` unregistered by object; CPython keys its poll set by descriptor read off the object at unregister time, and a closed socket answers -1, so the unregister raised, the sweep swallowed it and popped the slot anyway, and the descriptor stayed in the kernel set forever.  `poll()` then returns POLLNVAL instantly and `_POLL_ERROR_MASK` (POLLERR|POLLHUP) ignores it, so `wait()` never blocks again — a board that stops sleeping after its first completed fetch.  Slots now carry the descriptor recorded while the socket was open and the sweep retries with it, skipping the retry when a live slot already claims that descriptor (a freed fd goes straight to the next socket opened).  The pre-existing test used a poller keyed by `id(obj)`, which cannot strand anything, so it could never have caught this.  Costs 812 stripped B and leaves runner at **98.6 % of its ceiling (285 B headroom)** — the next `core.py` change needs a budget conversation.  The reported `send_budget_per_tick`/POLLHUP race does NOT reproduce: POLLHUP only reaches a service's `io_error` hook, which neither http_server nor websockets defines, and the sockets those two throttle never enter the poll set.  Parked: `_POLL_ERROR_MASK` still excludes POLLNVAL, so a socket closed out-of-band while its service still reports interest would busy-spin the same shape from a different cause; no caller demonstrated, mask left alone.

- [ ] **Release correlation manifest — LIVE on both channels (Decision 0112).**  `scripts/release_manifest.py` + a `manifest` job maintain a cumulative `releases.json` in the channel bundle repo (home chosen: the bundle repo overlays rather than wipes, so the index survives every push), tying each snapshot to its package versions + both channel tags.  Experimental populated organically 2026-07-18 (entry 20260718.1, mqtt 0.27.0, after the stage-before-diffing fix); the morning 20260718 wave backfilled from its real 13-library matrix and pushed 2026-07-18 night — both snapshots verified serving anonymously; current through 20260807.1.  **Stable half wired 2026-08-08**: only release.yml carried a manifest job, so the channel every documented install path points at had no correlation index at all — a stable bundle tag resolved to no package versions and a stable PyPI version to no snapshot, 11 stable bundle tags deep.  promote.yml now runs the same job with `--channel stable`; its `bundle` and `libraries-channel` jobs grew the `date_tag` / `snapshot_tag` outputs the manifest keys on (promote computed both inline and emitted neither).  `promote_matrix.py`'s `library_matrix` already carries the `library_name` + `version` fields `build_entry` reads, so the shapes matched with no producer change.  Verified by simulating the write against the live tags: correct entry, idempotent on re-run.  First stable entry appears on the next library promotion — the index is forward-looking, so the 11 prior stable bundle tags are NOT backfilled (they would need per-tag package sets reconstructed from promote run history; low value, not planned).  **Empty-value guard added 2026-08-08**, found while writing up the risk: argparse's `required` only proves a flag was passed, and every tag here arrives as a workflow expression, so an unset job output, a renamed step id or a skipped upstream job expanded to `""` and the script wrote a row keyed on the empty string while printing a success line — the same silent-success shape as the docs job that committed and never pushed.  `--bundle-tag` / `--libraries-tag` / `--published-at` now reject empty and whitespace-only values, and an empty `--matrix` (which would record a snapshot correlating nothing, meaning the job's gate and the call disagree) is rejected too; 6 tests cover it.  **EXERCISED LIVE 2026-08-08 and the loop is closed.**  A docs-only mqtt 0.28.2 (client-id section, user go: "a docs change can be considered a release") rode the full fabric to force a library promotion: release.yml green on all seven jobs, then promote.yml green on all eight including `Publish release manifest`.  Both wired outputs populated for real (`bundle_tag` 20260808, `libraries_tag` 20260808.145137, neither empty) and `BUNDLE_DEPLOY_KEY` pushed to ChuMicro-Bundle without the GH013 that killed the docs job on 08-07.  `ChuMicro-Bundle/releases.json` now exists with its first stable row, and every identifier in it was verified real rather than merely present: the bundle tag exists and its tree carries mqtt 0.28.2, the libraries tag exists and its index reports 0.28.2, and `chumicro-mqtt==0.28.2` downloads from stable PyPI.  Note PyPI's JSON API served a stale `info.version` for several minutes after both publishes today; `releases` keys and a real `pip download` are the reliable checks, `info.version` is not.

- [x] **mpy-ABI groundwork trued up — CLOSED 2026-08-08 (Decision 0112 seam, not the ABI work itself).**  `bundle_layout.py` claimed adding an mpy format was "a one-file change" and that "every consumer iterates over this tuple instead of referencing `MPY_FORMAT_FOLDER` directly".  Neither was true: `mpy_format_folders()` / `cp_mpy_folders()` were called only by one test asserting they return single-element tuples, while `bundle_manager` and `validate_mip_install` both read the singular constant, so a second live ABI folder would have gone silently unvalidated.  Fixed where iteration is real and said so where it is not.  `validate_mip_install` now loops `mpy_format_folders()` at both its bundle-repo and local-server sites, labelling results by folder name instead of the hardcoded string `"mpy6"`, so a longer tuple validates the new folder with no validator edit.  The producer cannot be symmetric — it stages one folder per pinned mpy-cross, so a new format needs a compiler pinned in `target-runtimes.toml` and a second staging pass, not a longer tuple — and the module docstring now says that instead of over-promising.  Within a staging pass the folder is named once and threaded through every URL and dep reference: `_dependency_to_mpy_mip_reference` takes the folder as a parameter, closing a latent cross-format bug where a manifest staged into a newer folder would have pointed its deps at the older one and had mip assemble a mixed-format tree on the board.  4 new tests.  The ABI work proper (parallel folders + runtime-aware resolver) stays deferred until a real `.mpy` bump lands; only two formats are live today (`mpy6` magic `4d06`, `circuitpython-10.x-mpy` magic `C\x06`).

- [ ] **Template-workspace readiness — audited live 2026-08-08, one blocker found and fixed, one user-side gap open.**  The user asked whether both ends are ready for real projects.  Acceptance test the release sequence names ("sister CI green on a fresh runner") had never actually run; ran it locally instead: fresh regular-mode clone of the template → `run.py setup` / `lint` / `test` all green against live stable PyPI, pulling workspace 0.52.1 / repl 0.4.3 / pytest-device 0.17.9 / test-harness 0.3.3 / checks 0.20.0 (every one current).  `library add` works end to end off the stable libraries channel: correct transitive closure, versions matching PyPI exactly, `workspace.yml` + `pyrightconfig.json` updated.  **Blocker found and fixed (deploy 0.38.2):** every mqtt deploy was refused.  `chumicro_mqtt.client` binds its hardware-UID fallback chain to `guarded = (ImportError, AttributeError, OSError, NotImplementedError)` (mqtt 0.27.0, Decision 0114, 2026-07-18), and `_handler_catches_import_error` only recognized a literal `except ImportError` / literal tuple, so `except guarded:` read as an unrelated guard and the guarded `import uuid` — not a device built-in on either runtime — refused the deploy.  Shipping broken on both channels since 07-18 and invisible to CI because nothing in preflight walks the deploy graph over library source.  The walker now resolves a name bound to an exception tuple, conservatively: a name qualifies only when EVERY binding of it in the file is a catching tuple, so a rebinding to `(ValueError,)` poisons it rather than guessing permissively (guessing the other way ships an import that raises at boot).  Fixed in the walker rather than in mqtt deliberately — zero device bytes against a library near its ceiling, and it fixes the same refusal for user libraries written in the template, which is the point of the template.  Blast radius audited: that was the only aliased-guard site across all 14 libraries.  Proven by re-running the refused deploy with the fix installed into the fresh workspace: 36 files, 200.1 KiB, full map.  **Open, user-side:** repo-level Actions are DISABLED on `ChuMicro-Workspace-Template` (`actions/permissions` → `enabled: false`), the exact failure mode the mono-repo had on 2026-07-05; the `test` workflow is individually active but nothing has fired since 2026-04-27, and those last five runs are red from the since-fixed B3 `[dev]`-install bug.  Flip Actions on and the workflow should go green on its first run — the local acceptance test above is that same three-step sequence.  **CLOSED 2026-08-08**: Actions enabled, workflow dispatched, green on 3.11/3.12/3.13/3.14, first green run since April.

- [x] **circup installed no dependencies for any library — CLOSED 2026-08-08, shipped broken since the channel existed.**  `circup install chumicro_mqtt` installed mqtt alone; config, sockets and timing never arrived, so the board raised ImportError on first import.  That was every ChuMicro library on the CircuitPython path, on both channels.  circup resolves deps from `requirements/<library>/requirements.txt` beside `lib/` (Adafruit's bundle layout, read by `Bundle.requirements_for`, which hardcodes the `py` platform); our zips carried `lib/` and nothing else, so the lookup returned None for all 13 and circup reported success anyway — the shortfall was only visible on the device.  **Why it survived: the validation asymmetry.**  mip carries deps in `package.json` and `validate-mip` installs from the live repo on every release; circup was exercised nowhere, because MicroPython has a unix port and mip installs into a plain directory while circup demands a real CircuitPython drive.  That barrier is one file: a fabricated `boot_out.txt` in a scratch dir makes circup install into `--path` happily, which is how this was found.  Fix projects the requirements body from the same `package.json` deps the mip manifest uses, so the two install paths cannot drift (pinned by test), and writes import names rather than hyphenated PyPI names because circup matches against `lib/` directory names and would skip a hyphenated line in silence.  Landed in both channels by dispatching release.yml and promote.yml with `include_tagged` rather than inventing version bumps: the zip builder walks the whole merged tree, so one run per channel fixed all 13 libraries.  Verified as a consumer following INSTALL.md verbatim on both channels: `Ready to install: ['chumicro_config', 'chumicro_mqtt', 'chumicro_msgpack', 'chumicro_sockets', 'chumicro_timing']`, 19 `.mpy` files, magic `C\x06`, with msgpack pulled transitively through config.  **Durable lesson:** an install path with no consumer-side test is an install path that is broken and reporting success.

- [x] **Test-harness promotion + publication — DONE 2026-07-19 (Decision 0111).**  Resolved by keeping it in `support/` (not workbench, honoring [Decision 0082](decisions/0082-test-harness-as-infrastructure-library.md)) and making `support/` a first-class publish tier: `chumicro-test-harness` publishes as `kind=support` (PyPI-only, no device bundle), now live on both channels (experimental 0.3.2 + stable 0.3.2).  The network-helper half of [workstreams/test-harness-promotion-and-network-helper.md](workstreams/test-harness-promotion-and-network-helper.md) is unrelated and stays open there.
- [ ] **Device-matrix reliability (user call 2026-07-05).**  [workstreams/device-matrix-reliability.md](workstreams/device-matrix-reliability.md) — phases 1+2 SHIPPED same day (serial-mode CP transport hardware-proven on tinypico-cp; wifi 0.7.1 station-clear fix validated on tinys3-cp, demo sweep green).  Open: tinypico-cp CP-runtime corruption discriminator (swap the TinyPICO pair's runtimes), phase 3 many-volume drive-resolution hardening, phase 4 first-association grace, phase 5 `wifi.tx_power_dbm` knob (UM P4 mitigation), tinys3-cp on RF watch (P4-family, RSSI −82).
- [ ] **Re-evaluate Decision 0041 §8 — CP-rp2 TLS server ban — after CircuitPython 10.3.0 ships.**  Checked 2026-07-04: [adafruit/circuitpython#10339](https://github.com/adafruit/circuitpython/issues/10339) still open, milestone 10.3.0, latest release 10.3.0-alpha.3 (2026-06-24); bench pins 10.2.0.  Installing an alpha now was considered and skipped (user + agent call 2026-07-04): the fix hasn't landed in the alphas either, so an early flash would re-measure a known-unfixed substrate — the real gate is #10339 closing, not the release number.  When it does: re-bench `listen_tls` on Pico W CP; if the ban lifts, drop the platform check + update 0041 §8 + the `docs/guide.md` runtime-support table.
