# Next Up

> **Agent-managed file.  Single source of truth for the work queue.**  Each top-level bullet is capped at 5 bullet points (lead + sub-bullets, CHU011) — anything that needs more lives in a workstream file under [`workstreams/`](workstreams/), surfaced from here as a one-line pointer.  `## Done (recent)` is capped at 5 entries — drop the oldest when adding a new one.  Each Done entry: subject + commit hashes + headline result + workstream pointer; aim for under ~500 chars.  Detail lives in the commit message and the workstream doc.  Session warm-up: `git --no-pager log --oneline -20` then this file.

## Now

_(nothing in flight — see `## Next`)_

## Next

Independent items.  Most have either shipped phases (status in the linked workstream) or are unscoped placeholders waiting on a forcing function.

- [ ] **CI infrastructure workstream (unscoped — fires when CI is re-enabled)** — CI / GitHub Actions is disabled and has more structurally wrong with it than any single audit surfaced; it gets its own workstream rather than point-patches. Two concrete starting items inherited from audit-remediation P1 (analysis preserved in [`workstreams/audit-remediation-and-drift-mechanization.md`](workstreams/audit-remediation-and-drift-mechanization.md) Phase 1): (a) `ci.yml` lint job installs only `ruff`, so `run.py lint`'s `python -m chumicro_checks` can't run there — CHU enforcement never executes in CI; (b) `release.yml` publishes to immutable PyPI on a VERSION bump with `needs: detect` only, ungated by ci.yml success and before its own `validate-mip`. Neither is a safe isolated point-fix while CI is off (can't validate against a real run; the file is due for rework) — both wait for the dedicated pass.

- [ ] **Workspace library curation — CLOSED for active work; Phases 3 + 4 demand-gated** (Phases 1 + 2 complete + validated 2026-05-17) — `chumicro-workspace library` is the on-device library host (six verbs; per-dep deselect + Q4 remove/forget done).  Validated as far as offline allows (real `pip download` from a local index, full transitive walk).  **Phase 3** (non-chumicro upstreams) fires only when a real workspace asks for an Adafruit/mp lib via `library add`; **Phase 4** (`library run-example`/`test`) fires after a release cycle of real Phase 2 use — neither built (YAGNI by design, explicit triggers in the workstream).  One CI-gated residual (live-PyPI smoke test) tracked in [`open-questions.md`](open-questions.md).  workspace-template README (gap #4b) belongs to the "Workspace-template gaps" item, not here.  `mip`/`circup` remain supported.  Detail: [`workstreams/workspace-library-curation.md`](workstreams/workspace-library-curation.md).

- [ ] **Multi-board CP deploy + FSKit follow-ups — Item 5 deferred to next natural wedge** — Items 1, 2, 3, 4 + the doctor wrapper shipped 2026-05-09; Item 6 considered and rejected.  Item 5 (end-to-end recovery-command bench proof) needs a real FSKit wedge.  A 2026-05-09 attempt at deliberate triggers (alternating-board deploys, repeated wipe-test, full functional suite, host-side write+delete loops, concurrent diskutil probes) didn't reproduce — the wedge is intermittent enough that try-on-demand isn't reliable.  Validation rides on the next natural occurrence; checklist in the workstream's Item 5 section ("when doctor reports MACOS FSKIT ✗, run through these five steps").  Detail: [`workstreams/deploy-multi-board-and-fskit-followups.md`](workstreams/deploy-multi-board-and-fskit-followups.md).

- [ ] **Beginner-comfort onramp Phase 3+** — eight-step sequence + four 2026-05-06 papercuts all shipped; workstream archived 2026-05-10.  Phase 3+ unscoped; reopen when the next first-encounter friction surfaces from real users.  Detail: [`workstreams/archive/beginner-onramp.md`](workstreams/archive/beginner-onramp.md).

- [ ] **Workspace-template gaps — 1 remaining of 9** — gaps doc archived 2026-05-10 after 7 of 9 fixes shipped; gap #8 dropped 2026-05-12 (re-evaluated and reversed — see archive section #8 for the chicken-and-egg + code-bloat reasoning).  Still open: (gap #4b) regular-mode README update on the [workspace-template repo](https://github.com/ChuMicro/ChuMicro-Workspace-Template) documenting `chumicro-workspace install-libraries <project>` + a manual `circup` / `mip` fallback recipe (mono-repo half shipped — `install-libraries` CLI lands now).  Detail: [`workstreams/archive/workspace-template-dev-and-regular-mode-gaps.md`](workstreams/archive/workspace-template-dev-and-regular-mode-gaps.md).

- [ ] **`discover --register` multi-board sweep** — `add-device` (no `--address`) now drops into the same port-picker `bootstrap` already had: auto-uses the only port, prompts on multiple, errors under `--non-interactive` (resolved-by-design 2026-05-14, what was historically option (a)).  `bootstrap` collapsed to a thin shim over `add-device` + next-steps footer; `--demo` is opt-in everywhere (was opt-out via `--no-demo`).  Still deferred: `discover --register` doing the sweep as a side-effect (registering N boards in one run).  Hold until a 5+-board user complains.

- [ ] **4-board validation campaign — pre-existing findings + memory-test tuning** (surfaced 2026-05-16 by a full 4-board campaign: 148 example deploys 144✓/0✗/4 skip, functional on all 4, unit-sweep bake flash×3+RAM×1).  MP-copy fix (`e2898522`) comprehensively validated — Pico W MP + Lolin S2 MP **flash 3/3 STABLE each** (407/12/0), MP RAM stable; ~770+ campaign deploys, 0 deploy failures.  Findings, all pre-existing, none regressions:
  - ~~`*_memory_fragmentation` flap on ESP32-S2 CircuitPython PSRAM / slow + buggy~~ **RESOLVED 2026-05-16**: all 6 fragmentation suites (3 `tests/` + 3 `functional_tests/`) **deleted** — they were over-aggressive allocate-and-measure soak files (flapping PSRAM tolerances, unbounded allocate-until-OOM could wedge a board), not unit tests, and redundant for coverage (21/21 libs still meet the 94 % gate without them).  Deliberate heap/fragmentation measurement is properly the job of the planned **Performance + resource benchmarking infrastructure** item below; git history preserves the deleted logic as reference for it.  Removing them also drops the ~1000 s Lolin S2 CP sweep materially (those tests were 24–30 s each).
  - `websockets/test_drop_with_event_drains_oversize_and_stays_open` — deterministic on Lolin S2 (MP **and** CP), passes on Pico W: oversize frame reports length 600, test expects `(0, 400]`.  Real S2-specific websockets/buffer-chunking behaviour question, not env.
  - ~~CP **RAM-mode** is not viable for the full sweep on the 264 KB Pico W (cumulative inline-bootstrap `MemoryError` at `ntp` after ~6 libs)~~ **CHARACTERIZED 2026-05-17**: a *clean, recoverable* `MemoryError` at `ntp` (board healthy after; libs after `ntp` still pass — `runner` 59/0, `wifi` 39/0), **not** a hard fault. It is the Decision 0072 resident-ceiling class (the sweep's output, not a gate), not a `wifi`/CP-RAM defect — see deploy-mode-unification 4c (falsified) / 4d.
  - Functional network tests need a bench with `mosquitto` up + host/boards on one subnet (campaign bench had neither: host 192.168.10.x vs boards 172.16.1.x) — environmental, not a code defect.

- [ ] **Expand the device test matrix beyond ESP32-S2** now that transport tooling is proven on both MicroPython and CircuitPython.

- [ ] **Performance + resource benchmarking infrastructure** — measure heap + CPU per library operation with explicit GC control; per-benchmark thresholds that fail on regression; separate `bench` task or deeper test tier so slow benchmarks don't run on standard `test`; CI on a schedule.


- [ ] **Deploy-path unification — one mechanism puts code on a board** (opened 2026-05-18 from a full `ChuMicro-Workspace-Template` revalidation) — `deploy`/`deploy-example`/`install-libraries`/functional-test-staging are divergent paths for one operation; additive-vs-`--delete` is the core defect (circup/mip libs orphaned, not reconciled). Decisions captured (clean-slate default + `--no-wipe`; closed keep set `{boot_out.txt, boot.py, _chu_kv.msgpack}`; `settings.toml` never preserved; example = shim; `install-libraries` retired; one `libraries/` root). Phase 1 = two independent regressions surfaced here: `preflight` dies `AttributeError '_env'` at `cli/quality.py:104` (lint+test gate dead); `repl <project>` ships a broken `chumicro_timing` on-device (deterministic, board-dead) while `deploy` works. Transport audit done; invariant ADR (Phase 0) `proposed` next. Detail: [`workstreams/deploy-path-unification.md`](workstreams/deploy-path-unification.md).

- [ ] **Deploy walker fails on unresolved imports** (proposed 2026-05-12, separate session) — `workbench/deploy/src/chumicro_deploy/sources.py:367-378` silently skips imports that don't resolve against `search_paths`.  Result: a file containing `from chumicro_sockets import ...` can be deployed without `chumicro_sockets` in `library_sources`, ImportError surfaces only at first boot.  Today's DI cleanup (Done) removed the unconditional top-level imports that were riding on this silent-skip; the walker fix is the load-bearing correction so that any *future* unresolved import is a deploy-time failure with a clear message naming the importing file + the missing module.  Decision 0062's bench-validation table also needs a corrective rerun once landed.  Detail: [`workstreams/walker-unresolved-import-failure.md`](workstreams/walker-unresolved-import-failure.md).

## Investigations

- [ ] **Investigate slow MicroPython RAM-mode functional test runs** — observed during 2026-04-19 live PyCharm testing that MicroPython RAM-mode functional tests took noticeably longer than expected.  CircuitPython RAM-mode is fast in comparison.  Suspects: per-file `mpremote mount` cost, cold-start interpreter overhead, batch-vs-per-test trade-off.  Profile against the new batch-execute path.

## Done (recent)

> Capped at 5 entries — subject + commit hashes + headline result + workstream pointer; under ~500 chars each. Verbose detail lives in the commit message body or [`workstreams/<name>.md`](workstreams/) (per-phase acceptance).

- [x] **Markdown/plans whitespace drift mechanized (CHU002–004 scope + CHU018)** (2026-05-18, this commit) — root-caused the `next-up.md` trailing-newline pile-up: CHU002–005 already enforce one-final-newline / ≤2-blank / no-trailing-ws and scan `.md`, but `_iter_text_files` never reached `plans/` or `docs/` (only `scripts/` + `<pkg>/<src|tests|…>`). Widened `_SCAN_TOP_LEVELS` to add both; repo-root loose files deliberately excluded — gitignored `devices.yml`/`secrets.toml` would false-positive (walker reads fs, not git). New **CHU018** (LF-only, CR/CRLF banned) closes the one real gap — no CR rule existed; reads bytes since `Path.read_text` universal-newline-strips `\r\n` before any rule sees it. Cleaned the 3 surfaced violations (`plans/patterns.md`, `requirements-docs.txt` EOF; an archived workstream's blank-run). 9 new tests incl. scope (plans/docs in, root out). All whitespace rules report 0 on main. workbench/checks 0.8.0→0.9.0.

- [x] **`remove-device` + `reset-device`** (2026-05-18, `ce7fc6da`) — resolves open-question item-3 (devices.yml audit follow-up). New owner primitive `remove_device` (deletes entry + nulls every matching `defaults:` ref — covers the CP↔MP-reflash dangling default). Two `--yes`-gated workspace verbs: `remove-device` (delete, no board needed) and `reset-device` (re-probe connected board, replace the entry wholesale, keep id + `defaults` binding, drop user-owned drift — the *replace* counterpart to `add-device --force`'s *update*; probe failure hard-fails steering to remove/add). README command table + device-testing.md + guide.md updated lockstep (CHU014 parity). 14 new tests (owner round-trip/defaults-prune + CLI exit-code matrix). deploy 0.23.1→0.24.0, workspace 0.30.0→0.31.0. Preflight green.

- [x] **`init` retired — workspace creation is clone-only** (2026-05-18, `ed23721c`) — Decision 0075 (supersedes 0038 §3; the §3 mechanism-vs-principle slip is now the ADR-README worked cautionary case). Removed `chumicro-workspace init` end-to-end: `template_apply.init`/`_git_init`/`_strip_dot_git`/`_report_init_files`, `_cmd_init`, the subparser, `ApplyAction.WRITTEN`. Ownership zones collapsed 3→2 (`INIT_ONLY` was behaviorally identical to the `classify()` USER_OWNED default once `init` was gone). Fallout fixed: `setup.py`/`health.py` hints, AGENTS + `workbench`/`workspace` READMEs; tests rewritten (`_clone_template` replaces `init()`). Companion `ChuMicro-Workspace-Template` fixes (run.py bootstrap self-heal, `--device`, ruamel test) shipped in that repo. chumicro-workspace 810, deploy 937, lint green. Open follow-ups (preflight/repl) moved to `## Next`.

- [x] **devices.yml ownership cleanup** (2026-05-18, `a788c628`) — lifecycle audit. `setup_command` deleted end-to-end (zero consumers — dead config, not future surface): schema zone / `DeviceEntry` / loader / tests / docs. `serial_baudrate` kept (consumed by pytest-device + functional fixtures) but de-advertised as a USB-CDC no-op (dropped from device-testing.md field table + examples). Three workspace→deploy ownership leaks fixed: `_cmd_devices` uses deploy's validated `load_devices` (was raw `YAML(typ=safe)`); device-resolution duplication killed via new `chumicro_deploy.config.default.load_devices_yml_raw` + extracted `_resolve_raw_entry` (single owner of selection logic; `_find_devices_yml_entry_for_args` now a thin policy wrapper); `_resolve_all_devices` uses `list_device_ids` not hardcoded `raw["devices"]`. 11 other workspace YAML/TOML reads audited + confirmed workspace-owned. Reset-vs-`--force` + multi-device-add deferred to [`open-questions.md`](open-questions.md). Lint green, 1967 deploy+workspace+pytest-device tests, gated coverage 95%.

- [x] **audit-remediation Phase 4 — drift class mechanized (CHU014–CHU017)** (2026-05-18, `487d2216` `d7c75139` `d5ec8487` + this commit) — the load-bearing phase per Decision 0074: every Phase 3 hand-fix is now a deterministic lint, one rule per commit, each reporting 0 on main. **CHU014** workspace command-table parity (AST `add_parser` literals vs README table; surfaced + fixed a real escaped-pipe GFM bug while validating live). **CHU015** module-docstring "future work" vs shipped public symbols (clause-split FP guard). **CHU016** example imports resolve per declared runtime (closes the verify_examples platform-builtin skip gap). **CHU017** coverage-claim honesty — tractability reassessed per Decision 0074's judgement-adjacent flag and found tractable: closed prohibited-phrase set + mechanical negator/qualifier exemption, validated to NOT flag the corrected AGENTS/ADR text. Each ships `# noqa: CHU0NN`. README catalog brought current (was stale at CHU012). `workbench/checks` 0.4.1→0.8.0. Decision 0074 consequences + workstream Status reconciled (workstream effectively complete; only deferred CI-gated P1.2-3 remain). Preflight green throughout (4032→4063). Detail: [`workstreams/audit-remediation-and-drift-mechanization.md`](workstreams/audit-remediation-and-drift-mechanization.md).
