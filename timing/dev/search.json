{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"chumicro-timing","text":"<p>Cross-runtime millisecond tick helpers and periodic timing utilities for CircuitPython, MicroPython, and CPython.</p> <p>All timing is non-blocking — nothing in this library calls <code>time.sleep()</code>.</p>","path":["chumicro-timing"],"tags":[]},{"location":"#quick-example","level":2,"title":"Quick example","text":"<pre><code>from chumicro_timing import Heartbeat, ticks_ms\n\nheartbeat = Heartbeat(period_ms=1000)\n\nwhile True:\n    now = ticks_ms()\n    if heartbeat.poll(now):\n        print(\"one second elapsed\")\n    # ... do other work ...\n</code></pre>","path":["chumicro-timing"],"tags":[]},{"location":"#documentation","level":2,"title":"Documentation","text":"<ul> <li>User Guide — getting started, usage patterns, platform notes</li> <li>API Reference — full API documentation</li> <li>Testing Helpers — using <code>FakeTicks</code> in your tests</li> </ul> <p>← All ChuMicro Libraries</p> <p>Source · PyPI · Bundle · Experimental Bundle</p>","path":["chumicro-timing"],"tags":[]},{"location":"api/","level":1,"title":"API Reference","text":"","path":["API Reference"],"tags":[]},{"location":"api/#tick-functions","level":2,"title":"Tick Functions","text":"","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.ticks","level":2,"title":"<code>chumicro_timing.ticks</code>","text":"<p>Cross-runtime millisecond tick helpers.</p> <p>Provides wraparound-safe tick functions that work identically on CircuitPython, MicroPython, and CPython.  The API mirrors MicroPython's <code>time.ticks_ms</code> / <code>time.ticks_diff</code> / <code>time.ticks_add</code> contract with a fixed wrap period of 2**29 ms (~6.2 days).</p> <p>The 229 period keeps add/subtract results below 230, avoiding heap-allocated long integers on boards without big-int support.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.ticks--design-note","level":4,"title":"Design note","text":"<p>The wraparound-safe tick contract and the 2**29 period originate from MicroPython's <code>time</code> module and are also used by Adafruit's <code>adafruit_ticks</code> library (MIT-licensed).  This module is an independent implementation written from the mathematical specification of modular/ring arithmetic for tick counters.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.ticks.ticks_ms","level":3,"title":"<code>ticks_ms()</code>","text":"<p>Return a monotonic millisecond count in [0 .. 2**29 - 1].</p> <p>Values wrap every ~6.2 days.  Use <code>ticks_diff</code> and <code>ticks_add</code> for arithmetic; plain subtraction gives wrong results near the wrap boundary.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.ticks.ticks_add","level":3,"title":"<code>ticks_add(ticks, delta)</code>","text":"<p>Add delta milliseconds to a tick value, wrapping at 2**29.</p> <p>Parameters:</p> Name Type Description Default <code>ticks</code> <code>int</code> <p>Base tick value.</p> required <code>delta</code> <code>int</code> <p>Milliseconds to add.</p> required <p>Returns:</p> Name Type Description <code>result</code> <code>int</code> <p>Wrapped tick value.</p> <p>Raises:</p> Type Description <code>OverflowError</code> <p>If delta is outside (-228 .. 228).</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.ticks.ticks_diff","level":3,"title":"<code>ticks_diff(end, start)</code>","text":"<p>Signed difference end minus start with wraparound handling.</p> <p>Correct as long as end and start are no more than 2**28 ms (~3.1 days) apart.</p> <p>Parameters:</p> Name Type Description Default <code>end</code> <code>int</code> <p>Later tick value.</p> required <code>start</code> <code>int</code> <p>Earlier tick value.</p> required <p>Returns:</p> Name Type Description <code>result</code> <code>int</code> <p>Signed difference in milliseconds.</p>","path":["API Reference"],"tags":[]},{"location":"api/#heartbeat","level":2,"title":"Heartbeat","text":"<p>← Home</p> <p>Source · PyPI · Bundle · Experimental Bundle</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.heartbeat","level":2,"title":"<code>chumicro_timing.heartbeat</code>","text":"<p>Periodic heartbeat driven by cross-runtime tick helpers.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.heartbeat.Heartbeat","level":3,"title":"<code>Heartbeat</code>","text":"<p>Track whether a periodic heartbeat is due based on monotonic ticks.</p> <p>Pass a shared <code>now_ms</code> timestamp to <code>poll()</code> and <code>is_due()</code> on each loop iteration.  The timestamp should be captured once per loop and shared across all components to avoid time drift.</p> <p>By default, uses the module-level <code>ticks_diff</code> helper for wraparound-safe comparison.  Pass a ticks object with <code>ticks_ms</code> and <code>ticks_diff</code> methods to override (e.g. for tests).</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.heartbeat.Heartbeat.period_ms","level":4,"title":"<code>period_ms</code>  <code>property</code>","text":"<p>Return the configured heartbeat period in milliseconds.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.heartbeat.Heartbeat.__init__","level":4,"title":"<code>__init__(period_ms, ticks=None)</code>","text":"<p>Create a heartbeat that becomes due once every period_ms milliseconds.</p> <p>Parameters:</p> Name Type Description Default <code>period_ms</code> <code>int</code> <p>Interval between beats.</p> required <code>ticks</code> <code>object | None</code> <p>Optional tick source (must have <code>ticks_ms</code> and <code>ticks_diff</code> methods).  Defaults to the real clock. Constructor injection per Decision 0010; pass <code>FakeTicks</code> from <code>chumicro_timing.testing</code> in tests.</p> <code>None</code>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.heartbeat.Heartbeat.reset","level":4,"title":"<code>reset(now_ms)</code>","text":"<p>Reset the heartbeat schedule to start counting from now_ms.</p> <p>Parameters:</p> Name Type Description Default <code>now_ms</code> <code>int</code> <p>Current tick value.</p> required","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.heartbeat.Heartbeat.is_due","level":4,"title":"<code>is_due(now_ms)</code>","text":"<p>Return whether the heartbeat period has elapsed since the last beat.</p> <p>Parameters:</p> Name Type Description Default <code>now_ms</code> <code>int</code> <p>Current tick value.</p> required <p>Returns:</p> Name Type Description <code>result</code> <code>bool</code> <p><code>True</code> if the period has elapsed.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.heartbeat.Heartbeat.poll","level":4,"title":"<code>poll(now_ms)</code>","text":"<p>Return <code>True</code> once per elapsed period and advance the heartbeat state.</p> <p>Parameters:</p> Name Type Description Default <code>now_ms</code> <code>int</code> <p>Current tick value.</p> required <p>Returns:</p> Name Type Description <code>result</code> <code>bool</code> <p><code>True</code> if the period elapsed and the heartbeat advanced.</p>","path":["API Reference"],"tags":[]},{"location":"guide/","level":1,"title":"User Guide","text":"","path":["User Guide"],"tags":[]},{"location":"guide/#overview","level":2,"title":"Overview","text":"<p><code>chumicro-timing</code> provides two things:</p> <ol> <li>Tick helpers — <code>ticks_ms()</code>, <code>ticks_diff()</code>, and <code>ticks_add()</code> that handle counter wraparound correctly across all three Python runtimes.</li> <li>Heartbeat — a periodic timer that tells you when a time interval has elapsed, without blocking.</li> </ol> <p>These are the building blocks for non-blocking timing on microcontrollers. Instead of calling <code>time.sleep()</code> (which blocks everything), you capture a timestamp once per loop and check <code>heartbeat.poll(now)</code> for each component.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#getting-started","level":2,"title":"Getting started","text":"","path":["User Guide"],"tags":[]},{"location":"guide/#basic-heartbeat","level":3,"title":"Basic heartbeat","text":"<p>The most common pattern is a periodic action in a main loop:</p> <pre><code>from chumicro_timing import Heartbeat, ticks_ms\n\nled_heartbeat = Heartbeat(period_ms=500)\n\nwhile True:\n    now = ticks_ms()\n    if led_heartbeat.poll(now):\n        # This runs twice per second\n        toggle_led()\n</code></pre> <p><code>poll(now_ms)</code> returns <code>True</code> once per elapsed period and advances the internal timer. Calling it again with the same timestamp returns <code>False</code> until the next period elapses.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#shared-timestamps","level":3,"title":"Shared timestamps","text":"<p>Always capture <code>ticks_ms()</code> once per loop iteration and pass the same value to every component. This prevents drift between independent clock reads:</p> <pre><code>from chumicro_timing import Heartbeat, ticks_ms\n\nfast = Heartbeat(period_ms=100)   # 10 Hz\nslow = Heartbeat(period_ms=5000)  # every 5 seconds\n\nwhile True:\n    now = ticks_ms()  # ONE reading per iteration\n    if fast.poll(now):\n        read_sensor()\n    if slow.poll(now):\n        send_report()\n</code></pre> <p>On a slow microcontroller, calling <code>ticks_ms()</code> separately for each component would return slightly different values. A heartbeat that should fire at the same moment as another might not. Sharing the timestamp eliminates this class of bug.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#checking-without-consuming","level":3,"title":"Checking without consuming","text":"<p><code>is_due(now_ms)</code> tells you whether the period has elapsed without advancing the timer. This is useful when you need to check timing state without committing to an action:</p> <pre><code>now = ticks_ms()\nif heartbeat.is_due(now):\n    # Period has elapsed, but the timer hasn't been reset yet.\n    # Calling is_due(now) again will still return True.\n    pass\n</code></pre> <p>Call <code>poll(now)</code> when you're ready to consume the beat and start the next period.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#resetting","level":3,"title":"Resetting","text":"<p><code>reset(now_ms)</code> restarts the timer from the given timestamp:</p> <pre><code>now = ticks_ms()\nheartbeat.reset(now)\n# The next beat is now period_ms from this moment,\n# regardless of when the last beat was.\n</code></pre>","path":["User Guide"],"tags":[]},{"location":"guide/#using-ticks-directly","level":2,"title":"Using ticks directly","text":"<p>For custom timing logic that doesn't fit the heartbeat pattern, use the tick functions directly:</p> <pre><code>from chumicro_timing import ticks_ms, ticks_diff, ticks_add\n\n# Record a timestamp\nstart = ticks_ms()\n\n# ... do work ...\n\n# Check elapsed time (handles wraparound correctly)\nelapsed = ticks_diff(ticks_ms(), start)\n\n# Compute a deadline\ndeadline = ticks_add(start, 3000)  # 3 seconds from start\n</code></pre> <p>Important: Do not use plain subtraction (<code>end - start</code>) on tick values. The counter wraps every ~6.2 days, and plain subtraction gives wrong results near the boundary. Always use <code>ticks_diff()</code>.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#wraparound-details","level":2,"title":"Wraparound details","text":"<p>The tick counter uses a 2²⁹ ms period (~6.2 days). This keeps all arithmetic within small integers, avoiding heap-allocated big integers on boards without big-int support.</p> <p><code>ticks_diff()</code> is correct as long as the two timestamps are no more than ~3.1 days apart (half the period). For any practical embedded timing, this is more than sufficient.</p> <p><code>ticks_add()</code> rejects deltas at or beyond the half-period (±2²⁸ ms) with an <code>OverflowError</code>.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#platform-behavior","level":2,"title":"Platform behavior","text":"<p>The tick source is selected automatically at import time:</p> Priority Source Runtime 1 <code>supervisor.ticks_ms</code> CircuitPython 7+ 2 <code>time.ticks_ms</code> MicroPython, some CircuitPython builds 3 <code>time.monotonic_ns</code> CPython, some CircuitPython boards 4 <code>time.monotonic</code> Final fallback (float seconds → int ms) <p>All sources are masked to the 2²⁹ period, so behavior is identical regardless of which source is used.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#using-with-runner","level":2,"title":"Using with Runner","text":"<p>For applications with many components, <code>chumicro-runner</code> provides a <code>Runner</code> that captures the timestamp for you and services active components. See the chumicro-runner guide for details.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#integration-with-a-tick-based-scheduler","level":2,"title":"Integration with a tick-based scheduler","text":"<p><code>Heartbeat</code> is designed to be polled from a main loop or tick-based scheduler — it never blocks. A typical pattern:</p> <pre><code>from chumicro_timing import Heartbeat, ticks_ms\n\nheartbeat = Heartbeat(period_ms=1000)\n\ndef on_tick():\n    \"\"\"Called once per scheduler tick.\"\"\"\n    now = ticks_ms()\n    if heartbeat.poll(now):\n        do_periodic_work()\n</code></pre>","path":["User Guide"],"tags":[]},{"location":"guide/#examples","level":2,"title":"Examples","text":"<p>The examples directory contains complete runnable scripts:</p> Example What it shows <code>heartbeat_blink.py</code> Basic heartbeat in a main loop (the embedded hello world) <code>multiple_heartbeats.py</code> Several heartbeats at different rates sharing one timestamp <code>timeout_check.py</code> Using <code>ticks_diff()</code> for deadline-based timeout detection <code>debounce.py</code> Button debounce using <code>ticks_ms()</code> and <code>ticks_diff()</code> <code>periodic_tick.py</code> Manual periodic action — what <code>Heartbeat</code> does under the hood <code>circuitpython_blink.py</code> LED blink on CircuitPython hardware <code>circuitpython_debounce.py</code> Button debounce on CircuitPython hardware <code>micropython_blink.py</code> LED blink on MicroPython hardware <code>micropython_debounce.py</code> Button debounce on MicroPython hardware <p>Simulated examples run on CPython.  Hardware examples (<code>circuitpython_*</code> / <code>micropython_*</code>) require a real board — see the setup notes in each file.</p> <p>← Home</p> <p>Source · PyPI · Bundle · Experimental Bundle</p>","path":["User Guide"],"tags":[]},{"location":"testing/","level":1,"title":"Testing Helpers","text":"<p><code>chumicro_timing.testing</code> provides deterministic fakes for host-side tests.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#usage-with-heartbeat","level":2,"title":"Usage with Heartbeat","text":"<p>Pass a <code>FakeTicks</code> instance as the <code>ticks</code> parameter to <code>Heartbeat</code>. Then use <code>FakeTicks.ticks_ms()</code> to get timestamps for <code>poll()</code> and <code>FakeTicks.advance()</code> to move time forward:</p> <pre><code>from chumicro_timing import Heartbeat\nfrom chumicro_timing.testing import FakeTicks\n\ndef test_heartbeat_fires_after_period():\n    fake = FakeTicks()\n    heartbeat = Heartbeat(period_ms=100, ticks=fake)\n\n    now = fake.ticks_ms()\n    assert heartbeat.poll(now) is False\n\n    fake.advance(99)\n    now = fake.ticks_ms()\n    assert heartbeat.poll(now) is False\n\n    fake.advance(1)\n    now = fake.ticks_ms()\n    assert heartbeat.poll(now) is True\n    # Timer has been reset — next poll returns False\n    assert heartbeat.poll(now) is False\n</code></pre>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#usage-from-other-libraries","level":2,"title":"Usage from other libraries","text":"<p>Libraries that depend on <code>chumicro-timing</code> can import <code>FakeTicks</code> directly:</p> <pre><code># In another library's test file\nfrom chumicro_timing.testing import FakeTicks\n</code></pre> <p>This follows the project convention from Decision 0010: libraries that expose injectable services ship their own test fakes.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#api-reference","level":2,"title":"API Reference","text":"<p>← Home</p> <p>Source · PyPI · Bundle · Experimental Bundle</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_timing.testing","level":2,"title":"<code>chumicro_timing.testing</code>","text":"<p>Test helpers for libraries that depend on chumicro-timing.</p> <p>Provides deterministic fakes that replace the real tick functions, allowing host-side tests to control time without wall-clock waits.</p> <p>Ships with the library per Decision 0010 so downstream consumers import ready-made fakes rather than inventing ad-hoc mocks.</p> <p>Usage from any library's tests::</p> <pre><code>from chumicro_timing.testing import FakeTicks\n\nfake = FakeTicks()\nheartbeat = Heartbeat(period_ms=100, ticks=fake)\nfake.advance(100)\nassert heartbeat.poll(fake.ticks_ms()) is True\n</code></pre>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_timing.testing.FakeTicks","level":3,"title":"<code>FakeTicks</code>","text":"<p>Deterministic tick source for host-side tests.</p> <p>Replaces the real <code>ticks_ms</code> / <code>ticks_diff</code> contract with values that only move when <code>advance()</code> is called explicitly.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_timing.testing.FakeTicks.__init__","level":4,"title":"<code>__init__(start_ms=0)</code>","text":"<p>Create a fake tick source starting at start_ms.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_timing.testing.FakeTicks.advance","level":4,"title":"<code>advance(amount_ms)</code>","text":"<p>Move the clock forward by amount_ms milliseconds.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_timing.testing.FakeTicks.ticks_ms","level":4,"title":"<code>ticks_ms()</code>","text":"<p>Return the current fake tick value.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_timing.testing.FakeTicks.ticks_diff","level":4,"title":"<code>ticks_diff(end, start)</code>","text":"<p>Return the signed difference end − start.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_timing.testing.FakeTicks.ticks_add","level":4,"title":"<code>ticks_add(ticks_val, delta)</code>","text":"<p>Add delta milliseconds to a tick value.</p>","path":["Testing Helpers"],"tags":[]}]}