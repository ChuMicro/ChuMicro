{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"chumicro-timing","text":"<p>Cross-runtime millisecond tick helpers and periodic timing for CircuitPython, MicroPython, and CPython.</p> <p>All timing is non-blocking — nothing in this library calls <code>time.sleep()</code>.</p>","path":["chumicro-timing"],"tags":[]},{"location":"#quick-example","level":2,"title":"Quick example","text":"<pre><code>from chumicro_timing import Rate, ticks_ms\n\nrate = Rate(1000, ticks_ms())\n\nwhile True:\n    now = ticks_ms()\n    if rate.due(now):\n        print(\"one second elapsed\")\n    # ... do other work ...\n</code></pre> <p>The value objects — <code>Deadline</code> (a single armed timeout) and <code>Rate</code> (drift-free periodic cadence) — build on the wrap-safe tick helpers.</p>","path":["chumicro-timing"],"tags":[]},{"location":"#documentation","level":2,"title":"Documentation","text":"<ul> <li>User Guide — getting started, usage patterns, platform notes</li> <li>API Reference — full API documentation</li> <li>Testing Helpers — using <code>FakeTicks</code> in your tests</li> </ul> <p>← All ChuMicro Libraries</p> <p>Source · \\ PyPI · \\ Bundle · \\ Experimental Bundle</p>","path":["chumicro-timing"],"tags":[]},{"location":"api/","level":1,"title":"API Reference","text":"","path":["API Reference"],"tags":[]},{"location":"api/#tick-functions","level":2,"title":"Tick Functions","text":"","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.ticks","level":2,"title":"<code>chumicro_timing.ticks</code>","text":"<p>Cross-runtime wrap-safe millisecond ticks and signed-difference math; values wrap every ~6.2 days.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.ticks.ticks_ms","level":3,"title":"<code>ticks_ms()</code>","text":"<p>Returns the current tick in <code>[0, TICKS_MAX]</code>; compare with <code>ticks_diff</code>, not subtraction.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.ticks.ticks_add","level":3,"title":"<code>ticks_add(ticks, delta)</code>","text":"<p>Returns <code>ticks</code> advanced by <code>delta</code> within the wrap-safe range.</p> <p>Raises:</p> Type Description <code>OverflowError</code> <p>When <code>delta</code> reaches half a period (~3.1 days) in either direction.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.ticks.ticks_diff","level":3,"title":"<code>ticks_diff(end, start)</code>","text":"<p>Returns the signed millisecond distance from <code>start</code> to <code>end</code>.</p> <p>Returns:</p> Type Description <code>int</code> <p>Signed value in <code>[-TICKS_HALFPERIOD, TICKS_HALFPERIOD)</code>; gaps over ~3.1 days</p> <code>int</code> <p>(half the wrap period) alias to the wrong sign.</p>","path":["API Reference"],"tags":[]},{"location":"api/#value-objects","level":2,"title":"Value Objects","text":"<p><code>Deadline</code> (a single armed timeout) and <code>Rate</code> (a drift-free periodic cadence).</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.deadline","level":2,"title":"<code>chumicro_timing.deadline</code>","text":"<p>Value objects built on the tick functions: <code>Deadline</code> and <code>Rate</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.deadline.Deadline","level":3,"title":"<code>Deadline</code>","text":"<p>A single armed deadline: <code>period_ms</code> from the <code>now_ms</code> it was built at.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.deadline.Deadline.expired","level":4,"title":"<code>expired(now_ms)</code>","text":"<p>Return <code>True</code> once <code>now_ms</code> has reached or passed the due tick.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.deadline.Deadline.remaining","level":4,"title":"<code>remaining(now_ms)</code>","text":"<p>Return milliseconds until due, clamped at 0 once past the due tick.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.deadline.Deadline.reset","level":4,"title":"<code>reset(now_ms)</code>","text":"<p>Re-arm the same <code>period_ms</code> from <code>now_ms</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.deadline.Rate","level":3,"title":"<code>Rate</code>","text":"<p>Fires at most once per <code>period_ms</code>, drift-free and phase-aligned.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.deadline.Rate.due","level":4,"title":"<code>due(now_ms)</code>","text":"<p>Return <code>True</code> once the period has elapsed, re-phasing to the next fire.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.deadline.Rate.reset","level":4,"title":"<code>reset(now_ms)</code>","text":"<p>Re-anchor the cadence so the next fire lands one <code>period_ms</code> after <code>now_ms</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#wait-vocabulary","level":2,"title":"Wait vocabulary","text":"<p><code>Signal</code> and <code>wait_for</code> — the opt-in completion-wait vocabulary for generator flows. Import explicitly from <code>chumicro_timing.waits</code>.</p> <p>← Home</p> <p>Source · PyPI · Bundle · Experimental Bundle</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.waits","level":2,"title":"<code>chumicro_timing.waits</code>","text":"<p>Completion-wait vocabulary: <code>Signal</code> and <code>wait_for</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.waits.Signal","level":3,"title":"<code>Signal</code>","text":"<p>One-slot completion token connecting callback-style services to generator tasks.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.waits.Signal.next_deadline","level":4,"title":"<code>next_deadline(now_ms)</code>","text":"<p>Absolute tick deadline gating the wait, or <code>None</code> for indefinite.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.waits.Signal.set","level":4,"title":"<code>set(value=None)</code>","text":"<p>Complete the signal, storing value for the waiting generator.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.waits.Signal.clear","level":4,"title":"<code>clear()</code>","text":"<p>Re-arm for reuse: drop the stored value and the set flag.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.waits.Signal.ready","level":4,"title":"<code>ready(now_ms)</code>","text":"<p>Return whether the waiting generator should resume: <code>True</code> once set.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_timing.waits.wait_for","level":3,"title":"<code>wait_for(signal, *, deadline_ms=None)</code>","text":"<p>Suspend until signal is set; return the value it carries.</p> <p>Parameters:</p> Name Type Description Default <code>signal</code> <code>Signal</code> <p>Signal to wait on; not cleared on return.</p> required <code>deadline_ms</code> <code>int | None</code> <p>Absolute <code>ticks_ms</code> deadline, or <code>None</code> to wait indefinitely.</p> <code>None</code> <p>Yields:</p> Type Description <code>object</code> <p>signal itself on each suspension.</p> <p>Returns:</p> Type Description <code>object</code> <p>The value passed to <code>signal.set</code>.</p> <p>Raises:</p> Type Description <code>OSError</code> <p><code>ETIMEDOUT</code> when deadline_ms elapses before the signal is set.</p>","path":["API Reference"],"tags":[]},{"location":"guide/","level":1,"title":"User Guide","text":"","path":["User Guide"],"tags":[]},{"location":"guide/#overview","level":2,"title":"Overview","text":"<p><code>chumicro-timing</code> provides two things:</p> <ol> <li>Tick helpers — <code>ticks_ms()</code>, <code>ticks_diff()</code>, and <code>ticks_add()</code> that handle counter wraparound correctly across all three Python runtimes.</li> <li>Value objects built on those helpers:<ul> <li><code>Deadline</code> — a single armed timeout: check <code>expired(now)</code> / <code>remaining(now)</code>, <code>reset(now)</code> to re-arm.</li> <li><code>Rate</code> — a drift-free periodic cadence (the replacement for the old <code>Heartbeat</code>): <code>due(now)</code> returns <code>True</code> at most once per period.</li> </ul> </li> </ol> <p>These are the building blocks for non-blocking timing on microcontrollers. Instead of calling <code>time.sleep()</code> (which blocks everything), you capture a timestamp once per loop and hand it to each timer.</p> <p>For generator-based flows that suspend until a completion event, the opt-in <code>chumicro_timing.waits</code> submodule adds <code>Signal</code> and <code>wait_for</code> — the completion-wait vocabulary. Import it explicitly: <code>from chumicro_timing.waits import Signal, wait_for</code>.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#getting-started","level":2,"title":"Getting started","text":"","path":["User Guide"],"tags":[]},{"location":"guide/#basic-periodic-cadence","level":3,"title":"Basic periodic cadence","text":"<p>The most common pattern is a periodic action in a main loop:</p> <pre><code>from chumicro_timing import Rate, ticks_ms\n\nled_rate = Rate(500, ticks_ms())\n\nwhile True:\n    now = ticks_ms()\n    if led_rate.due(now):\n        # This runs twice per second\n        toggle_led()\n</code></pre> <p><code>Rate(period_ms, now_ms)</code> takes the current time at construction so it can phase-align its schedule. <code>due(now_ms)</code> returns <code>True</code> at most once per elapsed period and advances the internal schedule. Calling it again with the same timestamp returns <code>False</code> until the next period elapses.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#shared-timestamps","level":3,"title":"Shared timestamps","text":"<p>Always capture <code>ticks_ms()</code> once per loop iteration and pass the same value to every component. This prevents drift between independent clock reads:</p> <pre><code>from chumicro_timing import Rate, ticks_ms\n\nnow = ticks_ms()\nfast = Rate(100, now)   # 10 Hz\nslow = Rate(5000, now)  # every 5 seconds\n\nwhile True:\n    now = ticks_ms()  # ONE reading per iteration\n    if fast.due(now):\n        read_sensor()\n    if slow.due(now):\n        send_report()\n</code></pre> <p>On a slow microcontroller, calling <code>ticks_ms()</code> separately for each component would return slightly different values. A timer that should fire at the same moment as another might not. Sharing the timestamp eliminates this class of bug.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#resetting","level":3,"title":"Resetting","text":"<p><code>reset(now_ms)</code> restarts the cadence from the given timestamp:</p> <pre><code>now = ticks_ms()\nled_rate.reset(now)\n# The next fire is now period_ms from this moment,\n# regardless of when the last fire was.\n</code></pre>","path":["User Guide"],"tags":[]},{"location":"guide/#behavior-under-late-polls","level":3,"title":"Behavior under late polls","text":"<p><code>Rate</code> is drift-free / phase-aligned. When the loop runs late, <code>due(now)</code> fires and advances the scheduled tick by whole periods — it does not re-anchor to <code>now</code>. A 200 ms cadence keeps landing on 200, 400, 600 … even when individual polls arrive a few milliseconds late, and that jitter never accumulates. If the loop stalls for longer than one period, the missed fires are skipped rather than replayed back-to-back: <code>due(now)</code> returns <code>True</code> once and the schedule jumps forward to the next period boundary after <code>now</code>.</p> <p>This is the opposite of the old <code>Heartbeat</code>, which re-anchored the next deadline to <code>now</code> on every fire and so drifted forward by each loop's late-arrival cost. <code>Rate</code> gives you the phase-locked behavior by default. If you want to see those mechanics spelled out by hand, <code>examples/phase_locked_tick.py</code> carries the deadline manually — that is exactly what <code>Rate</code> does for you internally.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#deadlines","level":2,"title":"Deadlines","text":"<p>A <code>Deadline</code> is a single armed timeout. Arm it with the current time, then poll it:</p> <pre><code>from chumicro_timing import Deadline, ticks_ms\n\ndeadline = Deadline(500, ticks_ms())  # due 500 ms from now\n\nwhile not deadline.expired(ticks_ms()):\n    if sensor_ready():\n        break\n    do_other_work()\n\nleft = deadline.remaining(ticks_ms())  # ms until due, clamped at 0\n</code></pre> <p><code>expired(now)</code> reports whether the deadline has passed; <code>remaining(now)</code> returns the milliseconds left (clamped at <code>0</code>). <code>reset(now)</code> re-arms the same period from a new moment.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#choosing-a-wait","level":2,"title":"Choosing a wait","text":"<p>Every ChuMicro wait answers one question.  Pick by the question, not the type.</p> You want Reach for Lives in \"run this every N ms\" <code>Rate</code> <code>chumicro_timing</code> \"give up after N ms\" <code>Deadline</code> <code>chumicro_timing</code> \"a flag one place sets, another awaits\" <code>Signal</code> + <code>wait_for</code> <code>chumicro_timing</code> \"pause this generator until the socket is readable/writable\" <code>ReadWait</code> / <code>WriteWait</code> <code>chumicro_sockets</code> \"tell the runner when my service next needs the CPU\" <code>next_deadline</code> / <code>io_interest</code> on your service the runner service contract \"block the loop until a task finishes, with a timeout\" <code>runner.run_until</code> <code>chumicro_runner</code> <p><code>Signal</code> and <code>wait_for</code> live in the opt-in <code>chumicro_timing.waits</code> submodule; <code>ReadWait</code> and <code>WriteWait</code> in <code>chumicro_sockets.waits</code>.  A service reports <code>next_deadline</code> and <code>io_interest</code> as methods the runner reads each tick, not something you import.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#using-ticks-directly","level":2,"title":"Using ticks directly","text":"<p>For custom timing logic, you can use the tick functions directly:</p> <pre><code>from chumicro_timing import ticks_ms, ticks_diff, ticks_add\n\n# Record a timestamp\nstart = ticks_ms()\n\n# ... do work ...\n\n# Check elapsed time (handles wraparound correctly)\nelapsed = ticks_diff(ticks_ms(), start)\n\n# Compute a deadline\ndeadline = ticks_add(start, 3000)  # 3 seconds from start\n</code></pre> <p>Important: Do not use plain subtraction (<code>end - start</code>) on tick values, and never compute a deadline as <code>now + delta</code>. The counter wraps every ~6.2 days, and plain arithmetic gives wrong results near the boundary. Use <code>ticks_diff()</code> to measure elapsed time and <code>ticks_add()</code> to offset a timestamp.</p> <p>The safe default is to reach for <code>Deadline</code> rather than arm a timeout by hand. <code>Deadline(timeout_ms, ticks_ms())</code> arms via <code>ticks_add</code> internally, so the <code>now + delta</code> footgun is simply unrepresentable — you never write the wrapping arithmetic yourself. Keep <code>ticks_add</code> / <code>ticks_diff</code> for the cases where you genuinely need the raw tick values.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#wraparound-details","level":2,"title":"Wraparound details","text":"<p>The tick counter uses a 2²⁹ ms period (~6.2 days). This keeps all arithmetic within small integers, avoiding heap-allocated big integers on boards without big-int support.</p> <p><code>ticks_diff()</code> is correct as long as the two timestamps are no more than ~3.1 days apart (half the period). For any practical embedded timing, this is more than sufficient.</p> <p><code>ticks_add()</code> rejects deltas at or beyond the half-period (±2²⁸ ms) with an <code>OverflowError</code>.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#platform-notes","level":2,"title":"Platform notes","text":"<p>The tick source is selected automatically at import time:</p> Priority Source Runtime 1 <code>supervisor.ticks_ms</code> CircuitPython 7+ 2 <code>time.ticks_ms</code> MicroPython, some CircuitPython builds 3 <code>time.monotonic_ns</code> CPython, some CircuitPython boards 4 <code>time.monotonic</code> Final fallback (float seconds → int ms) <p>All sources are masked to the 2²⁹ period, so behavior is identical regardless of which source is used.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#using-with-chumicro-runner","level":2,"title":"Using with chumicro-runner","text":"<p><code>Rate</code> is designed to be polled from a main loop or tick-based scheduler — it never blocks. A typical pattern:</p> <pre><code>from chumicro_timing import Rate, ticks_ms\n\nrate = Rate(1000, ticks_ms())\n\ndef on_tick() -&gt; None:\n    \"\"\"Called once per scheduler tick.\"\"\"\n    now = ticks_ms()\n    if rate.due(now):\n        do_periodic_work()\n</code></pre> <p>For applications with many components, <code>chumicro-runner</code> captures the timestamp once per tick and dispatches it to every registered service.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#examples","level":2,"title":"Examples","text":"<p>The examples directory contains complete runnable scripts:</p> Example What it shows <code>heartbeat_blink.py</code> Basic periodic Rate in a main loop (the embedded hello world) <code>multiple_heartbeats.py</code> Several Rate timers at different rates sharing one timestamp <code>timeout_check.py</code> Using <code>ticks_diff()</code> for deadline-based timeout detection <code>debounce.py</code> Button debounce using <code>ticks_ms()</code> and <code>ticks_diff()</code> <code>periodic_tick.py</code> Manual periodic action — the same logic <code>Rate</code> wraps <code>phase_locked_tick.py</code> Drift-free deadline carrier by hand — the phase-locked schedule <code>Rate</code> provides built-in <code>circuitpython_blink.py</code> LED blink on CircuitPython hardware <code>circuitpython_debounce.py</code> Button debounce on CircuitPython hardware <code>micropython_blink.py</code> LED blink on MicroPython hardware <code>micropython_debounce.py</code> Button debounce on MicroPython hardware <p>Simulated examples run on CPython.  Hardware examples (<code>circuitpython_*</code> / <code>micropython_*</code>) require a real board — see the setup notes in each file.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#sensor-timeouts-timeout_checkpy","level":3,"title":"Sensor timeouts (<code>timeout_check.py</code>)","text":"<p>Shows <code>ticks_add</code> for computing an absolute deadline and <code>ticks_diff</code> for checking it.  A <code>wait_for_sensor(timeout_ms)</code> helper polls a simulated sensor until it reads ready or the deadline expires, returning the elapsed time on success or <code>-1</code> on timeout.  The deadline is computed once with <code>ticks_add(start, timeout_ms)</code>; each iteration tests <code>ticks_diff(now, deadline) &lt; 0</code> to decide whether to keep polling.</p> <p>The same pattern generalises to any \"fail after N ms\" check — handshake completion, status-register polling, button-hold detection.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#button-debounce-debouncepy","level":3,"title":"Button debounce (<code>debounce.py</code>)","text":"<p>Shows <code>ticks_ms</code> and <code>ticks_diff</code> for the classic debounce pattern.  A raw button signal bounces for a few ms after each press; the debouncer records the timestamp of the last accepted transition and rejects further changes until a quiet period (<code>DEBOUNCE_MS</code>) has elapsed.</p> <p>The same shape applies any time you need to suppress rapid signal changes — button presses, sensor readings near a threshold, motion-detector pulses.</p> <p>← Home</p> <p>Source · PyPI · Bundle · Experimental Bundle</p>","path":["User Guide"],"tags":[]},{"location":"testing/","level":1,"title":"Testing Helpers","text":"<p><code>chumicro_timing.testing</code> provides deterministic fakes for host-side tests.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#usage-with-rate-and-deadline","level":2,"title":"Usage with Rate and Deadline","text":"<p>The value objects take the current time as an explicit <code>now_ms</code> argument, so there is nothing to inject — hand them <code>FakeTicks.ticks_ms()</code> at construction and on each poll, and use <code>FakeTicks.advance()</code> to move time forward:</p> <pre><code>from chumicro_timing import Rate\nfrom chumicro_timing.testing import FakeTicks\n\ndef test_rate_fires_after_period() -&gt; None:\n    \"\"\"Rate fires exactly when the period elapses.\"\"\"\n    fake = FakeTicks()\n    rate = Rate(100, fake.ticks_ms())\n\n    assert rate.due(fake.ticks_ms()) is False\n\n    fake.advance(99)\n    assert rate.due(fake.ticks_ms()) is False\n\n    fake.advance(1)\n    assert rate.due(fake.ticks_ms()) is True\n    # The schedule has advanced — polling again at the same time returns False\n    assert rate.due(fake.ticks_ms()) is False\n</code></pre> <p>A <code>Deadline</code> is driven the same way:</p> <pre><code>from chumicro_timing import Deadline\nfrom chumicro_timing.testing import FakeTicks\n\ndef test_deadline_expires() -&gt; None:\n    \"\"\"Deadline expires once the timeout elapses.\"\"\"\n    fake = FakeTicks()\n    deadline = Deadline(100, fake.ticks_ms())\n\n    assert deadline.expired(fake.ticks_ms()) is False\n    assert deadline.remaining(fake.ticks_ms()) == 100\n\n    fake.advance(100)\n    assert deadline.expired(fake.ticks_ms()) is True\n    assert deadline.remaining(fake.ticks_ms()) == 0\n</code></pre>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#usage-from-other-libraries","level":2,"title":"Usage from other libraries","text":"<p>Libraries that depend on <code>chumicro-timing</code> can import <code>FakeTicks</code> directly:</p> <pre><code># In another library's test file\nfrom chumicro_timing.testing import FakeTicks\n</code></pre> <p>Project convention: libraries that expose injectable services ship their own test fakes alongside the production code.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#api-reference","level":2,"title":"API Reference","text":"<p>← Home</p> <p>Source · PyPI · Bundle · Experimental Bundle</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_timing.testing","level":2,"title":"<code>chumicro_timing.testing</code>","text":"<p>Test-support helpers: a <code>sleep_ms</code> shim and a hand-driven <code>FakeTicks</code>.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_timing.testing.FakeTicks","level":3,"title":"<code>FakeTicks</code>","text":"<p>Hand-driven ticks source; <code>ticks_ms()</code> only changes when <code>advance()</code> is called.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_timing.testing.FakeTicks.advance","level":4,"title":"<code>advance(amount_ms)</code>","text":"<p>Bumps the current reading by <code>amount_ms</code>.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_timing.testing.FakeTicks.sleep_ms","level":4,"title":"<code>sleep_ms(duration_ms)</code>","text":"<p>Advance the fake clock by <code>duration_ms</code> instead of sleeping for real.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_timing.testing.FakeTicks.ticks_ms","level":4,"title":"<code>ticks_ms()</code>","text":"<p>Returns the current reading masked into the wrap-safe 29-bit range.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_timing.testing.FakeTicks.ticks_diff","level":4,"title":"<code>ticks_diff(end, start)</code>","text":"<p>Returns the signed wrap-safe difference <code>end - start</code>.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_timing.testing.FakeTicks.ticks_add","level":4,"title":"<code>ticks_add(ticks_val, delta)</code>","text":"<p>Returns <code>ticks_val + delta</code> folded into the wrap-safe range.</p>","path":["Testing Helpers"],"tags":[]}]}