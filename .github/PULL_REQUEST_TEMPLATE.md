## Summary

<!-- What does this PR do and why? One or two sentences.
     Link issues if applicable (e.g., "Fixes #42"). -->

## Changes

<!-- List the key changes. Group by library if multiple are affected. -->

## How to verify

<!-- How can a reviewer confirm this works? Examples:
     - "Run `python scripts/run.py test --libraries timing` and see X"
     - "Look at the diff: docs-only change"
     Keep it concrete. -->

## Device testing

N/A <!-- When your PR changes library source code that interacts with
         hardware, run `python scripts/run.py test-libraries-functional` (with any flags
         you needed) and paste the "PR summary" block it prints at the end
         here. That block already includes the exact command, per-device
         board ID, runtime, address, and pass/fail counts.

         If you don't have a device, say so and a maintainer can help test. -->

## Version impact

N/A <!-- Replace with the bump type (patch/minor/major) and affected
         libraries if your PR changes files under libraries/*/src/. -->

## Breaking changes

None <!-- If this PR removes or renames public API, describe the change
          and confirm the VERSION bump is major (or minor for 0.x). -->
