{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"chumicro-logging","text":"<p>Leveled logging for ChuMicro libraries — non-blocking, zero dependencies.</p> <p>Familiar stdlib-logging shape (level integers, named loggers, attached handlers) plus a <code>BufferedHandler</code> that drains records off the hot tick path on each runner tick.</p>","path":["chumicro-logging"],"tags":[]},{"location":"#quick-example","level":2,"title":"Quick example","text":"<pre><code>from chumicro_logging import INFO, Logger, StreamHandler\n\nlogger = Logger(\"boot\", level=INFO, handlers=[StreamHandler()])\nlogger.info(\"hello\")\n</code></pre>","path":["chumicro-logging"],"tags":[]},{"location":"#documentation","level":2,"title":"Documentation","text":"<ul> <li>User Guide — getting started and usage patterns</li> <li>API Reference — full API documentation</li> <li>Testing Helpers — fakes for downstream test suites</li> </ul> <p>← All ChuMicro Libraries</p> <p>Source · \\ PyPI · \\ Bundle · \\ Experimental Bundle</p>","path":["chumicro-logging"],"tags":[]},{"location":"api/","level":1,"title":"API Reference","text":"<p>← Home</p> <p>Source · \\ PyPI · \\ Bundle · \\ Experimental Bundle</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging","level":2,"title":"<code>chumicro_logging</code>","text":"<p>Leveled logging for chumicro libraries, with no chumicro dependencies.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.BufferedHandler","level":3,"title":"<code>BufferedHandler</code>","text":"<p>Runner-shaped handler that buffers records and flushes on <code>handle</code>.</p> <p>Parameters:</p> Name Type Description Default <code>downstream</code> <code>object</code> <p>A handler exposing <code>emit(level, name, message)</code>, typically a <code>StreamHandler</code>.</p> required <code>capacity</code> <code>int</code> <p>Maximum buffered records. Must be &gt;= 1.</p> <code>32</code> <code>level</code> <code>int</code> <p>Minimum level buffered. Defaults to <code>DEBUG</code>.</p> <code>DEBUG</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If capacity &lt; 1.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.BufferedHandler.buffered","level":4,"title":"<code>buffered</code>  <code>property</code>","text":"<p>Records currently buffered, awaiting flush.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.BufferedHandler.capacity","level":4,"title":"<code>capacity</code>  <code>property</code>","text":"<p>Maximum buffered records; read-only.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.BufferedHandler.emit","level":4,"title":"<code>emit(level, name, message)</code>","text":"<p>Buffer the record, dropping the oldest if full.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.BufferedHandler.check","level":4,"title":"<code>check(now_ms)</code>","text":"<p>Return <code>True</code> when the buffer has records to flush.</p> <p>Parameters:</p> Name Type Description Default <code>now_ms</code> <code>int</code> <p>Current tick value; unused but required by the runner contract.</p> required","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.BufferedHandler.handle","level":4,"title":"<code>handle(now_ms)</code>","text":"<p>Drain the buffer to the downstream handler and return the count flushed.</p> <p>Parameters:</p> Name Type Description Default <code>now_ms</code> <code>int</code> <p>Current tick value; unused but required by the runner contract.</p> required","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.Logger","level":3,"title":"<code>Logger</code>","text":"<p>A named logger with a level threshold and a list of handlers.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Logger name; appears in formatted records.</p> required <code>level</code> <code>int</code> <p>Minimum level emitted. Defaults to <code>INFO</code>.</p> <code>INFO</code> <code>handlers</code> <code>list | None</code> <p>Initial handlers; copied into an internal list.</p> <code>None</code>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.Logger.handlers","level":4,"title":"<code>handlers</code>  <code>property</code>","text":"<p>Snapshot of attached handlers as a tuple.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.Logger.add_handler","level":4,"title":"<code>add_handler(handler)</code>","text":"<p>Attach a handler, unless it is already attached.</p> <p>Parameters:</p> Name Type Description Default <code>handler</code> <code>object</code> <p>An object exposing <code>emit(level, name, message)</code>.</p> required","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.Logger.remove_handler","level":4,"title":"<code>remove_handler(handler)</code>","text":"<p>Detach a handler if it is currently attached.</p> <p>Parameters:</p> Name Type Description Default <code>handler</code> <code>object</code> <p>A previously attached handler.</p> required","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.Logger.is_enabled","level":4,"title":"<code>is_enabled(level)</code>","text":"<p>Return <code>True</code> if level would be emitted by this logger.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.Logger.log","level":4,"title":"<code>log(level, message, *args)</code>","text":"<p>Emit message at level to every attached handler.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.Logger.debug","level":4,"title":"<code>debug(message, *args)</code>","text":"<p>Emit at <code>DEBUG</code>, interpolating <code>message % args</code> past the gate.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.Logger.info","level":4,"title":"<code>info(message, *args)</code>","text":"<p>Emit at <code>INFO</code>, interpolating <code>message % args</code> past the gate.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.Logger.warning","level":4,"title":"<code>warning(message, *args)</code>","text":"<p>Emit at <code>WARNING</code>, interpolating <code>message % args</code> past the gate.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.Logger.error","level":4,"title":"<code>error(message, *args)</code>","text":"<p>Emit at <code>ERROR</code>, interpolating <code>message % args</code> past the gate.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.Logger.critical","level":4,"title":"<code>critical(message, *args)</code>","text":"<p>Emit at <code>CRITICAL</code>, interpolating <code>message % args</code> past the gate.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.StreamHandler","level":3,"title":"<code>StreamHandler</code>","text":"<p>Synchronous handler writing formatted records to a writable stream.</p> <p>Parameters:</p> Name Type Description Default <code>stream</code> <code>object | None</code> <p>A writable stream. Defaults to <code>sys.stdout</code>.</p> <code>None</code> <code>level</code> <code>int</code> <p>Minimum level emitted. Defaults to <code>DEBUG</code>.</p> <code>DEBUG</code> <code>formatter</code> <code>object | None</code> <p>Renders <code>(level, name, message)</code> to a string; defaults to <code>default_formatter</code>.</p> <code>None</code>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.StreamHandler.emit","level":4,"title":"<code>emit(level, name, message)</code>","text":"<p>Format the record and write it to the stream.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.default_formatter","level":3,"title":"<code>default_formatter(level, name, message)</code>","text":"<p>Render a record as <code>LEVEL:name:message</code> on a single line.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_logging.level_name","level":3,"title":"<code>level_name(level)</code>","text":"<p>Return the human name for level, or <code>\"LEVEL&lt;n&gt;\"</code> if unknown.</p>","path":["API Reference"],"tags":[]},{"location":"guide/","level":1,"title":"User Guide","text":"","path":["User Guide"],"tags":[]},{"location":"guide/#overview","level":2,"title":"Overview","text":"<p>Status: parked.  chumicro-logging works as documented and stays published, but it is not under active development and no other ChuMicro library integrates with it.  Adopt it as a standalone buffered logger, or skip it.</p> <p><code>chumicro-logging</code> is a small leveled logger that runs identically on CircuitPython, MicroPython, and CPython.  It exposes the familiar stdlib-<code>logging</code> shape (level integers, named loggers, attached handlers) without depending on any other chumicro library, and adds a runner-shaped <code>BufferedHandler</code> so log emission can be deferred off the hot tick path.</p> <p>The two key types are <code>Logger</code> (named, level-thresholded, owns a list of handlers) and <code>StreamHandler</code> (synchronous text output to a writable stream).  <code>BufferedHandler</code> is a runner-shaped front-end that batches records and flushes them on <code>handle(now_ms)</code>.</p> <p>An app builds a <code>Logger</code>, registers handlers on it, and calls its level methods (<code>debug</code>, <code>info</code>, <code>warning</code>, <code>error</code>, <code>critical</code>) directly from its own code, or hands the logger to its own modules.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#getting-started","level":2,"title":"Getting started","text":"<pre><code>from chumicro_logging import INFO, Logger, StreamHandler\n\nlogger = Logger(\"boot\", level=INFO, handlers=[StreamHandler()])\n\nlogger.info(\"up\")          # -&gt; stdout: INFO:boot:up\nlogger.warning(\"careful\")  # -&gt; stdout: WARNING:boot:careful\nlogger.debug(\"invisible\")  # below threshold; dropped silently\n</code></pre> <p><code>Logger</code> is not registered globally.  Every call to <code>Logger(\"foo\")</code> returns a fresh instance.  The caller owns and stores the reference. This avoids import-order surprises and keeps the library stateless.</p> <p>Switch threshold at runtime by assigning to <code>logger.level</code>:</p> <pre><code>from chumicro_logging import DEBUG\n\nlogger.level = DEBUG\nlogger.debug(\"now visible\")\n</code></pre> <p>Handler exceptions never escape the logger.  They increment <code>logger.handler_errors</code> and are otherwise swallowed.  A misbehaving handler can never crash the application that uses it.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#runner-pattern","level":2,"title":"Runner pattern","text":"<p>The hot path on a microcontroller cannot afford to write to the serial console synchronously.  <code>BufferedHandler</code> decouples emission from I/O: <code>emit</code> appends to a bounded buffer and returns immediately; <code>check(now_ms)</code> returns <code>True</code> when records are pending; <code>handle(now_ms)</code> drains the buffer to the downstream handler.</p> <pre><code>from chumicro_logging import BufferedHandler, DEBUG, Logger, StreamHandler\n\nstream = StreamHandler()\nbuffered = BufferedHandler(downstream=stream, capacity=32)\nlogger = Logger(\"sensor\", level=DEBUG, handlers=[buffered])\n\n# Hot loop, cheap, no I/O.\nfor index in range(100):\n    logger.info(f\"sample {index}\")\n\n# Runner tick: drains the buffer.  now_ms comes from your tick source\n# (ticks_ms(), or the value the runner passes into handle()).\nnow_ms = 0\nif buffered.check(now_ms):\n    buffered.handle(now_ms)\n</code></pre> <p>When the rate exceeds the flush cadence and the buffer fills, the oldest record is dropped and <code>buffered.dropped</code> is incremented.  Newest data wins on the assumption the operator wants to see recent activity rather than ancient backlog.</p> <p>Wiring into a <code>chumicro-runner.Runner</code> is direct.  <code>BufferedHandler</code> already implements the <code>check(now_ms) -&gt; bool</code> + <code>handle(now_ms)</code> contract.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#memory-notes","level":2,"title":"Memory notes","text":"<p><code>BufferedHandler</code> keeps a bounded buffer of <code>(level, name, message)</code> tuples up to <code>capacity</code> deep.  At the default capacity of 32, expect roughly <code>32 × (sizeof tuple + sizeof message)</code> bytes resident.  Tune <code>capacity</code> to your environment: small enough to bound the memory budget, large enough to absorb the worst-case rate between runner ticks.</p> <p><code>StreamHandler</code> allocates a fresh string per record (the formatter output) and immediately writes it; it keeps no internal buffer.  On embedded runtimes the per-record allocation is the dominant cost, so prefer <code>BufferedHandler</code> in front of it for hot paths.</p> <p><code>Logger</code> itself stores a reference to each handler in a list and an integer error counter.  <code>logger.handlers</code> builds a fresh tuple snapshot on every access, so read it once and hold the result rather than re-reading it in a loop.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#platform-notes","level":2,"title":"Platform notes","text":"<p>Runs identically on CPython, MicroPython, and CircuitPython.  No <code>sys.implementation</code> checks.  The library imports only <code>sys</code> and <code>collections.deque</code> from the stdlib; the one runtime branch is a <code>from micropython import const</code> try-import that falls back to an identity <code>const</code> on CPython.</p> <p><code>StreamHandler</code>'s default stream is <code>sys.stdout</code>, which on microcontrollers writes to the serial console; on CPython any file-like object works.  The handler calls <code>stream.flush()</code> after every write when the stream exposes it; streams without <code>flush</code> (some custom test doubles) are also fine.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#examples","level":2,"title":"Examples","text":"Example What it shows <code>examples/stream_handler.py</code> <code>Logger</code> + <code>StreamHandler</code> at INFO threshold, level switching at runtime. <code>examples/buffered_runner.py</code> <code>BufferedHandler</code> decoupling a hot loop from I/O via the runner-shaped <code>check</code> / <code>handle</code> contract. <p>Both examples run on every supported runtime; neither requires hardware.</p> <p>← Home</p> <p>Source · \\ PyPI · \\ Bundle · \\ Experimental Bundle</p>","path":["User Guide"],"tags":[]},{"location":"testing/","level":1,"title":"Testing Helpers","text":"<p><code>chumicro_logging.testing</code> provides handler fakes that downstream libraries and applications can use to assert against logger output without writing one-off mocks.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#recordinghandler","level":2,"title":"RecordingHandler","text":"<p>Captures every emitted record in a list for assertions.  Calls to <code>emit(level, name, message)</code> append a <code>(level, name, message)</code> tuple to <code>records</code>.  Pass the handler to a <code>Logger</code> and assert against its output.</p> <pre><code>from chumicro_logging import INFO, Logger\nfrom chumicro_logging.testing import RecordingHandler\n\n\ndef test_logger_emits_at_info():\n    handler = RecordingHandler()\n    logger = Logger(\"subsystem\", level=INFO, handlers=[handler])\n\n    logger.info(\"up\")\n\n    assert handler.records == [(INFO, \"subsystem\", \"up\")]\n</code></pre> <p><code>RecordingHandler</code> itself respects an optional <code>level</code> threshold — records below the threshold are dropped without being captured — which makes it easy to verify that a logger correctly filters at a particular level.  Call <code>clear()</code> between assertions to reset.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#failinghandler","level":2,"title":"FailingHandler","text":"<p>Raises a configured exception on every <code>emit</code> call.  Useful for verifying that misbehaving handlers never crash the logger:</p> <pre><code>from chumicro_logging import Logger\nfrom chumicro_logging.testing import FailingHandler, RecordingHandler\n\n\ndef test_failing_handler_is_swallowed():\n    failing = FailingHandler()\n    recorder = RecordingHandler()\n    logger = Logger(\"alpha\", handlers=[failing, recorder])\n\n    logger.warning(\"survive me\")\n\n    assert logger.handler_errors == 1\n    assert recorder.records[0][2] == \"survive me\"\n</code></pre> <p>The default exception is <code>RuntimeError(\"handler boom\")</code>.  Pass a custom exception via the <code>exception=</code> keyword to simulate specific failure modes.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#usage-from-applications","level":2,"title":"Usage from applications","text":"<p>No other ChuMicro library imports <code>chumicro-logging</code>, so these fakes are for application and integration tests that build a <code>Logger</code> directly. Import them the same way:</p> <pre><code>from chumicro_logging.testing import RecordingHandler\n</code></pre> <p>Project convention: libraries that expose injectable services ship their own test fakes alongside the production code.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#api-reference","level":2,"title":"API Reference","text":"<p>← Home</p> <p>Source · \\ PyPI · \\ Bundle · \\ Experimental Bundle</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_logging.testing","level":2,"title":"<code>chumicro_logging.testing</code>","text":"<p>Test helpers for libraries that consume chumicro-logging.</p> <p>Provides <code>RecordingHandler</code> and <code>FailingHandler</code>.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_logging.testing.RecordingHandler","level":3,"title":"<code>RecordingHandler</code>","text":"<p>Handler that captures records in a list for test assertions.</p> <p>Parameters:</p> Name Type Description Default <code>level</code> <code>int</code> <p>Minimum level captured; defaults to <code>0</code> so every record passes.</p> <code>0</code>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_logging.testing.RecordingHandler.level","level":4,"title":"<code>level</code>  <code>property</code> <code>writable</code>","text":"<p>Current minimum-capture level.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_logging.testing.RecordingHandler.records","level":4,"title":"<code>records</code>  <code>property</code>","text":"<p>All captured records as <code>(level, name, message)</code> tuples.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_logging.testing.RecordingHandler.clear","level":4,"title":"<code>clear()</code>","text":"<p>Drop all captured records.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_logging.testing.RecordingHandler.emit","level":4,"title":"<code>emit(level, name, message)</code>","text":"<p>Capture the record if it meets the level threshold.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_logging.testing.FailingHandler","level":3,"title":"<code>FailingHandler</code>","text":"<p>Handler that raises on every <code>emit</code>, to exercise error paths.</p> <p>Parameters:</p> Name Type Description Default <code>exception</code> <code>BaseException | None</code> <p>Exception instance to raise; defaults to <code>RuntimeError(\"handler boom\")</code>.</p> <code>None</code>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_logging.testing.FailingHandler.calls","level":4,"title":"<code>calls</code>  <code>property</code>","text":"<p>Number of times <code>emit</code> has been called.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_logging.testing.FailingHandler.emit","level":4,"title":"<code>emit(level, name, message)</code>","text":"<p>Increment the call counter and raise the configured exception.</p>","path":["Testing Helpers"],"tags":[]}]}