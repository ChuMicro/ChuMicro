{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"chumicro-kvstore","text":"<p>Tiny mutable key-value store for runtime state that survives reboot.</p> <p>Counters, timestamps, tokens, and retry budgets that your program writes while it runs and still finds after a power cycle, on CircuitPython, MicroPython, and CPython.  You get a dict you assign to and <code>commit()</code> when the change matters; the backend is chosen for the runtime you are on (NVM with CRC framing on CircuitPython, NVS on ESP32 MicroPython, LittleFS elsewhere, in-memory on a host).</p>","path":["chumicro-kvstore: storage that survives a reboot on CircuitPython and MicroPython"],"tags":[]},{"location":"#install","level":2,"title":"Install","text":"<pre><code># CircuitPython (after `circup bundle-add ChuMicro/ChuMicro-Bundle`)\ncircup install chumicro_kvstore\n\n# MicroPython\nmpremote mip install github:ChuMicro/ChuMicro-Bundle/chumicro_kvstore\n\n# CPython\npip install chumicro-kvstore\n</code></pre> <p>No board running yet?  Start here goes from a new board to your own code running on it.  Installing libraries covers registering the bundle, the experimental channel, and the pre-compiled <code>.mpy</code> packages.</p>","path":["chumicro-kvstore: storage that survives a reboot on CircuitPython and MicroPython"],"tags":[]},{"location":"#quick-example","level":2,"title":"Quick example","text":"<pre><code>from chumicro_kvstore import KVStore\nfrom chumicro_timing import ticks_ms  # separate install: chumicro-timing\n\nstore = KVStore(backend=\"auto\")              # picks the right backend per runtime\nstore[\"boot_count\"] = store.get(\"boot_count\", 0) + 1\nstore[\"last_seen_ms\"] = ticks_ms()\nstore.commit()                                # one flush per logical change\n</code></pre>","path":["chumicro-kvstore: storage that survives a reboot on CircuitPython and MicroPython"],"tags":[]},{"location":"#documentation","level":2,"title":"Documentation","text":"<ul> <li>User Guide: backends and <code>auto</code> selection, commit semantics, sizing and full-store handling, corruption handling, platform notes</li> <li>API Reference: <code>KVStore</code> methods, <code>commit_if_changed</code>, and the <code>KVStoreFull</code> / <code>KVStoreCorrupt</code> exceptions</li> <li>Testing Helpers: using <code>FakeKVStore</code> in your tests</li> </ul> <p>← All ChuMicro Libraries</p> <p>Source · \\ PyPI · \\ Bundle · \\ Experimental Bundle</p>","path":["chumicro-kvstore: storage that survives a reboot on CircuitPython and MicroPython"],"tags":[]},{"location":"api/","level":1,"title":"API Reference","text":"","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_kvstore_1","level":2,"title":"<code>chumicro_kvstore</code>","text":"<p>← Home</p> <p>Source · \\ PyPI · \\ Bundle · \\ Experimental Bundle</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_kvstore","level":2,"title":"<code>chumicro_kvstore</code>","text":"<p>Persisted runtime state for CircuitPython, MicroPython, and CPython.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_kvstore.KVStore","level":3,"title":"<code>KVStore</code>","text":"<p>Persisted key-value store with a mapping-style public API.</p> <p>Construction reads the backend once; a corrupt payload parks the store empty with <code>is_corrupt</code> set, but a backend that cannot reach its substrate at all (missing NVM, unreadable filesystem) raises.</p> <p>Parameters:</p> Name Type Description Default <code>backend</code> <code>Backend | str</code> <p><code>\"auto\"</code>, <code>\"memory\"</code>, <code>\"nvm\"</code>, <code>\"nvs\"</code>, <code>\"littlefs\"</code>, or a backend instance.</p> <code>'auto'</code>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_kvstore.KVStore.bytes_used","level":4,"title":"<code>bytes_used</code>  <code>property</code>","text":"<p>Encoded size of the current in-memory dict, not the persisted payload.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_kvstore.KVStore.reload","level":4,"title":"<code>reload()</code>","text":"<p>Discard in-memory state and reread from backend.</p> <p>Raises:</p> Type Description <code>KVStoreCorrupt</code> <p>Backend payload failed integrity check.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_kvstore.KVStore.commit","level":4,"title":"<code>commit()</code>","text":"<p>Encode the current dict and persist it through the backend.</p> <p>Raises:</p> Type Description <code>KVStoreFull</code> <p>Encoded payload exceeds <code>capacity</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_kvstore.KVStore.commit_if_changed","level":4,"title":"<code>commit_if_changed()</code>","text":"<p>Commit only when the encoded payload changed since the last persist.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> if a write happened, <code>False</code> if the commit was skipped.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_kvstore.KVStore.pop","level":4,"title":"<code>pop(key, *default)</code>","text":"<p>Remove key and return its value, or default when supplied.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_kvstore.KVStore.clear","level":4,"title":"<code>clear()</code>","text":"<p>Remove every key from the in-memory dict (commit not implied).</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_kvstore.KVStore.update","level":4,"title":"<code>update(other)</code>","text":"<p>Merge other into the in-memory dict (commit not implied).</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_kvstore.KVStoreCorrupt","level":3,"title":"<code>KVStoreCorrupt</code>","text":"<p>               Bases: <code>KVStoreError</code></p> <p>Persisted state failed its integrity check on load.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_kvstore.KVStoreError","level":3,"title":"<code>KVStoreError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Base for every kvstore-specific failure.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_kvstore.KVStoreFull","level":3,"title":"<code>KVStoreFull</code>","text":"<p>               Bases: <code>KVStoreError</code></p> <p>A commit would exceed <code>capacity</code>; the in-memory state is left unchanged.</p>","path":["API Reference"],"tags":[]},{"location":"guide/","level":1,"title":"User Guide","text":"","path":["User Guide"],"tags":[]},{"location":"guide/#overview","level":2,"title":"Overview","text":"<p><code>chumicro-kvstore</code> is a tiny mutable key-value store for runtime state that needs to survive a reboot (boot counters, last-seen timestamps, retry budgets, refreshed access tokens).  It exposes a familiar <code>dict</code>-shaped API (<code>store[key] = value</code>, <code>del store[key]</code>, <code>\"key\" in store</code>, iteration) plus three explicit lifecycle methods: <code>commit</code>, <code>commit_if_changed</code>, <code>reload</code>.</p> <p>State is read-write at runtime, flat, and lives in the right per-runtime persistent backend (CP NVM with CRC framing, MP NVS, MP LittleFS, or in-memory).  You write to the in-memory dict as you go and call a <code>commit*</code> method when you want the change to survive the next reboot.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#getting-started","level":2,"title":"Getting started","text":"<p>The \"boot counter that survives reboot\" pattern in five lines:</p> <pre><code>from chumicro_kvstore import KVStore\nfrom chumicro_timing import ticks_ms  # separate install: chumicro-timing\n\nstore = KVStore(backend=\"auto\")\nstore[\"boot_count\"] = store.get(\"boot_count\", 0) + 1\nstore[\"last_seen_ms\"] = ticks_ms()\nstore.commit_if_changed()\n</code></pre> <p><code>backend=\"auto\"</code> picks the right backend per runtime (see below).  Reads are pure-memory after the constructor's auto-load, with no I/O on the hot path.  Writes update the in-memory dict immediately; persistence happens on the next <code>commit*</code> call.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#backends-and-auto-selection","level":2,"title":"Backends and <code>auto</code> selection","text":"Backend Where the bytes live Selected on <code>nvm</code> CircuitPython <code>microcontroller.nvm</code> byte slab with CRC32 framing CircuitPython on every supported board <code>nvs</code> MicroPython <code>esp32.NVS</code> namespaced K-V (single <code>payload</code> blob in the <code>chu_kv</code> namespace) MicroPython on ESP32-family boards (auto-detected via <code>import esp32</code>) <code>littlefs</code> MicroPython LittleFS file at <code>/_chu_kv.msgpack</code>, atomic via tmp-file + rename MicroPython on non-NVS boards (Pi Pico W, etc.) <code>memory</code> In-process <code>bytes</code> that does not survive process exit CPython default, plus <code>FakeKVStore</code> for tests <p>The auto-select ladder is one short function:</p> <pre><code>import sys\n\ndef _select_backend():\n    if sys.implementation.name == \"circuitpython\":\n        return CpNvmBackend()\n    if sys.implementation.name == \"micropython\":\n        try:\n            import esp32          # ESP32-family probe\n            return MpNvsBackend()\n        except ImportError:\n            return MpLittlefsBackend()\n    return MemoryBackend()        # CPython\n</code></pre> <p>If you want a specific backend regardless of runtime (for tests, or to force <code>littlefs</code> on an ESP32 with NVS issues), pass it by name:</p> <pre><code>store = KVStore(backend=\"littlefs\")\nstore = KVStore(backend=\"memory\")\n</code></pre> <p>Or pass a <code>Backend</code> instance directly (a <code>MemoryBackend</code>, or your own <code>load</code>/<code>save</code> pair).  In tests, construct a <code>FakeKVStore()</code> in place of the whole store; it is a <code>KVStore</code>, not a backend (see Testing Helpers).</p>","path":["User Guide"],"tags":[]},{"location":"guide/#commit-semantics","level":2,"title":"Commit semantics","text":"<p>Three lifecycle methods, distinct intents:</p> Method What it does When to use <code>commit()</code> Always re-encode + write. After a logical change you know is significant. <code>commit_if_changed()</code> Re-encode; skip the write if bytes match the last persisted payload. Hot loops or once-per-tick \"save current state\" calls, the first-line defense against flash wear on the raw NVM backend. <code>reload()</code> Discard in-memory state, reread from backend, raise on corruption. Recovery: explicit re-read after suspicion of external write or corruption. <pre><code># Safe to call every tick; only writes when something actually changed.\nrunner.add_periodic(store.commit_if_changed, period_ms=1000)\n</code></pre>","path":["User Guide"],"tags":[]},{"location":"guide/#sizing-and-full-store-handling","level":2,"title":"Sizing and full-store handling","text":"<p>Each backend exposes a <code>capacity</code> (bytes-of-encoded-payload).  <code>KVStore.bytes_used</code> reports the current encoded size; <code>KVStore.capacity</code> reports the backend's limit.</p> <p>CP NVM is the smallest, typically 256 bytes on SAMD21 boards, ~4 KB on RP2040 (the reference Pi Pico W), and 8 KB on SAMD51 / ESP32 boards (it's per-chip; check your board's <code>microcontroller.nvm</code>).  After CRC framing overhead (10 bytes), you have your usable budget.  <code>commit()</code> raises <code>KVStoreFull</code> if the encoded payload won't fit; the in-memory dict is unchanged so you can drop a key and retry:</p> <pre><code>try:\n    store.commit()\nexcept KVStoreFull:\n    del store[\"debug_log\"]\n    store.commit()\n</code></pre> <p>A few keys with short string / int values (boot counters, timestamps, simple flags) easily fit in 256 B.  Larger state (captured sensor traces, queued telemetry) wants the LittleFS or NVS backend, which give you tens of KB.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#corruption-handling","level":2,"title":"Corruption handling","text":"<p>The CP NVM backend is the only one with explicit framing (magic <code>b\"CKVS\"</code> + length + CRC32 + payload).  A blank slab from <code>storage.erase_filesystem()</code> reads as empty.  A bad-magic or CRC-mismatch reads as corrupt.</p> <p>Construction never raises on corruption: the store resets to empty and reports the event via <code>is_corrupt</code> (a backend that cannot reach its substrate at all, e.g. NVM missing, still raises):</p> <pre><code>store = KVStore(backend=\"auto\")\nif store.is_corrupt:\n    log.warning(\"kvstore was corrupt; starting fresh\")\n    # store[\"boot_count\"] etc. starts at 0\n</code></pre> <p><code>reload()</code> is the explicit form that does raise (<code>KVStoreCorrupt</code>); use it when you want to surface the failure rather than silently reset.</p> <p>NVS is atomic-on-commit at the backend level (no CRC needed).  LittleFS uses tmp-file + rename for atomicity (no CRC needed).  Memory backend can't corrupt.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#iteration-and-update","level":2,"title":"Iteration and update","text":"<p>Standard mapping API works:</p> <pre><code>for key, value in store.items():\n    print(key, value)\n\nstore.update({\"counter_a\": 42, \"counter_b\": 99})\nstore.commit_if_changed()\n</code></pre> <p><code>clear()</code>, <code>pop(key, default)</code>, <code>keys()</code>, <code>values()</code>, <code>items()</code> follow <code>dict</code> semantics.  <code>commit</code> is not implied by mutating methods; you call it explicitly when you want the change persisted.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#value-types","level":2,"title":"Value types","text":"<p>Values are stored via msgpack in the chumicro subset: <code>None</code>, <code>bool</code>, <code>int</code> (32-bit), <code>str</code>, <code>bytes</code>, and nested <code>list</code> / <code>dict</code> up to 8 levels deep.  Floats encode as 32-bit (<code>float32</code>), so a value like a <code>time.time()</code> timestamp loses precision through a commit / reload round-trip (e.g. <code>1751414400.5</code> reads back as <code>1751414400.0</code>).  Store timestamps and durations as integer milliseconds or seconds, not floats.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#platform-notes","level":2,"title":"Platform notes","text":"Runtime Backend chosen by <code>auto</code> Capacity (typical) CircuitPython <code>nvm</code> (with CRC framing) 256 B – 8 KB depending on chip MicroPython on ESP32-family <code>nvs</code> (single blob in <code>chu_kv</code> namespace) 512 B default; raise it with <code>KVStore(backend=MpNvsBackend(capacity=N))</code> (~24 KB partition headroom) MicroPython on Pi Pico W (rp2) <code>littlefs</code> (atomic file at <code>/_chu_kv.msgpack</code>) 16 KB default; raise it with <code>KVStore(backend=MpLittlefsBackend(capacity=N))</code> (then filesystem-bounded) CPython <code>memory</code> (in-process bytes) unbounded <p><code>MemoryBackend</code> is lazy-imported, so device runtimes that resolve <code>auto</code> to <code>nvm</code> / <code>nvs</code> / <code>littlefs</code> never pay the ~700 B import cost.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#usb-msc-read-only-window-circuitpython","level":3,"title":"USB-MSC read-only window (CircuitPython)","text":"<p>While the host has CIRCUITPY mounted, the device can't <code>storage.remount(readonly=False)</code> to write to flash.  The default <code>nvm</code> backend on CircuitPython writes to NVM (not the FAT volume) so it's unaffected; the boot-counter pattern continues to persist across reboots even with the drive mounted.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#examples","level":2,"title":"Examples","text":"Example What it shows <code>examples/boot_counter.py</code> Boot-counter pattern: <code>commit_if_changed</code>, <code>bytes_used</code>, <code>backend_name</code>.  Runs on every runtime; on CPython the count resets each invocation, on a real device it survives reboot. <p>← Home</p> <p>Source · \\ PyPI · \\ Bundle · \\ Experimental Bundle</p>","path":["User Guide"],"tags":[]},{"location":"testing/","level":1,"title":"Testing Helpers","text":"<p><code>chumicro_kvstore.testing</code> provides <code>FakeKVStore</code>, an in-memory <code>KVStore</code> with explicit corruption and capacity hooks for your tests.  It runs on the same in-memory backend the auto-selector picks on a host, so every assertion you write goes through the real <code>KVStore</code> code path rather than a mock of it.  The module declares the <code>__chumicro_test_support__</code> marker, and the deploy tool drops every module carrying it, so the fake never ships to a board.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#usage","level":2,"title":"Usage","text":"<pre><code>from chumicro_kvstore.testing import FakeKVStore\n\ndef test_boot_counter_persists():\n    store = FakeKVStore()\n    store[\"boot_count\"] = 1\n    store.commit()\n\n    # Re-construct against the same underlying payload to simulate reboot.\n    fresh = FakeKVStore(initial_payload=store.raw_payload)\n    assert fresh[\"boot_count\"] == 1\n</code></pre>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#simulating-a-small-nvm","level":2,"title":"Simulating a small NVM","text":"<p>CircuitPython NVM is the tightest backend: typically 256 bytes on SAMD21 boards, around 4 KB on RP2040, and 8 KB on SAMD51 and ESP32 boards, minus 10 bytes of CRC framing.  Use <code>capacity=</code> to drive <code>KVStoreFull</code> deterministically without needing the real hardware:</p> <pre><code>from chumicro_kvstore import KVStoreFull\nfrom chumicro_kvstore.testing import FakeKVStore\n\ndef test_dropping_keys_recovers_from_full():\n    store = FakeKVStore(capacity=64)            # tight on purpose\n    store[\"a\"] = \"x\" * 32\n    store[\"b\"] = \"y\" * 32\n    try:\n        store.commit()\n        raise AssertionError(\"expected KVStoreFull\")\n    except KVStoreFull:\n        del store[\"b\"]\n        store.commit()                          # succeeds now\n\n    assert store.bytes_used &lt;= 64\n</code></pre> <p><code>set_capacity(new_capacity)</code> adjusts the limit mid-test if you want to cross the threshold from below:</p> <pre><code>store = FakeKVStore(capacity=1024)\nstore[\"a\"] = \"x\" * 200\nstore.commit()                                   # fits\n\nstore.set_capacity(64)                           # tighten\ntry:\n    store[\"a\"] = \"x\" * 200\n    store.commit()\nexcept KVStoreFull:\n    pass                                         # expected\n</code></pre>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#simulating-corruption","level":2,"title":"Simulating corruption","text":"<p><code>simulate_corrupt()</code> marks the underlying backend corrupt; the next <code>reload()</code> (or <code>KVStore</code> re-construction) surfaces a corruption event.  In-memory state stays intact until you explicitly reload, matching the real device behavior, where a backend-level fault doesn't poison state already in hand.</p> <pre><code>from chumicro_kvstore import KVStoreCorrupt\nfrom chumicro_kvstore.testing import FakeKVStore\n\ndef test_reload_raises_on_corrupt():\n    store = FakeKVStore()\n    store[\"x\"] = 1\n    store.commit()\n    store.simulate_corrupt()\n\n    try:\n        store.reload()\n        raise AssertionError(\"expected KVStoreCorrupt\")\n    except KVStoreCorrupt:\n        pass\n</code></pre> <p>The construction path treats corruption as recoverable: <code>is_corrupt</code> becomes <code>True</code> and the store resets to empty.</p> <pre><code>def test_construction_recovers_from_corrupt():\n    payload = b\"\\xff\" * 32                       # garbage\n    store = FakeKVStore(initial_payload=payload)\n    assert store.is_corrupt is True\n    assert len(store) == 0                       # blank, but usable\n</code></pre>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#recording-calls","level":2,"title":"Recording calls","text":"<p>Pass <code>record_calls=True</code> to capture every public-API call as a <code>(method, args)</code> tuple in <code>store.calls</code>:</p> <pre><code>def test_publisher_only_commits_once_per_minute():\n    store = FakeKVStore(record_calls=True)\n    publisher = TelemetryPublisher(store=store, ...)\n\n    for _ in range(60):\n        publisher.tick()\n\n    commit_calls = [event for event in store.calls if event[0] == \"commit\"]\n    assert len(commit_calls) == 1\n</code></pre> <p>Recorded methods: <code>__setitem__</code>, <code>__delitem__</code>, <code>commit</code>, <code>commit_if_changed</code>, <code>reload</code>.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#inspecting-the-raw-payload","level":2,"title":"Inspecting the raw payload","text":"<p><code>raw_payload</code> returns the encoded msgpack bytes the backend currently holds.  Useful for round-trip assertions or for seeding a second <code>FakeKVStore</code> from the first's persisted state:</p> <pre><code>seeded = FakeKVStore()\nseeded[\"boot_count\"] = 7\nseeded.commit()\n\nrestarted = FakeKVStore(initial_payload=seeded.raw_payload)\nassert restarted[\"boot_count\"] == 7\n</code></pre>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#using-these-fakes-in-your-own-tests","level":2,"title":"Using these fakes in your own tests","text":"<p>Installing <code>chumicro-kvstore</code> puts <code>chumicro_kvstore.testing</code> on your path, so your own suite imports the fake and passes it wherever your code expects a <code>KVStore</code>:</p> <pre><code>from chumicro_kvstore.testing import FakeKVStore\n</code></pre> <p>Project convention: libraries that expose injectable services ship their own test fakes alongside the production code.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#api-reference","level":2,"title":"API Reference","text":"<p>← Home</p> <p>Source · \\ PyPI · \\ Bundle · \\ Experimental Bundle</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_kvstore.testing","level":2,"title":"<code>chumicro_kvstore.testing</code>","text":"<p>Test helpers for libraries that depend on <code>chumicro-kvstore</code>.</p> <p><code>FakeKVStore</code> gives downstream tests a real store to run against.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_kvstore.testing.FakeKVStore","level":3,"title":"<code>FakeKVStore</code>","text":"<p>               Bases: <code>KVStore</code></p> <p>In-memory <code>KVStore</code> with explicit corruption and capacity hooks.</p> <p>Parameters:</p> Name Type Description Default <code>capacity</code> <code>int | None</code> <p>Optional capacity override in bytes, to drive the <code>KVStoreFull</code> path.</p> <code>None</code> <code>initial_payload</code> <code>bytes | None</code> <p>Optional pre-seeded msgpack payload.</p> <code>None</code> <code>record_calls</code> <code>bool</code> <p>When <code>True</code>, each public-API call is appended to <code>self.calls</code>.</p> <code>False</code>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_kvstore.testing.FakeKVStore.raw_payload","level":4,"title":"<code>raw_payload</code>  <code>property</code>","text":"<p>Raw msgpack bytes currently held by the backend.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_kvstore.testing.FakeKVStore.simulate_corrupt","level":4,"title":"<code>simulate_corrupt()</code>","text":"<p>Mark the underlying memory backend corrupt.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_kvstore.testing.FakeKVStore.reset_corrupt","level":4,"title":"<code>reset_corrupt()</code>","text":"<p>Clear the simulated-corrupt flag.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_kvstore.testing.FakeKVStore.set_capacity","level":4,"title":"<code>set_capacity(capacity)</code>","text":"<p>Adjust the simulated capacity mid-test.</p>","path":["Testing Helpers"],"tags":[]}]}