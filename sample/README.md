# chumicro-sample

A tiny sample library that proves the Chumicro workspace shape with a timing-first API.

The first sample focuses on a `Heartbeat` object driven by an injected tick source. The goal is to keep the core logic runtime-agnostic while still proving a real embedded seam.

