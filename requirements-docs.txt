# Documentation build dependencies.
# Used by docs-deploy.yml and promote.yml to keep tooling versions in sync.
# Pinned exact to match requirements-dev.txt — unpinned zensical once
# resolved to different versions on two machines and fail-the-build'd on
# cross-tree links.
zensical==0.0.39
mkdocstrings[python]==1.0.4
# mike is installed from the Zensical fork (squidfunk/mike) which includes
# alias management improvements not yet on PyPI.
mike @ git+https://github.com/squidfunk/mike.git@2d4ad799442f4592db8ad53b179bfb33db8c69ac
