{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"chumicro-requests","text":"<p>Non-blocking HTTP/1.1 client for CircuitPython, MicroPython, and CPython.</p> <p>An LED keeps blinking on the same board while a request is in flight, in a TLS handshake, or mid-timeout against a stalled peer.  Built on <code>chumicro-sockets</code> and <code>chumicro-timing</code>.</p>","path":["chumicro-requests"],"tags":[]},{"location":"#quick-example","level":2,"title":"Quick example","text":"<pre><code>from chumicro_requests import HttpClient\nfrom chumicro_sockets.sockets_factory import connector_factory\nfrom chumicro_timing import ticks_ms\nfrom chumicro_wifi import WifiConfig, WifiService\n\nwifi = WifiService(WifiConfig(ssid=\"home-wifi\", password=\"s3cret\"))\nclient = HttpClient(transport_factory=connector_factory(radio=wifi.adapter.radio))\nhandle = client.get(\"http://api.example.com/now\", timeout_ms=5000)\n\nwhile not handle.done:\n    now = ticks_ms()\n    if client.check(now):\n        client.handle(now)\n\nresponse = handle.result   # raises HttpError on failure\nprint(response.status_code, response.headers[\"content-type\"])\nprint(response.text)       # decoded str (charset sniffed from Content-Type)\nprint(response.json())     # parsed JSON when Content-Type is application/json\n</code></pre> <p><code>wifi.adapter.radio</code> is the board radio on CircuitPython and <code>None</code> on MicroPython and CPython, where the connector needs no radio.  Bringing your own socket instead of <code>chumicro-sockets</code> works too: <code>transport_factory</code> takes any <code>(host, port, use_tls)</code> callable.</p>","path":["chumicro-requests"],"tags":[]},{"location":"#documentation","level":2,"title":"Documentation","text":"<ul> <li>User Guide: generator flows and the <code>HttpClient</code> service, the POST / PUT / PATCH / DELETE verbs, redirects, body framing and decoding, streaming large bodies, bringing your own transport</li> <li>API Reference: <code>HttpClient</code>, <code>RequestHandle</code>, and <code>Response</code>, plus the <code>yield from</code> generator helpers</li> <li>Testing Helpers: using <code>FakeHttpClient</code> in your tests</li> </ul> <p>← All ChuMicro Libraries</p> <p>Source · \\ PyPI · \\ Bundle · \\ Experimental Bundle</p>","path":["chumicro-requests"],"tags":[]},{"location":"api/","level":1,"title":"API Reference","text":"","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests_1","level":2,"title":"<code>chumicro_requests</code>","text":"<p>The error types every call can raise, the case-insensitive header dict, and the wire helpers (<code>encode_request</code>, <code>parse_url</code>, <code>parse_charset</code>, <code>resolve_redirect_url</code>, <code>ResponseParser</code>) for code that frames its own HTTP.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests","level":2,"title":"<code>chumicro_requests</code>","text":"<p>Non-blocking HTTP/1.1 client for CircuitPython, MicroPython, and CPython.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.CaseInsensitiveDict","level":3,"title":"<code>CaseInsensitiveDict</code>","text":"<p>Header dict whose lookups fold to lowercase.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.CaseInsensitiveDict.get","level":4,"title":"<code>get(name, default=None)</code>","text":"<p>Return the value for name or default if missing.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.CaseInsensitiveDict.items","level":4,"title":"<code>items()</code>","text":"<p>Yield <code>(original_name, value)</code> pairs in insertion order.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.CaseInsensitiveDict.add","level":4,"title":"<code>add(name, value)</code>","text":"<p>Append value to the existing header, joining with <code>,</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.HttpBusyError","level":3,"title":"<code>HttpBusyError</code>","text":"<p>               Bases: <code>HttpError</code></p> <p>Caller issued a request while another was still in flight.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.HttpError","level":3,"title":"<code>HttpError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Base class for every chumicro-requests failure.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.HttpOversizedError","level":3,"title":"<code>HttpOversizedError</code>","text":"<p>               Bases: <code>HttpError</code></p> <p>Response body exceeded <code>max_body_bytes</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.HttpProtocolError","level":3,"title":"<code>HttpProtocolError</code>","text":"<p>               Bases: <code>HttpError</code></p> <p>Server sent bytes the spec doesn't allow.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.HttpTimeoutError","level":3,"title":"<code>HttpTimeoutError</code>","text":"<p>               Bases: <code>HttpError</code></p> <p>Per-request <code>timeout_ms</code> budget elapsed before the response completed.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.HttpURLError","level":3,"title":"<code>HttpURLError</code>","text":"<p>               Bases: <code>HttpError</code></p> <p>URL doesn't parse as a supported HTTP/HTTPS URL.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.ParseState","level":3,"title":"<code>ParseState</code>","text":"<p>Streaming response parser states.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.ResponseParser","level":3,"title":"<code>ResponseParser</code>","text":"<p>Streaming HTTP/1.1 response parser.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.ResponseParser.body","level":4,"title":"<code>body</code>  <code>property</code>","text":"<p>Body bytes received so far (final once :attr:<code>state</code> is <code>DONE</code>).</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.ResponseParser.__init__","level":4,"title":"<code>__init__(*, max_body_bytes=DEFAULT_MAX_BODY_BYTES, max_header_bytes=DEFAULT_MAX_HEADER_BYTES, body_buffer=None, body_buffer_view=None, stream_body=False)</code>","text":"<p>Construct the parser.</p> <p>Parameters:</p> Name Type Description Default <code>max_body_bytes</code> <code>int</code> <p>Cap on the buffered body size; ignored when stream_body is set.</p> <code>DEFAULT_MAX_BODY_BYTES</code> <code>max_header_bytes</code> <code>int</code> <p>Cap on status-line and header bytes staged before the body.</p> <code>DEFAULT_MAX_HEADER_BYTES</code> <code>body_buffer</code> <code>bytearray | None</code> <p>Optional caller-owned <code>bytearray</code> reused as the body buffer.</p> <code>None</code> <code>body_buffer_view</code> <code>memoryview | None</code> <p>Pre-cached <code>memoryview(body_buffer)</code>; required with body_buffer.</p> <code>None</code> <code>stream_body</code> <code>bool</code> <p>When <code>True</code>, stage the body in a fixed window instead of buffering whole.</p> <code>False</code>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.ResponseParser.body_free","level":4,"title":"<code>body_free()</code>","text":"<p>Writable staging space in the streamed-body window, in bytes.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.ResponseParser.read_body_into","level":4,"title":"<code>read_body_into(buffer)</code>","text":"<p>Copy staged body bytes into caller-owned buffer; return the count.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.ResponseParser.discard_body","level":4,"title":"<code>discard_body()</code>","text":"<p>Drop every staged body byte and reset both cursors.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.ResponseParser.feed","level":4,"title":"<code>feed(chunk)</code>","text":"<p>Append chunk to the parser's buffer and advance the state.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.ResponseParser.feed_eof","level":4,"title":"<code>feed_eof()</code>","text":"<p>Signal that the peer closed the connection.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.encode_request","level":3,"title":"<code>encode_request(method, host, path, *, headers=None, body=None, user_agent=None)</code>","text":"<p>Encode an HTTP/1.1 request into bytes ready for the wire.</p> <p>Parameters:</p> Name Type Description Default <code>method</code> <code>str</code> <p>HTTP verb, sent verbatim.</p> required <code>host</code> <code>str</code> <p>Value for the <code>Host:</code> header.</p> required <code>path</code> <code>str</code> <p>Request-target, typically the URL path + query.</p> required <code>headers</code> <code>CaseInsensitiveDict | dict | list | tuple | None</code> <p>Optional <code>(name, value)</code> pairs, dict, or CaseInsensitiveDict; override the defaults.</p> <code>None</code> <code>body</code> <code>bytes | None</code> <p>Optional <code>bytes</code> body; adds <code>Content-Length</code> when set.</p> <code>None</code> <code>user_agent</code> <code>str | None</code> <p>Override for the default <code>User-Agent</code> string.</p> <code>None</code> <p>Returns:</p> Type Description <code>bytes</code> <p>Encoded request as <code>bytes</code>.</p> <p>Raises:</p> Type Description <code>HttpURLError</code> <p>A method, path, or header holds CR / LF / NUL or non-ASCII.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.parse_charset","level":3,"title":"<code>parse_charset(content_type)</code>","text":"<p>Extract the <code>charset=...</code> parameter from a Content-Type header.</p> <p>Parameters:</p> Name Type Description Default <code>content_type</code> <code>str | None</code> <p>Raw <code>Content-Type</code> header value, or <code>None</code>.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The detected charset name, or <code>\"utf-8\"</code> as the safe default.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.parse_url","level":3,"title":"<code>parse_url(url)</code>","text":"<p>Split url into <code>(scheme, host, port, path)</code>.</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>HTTP or HTTPS URL to split.</p> required <p>Returns:</p> Type Description <code>tuple[str, str, int, str]</code> <p>4-tuple <code>(scheme, host, port, path)</code>; path starts with <code>/</code>.</p> <p>Raises:</p> Type Description <code>HttpURLError</code> <p>Bad scheme, missing host, or non-integer port.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.resolve_redirect_url","level":3,"title":"<code>resolve_redirect_url(current_url, location)</code>","text":"<p>Resolve a <code>Location</code> header value against the current request URL.</p> <p>Parameters:</p> Name Type Description Default <code>current_url</code> <code>str</code> <p>The URL of the request being redirected.</p> required <code>location</code> <code>str</code> <p>The raw <code>Location</code> header value from the response.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Absolute URL the redirected request should target.</p> <p>Raises:</p> Type Description <code>HttpURLError</code> <p>current_url doesn't parse, or location is empty.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requestsclient","level":2,"title":"<code>chumicro_requests.client</code>","text":"<p><code>HttpClient</code> itself, the <code>RequestHandle</code> each call returns, the <code>Response</code> it resolves to, and the <code>WhenOversized</code> policy for bodies past <code>max_body_bytes</code>.  <code>from chumicro_requests import HttpClient</code> gives you the same class.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client","level":2,"title":"<code>chumicro_requests.client</code>","text":"<p>HTTP/1.1 client built on chumicro-sockets + chumicro-timing.</p> <p>:class:<code>HttpClient</code> is the entry point.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.WhenOversized","level":3,"title":"<code>WhenOversized</code>","text":"<p>Policy for response bodies exceeding <code>max_body_bytes</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.Response","level":3,"title":"<code>Response</code>","text":"<p>Result of a completed HTTP request.</p> <p>Attributes:</p> Name Type Description <code>status_code</code> <p>Integer HTTP status (e.g. <code>200</code>).</p> <code>reason</code> <p>Reason phrase from the status line (e.g. <code>\"OK\"</code>).</p> <code>http_version</code> <p>Protocol version string (e.g. <code>\"HTTP/1.1\"</code>).</p> <code>headers</code> <p>:class:<code>CaseInsensitiveDict</code> of response headers.</p> <code>body</code> <p>Raw response body as <code>bytes</code>; empty when :attr:<code>streamed</code>.</p> <code>url</code> <p>The URL that was requested.</p> <code>oversized_dropped</code> <p><code>True</code> when the body was dropped per the <code>when_oversized</code> policy.</p> <code>streamed</code> <p><code>True</code> when issued with <code>stream=True</code>; the body is consumed incrementally.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.Response.encoding","level":4,"title":"<code>encoding</code>  <code>property</code> <code>writable</code>","text":"<p>Charset used to decode :attr:<code>body</code> into :attr:<code>text</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.Response.text","level":4,"title":"<code>text</code>  <code>property</code>","text":"<p>:attr:<code>body</code> decoded to <code>str</code> using :attr:<code>encoding</code>.</p> <p>Raises:</p> Type Description <code>UnicodeError</code> <p>The body bytes don't match the encoding.</p> <code>HttpError</code> <p>The response is :attr:<code>streamed</code>, so has no whole body.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.Response.json","level":4,"title":"<code>json()</code>","text":"<p>Parse :attr:<code>body</code> as JSON and return the decoded object.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>The body is not valid JSON.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.RequestHandle","level":3,"title":"<code>RequestHandle</code>","text":"<p>Caller-visible handle to an in-flight (or completed) request.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.RequestHandle.result","level":4,"title":"<code>result</code>  <code>property</code>","text":"<p>Return the :class:<code>Response</code>; raise the failure if any.</p> <p>Raises:</p> Type Description <code>HttpError</code> <p>The request failed, or <code>result</code> was read before <code>done</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.RequestHandle.read_body_into","level":4,"title":"<code>read_body_into(buffer)</code>","text":"<p>Copy received body bytes into caller-owned buffer; return the count.</p> <p>Raises:</p> Type Description <code>HttpError</code> <p>The request was not issued with <code>stream=True</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.HttpClient","level":3,"title":"<code>HttpClient</code>","text":"<p>Non-blocking HTTP/1.1 client.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.HttpClient.busy","level":4,"title":"<code>busy</code>  <code>property</code>","text":"<p><code>True</code> while a request is in flight.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.HttpClient.io_socket","level":4,"title":"<code>io_socket</code>  <code>property</code>","text":"<p>Underlying pollable socket while in flight, else <code>None</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.HttpClient.from_config","level":4,"title":"<code>from_config(config, *, radio=None, ssl_context=None, transport_factory=None)</code>  <code>classmethod</code>","text":"<p>Build an :class:<code>HttpClient</code> from runtime config.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.HttpClient.__init__","level":4,"title":"<code>__init__(*, transport_factory, recv_budget_per_tick=DEFAULT_RECV_BUDGET_PER_TICK, max_body_bytes=DEFAULT_MAX_BODY_BYTES, when_oversized=WhenOversized.DROP_WITH_EVENT, default_timeout_ms=DEFAULT_TIMEOUT_MS, default_max_redirects=DEFAULT_MAX_REDIRECTS, stream_buffer_size=DEFAULT_STREAM_BUFFER_SIZE, user_agent=None, ticks=None)</code>","text":"<p>Wire up the client.</p> <p>Parameters:</p> Name Type Description Default <code>transport_factory</code> <code>object</code> <p>Callable <code>(host, port, use_tls)</code> opening a socket per hop.</p> required <code>recv_budget_per_tick</code> <code>int</code> <p>Soft cap on bytes drained per :meth:<code>handle</code> call.</p> <code>DEFAULT_RECV_BUDGET_PER_TICK</code> <code>max_body_bytes</code> <code>int</code> <p>Cap on a buffered body; not applied to <code>stream=True</code>.</p> <code>DEFAULT_MAX_BODY_BYTES</code> <code>when_oversized</code> <code>str</code> <p>Policy for over-cap responses (see :class:<code>WhenOversized</code>).</p> <code>DROP_WITH_EVENT</code> <code>default_timeout_ms</code> <code>int</code> <p>Default per-request timeout in ms.</p> <code>DEFAULT_TIMEOUT_MS</code> <code>default_max_redirects</code> <code>int</code> <p>Default cap on 3xx hops; <code>0</code> returns the 3xx as-is.</p> <code>DEFAULT_MAX_REDIRECTS</code> <code>stream_buffer_size</code> <code>int</code> <p>Staging capacity in bytes for each <code>stream=True</code> body window.</p> <code>DEFAULT_STREAM_BUFFER_SIZE</code> <code>user_agent</code> <code>str | None</code> <p>Override for the default <code>User-Agent</code> header.</p> <code>None</code> <code>ticks</code> <code>object | None</code> <p>Optional <code>chumicro_timing.ticks</code>-shaped source; defaults to that submodule.</p> <code>None</code>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.HttpClient.io_interest","level":4,"title":"<code>io_interest(now_ms)</code>","text":"<p>Poll-interest bitmask OR-ing <code>_IO_READ</code> / <code>_IO_WRITE</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.HttpClient.next_deadline","level":4,"title":"<code>next_deadline(now_ms)</code>","text":"<p>Return the per-request timeout deadline, or <code>None</code> when idle.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.HttpClient.request","level":4,"title":"<code>request(method, url, *, body=None, json=None, headers=None, timeout_ms=None, max_redirects=None, on_done=None, stream=False)</code>","text":"<p>Issue method against url; return a :class:<code>RequestHandle</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.HttpClient.get","level":4,"title":"<code>get(url, *, headers=None, timeout_ms=None, max_redirects=None, on_done=None, stream=False)</code>","text":"<p>Issue a GET request; return a :class:<code>RequestHandle</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.HttpClient.post","level":4,"title":"<code>post(url, *, body=None, json=None, headers=None, timeout_ms=None, max_redirects=None, on_done=None, stream=False)</code>","text":"<p>Issue a POST request; return a :class:<code>RequestHandle</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.HttpClient.put","level":4,"title":"<code>put(url, *, body=None, json=None, headers=None, timeout_ms=None, max_redirects=None, on_done=None, stream=False)</code>","text":"<p>Issue a PUT request; same body / json / stream semantics as :meth:<code>post</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.HttpClient.patch","level":4,"title":"<code>patch(url, *, body=None, json=None, headers=None, timeout_ms=None, max_redirects=None, on_done=None, stream=False)</code>","text":"<p>Issue a PATCH request; same body / json / stream semantics as :meth:<code>post</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.HttpClient.delete","level":4,"title":"<code>delete(url, *, headers=None, timeout_ms=None, max_redirects=None, on_done=None, stream=False)</code>","text":"<p>Issue a DELETE request; v1 sends no body.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.HttpClient.check","level":4,"title":"<code>check(now_ms)</code>","text":"<p>Return <code>True</code> if there's outbound bytes to send or readable bytes.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.HttpClient.handle","level":4,"title":"<code>handle(now_ms)</code>","text":"<p>One tick of progress on the in-flight request.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.client.HttpClient.cancel","level":4,"title":"<code>cancel()</code>","text":"<p>Abort the in-flight request; no-op when idle.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requestsgenerators","level":2,"title":"<code>chumicro_requests.generators</code>","text":"<p>Opt-in submodule for <code>yield from</code> flows driven by <code>Runner.add_generator</code>.  <code>fetch</code> runs a whole request top to bottom and returns the <code>Response</code>; <code>get</code> / <code>post</code> / <code>put</code> / <code>patch</code> / <code>delete</code> are the per-verb forms.  For a body too big for RAM, <code>stream</code> returns a <code>BodyReader</code> you pull one chunk per <code>yield from</code>.  Import it explicitly; a program that never uses a generator never loads it.</p> <p>← Home</p> <p>Source · \\ PyPI · \\ Bundle · \\ Experimental Bundle</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.generators","level":2,"title":"<code>chumicro_requests.generators</code>","text":"<p>One-shot HTTP fetch and streamed-body reads as generators.</p> <p>Public entry points: :func:<code>fetch</code>, :func:<code>get</code>, :func:<code>post</code>, :func:<code>put</code>, :func:<code>patch</code>, :func:<code>delete</code>, and :func:<code>stream</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.generators.BodyReader","level":3,"title":"<code>BodyReader</code>","text":"<p>Streamed-body pull surface returned by :func:<code>stream</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.generators.BodyReader.read_into","level":4,"title":"<code>read_into(buffer)</code>","text":"<p>Fill caller-owned buffer with body bytes; return the count.</p> <p>Raises:</p> Type Description <code>HttpError</code> <p>The request failed mid-body.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.generators.BodyReader.cancel","level":4,"title":"<code>cancel()</code>","text":"<p>Abort the transfer: close the socket, fail the handle.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.generators.fetch","level":3,"title":"<code>fetch(transport_factory, method, url, *, headers=None, body=None, json=None, max_redirects=None, max_body_bytes=DEFAULT_MAX_BODY_BYTES, timeout_ms=DEFAULT_TIMEOUT_MS, user_agent=None, ticks=None)</code>","text":"<p>Issue one HTTP request and return the :class:<code>Response</code>.</p> <p>Parameters:</p> Name Type Description Default <code>transport_factory</code> <code>object</code> <p>Callable <code>(host, port, use_tls) -&gt; connector</code> (the <code>HttpClient</code> contract).</p> required <code>method</code> <code>str</code> <p>HTTP verb, sent verbatim.</p> required <code>url</code> <code>str</code> <p>Absolute <code>http://</code> / <code>https://</code> URL.</p> required <code>headers</code> <code>object | None</code> <p>Optional dict / iterable of <code>(name, value)</code> pairs.</p> <code>None</code> <code>body</code> <code>object | None</code> <p>Optional <code>bytes</code> / <code>str</code> body (mutually exclusive with json).</p> <code>None</code> <code>json</code> <code>object | None</code> <p>Optional JSON body; sets <code>Content-Type: application/json</code>.</p> <code>None</code> <code>max_redirects</code> <code>int | None</code> <p>Hops to follow before returning the 3xx as-is.</p> <code>None</code> <code>max_body_bytes</code> <code>int</code> <p>Hard cap on the response body.</p> <code>DEFAULT_MAX_BODY_BYTES</code> <code>timeout_ms</code> <code>int</code> <p>Deadline for the whole request, DNS lookup excluded.</p> <code>DEFAULT_TIMEOUT_MS</code> <code>user_agent</code> <code>str | None</code> <p>Override the default <code>User-Agent</code>.</p> <code>None</code> <code>ticks</code> <code>object | None</code> <p>Optional <code>chumicro_timing</code>-shaped tick source.</p> <code>None</code> <p>Returns:</p> Type Description <code>object</code> <p>class:<code>~chumicro_requests.client.Response</code>.</p> <p>Raises:</p> Type Description <code>HttpTimeoutError</code> <p>The request exceeded timeout_ms (DNS stall excluded).</p> <code>HttpOversizedError</code> <p>Body exceeded max_body_bytes.</p> <code>HttpProtocolError</code> <p>Response was not valid HTTP/1.1 or peer closed mid-response.</p> <code>HttpURLError</code> <p>url or a redirect <code>Location</code> did not parse.</p> <code>HttpError</code> <p>The transport failed.</p> <code>ValueError</code> <p>Both body and json were given.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.generators.stream","level":3,"title":"<code>stream(transport_factory, method, url, *, headers=None, body=None, json=None, max_redirects=None, timeout_ms=DEFAULT_TIMEOUT_MS, stream_buffer_size=DEFAULT_STREAM_BUFFER_SIZE, user_agent=None, ticks=None)</code>","text":"<p>Issue one request and return a :class:<code>BodyReader</code> for its body.</p> <p>Raises:</p> Type Description <code>HttpTimeoutError</code> <p>timeout_ms elapsed before headers arrived.</p> <code>HttpProtocolError</code> <p>The response was not valid HTTP/1.1.</p> <code>HttpURLError</code> <p>url or a redirect <code>Location</code> did not parse.</p> <code>HttpError</code> <p>The transport failed before headers arrived.</p> <code>ValueError</code> <p>Both body and json were given.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.generators.get","level":3,"title":"<code>get(transport_factory, url, **kwargs)</code>","text":"<p>One-shot GET; call as <code>yield from get(transport_factory, url)</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.generators.post","level":3,"title":"<code>post(transport_factory, url, **kwargs)</code>","text":"<p>One-shot POST; call as <code>yield from post(transport_factory, url, json=...)</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.generators.put","level":3,"title":"<code>put(transport_factory, url, **kwargs)</code>","text":"<p>One-shot PUT; call as <code>yield from put(transport_factory, url, body=...)</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.generators.patch","level":3,"title":"<code>patch(transport_factory, url, **kwargs)</code>","text":"<p>One-shot PATCH; call as <code>yield from patch(transport_factory, url, body=...)</code>.</p>","path":["API Reference"],"tags":[]},{"location":"api/#chumicro_requests.generators.delete","level":3,"title":"<code>delete(transport_factory, url, **kwargs)</code>","text":"<p>One-shot DELETE; call as <code>yield from delete(transport_factory, url)</code>.</p>","path":["API Reference"],"tags":[]},{"location":"guide/","level":1,"title":"User Guide","text":"","path":["User Guide"],"tags":[]},{"location":"guide/#overview","level":2,"title":"Overview","text":"<p><code>chumicro-requests</code> is a non-blocking HTTP/1.1 client built on <code>chumicro-sockets</code>.  <code>HttpClient</code> is the single entry point for every verb.  Its <code>check(now_ms)</code> / <code>handle(now_ms)</code> methods drive the request forward one tick at a time.  An LED keeps blinking on the same board while a request is in flight or mid-timeout against a stalled peer.  Two connect phases are the exception: the DNS lookup, and on MicroPython / CircuitPython the TLS handshake, each block the reactor for their duration (see Bring your own transport).  The library is single-in-flight today: a second <code>client.get(...)</code> while another request is running raises <code>HttpBusyError</code>.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#getting-started-with-generators","level":2,"title":"Getting started with generators","text":"<p>The generator surface runs a whole request top to bottom (connect, send, receive, return) with no handle to poll and no <code>on_done</code> callback.  A single <code>response = yield from get(...)</code> drives the request under <code>Runner.add_generator</code>, and other runner services keep getting the CPU between yields.</p> <pre><code>from chumicro_requests.generators import get\nfrom chumicro_runner import Runner\nfrom chumicro_sockets.sockets_factory import connector_factory\n\ntransport_factory = connector_factory(radio=wifi.radio)\n\n\ndef fetch_once():\n    response = yield from get(transport_factory, \"http://api.example.com/now\")\n    print(response.status_code, len(response.body))\n\n\nrunner = Runner()\nhandle = runner.add_generator(fetch_once())\nwhile not handle.done:\n    now_ms = runner.tick()\n    runner.wait(now_ms)\n</code></pre> <p><code>chumicro_requests.generators</code> also exposes <code>post</code>, <code>put</code>, <code>patch</code>, <code>delete</code>, and the lower-level <code>fetch(transport_factory, method, url)</code>.  For a body too big for RAM, <code>stream(...)</code> returns a reader you pull one chunk per <code>yield from</code> (see Streaming large bodies).</p>","path":["User Guide"],"tags":[]},{"location":"guide/#getting-started-with-a-service","level":2,"title":"Getting started with a service","text":"<p>Reach for the <code>HttpClient</code> service when you make repeated requests on one client, or when you want the <code>check</code> / <code>handle</code> shape that drops straight into a <code>Runner</code> alongside other services.</p> <pre><code>from chumicro_requests import HttpClient\nfrom chumicro_sockets.sockets_factory import connector_factory\nfrom chumicro_timing import ticks_ms\n\nclient = HttpClient(transport_factory=connector_factory(radio=wifi.radio))\nhandle = client.get(\"http://api.example.com/now\", timeout_ms=5000)\n\nwhile not handle.done:\n    if client.check(ticks_ms()):\n        client.handle(ticks_ms())\n\nresponse = handle.result\nprint(response.status_code, response.body)\nprint(response.text)               # decoded str\nprint(response.json())             # parsed JSON\n</code></pre> <p>Build your client at startup. The first <code>HttpClient</code> reference imports the client module, so let that one-time cost land on a fresh heap.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#post-put-patch-delete","level":2,"title":"POST / PUT / PATCH / DELETE","text":"<p><code>HttpClient</code> exposes a method per verb. Bodies can be raw bytes / str or a Python object that gets JSON-encoded:</p> <pre><code># Raw bytes / str body\nhandle = client.post(\"http://api/widgets\", body=b\"&lt;custom-bytes&gt;\")\nhandle = client.post(\"http://api/widgets\", body=\"text/plain payload\")\n\n# JSON helper, auto-encodes + sets Content-Type: application/json\nhandle = client.post(\"http://api/widgets\", json={\"name\": \"thing\", \"qty\": 3})\n\n# PUT / PATCH share the same body / json semantics\nhandle = client.put(\"http://api/widgets/42\", json={\"name\": \"renamed\"})\nhandle = client.patch(\"http://api/widgets/42\", body=b\"diff-bytes\")\n\n# DELETE is intransitive in v1, no body parameter\nhandle = client.delete(\"http://api/widgets/42\")\n</code></pre> <p>Caller-supplied <code>headers={\"Content-Type\": \"...\"}</code> always wins over the JSON-helper default. Pass exactly one of <code>body=</code> or <code>json=</code>; passing both raises <code>ValueError</code>.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#redirects","level":2,"title":"Redirects","text":"<p><code>HttpClient</code> follows <code>301</code> / <code>302</code> / <code>303</code> / <code>307</code> / <code>308</code> redirects automatically up to a budget. Default cap is 5. Override per-call or per-client:</p> <pre><code># Per-call: don't follow at all (return the 3xx response as-is)\nhandle = client.get(url, max_redirects=0)\n\n# Per-call: raise the cap\nhandle = client.get(url, max_redirects=20)\n\n# Per-client default\nclient = HttpClient(transport_factory=..., default_max_redirects=10)\n</code></pre> <p>Method handling follows long-standing browser + RFC 7231 §6.4 rules:</p> <ul> <li><code>301</code> / <code>302</code> / <code>303</code> switch the next hop to GET with no body</li> <li><code>307</code> / <code>308</code> preserve the original method and body</li> </ul> <p><code>response.url</code> reflects the URL of the FINAL hop, not the original request. If the budget is exhausted before reaching a non-3xx response, the last 3xx is returned to the caller (matching CPython <code>requests</code>' default behavior without the <code>raise_for_status()</code> step).</p> <p>The <code>Location</code> header may be absolute (<code>https://other.com/dest</code>), absolute-path (<code>/api/v2</code>), or path-relative (<code>trinkets</code>). All three shapes are resolved against the current URL.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#body-framing","level":2,"title":"Body framing","text":"<p><code>HttpClient</code> accepts three RFC 7230 body framings transparently:</p> <ul> <li><code>Content-Length: N</code> reads exactly N bytes (most common case).</li> <li><code>Transfer-Encoding: chunked</code> selects RFC 7230 §4.1 chunked   decoding.  Chunk-extensions and trailer headers are accepted and   discarded.  <code>Content-Length</code> is ignored when chunked is present per   §3.3.3.</li> <li>Neither header reads until the peer closes the connection   (HTTP/1.0-style framing).</li> </ul> <p>In all cases <code>response.body</code> returns the decoded bytes (chunks concatenated for chunked responses).  <code>response.text</code> and <code>response.json()</code> work the same way regardless of framing.</p> <p>Other <code>Transfer-Encoding</code> values (<code>gzip</code>, <code>deflate</code>, <code>identity</code> stacked with chunked, etc.) are rejected with <code>HttpProtocolError</code> in v1, since the caller would otherwise silently get garbled bytes.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#streaming-large-bodies","level":2,"title":"Streaming large bodies","text":"<p>By default the whole body is buffered in RAM, capped at <code>max_body_bytes</code> (64 KB).  Pass <code>stream=True</code> (on any verb, or on the generic <code>client.request(method, url, ...)</code>) to consume the body incrementally instead: firmware images, log pulls, and any payload bigger than the heap become readable at a fixed RAM cost (the <code>stream_buffer_size</code> staging window, default 1024 bytes, plus your own buffer).</p> <pre><code>handle = client.get(\"http://host/firmware.bin\", stream=True,\n                    timeout_ms=120_000)\nbuffer = bytearray(512)\nview = memoryview(buffer)\n\n# Inside your service's handle(now_ms), one slice per tick:\nif handle.done and handle.error is not None:\n    report(handle.error)                    # failed mid-transfer\nelif handle.response is not None:           # headers are in\n    count = handle.read_body_into(view)\n    if count:\n        flash.write(view[:count])\n    elif handle.done:\n        finish()                             # 0 after done == end of body\n</code></pre> <p>The contract:</p> <ul> <li><code>handle.response</code> is set as soon as the final hop's headers arrive   (before <code>handle.done</code>), so you can branch on <code>status_code</code> /   <code>headers</code> first.  Its <code>body</code> is <code>b\"\"</code> and <code>streamed</code> is <code>True</code>;   <code>.text</code> / <code>.json()</code> raise <code>HttpError</code>.</li> <li><code>handle.read_body_into(buffer)</code> copies decoded body bytes into your   buffer and returns the count.  <code>0</code> means \"nothing this tick\"; once   <code>handle.done</code> is <code>True</code> (with no <code>error</code>), <code>0</code> means end of body.</li> <li>Backpressure is automatic: when the staging window is full the   client stops reading the socket (and reports no poll interest, so a   runner parks instead of spinning) until you drain it.</li> <li><code>max_body_bytes</code> and <code>WhenOversized</code> do not apply to streamed   bodies; the staging window is the RAM bound.  To enforce your own   ceiling, count what you read and call <code>client.cancel()</code>.</li> <li><code>timeout_ms</code> covers the whole transfer, your reads included, so size   it for the download, not the round-trip.</li> <li><code>client.cancel()</code> aborts the request immediately (the handle fails   with <code>HttpError</code>), for early exits that shouldn't wait out the   timeout.</li> </ul> <p>The generator form wraps the same machinery, one chunk per <code>yield from</code>:</p> <pre><code>from chumicro_requests.generators import stream\n\ndef download(transport_factory, url, sink):\n    reader = yield from stream(transport_factory, \"GET\", url,\n                               timeout_ms=120_000)\n    if reader.response.status_code != 200:\n        reader.cancel()\n        return\n    buffer = bytearray(512)\n    view = memoryview(buffer)\n    while True:\n        count = yield from reader.read_into(view)\n        if count == 0:\n            break\n        sink(view[:count])\n\nrunner.add_generator(download(transport_factory, url, flash.write))\n</code></pre>","path":["User Guide"],"tags":[]},{"location":"guide/#body-decoding","level":2,"title":"Body decoding","text":"<p><code>Response.body</code> is always raw <code>bytes</code>.  <code>Response.text</code> decodes those bytes using <code>Response.encoding</code>, which is sniffed from the <code>Content-Type</code> header's <code>charset=</code> parameter (default <code>utf-8</code>). Override the encoding when a server's Content-Type lies:</p> <pre><code>response = handle.result\nresponse.encoding = \"latin-1\"\nprint(response.text)\n</code></pre> <p><code>Response.json()</code> decodes via <code>text</code> first, then runs <code>json.loads</code>, so charset overrides apply to JSON responses too.</p> <p>The <code>transport_factory</code> argument is a callable <code>(host, port, use_tls) -&gt; SocketConnector</code>, a tick-driven connector, not a ready socket (see Bring your own transport below for the connector contract). The bundled <code>chumicro_sockets.sockets_factory.connector_factory(radio=..., ssl_context=...)</code> returns one wired to <code>chumicro-sockets</code>. The helper lives in an opt-in submodule so users with a custom transport never trigger the <code>chumicro-sockets</code> deploy. Tests pass a factory returning a <code>chumicro_sockets.testing.FakeSocketConnector</code> (which wraps a <code>FakeSocket</code>).</p>","path":["User Guide"],"tags":[]},{"location":"guide/#bring-your-own-transport","level":2,"title":"Bring your own transport","text":"<p><code>HttpClient</code> does not care which library produces its sockets.  The <code>transport_factory</code> you pass is a callable of shape <code>(host: str, port: int, use_tls: bool) -&gt; SocketConnector</code>.  The connector advances the TCP connect one tick at a time, but the DNS lookup and, on MicroPython / CircuitPython, the TLS handshake block the reactor for their duration.  On a slow or unreachable host that can be seconds, freezing every other runner service, so connect before starting time-critical work.  Once <code>connector.state == \"ready\"</code>, the underlying socket must expose the four-method contract:</p> Method Contract <code>recv_into(buffer, nbytes) -&gt; int</code> Reads up to <code>nbytes</code> into <code>buffer</code> (a <code>memoryview</code>).  Raises <code>OSError(EAGAIN \\| EWOULDBLOCK)</code> on no data, returns 0 on peer-close, otherwise returns bytes written. <code>send(payload) -&gt; int</code> Sends <code>payload</code> (a <code>bytes</code>).  Raises <code>OSError(EAGAIN \\| EWOULDBLOCK)</code> when the send buffer is full, otherwise returns bytes sent (may be partial). <code>close() -&gt; None</code> Releases the connection. <code>setblocking(flag) -&gt; None</code> Best-effort.  Absence is tolerated. <p><code>chumicro_sockets.connector</code> is one producer.  See <code>chumicro_sockets._connector.SocketConnector</code> for the connector contract (<code>tick(now_ms)</code>, <code>state</code>, <code>socket</code>, <code>io_*</code>, <code>next_deadline</code>, <code>cancel</code>).  Any tick-driven state machine with that surface works as a custom factory.</p> <p>If you supply your own factory and want <code>chumicro_sockets</code> dropped from the deploy, add a module-level constant to your entrypoint and the chumicro-workspace deployer will filter the default factory out of the import graph:</p> <pre><code># code.py / app.py\n__chumicro_skip_factories__ = (\"sockets_factory\",)\n</code></pre> <p>The constant accepts a family form (the bare stem, matches every <code>chumicro_*.sockets_factory</code>) or an exact dotted path (<code>chumicro_sockets.sockets_factory</code>).  An unmatched entry fails the deploy with a typo message rather than silently shipping the default.  Calling <code>HttpClient.from_config(...)</code> when <code>chumicro_sockets.sockets_factory</code> is missing (either skipped at deploy time or not installed by <code>circup</code> / <code>mip</code>) raises <code>RuntimeError</code> naming the bypass kwarg.</p> <p>Adopting <code>chumicro-requests</code> on its own needs nothing from the rest of the family: pass your own <code>transport_factory</code> (and, if you want, your own <code>ticks=</code> clock), drive the client from a plain <code>while</code> loop as in Getting started with a service, and run host tests with a factory returning a <code>chumicro_sockets.testing.FakeSocketConnector</code>, no board required.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#runner-pattern","level":2,"title":"Runner pattern","text":"<p><code>HttpClient.check(now_ms) -&gt; bool</code> and <code>handle(now_ms) -&gt; None</code> satisfy the runner contract. Drop the client into a <code>Runner</code> alongside an LED-heartbeat task:</p> <pre><code>from chumicro_runner import Runner\nfrom chumicro_requests import HttpClient\nfrom chumicro_sockets.sockets_factory import connector_factory\n\nhttp_client = HttpClient(transport_factory=connector_factory(radio=radio))\nrunner = Runner([http_client, blink_task])\nwhile True:\n    runner.tick(ticks_ms())\n</code></pre>","path":["User Guide"],"tags":[]},{"location":"guide/#memory-notes","level":2,"title":"Memory notes","text":"<p>The default 64 KB <code>max_body_bytes</code> cap is sized for the minimum supported board class (256 KB MCU RAM). Bump it for larger boards if needed; the <code>Response.body</code> buffer grows up to that cap. The default 1024-byte <code>recv_budget_per_tick</code> matches <code>chumicro-mqtt</code>'s; bytes drained per tick are bounded so concurrent runner tasks (LED blink, control loop) keep getting CPU time even mid-large-body. For bodies that shouldn't (or can't) sit in RAM at all, use <code>stream=True</code> (see Streaming large bodies): the per-request cost drops to the <code>stream_buffer_size</code> staging window (default 1024 bytes) regardless of body size.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#platform-notes","level":2,"title":"Platform notes","text":"<p>Pure Python, no third-party deps beyond <code>chumicro-sockets</code> and <code>chumicro-timing</code>. Works identically on CPython, MicroPython, and CircuitPython once the connection factory is wired up. HTTPS uses the same <code>chumicro_sockets.sockets_factory.connector_factory(ssl_context=...)</code> pattern as plain HTTP.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#https-heap-headroom-on-minimum-class-boards","level":3,"title":"HTTPS heap headroom on minimum-class boards","text":"<p>mbedTLS handshake costs heap.  Bench-tested on Pi Pico W MP with a raw-stdlib <code>ssl.wrap_socket()</code> against <code>letsencrypt.org:443</code>, the handshake consumed about 25 KB of heap on top of whatever the app already had loaded.  Headroom matters: an app that's loaded a lot into RAM (chumicro stack + workspace fixtures + per-example helpers) can run the handshake out of memory and surface as <code>OSError(12)</code> (ENOMEM) inside <code>wrap_socket()</code>.  If you see ENOMEM on HTTPS, the two levers are (a) drop unused imports before the request, or (b) switch to flash deploy mode (chumicro-deploy <code>--mode flash</code>) so the library bootstrap lives on flash and the heap is free for the handshake.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#tls-context-bring-your-own-ca","level":3,"title":"TLS context: bring your own CA","text":"<p><code>chumicro_sockets.sockets_factory.connector_factory(ssl_context=...)</code> accepts an SSL context built via <code>chumicro_sockets.ssl_context_with_ca(pem)</code>. CA-pinning is required on both supported embedded runtimes, but for different reasons:</p> <ul> <li>MicroPython doesn't have <code>ssl.create_default_context()</code> at all;   every TLS context must be built explicitly.</li> <li>CircuitPython has <code>ssl.create_default_context()</code> (and it builds   cheaply, ~80 bytes of heap on a Pi Pico W), but the returned context   carries no CAs and has <code>check_hostname=False</code>, so a handshake against   any real cert would fail.</li> </ul> <p>So on both runtimes, pass a context with a CA loaded. The CPython \"default context loads a 100-200 KB system trust store\" intuition doesn't apply: neither MP nor CP bundles a trust store, by design.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#device-rtc-must-be-set-before-tls","level":3,"title":"Device RTC must be set before TLS","text":"<p>mbedTLS <code>CERT_REQUIRED</code> checks the cert validity window against the device clock. A board with no RTC battery and no NTP boots at 2021-01-01 (or epoch), which is \"before\" every modern cert's <code>not_valid_before</code> field, so the handshake fails with <code>ValueError(\"certificate validity starts in the future\")</code>. Use <code>chumicro-ntp</code> to set the device clock from a public NTP server before the TLS handshake.  Cross-runtime, non-blocking, takes a UDP socket you provide.</p>","path":["User Guide"],"tags":[]},{"location":"guide/#examples","level":2,"title":"Examples","text":"Example What it shows <code>periodic_get.py</code> Periodic GET on a real CP/MP board: wifi up, hits a configured URL every N seconds, drives an LED-blink counter to verify the request never blocks the loop.  Cross-runtime (CP + MP). <p>← Home</p> <p>Source · \\ PyPI · \\ Bundle · \\ Experimental Bundle</p>","path":["User Guide"],"tags":[]},{"location":"testing/","level":1,"title":"Testing Helpers","text":"<p>Host-only fakes for testing code that depends on <code>chumicro-requests</code>.  <code>chumicro_requests.testing</code> declares itself test support, so the deploy walker and the bundle builder drop it and it never lands on a microcontroller.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#fakehttpclient","level":2,"title":"<code>FakeHttpClient</code>","text":"<p>In-memory <code>HttpClient</code> stand-in.  It answers the same <code>get</code> / <code>check</code> / <code>handle</code> / <code>busy</code> / <code>on_oversized</code> calls the real client answers, and tests script responses with <code>enqueue_response()</code> (success) or <code>enqueue_error()</code> (failure).  Each <code>get()</code> pops one scripted entry, and the next <code>handle()</code> tick completes the <code>RequestHandle</code>.</p> <pre><code>from chumicro_requests.testing import FakeHttpClient\n\nfake = FakeHttpClient()\nfake.enqueue_response(status=200, body=b'{\"temp_f\": 72}')\n\nweather = WeatherFetcher(http_client=fake)\nweather.tick(now_ms=0)        # internally calls fake.get(...)\nweather.tick(now_ms=10)       # one handle() tick completes the request\n\nassert weather.last_temperature == 72\nassert fake.calls[0].url == \"http://api.example.test/weather\"\n</code></pre>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#streamed-requests","level":2,"title":"Streamed requests","text":"<p>A <code>stream=True</code> request against the fake compresses the real client's contract into one tick: <code>handle()</code> completes the request, and the scripted body drains through <code>read_body_into</code>, bytes first, then <code>0</code> for end of body.</p> <pre><code>fake = FakeHttpClient()\nfake.enqueue_response(status=200, body=b\"blob-bytes\")\n\nhandle = fake.get(\"http://example.test/blob\", stream=True)\nfake.handle(now_ms=0)\n\nbuffer = bytearray(4)\nassert handle.read_body_into(buffer) == 4      # b\"blob\"\nassert handle.result.streamed is True\nassert fake.calls[0].stream is True\n</code></pre> <p><code>fake.cancel()</code> mirrors <code>HttpClient.cancel()</code>: the in-flight handle finishes with an <code>HttpError</code> and its <code>on_done</code> callback fires.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#enqueue_error-example","level":2,"title":"<code>enqueue_error</code> example","text":"<pre><code>from chumicro_requests import HttpTimeoutError\nfrom chumicro_requests.testing import FakeHttpClient\n\nfake = FakeHttpClient()\nfake.enqueue_error(HttpTimeoutError(\"simulated timeout\"))\n\nweather = WeatherFetcher(http_client=fake)\nweather.tick(now_ms=0)\nweather.tick(now_ms=10)\nassert weather.last_error_message == \"simulated timeout\"\n</code></pre>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#using-these-fakes-in-your-own-tests","level":2,"title":"Using these fakes in your own tests","text":"<p>Your test suite imports the fake straight from the installed package, the same way this library's own tests do:</p> <pre><code>from chumicro_requests.testing import FakeHttpClient\n</code></pre> <p>Project convention: libraries that expose injectable services ship their own test fakes alongside the production code.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#api-reference","level":2,"title":"API Reference","text":"<p>← Home</p> <p>Source · \\ PyPI · \\ Bundle · \\ Experimental Bundle</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_requests.testing","level":2,"title":"<code>chumicro_requests.testing</code>","text":"<p><code>FakeHttpClient</code> plus low-level fixtures for the client test suite.</p> <p>Public entry points: :class:<code>FakeHttpClient</code>, :func:<code>make_factory</code>, :func:<code>canned_response</code>, :func:<code>make_client</code>, and :func:<code>drive_until_done</code>.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_requests.testing.FakeHttpClient","level":3,"title":"<code>FakeHttpClient</code>","text":"<p>In-memory :class:<code>HttpClient</code> stand-in for tests.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_requests.testing.FakeHttpClient.busy","level":4,"title":"<code>busy</code>  <code>property</code>","text":"<p><code>True</code> while a request is in flight (between request method and handle).</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_requests.testing.FakeHttpClient.enqueue_response","level":4,"title":"<code>enqueue_response(*, status=200, reason='OK', http_version='HTTP/1.1', headers=None, body=b'', oversized_dropped=False)</code>","text":"<p>Script the next request to succeed with this response.</p> <p>Parameters:</p> Name Type Description Default <code>status</code> <code>int</code> <p>HTTP status code (default 200).</p> <code>200</code> <code>reason</code> <code>str</code> <p>Reason phrase (default <code>\"OK\"</code>).</p> <code>'OK'</code> <code>http_version</code> <code>str</code> <p>Protocol version string (default <code>\"HTTP/1.1\"</code>).</p> <code>'HTTP/1.1'</code> <code>headers</code> <code>CaseInsensitiveDict | dict | list | tuple | None</code> <p><code>dict</code> / <code>CaseInsensitiveDict</code> / iterable of <code>(name, value)</code>.</p> <code>None</code> <code>body</code> <code>bytes</code> <p>Response body bytes (default empty).</p> <code>b''</code> <code>oversized_dropped</code> <code>bool</code> <p>Set the matching flag on the response.</p> <code>False</code>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_requests.testing.FakeHttpClient.enqueue_error","level":4,"title":"<code>enqueue_error(error)</code>","text":"<p>Script the next request to fail with error.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_requests.testing.FakeHttpClient.request","level":4,"title":"<code>request(method, url, *, body=None, json=None, headers=None, timeout_ms=None, max_redirects=None, on_done=None, stream=False)</code>","text":"<p>Mirror :meth:<code>HttpClient.request</code> against the scripted queue.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_requests.testing.FakeHttpClient.get","level":4,"title":"<code>get(url, *, headers=None, timeout_ms=None, max_redirects=None, on_done=None, stream=False)</code>","text":"<p>Mirror :meth:<code>HttpClient.get</code> against the scripted queue.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_requests.testing.FakeHttpClient.post","level":4,"title":"<code>post(url, *, body=None, json=None, headers=None, timeout_ms=None, max_redirects=None, on_done=None, stream=False)</code>","text":"<p>Mirror :meth:<code>HttpClient.post</code> against the scripted queue.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_requests.testing.FakeHttpClient.put","level":4,"title":"<code>put(url, *, body=None, json=None, headers=None, timeout_ms=None, max_redirects=None, on_done=None, stream=False)</code>","text":"<p>Mirror :meth:<code>HttpClient.put</code> against the scripted queue.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_requests.testing.FakeHttpClient.patch","level":4,"title":"<code>patch(url, *, body=None, json=None, headers=None, timeout_ms=None, max_redirects=None, on_done=None, stream=False)</code>","text":"<p>Mirror :meth:<code>HttpClient.patch</code> against the scripted queue.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_requests.testing.FakeHttpClient.delete","level":4,"title":"<code>delete(url, *, headers=None, timeout_ms=None, max_redirects=None, on_done=None, stream=False)</code>","text":"<p>Mirror :meth:<code>HttpClient.delete</code> against the scripted queue.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_requests.testing.FakeHttpClient.check","level":4,"title":"<code>check(now_ms)</code>","text":"<p>Return <code>True</code> while a request is in flight.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_requests.testing.FakeHttpClient.handle","level":4,"title":"<code>handle(now_ms)</code>","text":"<p>Complete the in-flight request from the scripted outcome.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_requests.testing.FakeHttpClient.cancel","level":4,"title":"<code>cancel()</code>","text":"<p>Mirror :meth:<code>HttpClient.cancel</code>: fail the in-flight request.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_requests.testing.canned_response","level":3,"title":"<code>canned_response(*, status=200, reason='OK', body=b'', extra_headers=())</code>","text":"<p>Build an HTTP/1.1 response byte-string with <code>Content-Length</code>.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_requests.testing.make_factory","level":3,"title":"<code>make_factory(socket_or_factory)</code>","text":"<p>Return a transport_factory wrapping socket_or_factory in a :class:<code>FakeSocketConnector</code>.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_requests.testing.make_client","level":3,"title":"<code>make_client(*, socket_or_factory=None, **kwargs)</code>","text":"<p>Build an :class:<code>HttpClient</code> against <code>FakeTicks</code> + a <code>FakeSocket</code>.</p>","path":["Testing Helpers"],"tags":[]},{"location":"testing/#chumicro_requests.testing.drive_until_done","level":3,"title":"<code>drive_until_done(client, handle, ticks, *, max_ticks=200, advance_ms=1)</code>","text":"<p>Tick client until handle <code>.done</code> flips True.</p> <p>Raises:</p> Type Description <code>AssertionError</code> <p>The handle never completed within max_ticks.</p>","path":["Testing Helpers"],"tags":[]}]}