# {distribution}

<!-- Replace the tagline, the supporting sentence, the quick example, and
     the "What you get" bullets with this tool's real shape.  Keep the
     Documentation bullets, the Install block, and the footer, and drop
     the Testing Helpers bullet if the package ships no `testing` module.
     Delete this comment when you're done. -->

**One bold sentence saying what the tool does.**

One supporting sentence naming the concrete value, who runs it, and any non-obvious caveat.  Runs on your laptop, not on the board.

## Quick example

```bash
{distribution} --help
```

## What you get

<!-- One bullet per surface the tool exposes: bold the surface name, then
     say in a sentence what it does for the reader. -->

- **`<surface name>`**: what the reader gets from it.

## Documentation

- [User Guide](guide.md): install, the command-line interface, each surface explained, runtime notes.
- [API Reference](api.md): full API from the source docstrings.
- [Testing Helpers](testing.md): the fakes this package ships for host-side tests.

## Install

```bash
pip install {distribution}
```

No bundle registration needed.  {distribution} is a host tool, not on-device code.
{footer_index}
