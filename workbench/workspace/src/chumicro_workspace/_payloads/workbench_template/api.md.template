# API Reference

<!-- Starts as a single autodoc directive for the whole package.  As
     the workbench tool grows multiple modules, split into per-module
     sections with `## Heading` + `::: {import_name}.<module>`
     directives. -->

::: {import_name}
{footer_page}
