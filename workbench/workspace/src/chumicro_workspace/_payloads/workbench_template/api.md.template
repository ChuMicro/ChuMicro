# API Reference

<!-- Delete this comment before you publish: a finished api.md carries no
     scaffold guidance.  The whole-package directive below is the right
     shape until the tool grows a second public module; after that, split
     into one `## ` heading per module with the fully qualified module
     path in backticks and a single `::: {import_name}.<module>`
     directive under each. -->

::: {import_name}
{footer_page}
