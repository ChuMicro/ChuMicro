# User Guide

<!-- GENERATION INSTRUCTIONS: delete this block once the guide is written.

     Workbench packages are host-only CPython tools that run on your
     laptop, not on the microcontroller.  This template differs from the
     library template: no cross-runtime story, no Runner pattern, no
     Memory notes, and no bundle install, because workbench packages ship
     through PyPI only.  The section shape follows the published workbench
     guides: an opening paragraph, Install, Command-line interface, one
     section per surface the tool exposes, then Runtime notes last.  Every
     section is required unless marked conditional. -->

<!-- Required. One sentence on what the tool does and who runs it, then
     one sentence listing the surfaces this guide walks, in the order the
     sections below cover them. -->

## Install

```bash
pip install {distribution}
```

<!-- Required. Name the third-party dependencies that come along, any host
     prerequisite the user has to install themselves (a CLI on PATH, a
     Python version floor), and any platform that isn't supported.  Say
     that there is no device-side install. -->

## Command-line interface

<!-- Conditional. Include if the package ships a console script.  Show the
     real invocations in one bash block with a comment above each, then
     say what `--help` covers and how the CLI reports errors. -->

```bash
{distribution} --help
```

## <One heading per surface>

<!-- Required. One `## ` section per surface a user reaches for: a class,
     an entry-point function, a file the tool reads.  Name the surface in
     the heading, show a snippet that runs as written, and say what
     happens when it fails. -->

## Test fakes

<!-- Conditional. Include if the package ships a `testing` module.  Name
     each fake and what it stands in for, and point at
     [Testing Helpers](testing.md) for the full surface. -->

## Runtime notes

<!-- Required. Host constraints (operating system, Python version, a
     binary that has to be on PATH) plus any device-runtime difference
     this tool absorbs so the reader doesn't have to. -->
{footer_page}
