# User Guide

<!-- GENERATION INSTRUCTIONS — delete this block once the guide is written.

     Workbench packages are host-only CPython tools that run on your
     laptop, not on the microcontroller.  This template differs from the
     library template: no cross-runtime story, no Runner pattern, no
     Memory notes, no Bundle install — workbench packages only ship via
     PyPI.  Every section below is required unless marked conditional. -->

## Overview

<!-- Required. 2-4 sentences: what the tool does, who runs it, the core
     concept.  Name the key CLIs / classes. -->

## Install

```bash
pip install {distribution}
```

## Getting started

<!-- Required. The most common usage pattern — CLI invocation OR a
     copy-pasteable Python snippet — that runs end-to-end. -->

## CLI reference

<!-- Conditional. Include if the package ships an entry-point CLI.
     One section per subcommand, or a single table for short surfaces. -->

## Host platform notes

<!-- Required. Operating-system and Python-version constraints.  Call
     out any platform-specific behavior (e.g. macOS uses FSKit for
     CIRCUITPY mounts; Windows requires admin for raw serial). -->

## Examples

<!-- Required. List every example from the examples/ directory in a
     table:
     | Example | What it shows | -->
{footer_page}
