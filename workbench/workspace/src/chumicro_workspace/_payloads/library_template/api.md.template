# API Reference

<!-- Starts as a single autodoc directive for the whole package.  As
     the library grows multiple modules, split this into per-module
     sections with `## Heading` + `::: {import_name}.<module>`
     directives — see libraries/timing/docs/api.md for an example.
     The autodoc renderer pulls docstrings + type hints directly from
     the source, so the page stays in sync with the code without
     manual edits here. -->

::: {import_name}
{footer_page}
