# {distribution}

<!-- This template is a worked example for a fictional single-shot
     countdown timer, the same fictional library the README and the other
     three docs templates use.  Replace the tagline, the body sentence,
     and the quick example with your real library's shape.  Keep the
     Documentation bullets and the footer, and drop the Testing Helpers
     bullet if the package ships no `testing` module.  Delete this comment
     when you're done. -->

**A single-shot timer that fires once after N milliseconds.**

Set a deadline, poll each tick, and get `True` once when time's up.  No `time.sleep()`, no callbacks.

## Quick example

```python
from {import_name} import Countdown
from chumicro_timing import ticks_ms

countdown = Countdown(period_ms=5000)

while True:
    now = ticks_ms()
    if countdown.fired(now):
        print("time's up")
        break
```

## Documentation

- [User Guide](guide.md): firing once, sharing timestamps, resetting, wiring into a runner
- [API Reference](api.md): every public class and method, generated from the source docstrings
- [Testing Helpers](testing.md): using `FakeCountdown` in your tests
{footer_index}
